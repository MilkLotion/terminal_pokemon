# 작업 역할

채택한 범위 안에서 변경한다.

- 같은 작업의 기록을 갱신한다.
- 실제 변경과 관련 파일을 적는다.
- 현재 결정이 바뀌면 기준 문서를 갱신한다.
- 현재 상태가 바뀌면 현황을 갱신한다.
- 같은 설명을 모든 문서에 복사하지 않는다.
- 생성된 `dist/`를 직접 편집하지 않는다.

작업이 끝나면 월별 이력에 결과와 상세 링크를 추가한다.
