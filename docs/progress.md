# pokebuddy 진행 현황과 다음 작업

기준: `85daaa7` (S4 구현 커밋) 이후 S5 설계.
갱신: 2026-09-18. S5 설계 문서는 미커밋이다. S5 기능 구현은 시작하지 않았다.

새 세션은 이 문서, [설계](design.md), [S4 검수](review-s4.md), [설명서](guide.md) 순서로 읽는다.

## 현재 상태

| 단계 | 상태 | 내용 |
|---|---|---|
| S0 설계 | 완료 | 무대 하나, 최대 6마리, 원작 25개 성격, TypeScript |
| S1 기반 | 완료 | 저장 v2, 잠금, mailbox, 명령 처리기, 도감과 에이전트 자료 |
| S2 무대 | 구현 완료 | 마리별 클릭, 드래그, 집 저장. 이번 작업에서 겹침 허용과 소환 순서 표시로 변경 |
| S3 움직임과 육성 | 구현·자동 검사 완료 | 성격, 배고픔, 기분, 친밀도, 토큰, 돌봄. [검수 기록](review-s3.md) |
| S4 해금·상점·진화 | 구현·자동 검사 완료 | 해금, 칸, 새 종, 민트, 먹이, 에버스톤, 색, 진화, 모습 선택. 메뉴와 CLI 연결 |
| S5 설정창·메뉴·연결 | 설계안 작성·시안 검수 완료 | [상세 설계](plan-s5.md), [화면 시안](s5-preview.html). 기능 구현 전 |
| S6 배포 | 대기 | 패키징, 게시, 자동 시작, 플랫폼 실기 |

## S5 설계 — 이번 변경

사용자 요청에 따라 설계까지만 진행했다. [상세 설계](plan-s5.md)에 화면, 상태, 명령, 저장, 검수 기준을 정리했다. [화면 시안](s5-preview.html)은 예시 자료로 탭과 상세 화면을 확인하는 독립 HTML이다. 게임과 사용자 설정은 변경하지 않는다.

시안의 탭 이동, 여섯 칸, 좁은 화면, 실패 안내를 확인했다. 기능 코드는 변경하지 않았다. 제안한 기본값은 설계 문서 1절에 모았다.

## S4 구현 기록

계획 → 설계 → 구현 → 검수 → 피드백 기록 → 수정 순서로 진행했다.
[계획과 가격](plan-s4.md), [발견한 문제와 수정](review-s4.md)을 확인한다.

- 포켓몬은 겹칠 수 있다. 시작 시에도 강제로 벌리지 않는다.
- 나중에 소환한 포켓몬이 앞에 보인다. 드래그와 모습 변경은 순서를 유지한다.
- 숨긴 뒤 다시 소환하면 마지막에 그린다. 재시작하면 저장된 파티 순서로 소환한다.
- 상점 구매와 진화는 그림과 최신 상태를 확인한 뒤 저장한다.
- 실패한 거래는 포인트를 차감하지 않는다.
- 먹이는 다음 밥의 효과를 두 배로 만든다. 기본 밥은 무료다.
- 색과 에버스톤 구매 권리는 마리별로 유지한다.
- `pokebuddy game --help`에서 S4 명령을 확인할 수 있다.

## S4 검증 결과

- 타입 검사와 빌드 통과.
- 자체 검사 8벌 통과: save, dex, agents, follow, motion, stage, state, shop.
- 숨긴 Electron 창에서 겹침 픽셀, 투명 영역 히트, 우클릭 대상 검사 통과.
- 실제 PMD 이브이 일반색, 다른 색, 블래키 그림 렌더링 확인.
- 별도 CLI 프로세스와 임시 HOME의 mailbox 왕복 확인.
- 실제 사용자 저장은 변경하지 않았다.

## 다음 작업 — S5

1. [S5 설계](plan-s5.md)를 기준으로 설정창을 구현한다. 아래 작업은 아직 시작하지 않았다.
2. 파티는 여섯 칸 카드로 표시한다. 카드에서 상세 화면을 연다.
3. 도감은 번호, 이름, 조건, 진화 사슬, 저작자를 표시한다. 미해금 종은 실루엣으로 표시한다.
4. 상점은 프렌들리숍 목록으로 표시한다. S4 명령과 가격표를 재사용한다.
5. 연결 탭은 Claude Code, Codex, Gemini를 각각 연결한다. 지원되는 경우 오늘 토큰을 표시한다. 미지원 값은 작업 시간으로 표시한다.
6. 설정 탭은 표시, 고스트 모드, 잠들기, 언어, 자동 시작을 다룬다.
7. 설정창 연결 후 우클릭과 트레이 메뉴를 줄인다.
8. 남은 JS를 TS로 옮긴다. `config.js`, `cli/`, `lib/`, `art/`, 빌드 스크립트가 대상이다.
9. 설치 전에 실행되는 `scripts/postinstall.js`는 JS로 유지한다.
10. 훅 원본이 없는 경우의 에이전트 연결 결과 코드를 분리한다.

## 남은 플랫폼 확인

- Windows 125%와 150% DPI, 실제 드래그, 클릭 통과, 우클릭, 앱 전환.
- 6마리 CPU와 GPU 사용량. 기존 mac 측정값 10.5%는 S2 당시 기록이다.
- macOS 회귀, 다중 화면, 여러 VS Code 창의 파티 배분.
- Codex와 Gemini의 토큰 읽기. 읽지 못하면 작업 시간으로 적립한다.
- PMD 특수 폼 매핑. 기존 그림 로더는 도감번호의 기본 폼을 사용한다.
- S6: 앱 아이콘 번들, npm과 vsix 게시, 로그인 자동 시작.

## 작업 규칙

- 설계와 검사 기준을 먼저 기록한다.
- 검수에서 발견한 문제를 적고 수정한다.
- 새 기능 코드는 `src/`의 TypeScript로 작성한다. 배럴 `index.ts`는 만들지 않는다.
- 주석은 한국어 개조식으로 작성한다. 미확정 항목은 한국어 대괄호 태그로 표시한다.
- 새 npm 의존성은 사용자 승인 후 추가한다.
- 명령은 macOS와 Windows에서 동작해야 한다. `.ps1`의 UTF-8 BOM을 유지한다.
- 시험은 임시 HOME을 사용한다. 실제 게임 저장은 변경하지 않는다.
- 실행한 펫은 해당 pid 파일 또는 companion.lock을 삭제해 종료한다.
- 커밋과 푸시는 사용자가 요청할 때 진행한다.
- 기존 외부 설계 Artifact는 이전 기록이다. 현재 결정은 `design.md`와 S4 문서를 우선한다.

## 검사 명령

```text
npm run check
npm run selftest
node_modules/.bin/electron dist/tools/smoke-renderer.js
```

Windows에서는 Electron 실행 파일에 `.cmd`를 붙일 수 있다.
`POKEBUDDY_SMOKE_ART`에 실제 시트 JSON이 있는 임시 폴더를 지정하면 실제 그림도 검사한다.
가격과 보상은 `data/shop.json`에서 조정한다. 종별 가격과 해금 조건은 `data/unlocks.json`에 있다.

## 설치 기록 주의

이전 문서의 `npm link`, 훅, 확장 설치 완료 기록은 다른 mac 세션의 상태다.
현재 Windows의 전역 설치 확인 결과로 사용하지 않는다.
