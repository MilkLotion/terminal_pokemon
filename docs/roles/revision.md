# Revision Role

Close every review finding before delivery.

- Fix open findings with a focused change.
- Rerun every affected check.
- Update `docs/design.md`, `docs/progress.md`, the review file, and `docs/history.md`.
- Record the final commit ID in the history entry.

Commit and push only when no required finding remains open.
