# Work Role

Implement only the approved design.

- Make the smallest focused change in the correct feature module.
- Keep rules in `data/` and runtime code in `src/`.
- Update feature documentation and `docs/history.md` in the same change.
- Do not edit generated `dist/` files by hand.

Record the commands needed to reproduce the change.
