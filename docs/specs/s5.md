# S5 설정창 설계

갱신: 2026-09-21. 상태: Figma v2 반영 설계. 기능 구현 미시작.
범위: 현재 Figma 기준과 기능 계약을 정리한다. 게임 저장, 규칙표, 훅, 런타임은 변경하지 않는다.

사용자 흐름과 수용 조건은 [S5 사용자 시나리오](s5-scenarios.md)를 따른다. SC-01부터 SC-11까지의 정상 흐름과 예외를 작성했다. 확정한 규칙과 미정인 세부 UI·저장 계약을 구분한다.

[현재 Figma 검수](../work/s5-design-system-v2/review.md) · [현재 설계](../design.md) · [진행 현황](../progress.md) · [명칭 사전](../terms.md)

## 1. 목표와 결정

2026-09-21 후속 요청: 제품 목적부터 기존 설계를 재검토한다. 새 판단 기준은 [제품 목적](../design.md#제품-목적)을 따른다. 시나리오 초안과 미정 사항은 [후속 설계 기록](../work/s5-design-system-v2/plan.md#제품-목적부터-재검토)에 있다.
아래 기존 계약의 유지 여부는 이 검토에서 판단한다. 이번 요청만으로 기존 기능의 삭제나 새 규칙의 채택을 확정하지 않는다.
친밀도 획득 방향과 탐험 후보는 [친밀도 획득과 확장 후보](../design.md#친밀도-획득과-확장-후보)를 따른다. 에이전트 보상은 별도의 추가 이득으로 설계한다. 추가 보상의 계산 방식은 미정이다.

2026-09-21 정정: 기존 문서의 상품·부화 관련 사용자 결정 표기는 직접 근거가 확인되지 않았다. 아래 해당 내용은 기존 제안 기록이며, 기능 자체의 채택부터 재검토한다. [명칭 검수](../work/s5-terminology/review.md)를 따른다.

2026-09-21 정정: 개체별 육성에 따라 파티 밖 개체의 박스 보관을 제공한다. 기존 종 등록 기반 소환은 재검토한다. 보관과 숨김은 [파티 표시와 박스 보관](#파티-표시와-박스-보관)을 따른다.
구매한 도구는 가방에 보관한다. 가방에서 사용한다.
별명과 외형만 바꾸는 모습 선택을 제거한다. 연결은 설정 안으로 옮긴다.
화면에서 '무대'를 쓰지 않는다. 숨김 상태는 이미지 우측 위의 작은 닫힌 몬스터볼로 나타낸다. 표시 조작은 상세의 숨기기·다시 표시를 사용한다.
이로치 명칭과 색상 변경 약은 후속 사용자 결정으로 채택했다. 아래 이로치 계약을 따른다. 실행 환경 명칭은 명칭 사전의 제안을 따른다.
용어의 의미와 적용 상태는 [용어사전](../terms.md)을 따른다. 정리 근거는 [명칭 검수](../work/s5-terminology/review.md)에 있다.
도감은 전체 종을 다룬다. 지방 선택을 제공한다. 무료 밥 주기는 유지한다.
2026-09-21 정정: 먹이 판매 제거 방향은 아래의 기본먹이·프리미엄먹이 계약으로 대체한다.

기존 문서에 기록된 설계와 제안:

- 과거 설계는 박스 없이 도감에서 등록된 종을 소환하는 방식이었다. 후속 개체별 육성과 박스 보관 결정으로 재검토한다.
- 진화의돌은 2026-09-22 사용자 결정으로 채택했다. 아래 상세와 진화 계약을 따른다.
- 진화 전 종도 도감 등록을 유지한다. 등록되어 있으면 다시 소환할 수 있다.
- 후속 사용자 결정으로 랜덤알 구매와 부화를 첫 구매 흐름에 채택했다. 작업 부화 방식은 여전히 미정이다.

단일 진실 공급원(SSOT)은 다음과 같다.
이 문서는 상세 계약의 SSOT다. `design.md`는 결정의 SSOT다. `terms.md`는 명칭의 SSOT다. `progress.md`는 상태의 SSOT다.
기존 S4 기능은 아직 그대로 동작한다. 이 문서는 구현 완료 기록이 아니다.

### 일상 사용 계약

출처: 2026-09-21 사용자 답변. 직접 발언과 정정은 [놀이공간·돌봄·재개 결정](../work/s5-design-system-v2/plan.md#놀이공간-돌봄-재개-결정)에 있다. 기능 구현은 미시작이다.

| 항목 | 사용자 확인 방향 | 미정 항목 |
|---|---|---|
| 놀이공간 | 처음에는 화면 전체에서 시작한다. 사용자가 영역을 지정하는 옵션을 제공한다. | 지정 단위, 다중 화면, 화면 구성 변경 시 복원 |
| 첫 포켓몬 | 별도로 지정한 첫 선택 후보 안에서 원하는 진화 전 포켓몬을 자유롭게 고른다. | 실제 후보 종, 이후 입수처와의 관계, 진화 정보 표시 |
| 이후 획득 | 랜덤·구매·해금 경로로 설계한다. | 경로별 조건, 대상, 확률, 가격, 해금과 지급의 관계 |
| 튜토리얼 | 밥 주기와 놀아주기를 직접 해본다. 닫기와 스킵 후 해당 튜토리얼을 다시 표시하지 않는다. 다시보기 대신 설정의 가이드북을 제공한다. 놀이공간 변경은 화면 설명으로 알려준다. | 튜토리얼 시점, 단계 구성, 가이드북 문구 |
| 영역 지정 | 사용자는 드래그로 지정하는 방식을 제안했다. | 확정·취소 조작, 최소 크기, 경계에서의 행동 |
| 직접 돌봄 | 클릭 한 번으로 반응을 구경한다. 원하면 더 놀아주며 다양한 반응을 본다. 10초 안에 이어 놀아주며 약 30초 유지하면 오래 놀아주기 버프가 켜진다. 장난감 놀이는 같은 반응에 파티클 같은 효과를 더한다. | 구체적인 반응, 두 버프의 배율·지속시간, 장난감 입수처·가격 |
| 기본먹이 | 무료로 무제한 제공한다. 만복도를 일정량 회복하되 회복 효율은 매우 낮게 설정한다. 만복도가 최대이거나 밥 주기 쿨타임 중이면 사용할 수 없다. | 실제 회복량, 쿨타임 길이, 만복도 감소 속도와의 관계 |
| 프리미엄먹이 | 상점에서 구매한다. 대상의 만복도를 최대까지 채운다. 그 개체의 친밀도 증가량을 일정 시간 높인다. 같은 먹이를 다시 사용하면 지속시간을 갱신한다. | 가격, 지속시간, 적용 획득 경로, 서로 다른 버프 관계, 디버프와의 계산 순서 |
| 종료와 재개 | PC 잠금·절전·앱 종료 중에는 게임 시간을 멈춘다. 복귀하면 보존한 상태에서 이어간다. 관리 창 닫기·포켓몬 숨김은 계속 진행한다. | 플랫폼 감지, 저장·복원 구현 |

기본먹이의 제공 방식과 효율은 [사용자 결정](../work/s5-design-system-v2/plan.md#기본먹이-무제한-제공과-낮은-회복-효율)을 따른다.
쿨타임을 지키며 기본먹이를 여러 번 주면 배고픔에서 서서히 회복할 수 있다. 자연적인 만복도 감소를 포함해도 배고픔 구간에서 벗어날 수 있도록 수치를 정한다. 근거는 [기본먹이만으로 점진적 회복](../work/s5-design-system-v2/plan.md#기본먹이만으로-점진적-회복)을 따른다.
`[스펙 미확정]` 실제 회복량, 쿨타임, 자연 감소 속도, 필요한 돌봄 횟수와 간격은 후속 밸런스 설계에서 정한다. 기본먹이만으로 최대 만복도까지 도달 가능한지는 아직 정하지 않았다.
`규토리`는 프리미엄먹이의 이름 후보다. `1.1배`는 효과 예시다. 둘 다 확정 값으로 사용하지 않는다.
프리미엄먹이 버프는 친밀도 증가량에 적용한다. 이미 쌓인 친밀도에 배율을 곱하지 않는다.
먹이 사용은 만복도를 기준으로 한다. 만복도가 최대이면 기본먹이와 프리미엄먹이를 모두 사용할 수 없다. 이때 먹이를 소비하거나 버프만 갱신하지 않는다.
기본먹이는 만복도를 일정량 회복한다. 밥 주기 자체에 쿨타임을 적용한다. 쿨타임 중에는 먹이를 사용할 수 없다. 사용을 거부하면 먹이와 만복도·버프를 바꾸지 않는다. 근거는 [기본먹이 회복과 쿨타임](../work/s5-design-system-v2/plan.md#기본먹이-일정량-회복과-밥-주기-쿨타임)을 따른다.
쿨타임은 개체별로 적용한다. 같은 개체의 기본먹이와 프리미엄먹이가 쿨타임을 공유한다. 다른 개체의 밥 주기는 막지 않는다. 근거는 [개체별 먹이 공통 쿨타임](../work/s5-design-system-v2/plan.md#개체별-먹이-공통-쿨타임)을 따른다.
먹이 쿨타임은 [실행 상태별 시간](#실행-상태별-시간)을 따른다. 박스 보관 중에는 남은 시간을 보존한다. 파티로 복귀하면 보존한 시간부터 이어간다.
`[스펙 미확정]` 실제 대기시간과 성공 시 쿨타임 시작 시점은 별도로 정한다. 사용 실패 시에는 거래 원칙에 따라 기존 상태를 유지한다.
프리미엄먹이를 사용하면 대상 개체의 만복도를 최대까지 채운다. 같은 프리미엄먹이의 버프가 남아 있으면 해당 먹이의 기본 지속시간으로 갱신한다. 이전 잔여 시간을 더하지 않는다. 효과 배율을 누적하지 않는다.
만복도는 높을수록 배부른 수치다. 이전 배고픔 표현과 같은 돌봄 상태를 다루며 별도 자원을 추가하는 것은 아니다. 기존 시간 진행·정지 원칙을 그대로 적용한다. 실제 상한·감소 속도와 기존 런타임 저장 변환은 미정이다.
근거는 [만복도와 프리미엄먹이 버프 갱신](../work/s5-design-system-v2/plan.md#만복도와-프리미엄먹이-버프-갱신)을 따른다.
PC 잠금·절전·앱 종료 중에는 배고픔과 친밀도가 변하지 않는다. 포인트 적립과 버프 시간 및 알 준비 시간도 멈춘다. 남은 시간과 적립 중인 진행량을 유지한다. 복귀할 때 중단 기간을 소급 진행하지 않는다.
기존 S4 먹이 재고와 효과의 이식은 별도로 설계한다. 기존 재고를 삭제하지 않는다.
2026-09-22 결정: 오래 놀아주기 버프와 장난감을 채택한다. 근거는 [오래 놀아주기와 장난감](../work/s5-design-system-v2/plan.md#오래-놀아주기-버프와-장난감-채택)을 따른다.
놀아주기를 누르면 놀이가 시작된다. 마지막 놀아주기 후 10초 안에 다시 누르면 놀이가 이어진다. 놀이가 약 30초 이어지면 오래 놀아주기 버프를 활성화한다. 짧은 시간에 빠르게 여러 번 누르는 것은 조건이 아니다. 10초 안에 다시 누르지 않으면 놀이가 끊긴다.
장난감으로 놀아줄 때의 반응 애니메이션은 일반 놀아주기 반응과 같다. 파티클 같은 효과만 추가로 표시한다.
오래 놀아주기 버프는 프리미엄먹이 버프와 같은 방식으로 놀아준 한 마리의 친밀도 증가량을 일정 시간 높인다. 효과는 프리미엄먹이 버프보다 낮다. 버프가 남아 있을 때 다시 달성하면 기본 지속시간으로 갱신한다. 남은 시간을 더하지 않는다.
장난감은 1회성이다. 장난감으로 한 번 놀아주면 1개를 소비한다. 장난감이 없어도 일반 놀아주기는 쓸 수 있다. 장난감은 가방에 보관한다. 가방에서 장난감을 드래그해 포켓몬 위에 드롭하면 사용한다. 장난감을 사용하면 오래 놀아주기 버프와 같은 버프를 준다. 버프가 남아 있으면 기본 지속시간으로 갱신한다. 드롭을 취소하거나 포켓몬이 아닌 곳에 놓으면 장난감을 소비하지 않는다. 근거는 [장난감의 드래그 사용](../work/s5-design-system-v2/plan.md#장난감의-드래그-사용과-같은-버프)을 따른다. 근거는 [버프 효과와 1회성 장난감](../work/s5-design-system-v2/plan.md#오래-놀아주기-버프-효과와-1회성-장난감)을 따른다.
`[스펙 미확정]` 두 버프의 실제 배율과 지속시간, 프리미엄먹이 버프와 동시에 있을 때의 계산, 장난감의 입수처·가격, 드롭 대상 화면, 숨긴 개체와 박스 개체에게 사용할 수 있는지는 정하지 않았다.
튜토리얼과 놀이 후보의 출처는 [첫 사용과 추가 놀이](../work/s5-design-system-v2/plan.md#첫-사용과-추가-놀이)를 따른다.
튜토리얼 참여 방식의 채택 근거는 [튜토리얼 결정과 첫 포켓몬 비교](../work/s5-design-system-v2/plan.md#튜토리얼-결정과-첫-포켓몬-비교)를 따른다. 튜토리얼 완료 조건으로 실제 놀이공간 변경을 요구하지 않는다.
첫 포켓몬과 이후 획득의 근거는 [첫 포켓몬과 이후 획득](../work/s5-design-system-v2/plan.md#첫-포켓몬과-이후-획득)을 따른다. 랜덤알 구매는 아래의 첫 구매 흐름에서 후속 채택했다. 작업 부화 방식은 확정하지 않았다.
첫 선택 후보의 후속 결정은 [첫 선택 후보 목록](../work/s5-design-system-v2/plan.md#첫-선택-후보-목록)을 따른다. 2026-09-23 결정: 후보는 `data/unlocks.json`에 `starter: true`로 표시한 29종이다. 역대 스타터 27종과 피카츄, 이브이다. 초기 알 후보도 같은 29종이다. 알 행동 조건은 인기 종과 600족 계열의 진화 전 종에 붙인다. 그 종들은 일반 무작위 후보에 넣지 않는다. 근거는 [콘텐츠 데이터 결정](../work/s5-design-system-v2/plan.md#콘텐츠-데이터-결정)을 따른다. 후보 목록은 일반 상점과 랜덤알 대상 목록과 별도로 지정한다. 구체적인 종 배정은 아직 정하지 않았다.

### 배고픔 상태 표시

화면에 보이는 포켓몬은 배고픔 알림 구간에서 작은 말풍선이나 행동으로 표현한다. 숨겨진 포켓몬의 배고픔은 파티창의 상태 표시로만 확인한다. 배고픔 때문에 숨김을 해제하거나 창을 자동으로 열지 않는다. 숨김 상태에서도 만복도는 기존 시간 규칙대로 감소한다.
근거는 [숨김 개체의 배고픔 상태 표시](../work/s5-design-system-v2/plan.md#숨김-개체의-배고픔-상태-표시)를 따른다.
보이는 포켓몬이 배고픔 알림 구간에 진입하면 말풍선을 한 번 표시한다. 같은 구간에 머무르면 말풍선을 반복하지 않는다. 이후에는 가끔 배고픈 행동을 한다. 더 심한 구간에 진입하면 말풍선을 다시 한 번 표시한다. 근거는 [배고픔 표현 반복 정책](../work/s5-design-system-v2/plan.md#배고픔-말풍선과-행동의-반복-정책)을 따른다.
`[스펙 미확정]` 행동 간격, 말풍선 표시 시간, 여러 개체의 동시 표현은 후속 설계에서 정한다. 회복 후 재진입과 숨김 해제·재실행 때의 알림 이력 처리도 미정이다. 부화·진화·업적 알림에는 이 결정을 자동 적용하지 않는다.

### 알림 배너의 개별 표시

부화 준비 완료는 알별로 별도 배너를 만든다. 진화 가능은 개체별로 별도 배너를 만든다. 업적 달성은 업적별로 별도 배너를 만든다. 같은 종류도 개수 요약으로 묶지 않는다. 배너는 한 번에 하나씩 순차 표시한다. 다른 배너가 표시 중이면 새 알림은 대기한다.
세 배너는 모두 제목, 대상 표시와 이름, `바로가기` 버튼으로 구성한다. 부화 배너의 `바로가기`는 돌보미집을 연다. 진화 배너의 `바로가기`는 해당 개체의 상세 화면을 연다. 부화 배너는 결과 포켓몬을, 진화 배너는 결과 종을 보여주지 않는다. 배너 본문 클릭 동작은 두지 않는다. 근거는 [빈 화면 제안 검토](../work/s5-design-system-v2/plan.md#빈-화면-제안-검토)를 따른다. 업적 배너의 `바로가기`를 누르면 해당 업적의 보상 수령 화면을 연다. 업적 배너는 `업적 달성` 제목, 업적 표시와 이름, `바로가기` 버튼으로 구성한다. 근거는 [업적 배너의 바로가기](../work/s5-design-system-v2/plan.md#업적-배너의-바로가기와-업적-안내-제거)를 따른다. 업적 배너는 화면 우측 아래에 한 번 표시한다. 미수령 보상이 있으면 헤더의 업적창 아이콘에 dot를 표시한다. 근거는 [우측 아래 표시와 dot](../work/s5-design-system-v2/plan.md#업적-알림의-우측-아래-표시와-업적창-dot)를 따른다. 각 행동은 이동 후 사용자가 직접 실행한다.
근거는 [개별 표시 정정](../work/s5-design-system-v2/plan.md#알림-배너-개별-표시-정정)을 따른다. 배고픔 말풍선은 이 배너 정책의 대상이 아니다.
배너 알림 소리는 기본으로 켠다. 설정에서 무음으로 바꿀 수 있다. 사용하는 OS의 기본 시스템 알림음을 사용한다. 무음 설정에서도 배너 표시와 클릭 이동은 유지한다. 근거는 [알림 소리 정정](../work/s5-design-system-v2/plan.md#기본-알림-소리와-os-시스템-알림음)을 따른다.
같은 부화·진화·업적 상태를 한 번 배너로 표시했으면 미처리 상태여도 반복하지 않는다. 클릭하지 않았어도 다시 표시하지 않는다. 앱 재실행 후에도 이 원칙을 유지한다. 해당 메뉴에서는 부화 가능·진화 가능·업적 미수령 상태를 계속 확인할 수 있다. 다른 대상의 새 알림까지 차단하지 않는다. 근거는 [상태 알림 반복 금지](../work/s5-design-system-v2/plan.md#표시한-상태-알림의-반복-금지)를 따른다.
배너가 기다리는 동안 대상이 더 이상 부화 가능·진화 가능·업적 미수령 상태가 아니면 그 배너를 표시하지 않는다. 대기열에서 제거한다. 아직 표시하지 않은 배너가 있는 상태에서 앱이 종료되면 재실행 후 이어서 표시한다. 대상이 여전히 해당 미처리 상태일 때만 표시한다. 배너를 표시하는 중에 앱이 종료되면 표시한 것으로 처리한다. 배너 폐기는 대상의 게임 상태를 바꾸지 않는다. 근거는 [대기 배너 처리](../work/s5-design-system-v2/plan.md#대기-배너의-무효화와-재실행-처리)를 따른다.
여러 배너가 기다리면 먼저 생긴 배너부터 표시한다. 같은 순간에 생긴 배너의 종류가 다르면 부화 → 진화 → 업적 순서로 표시한다. 같은 종류이면 화면의 목록 순서를 따른다. 부화 배너는 돌보미집 칸 순서를 따른다. 진화 배너는 파티 칸 순서를 먼저 따르고 그다음 박스 순서를 따른다. 업적 배너는 업적창 목록 순서를 따른다. 근거는 [배너 표시 순서](../work/s5-design-system-v2/plan.md#배너-동시-발생의-표시-순서)를 따른다.
`[스펙 미확정]` 표시 시간과 박스 안 정렬 기준은 별도로 정한다. 배너 상태의 저장 구현과 저장 실패 처리도 미정이다. OS별 재생 방법과 지원, OS 무음·집중 모드 연동, 재생 실패 처리, 대기열의 소리 재생 시점은 구현 전에 확인한다.

### 실행 상태별 시간

근거는 [PC 잠금과 절전의 시간 정지](../work/s5-design-system-v2/plan.md#pc-잠금과-절전의-시간-정지)를 따른다. 아래 진행은 기존 파티·박스 규칙 안에서 적용한다.

먹이 쿨타임에도 아래 시간 정책을 적용한다. 근거는 [먹이 쿨타임의 진행과 정지](../work/s5-design-system-v2/plan.md#먹이-쿨타임의-진행과-정지)를 따른다.

| 실행 상태 | 배고픔·친밀도·포인트·버프 시간·알 준비 시간·먹이 쿨타임 |
|---|---|
| 앱 실행 중이며 PC 사용 가능 | 기존 규칙대로 진행 |
| 관리 창만 닫기 | 기존 규칙대로 진행 |
| 포켓몬 숨기기 | 기존 규칙대로 진행 |
| PC 잠금 | 정지. 값과 부분 진행 보존 |
| PC 절전 | 정지. 값과 부분 진행 보존 |
| 앱 종료 | 정지. 값과 부분 진행 보존 |

PC가 사용 가능한 상태이고 앱이 실행 중이면 중단한 진행을 이어간다. 절전 해제 후에도 잠금 상태이면 정지를 유지한다. 이 표는 박스 개체의 성장을 진행시키는 예외가 아니다.
`[스펙 미확정]` 플랫폼별 감지 방식과 저장 단위는 구현 단계에서 정한다. 시간 정책의 승인만으로 플랫폼 동작 검증을 완료한 것으로 표시하지 않는다.

## 2. 화면 구조

탭은 `파티 / 박스 / 도감 / 상점 / 가방`이다. 박스는 파티 바로 다음 메뉴다. 근거는 [박스 메뉴 분리](../work/s5-design-system-v2/plan.md#박스-메뉴-분리)를 따른다. 업적창과 설정은 헤더 아이콘으로 여는 모달이다. 보던 탭 위에 대화상자로 뜨고, 닫으면 보던 탭으로 돌아간다. 근거는 [업적창과 설정의 모달](../work/s5-design-system-v2/plan.md#업적창과-설정의-모달)을 따른다. 미수령 보상이 있으면 업적창 아이콘에 dot를 표시한다. 2026-09-22 결정이다. Figma 내비게이션의 Active 5종은 이전 구성이며 수정 대상이다. 근거는 [관리 창 구성](../work/s5-design-system-v2/plan.md#관리-창의-탭과-헤더-구성)을 따른다.
상단에는 보유 포인트를 표시한다. 흰 헤더·카드와 `#F1F2EE` 본문, `#0F766E` 강조색을 사용한다. `PB/Type/*`의 Inter 스타일을 사용한다.
Figma Screen은 720×780px다. 상세 Template은 전체 콘텐츠를 펼친 720×1030px다.
[스펙 미확정] Electron 창 기본·최소 크기와 크기 변경 정책은 구현 전에 확정한다. Figma px를 DIP로 그대로 확정하지 않는다.
[스펙 미확정] 작업 결과의 공통 표시 위치는 정하지 않았다. 현재 Template의 footer-space는 비어 있다. 상태별 안내와 오류 모달은 별도로 검수한다.

| 화면 | 구성 | 주요 동작 |
|---|---|---|
| 파티 | 2열×3행. 포켓몬·빈 칸·잠긴 칸 | 상세, 숨김 상태 확인, 박스 개체와 교체 |
| 박스 | 돌보미집 알 6칸, 검색 줄, 30칸 박스와 박스 넘김 | 개체 상세, 파티에 배치·교체, 알 돌봄과 직접 부화 |
| 상세 | 종 이름, 성격, 친밀도, 기분, 배고픔, 크기 | 돌봄, 진화, 교체, 박스에 보관, 가방. 박스 개체는 파티에 배치·교체 |
| 도감 | 지방·등록 상태 선택, 검색, 목록, 상세 | 등록된 종 소환·교체, 획득 방법과 진화 조건 확인 |
| 상점 | [스펙 미확정] 기존 분류 제안: 포켓몬 / 알 / 도구 / 파티 칸 | 상품 구성 재검토 |
| 가방 | 도구, 수량과 설명 | 대상 선택 후 사용 |
| 업적창 (헤더 아이콘, 모달) | 업적 목록, 달성·미수령·수령 완료 상태 | 보상 받기. 닫기. 미수령이 있으면 아이콘에 dot |
| 설정 (헤더 아이콘, 모달) | 일반 / 연결. 모달 안에서 스크롤 | 표시, 동작, 언어, 시작, CLI별 연결. 닫기 |

Tab과 방향키로 탭을 이동한다. Enter로 선택한다. Escape로 상세에서 돌아간다.
아이콘마다 툴팁과 접근성 이름을 제공한다. 색상만으로 상태를 구분하지 않는다.
상세 본문은 638px 높이에서 세로 스크롤한다. 헤더와 탭은 유지한다. 현재 05에는 스크롤 연결이 없으므로 구현 완료로 취급하지 않는다.
도감·상점·가방·설정 본문은 기능 계획이다. 현재 v2 Figma의 여섯 화면은 파티와 상세 상태에 한정된다.

## 3. 도감 등록과 파티 소환

2026-09-21 후속 방향: 친밀도는 개체별로 관리한다. 아래의 종 등록 기반 소환과 종별 기록 제안은 재검토 대상이다. 새 개체 모델의 확정 계약으로 사용하지 않는다.
도감 해금에는 개별 포켓몬의 레벨을 사용한다. 해금 후의 입수처는 종별로 구분한다. 근거는 [개체 레벨과 종별 입수처](../work/s5-design-system-v2/plan.md#개체-레벨과-종별-입수처)를 따른다.

### 개체 레벨과 입수처

2026-09-21 후속 결정: 첫 선택 후보는 모두 초기 도감에 해금한다. 실제로 지급하는 첫 개체는 사용자가 선택한 한 마리다. 초기 랜덤알 후보는 첫 선택 후보 중 알 입수가 허용된 종이다. 종별 전용 입수처를 유지한다.
시작 포인트는 랜덤알 하나를 구매할 수 있는 금액이다. 시작 포인트의 사용처는 제한하지 않는다. 실제 종 목록·확률·가격은 미정이다. 근거는 [초기 해금과 첫 알 구매](../work/s5-design-system-v2/plan.md#초기-해금과-첫-알-구매)를 따른다.
데이터를 정할 때 초기 알 후보가 하나 이상인지 확인한다. 이후 새로 해금한 종에는 기존 해금·입수처 계약을 적용한다. 초기 후보 규칙을 영구적인 알 결과 제한으로 사용하지 않는다.

| 구분 | 해금 후 가능한 행동 | 실제 획득 |
|---|---|---|
| 상점 구매 종 | 상점 구매 항목에 추가된다. | 사용자가 구매한다. |
| 진화 전용 종 | 조건을 충족한 개체에서 진화를 선택한다. | 사용자가 해당 개체를 진화시킨다. 상점에서 구매하지 않는다. |
| 랜덤알 | 해금한 종 중 알 입수처가 허용된 종을 대상으로 한다. | 직접 부화해서 획득한다. 진화 전용 종은 제외한다. |

2026-09-21 후속 결정: 입수처는 종마다 지정한다. 구매·알 양쪽에서 입수 가능한 종 외에 알 전용 종과 특수 기믹 전용 종도 둔다. 모든 종을 일반 구매나 알에 포함하지 않는다.
알 전용 종은 상점에서 종을 지정해 구매하지 않는다. 특수 기믹 전용 종은 알에서도 나오지 않는다. 진화 전용 계약은 유지한다.
랜덤알은 더 저렴한 무작위 획득 경로다. 종 지정 구매는 더 비싼 확정 획득 경로다. 가격과 종별 예외는 아직 정하지 않았다.
근거는 [종별 입수처 지정](../work/s5-design-system-v2/plan.md#종별-입수처-지정)을 따른다.
`[스펙 미확정]` 종별 계약은 해금 조건, 허용 입수처, 입수처별 실행 조건, 안내 문구를 분리하는 안을 제안한다. 선행 해금 검사를 모든 입수처에 공통 적용하지 않는다.
특수 기믹 획득은 선행 도감 해금을 요구하지 않는다. 해당 기믹을 수행하면 도감 해금과 개체 획득이 함께 일어난다. 근거는 [특수 기믹의 선행 해금 제외](../work/s5-design-system-v2/plan.md#특수-기믹의-선행-해금-제외)를 따른다.
후속 정정: 해금 후의 입수처와 획득 제한도 종별로 지정한다. 기믹으로 처음 얻었어도 이후 상점이나 알에서 얻을 수 있는 종을 둔다. 계속 기믹으로만 얻는 종과 단일 포켓몬도 따로 지정한다. ‘특수 기믹 전용’은 이후에도 다른 입수처를 허용하지 않는 종에만 사용한다. 근거는 [종별 후속 입수처와 조건부 알 후보](../work/s5-design-system-v2/plan.md#종별-후속-입수처와-조건부-알-후보)를 따른다.
알 전용 종도 도감 해금 수 같은 조건을 만족해야 무작위 획득 후보에 들어가도록 지정할 수 있다. 사용자 예시인 도감 100종 이상 해금은 확정 수치가 아니다. 조건 달성만으로 개체를 지급하지 않는다. 개체 레벨만을 모든 종의 해금·입수 조건으로 사용하지 않는다.
단일 포켓몬으로 지정한 종은 해당 저장에서 딱 한 번만 획득할 수 있다. 현재 보유 수가 아닌 획득 이력을 기준으로 한다. 여러 입수처를 허용해도 종별 총 획득 1회 제한은 공유한다. 숨김·박스 보관·재배치로 제한을 초기화하지 않는다.
단일 포켓몬은 알에서 획득할 수 없다. 알별 후보 범위와 행동 조건에 따른 부화 결과에서 모두 제외한다. 근거는 [단일 포켓몬의 알 입수 제외](../work/s5-design-system-v2/plan.md#단일-포켓몬의-알-입수-제외)를 따른다.
후속 진화 예외: 전설·환상·준전설 등 지정한 진화 계열은 같은 `sid`의 한 개체로 관리한다. 별도 개체를 추가 생성하지 않는다. 친밀도·레벨·경험치·만복도·버프·쿨타임 등 진행 상태를 공유한다. 선택 가능한 종 중 하나만 꺼낼 수 있다. 베베놈을 꺼낸 동안 아고용을 동시에 꺼낼 수 없다. 스탯은 현재 선택한 종의 기준을 적용한다. 단일 포켓몬 전용 재획득 불가 경고는 추가하지 않는다. 근거는 [공유 sid 정정](../work/s5-design-system-v2/plan.md#단일-포켓몬-진화-계열의-공유-sid-정정)을 따른다.
종을 바꿔도 진행 상태를 초기화하거나 과거 상태로 되돌리지 않는다. 같은 `sid`의 시간 진행과 포인트를 중복 계산하지 않는다. 도감의 종 항목과 개체 ID는 구분한다. 일반 진화는 기존 철회 불가 규칙을 유지한다.
해당 계열의 종 선택은 박스에서 교체하는 흐름을 사용한다. 상세 화면의 종 선택 제안은 채택하지 않는다. 박스의 선택 대상을 별도의 독립 개체로 생성하지 않는다. 근거는 [박스 교체 결정](../work/s5-design-system-v2/plan.md#공유-sid-진화-계열의-박스-교체-흐름)을 따른다.
해당 `sid`가 파티에 있으면 박스에서 선택한 종으로 기존 파티 칸을 교체한다. 빈 파티 칸이 있어도 추가 배치하지 않는다. 같은 `sid`는 두 파티 칸을 차지하지 않는다. 교체 후에는 기존 박스 교체 규칙대로 숨김 상태로 둔다. 별도로 꺼내기를 해야 화면에 표시한다. 근거는 [파티 칸 유지 결정](../work/s5-design-system-v2/plan.md#공유-sid-종-교체의-기존-파티-칸-유지)을 따른다.
코스모움을 결과별 두 개체로 따로 얻는 구상은 채택하지 않았다. 기존 공유 개체·동시 제공 규칙을 유지한다. [기존안 유지 결정](../work/s5-design-system-v2/plan.md#코스모움-기존-공유-개체안-유지)을 따른다.
코스모움에서 진화를 한 번 실행하면 솔가레오와 루나아라를 함께 제공한다. 두 종 모두 같은 `sid`의 교체 대상으로 열린다. 다른 한 종을 위해 별도 진화를 요구하지 않는다. 별도 개체를 만들거나 두 종을 동시에 꺼내지 않는다. 일반 분기 진화 전체에 이 제공 방식을 확대하지 않는다. 근거는 [동시 제공 정정](../work/s5-design-system-v2/plan.md#솔가레오와-루나아라의-동시-제공-정정)을 따른다.
진화 실행 시 게임 시간의 낮이면 현재 종은 솔가레오가 된다. 밤이면 루나아라가 된다. 사용자가 진화 결과 종을 따로 선택하지 않는다. 두 종의 동시 제공과 이후 박스 교체는 유지한다. 게임 시간 기능은 향후 구현한다. OS 시간을 대체 기준으로 확정하지 않는다. 네크로즈마는 이 공유 `sid` 계열에 포함하지 않는 별개의 포켓몬이다. 근거는 [게임 시간에 따른 결과](../work/s5-design-system-v2/plan.md#게임-시간에-따른-솔가레오와-루나아라-진화-결과)를 따른다.
두 종을 함께 제공하는 순간 도감에 솔가레오와 루나아라 모두 획득한 것으로 기록한다. 현재 종이나 화면에 꺼낸 이력에 관계없이 두 종을 기록한다. 공유 `sid`는 하나로 유지한다. 박스에서 종을 교체해도 획득 기록을 지우거나 다시 지급하지 않는다. 근거는 [동시 획득 기록](../work/s5-design-system-v2/plan.md#솔가레오와-루나아라의-도감-동시-획득-기록)을 따른다.
2026-09-23 결정: 박스에서 공유 계열의 두 종은 각각 칸으로 보인다. 파티에 나가 있는 종의 칸은 `파티에 있음`이며 고를 수 없다. 두 칸에 공유 표시를 붙이고 박스 사용 칸 수는 1마리로 센다. 근거는 [공유 개체의 박스 표시](../work/s5-design-system-v2/plan.md#공유-개체의-박스-표시-sr-08)를 따른다.
`[스펙 미확정]` 게임 시간의 낮·밤 경계와 진행 속도, 공유 표시의 모양, 다른 대상의 제공 묶음은 후속 설계한다.
전설의 포켓몬과 환상의 포켓몬은 사용자가 든 적용 예시다. 실제 지정 목록은 미정이다. 근거는 [단일 포켓몬의 일회 획득](../work/s5-design-system-v2/plan.md#단일-포켓몬의-일회-획득)을 따른다.
조건부 입수 종은 조건 충족 시 도감에 해금하고 허용된 입수처를 활성화한다. 실제 획득은 구매·부화 등 해당 경로를 이용했을 때 일어난다. 알 전용 종도 조건을 만족하면 도감 해금 후 알 후보에 포함한다. 해금한 종만 알에 포함하는 원칙을 유지한다.
도감 해금 수는 해금된 서로 다른 종의 수다. 초기 해금한 첫 선택 후보와 레벨 조건으로 해금했지만 아직 진화하지 않은 종도 포함한다. 현재 보유 여부와 무관하게 센다. 같은 종의 개체를 여러 마리 획득해도 종 수는 늘지 않는다.
일반 폼과 리전폼은 별도 종으로 취급하는 독립 도감 항목이다. 기본 도감번호가 같아도 집계할 때 합치지 않는다. 일반 대검귀 `503`과 히스이 대검귀 `503-1`은 각자 해금 상태를 관리한다. 둘 다 해금하면 도감 해금 수에 2종으로 센다. 같은 항목의 중복 개체는 추가 집계하지 않는다. 근거는 [리전폼 도감 항목과 집계](../work/s5-design-system-v2/plan.md#리전폼의-독립-도감-항목과-집계)를 따른다.
근거는 [조건 충족에 따른 해금과 입수처 활성화](../work/s5-design-system-v2/plan.md#조건-충족에-따른-해금과-입수처-활성화)를 따른다. 특수 기믹 수행으로 최초 해금과 획득이 함께 일어나는 기존 계약은 유지한다.
`[스펙 미확정]` 실제 조건표의 도달 가능성과 연쇄 해금의 평가 방식은 별도로 검증한다. 구매 후 새로 해금한 종은 이미 구매한 알에 포함하지 않는다. 단일 종의 알 결과 예약은 필요하지 않다.
도감에서 구매·알·진화의 입수 조건은 명확히 표시한다. 특수 기믹은 발견 전에는 힌트만 표시하고 발견 후 정확한 방법을 공개한다. 근거는 [도감 입수 방법 공개](../work/s5-design-system-v2/plan.md#도감-입수-방법-공개)를 따른다.
`[스펙 미확정]` 구체적인 종별 배정, 특수 기믹의 내용, 발견 판정의 세부 시점과 힌트 문구는 정하지 않았다. 반복 수행으로 재획득할 수 있는지도 별도로 정한다.
`[스펙 미확정]` 특수 기믹을 성공적으로 수행해 해당 종을 처음 획득하면 발견으로 기록하는 안을 제안한다. 공개 후에는 보유·파티 상태와 무관하게 방법을 다시 확인할 수 있는 안이다.

사용자 예시는 파이리 레벨 16에서 리자드 해금이다. 리자드는 진화 전용이다. 이 예시를 전체 종의 최종 조건표로 확대하지 않는다.
진화를 선택하지 않으면 현재 개체를 유지한다. 해금됐다는 이유만으로 새 개체를 지급하지 않는다.
친밀도와 경험치는 별도로 관리한다. 현재 재설계의 레벨 성장은 아래의 친밀도·포인트·경험사탕 경로를 따른다.
`[스펙 미확정]` 종별 조건표와 진화 외 추가 조건은 정하지 않았다.
`[스펙 미확정]` 계정의 종 해금 기록과 각 개체의 진화 가능 상태를 분리하는 안을 제안한다. 다른 파이리가 리자드를 해금했더라도 현재 파이리의 진화 조건을 별도로 검사하는 안이다.
같은 종의 여러 개체 보유와 동시 파티 배치를 허용한다. 단일 포켓몬으로 지정한 종에는 위의 총 획득 1회 제한을 적용한다. `[스펙 미확정]` 해금 유지 기간, 구매 후 새 개체의 초기값과 재구매 조건은 정하지 않았다.

### 친밀도와 경험치의 역할

2026-09-21 사용자 결정: 친밀도와 경험치를 분리한다. 친밀도를 통해 포인트를 얻는다. 포인트로 상점에서 경험사탕을 구매한다. 경험사탕으로 경험치를 얻어 레벨을 올린다.
던전 탐험을 추가할 때 경험사탕과 경험치를 얻는 경로를 제공한다. 탐험은 향후 확장이다. 현재 기능 범위에 자동 포함하지 않는다.
출처는 [친밀도와 경험치 분리](../work/s5-design-system-v2/plan.md#친밀도와-경험치-분리)를 따른다.

| 값 또는 상품 | 역할 | 미정 계약 |
|---|---|---|
| 친밀도 | 개체와 함께한 시간과 돌봄으로 쌓는다. 현재 값이 높을수록 포인트 적립 간격이 짧아진다. 포인트 적립으로 소모하지 않는다. | 적립 간격 계산식 |
| 포인트 | 파티 개체가 각자의 친밀도에 따라 적립한 결과를 합산한다. 경험사탕 등 상점 상품을 구매한다. | 부분 진행 저장 구현, 기존 에이전트 보상과의 관계 |
| 경험사탕 | XS·S·M·L·XL 5종이다. 포인트로 구매해 파티와 박스의 보유 개체를 성장시킨다. 한 대상과 사용 수량을 선택하고 결과를 미리 확인한다. | 가격, 경험치 지급량, 화면 구성 |
| 이상한사탕 | 사용한 개체의 레벨을 1 올린다. 새 레벨의 경험치 진행은 0부터 시작한다. 100레벨에서는 사용할 수 없고 소비하지 않는다. | 가격, 개별 보상표, 수량 조작 화면 |
| 경험치 | 친밀도와 별도로 개체의 레벨을 최대 100까지 올린다. 진화 후에도 레벨과 누적 경험치를 유지한다. | 레벨별 필요량, 종별 곡선 호환, 향후 획득 경로의 초과 처리 |

기존 배고픔 디버프와 프리미엄먹이 버프의 확정 대상은 친밀도 증가량이다. 경험사탕의 경험치에도 적용하는 것으로 확대하지 않는다.
경험사탕 XS·S·M·L·XL과 이상한사탕의 공통 입수 경로는 상점 구매, 업적 보상, 탐험 보상이다. 탐험은 향후 확장으로 유지한다. 실제 가격과 업적·탐험별 보상 종류 및 수량은 미정이다. 모든 업적과 탐험이 사탕 전종을 지급하는 뜻이 아니다. 근거는 [공통 입수 경로 결정](../work/s5-design-system-v2/plan.md#사탕-공통-입수-경로)을 따른다.
사탕 6종은 처음부터 상점에서 판매한다. 별도 해금 조건을 두지 않는다. 가격으로 구매 시점을 조절한다. 근거는 [초기 상점 판매 결정](../work/s5-design-system-v2/plan.md#사탕-전종의-초기-상점-판매)을 따른다.
사탕 구성의 근거는 [사용자 결정과 이미지 조사](../work/s5-design-system-v2/plan.md#경험사탕-5종과-이상한사탕-및-이미지-조사)를 따른다. 공개 자료의 효과량과 가격은 게임의 확정 수치가 아니다.
이상한사탕은 한 개체와 사용 수량을 선택한다. 예상 레벨을 확인한 뒤 여러 개를 한 번에 사용할 수 있다. 최대 선택 수량은 보유량과 `100 - 현재 레벨` 중 작은 값이다. 사용 후 레벨 내 경험치 진행은 0이다. 미리보기만으로 개체 상태와 재고를 바꾸지 않는다. 근거는 [수량 선택 결정](../work/s5-design-system-v2/plan.md#이상한사탕-수량-선택과-세션-인계)을 따른다.
이상한사탕은 파티와 박스의 보유 개체 모두에게 사용할 수 있다. 숨김 여부로 제한하지 않는다. 파티가 비어 있어도 박스 개체에게 사용할 수 있다. 사용으로 파티·박스 위치와 숨김 상태를 바꾸지 않는다. 박스의 시간에 따른 성장 정지는 유지한다. 근거는 [대상 범위 결정](../work/s5-design-system-v2/plan.md#이상한사탕의-파티와-박스-대상-허용)을 따른다.
이상한사탕은 사용 전 경험치 진행을 새 레벨로 이월하지 않는다. 누적 경험치로 저장한다면 새 레벨의 시작 값에 맞춘다. 누적 경험치 전체를 0으로 만들지 않는다. 대상이 100레벨이면 사용을 거부하고 재고를 유지한다. 근거는 [상한 사용 결정](../work/s5-design-system-v2/plan.md#이상한사탕의-100레벨-사용-차단)을 따른다. 근거는 [사용자 정정](../work/s5-design-system-v2/plan.md#이상한사탕의-다음-레벨-경험치-초기화)을 따른다.
개체의 최대 레벨은 100이다. 레벨 100에 도달해도 친밀도 성장과 포인트 적립은 기존 규칙대로 계속한다. 박스 보관과 PC 잠금·절전·앱 종료의 정지 규칙은 유지한다. 근거는 [개체 레벨 상한](../work/s5-design-system-v2/plan.md#개체-레벨-상한-100)을 따른다.
2026-09-21 사용자 정정: 포인트는 현재 친밀도에 따른 시간 간격으로 적립한다. 친밀도 증가량에 비례해 일회성 지급하는 제안은 채택하지 않는다. 친밀도는 소모하지 않는다.
친밀도 100에서 100초마다 1포인트, 90에서 110초마다 1포인트는 사용자 예시다. 두 값만으로 전체 계산식을 정하지 않는다.
PC 잠금·절전·앱 종료 중에는 적립하지 않는다. 적립 중인 진행량은 보존하고 복귀 후 이어간다.
친밀도가 올라 적립 간격이 바뀌면 기존 진행 비율을 유지한다. 남은 부분부터 새 속도로 적립한다. 이전 경과 시간 전체를 새 속도로 다시 계산하지 않는다. 근거는 [진행 비율 유지](../work/s5-design-system-v2/plan.md#친밀도-변화-시-포인트-진행-비율-유지)를 따른다.
예시: 50% 진행 상태에서 간격이 100초에서 80초로 바뀌면 남은 시간은 40초다. 추가 친밀도 변화가 없을 때의 설명용 수치다. 실제 적립 간격은 미정이다.
`[스펙 미확정]` 진행 비율의 저장 정밀도, 반올림, 지급과 상태 변경이 같은 시점인 경우의 처리, 저장 실패 복원은 별도로 정한다.
근거는 [현재 친밀도에 따른 포인트 적립](../work/s5-design-system-v2/plan.md#현재-친밀도에-따른-포인트-적립)을 따른다.
후속 결정: 파티에 배치한 개체만 각자 적립한다. 숨겨도 파티 배치를 유지하면 적립한다. 파티 밖의 개체는 적립하지 않는다. 근거는 [파티 개체의 포인트 적립](../work/s5-design-system-v2/plan.md#파티-개체의-포인트-적립)을 따른다.
박스 보관 시 개체별 포인트 부분 진행을 보존한다. 박스에서는 진행하지 않는다. 파티 복귀 후 보존한 지점부터 이어간다. 숨김 상태여도 적립을 재개한다. 맞바꾸기에서도 진행은 각 개체에 귀속한다. 파티 칸의 이전 개체 진행을 새 개체에 복사하지 않는다. 근거는 [포인트 부분 진행 보존](../work/s5-design-system-v2/plan.md#박스-보관-시-포인트-부분-진행-보존)을 따른다.
경험사탕은 대상 한 개체를 선택한 뒤 사용 수량을 지정한다. 여러 개를 한 번에 사용할 수 있다. 사용 전에 획득 경험치와 예상 레벨을 보여준다. 사용자가 확인한 뒤 사용한다. 미리보기만으로 재고와 경험치를 바꾸지 않는다. 근거는 [수량 선택과 결과 미리보기](../work/s5-design-system-v2/plan.md#경험사탕-수량-선택과-결과-미리보기)를 따른다.
100레벨인 개체에는 경험사탕을 사용할 수 없다. 사용을 거부할 때는 사탕을 소비하지 않는다. 100레벨 미만이면 선택한 사탕으로 상한에 도달하는 데 필요한 최소 개수까지만 선택할 수 있다. 마지막 사탕에서 상한을 넘는 경험치는 소멸한다. 사용 전에 실제 적용 경험치와 소멸할 초과 경험치를 구분해서 보여준다. 초과 경험치는 이후를 위해 저장하지 않는다. 근거는 [경험사탕 상한 처리](../work/s5-design-system-v2/plan.md#경험사탕-상한-사용과-초과-경험치)를 따른다.
경험사탕 5종과 이상한사탕은 획득 경로와 관계없이 공통 가방에 종류별로 보관한다. 사용자는 가방에서 사탕을 선택한다. 이어서 대상 개체와 수량을 선택한다. 결과를 확인한 뒤 직접 사용한다. 획득만으로 사탕을 자동 사용하지 않는다. 근거는 [가방 보관과 직접 사용 결정](../work/s5-design-system-v2/plan.md#사탕의-공통-가방-보관과-직접-사용)을 따른다.
가방은 칸 수와 종류별 보유 수량을 제한하지 않는다. 같은 종류의 사탕은 수량을 합산한다. 한 번에 사용할 수 있는 수량은 보유량과 기존 레벨 상한 규칙을 따른다. 돌보미집의 알 최대 6개 제한은 유지한다. 근거는 [가방 보관 결정](../work/s5-design-system-v2/plan.md#가방-보관-제한-없음)을 따른다.
`[스펙 미확정]` 큰 수량의 표시·저장 방식, 수량 조작 UI, 재고 검증, 사용 직전 상태 변경과 실패 처리는 별도로 정한다.
경험사탕의 대상은 파티와 박스의 보유 개체다. 파티에서 숨긴 개체에도 사용할 수 있다. 박스에서 파티로 옮기거나 화면에 표시할 필요가 없다. 사용으로 배치·표시 상태를 바꾸지 않는다. 박스의 시간에 따른 성장 정지는 유지하지만 직접 사탕을 주면 경험치와 레벨이 증가할 수 있다. 파티가 비어 있어도 박스 대상에 사용할 수 있다. 근거는 [경험사탕의 대상 범위](../work/s5-design-system-v2/plan.md#경험사탕의-파티와-박스-대상-허용)를 따른다.

### 파티 표시와 박스 보관

2026-09-21 사용자 정정: 파티에서 빠진 상태는 박스 보관을 뜻한다. 숨긴 포켓몬은 파티에 남는다. 숨김 상태에서도 배고픔 등 상태 변화가 계속된다.
근거는 [숨김과 박스 보관의 구분](../work/s5-design-system-v2/plan.md#숨김과-박스-보관의-구분)을 따른다.

| 상태 | 포인트 적립 | 시간에 따른 친밀도 성장 | 배고픔 변화 | 남은 버프 시간 |
|---|---|---|---|---|
| 파티에 배치하고 표시 | 개체별로 적립 | 진행 | 진행 | 경과 |
| 파티에 배치하고 숨김 | 표시 상태와 동일 | 표시 상태와 동일 | 표시 상태와 동일 | 경과 |
| 박스 보관 | 중단 | 중단. 기존 값 보존 | 중단. 기존 값 보존 | 보존 |

앱 종료 중에는 기존 상태 유지 원칙을 따른다. 박스 보관에서 파티로 돌아오면 같은 개체의 보존한 상태에서 이어간다.
숨김은 파티 칸을 비우지 않는다. 박스 보관은 파티 배치를 해제한다. 화면의 숨김 마커가 닫힌 몬스터볼이어도 보관 상태를 뜻하지 않는다.
표시 중인 개체가 없어도 트레이에서 관리 화면을 열고 파티로 이동할 수 있다. 모두 숨겼으면 파티에서 원하는 개체를 다시 표시한다. 파티가 비었으면 박스에서 보유 개체를 선택해 배치한다. 개체를 클릭해야만 복귀할 수 있는 구조로 만들지 않는다.
근거는 [표시 개체가 없을 때 복귀](../work/s5-design-system-v2/plan.md#표시-개체가-없을-때-복귀)를 따른다. 트레이가 관리 화면의 마지막 탭을 여는 기존 계약은 유지한다. 파티로의 이동은 사용자가 선택한다.
박스 보관으로 친밀도·배고픔을 초기화하거나 버프를 새로 지급하지 않는다. 숨김만으로 디버프를 해제하거나 버프 시간을 정지하지 않는다.
박스에는 게임 규칙상 보유 개체 수 제한을 두지 않는다. 포인트 부분 진행은 보존한다.
박스는 원작처럼 30칸(6열×5행) 박스 여러 개로 나눈다. `◀ 박스 1 ▶`처럼 넘겨 보며 각 박스는 이름과 사용 칸 수를 보여준다. 박스가 모두 차면 새 박스를 자동으로 추가한다. 검색은 모든 박스를 대상으로 하며 결과를 누르면 그 박스로 이동한다. 박스 탭, 박스 개체 고르기, 가방 사용 패널의 박스 목록은 같은 검색 줄을 쓴다. 근거는 [원작식 박스 구조](../work/s5-design-system-v2/plan.md#원작식-박스-구조와-검색)를 따른다.
`[스펙 미확정]` 새 개체가 들어갈 칸의 순서, 박스 이름 변경, 박스 안 이동과 정렬 기준, 부분 진행의 저장 구현은 정하지 않았다. 파티 교체는 아래 계약을 따른다.

### 개체 획득과 파티 교체

2026-09-21 사용자 결정: 같은 종을 여러 개체 보유할 수 있다. 같은 종을 동시에 파티에 배치할 수 있다.
새 개체를 얻으면 사용 가능한 빈 파티 칸에 바로 배치한다. 파티가 가득 차면 박스로 보낸다.
첫 포켓몬 이후 새 개체가 파티에 들어오면 숨김 상태로 배치한다. 구매·부화·특수 획득에 동일하게 적용한다. 사용자가 직접 표시하기 전에는 놀이공간에 나타나지 않는다. 숨김 중의 성장·만복도·적립은 기존 파티 시간 규칙을 따른다. 근거는 [새 개체의 숨김 배치](../work/s5-design-system-v2/plan.md#새-개체-획득-시-숨김-배치)를 따른다.
파티에서 교체할 개체와 박스의 개체를 선택하면 한 번에 맞바꾼다. 먼저 빈 칸을 만드는 조작을 요구하지 않는다.
파티 개체의 상세에는 `교체`와 나란히 `박스에 보관`을 둔다. 누르면 확인 창을 열고, 확인하면 파티 칸이 비고 개체는 박스에 들어간다. 근거는 [빈 화면 제안 검토](../work/s5-design-system-v2/plan.md#빈-화면-제안-검토)를 따른다. 박스 개체의 상세에서 빈 칸이 있으면 `파티에 배치`, 없으면 `교체`를 제공한다. `교체`는 파티 칸 고르기로 이어진다. 파티 개체의 상세에서 `교체`를 누르거나 빈 파티 칸을 누르면 박스 개체 고르기로 이어진다. 세 경로는 같은 개체 선택 창을 쓴다. 박스 개체 고르기는 검색·정렬을 제공한다. 근거는 [배치·교체 흐름](../work/s5-design-system-v2/plan.md#박스와-파티-사이-배치교체-흐름)을 따른다.
박스 개체를 빈 파티 칸에 배치하면 숨김 상태로 시작한다. 맞바꾸기로 들어온 박스 개체도 숨김 상태다. 교체 전 칸의 표시 상태를 이어받지 않는다. 사용자가 별도로 꺼내기를 해야 놀이공간에 나타난다. 파티에 배치된 동안에는 숨김 파티의 기존 시간·적립 규칙을 따른다. 근거는 [박스 복귀와 교체 후 숨김](../work/s5-design-system-v2/plan.md#박스-복귀와-교체-후-숨김-유지)을 따른다.
근거는 [동일 종 보유와 파티 교체](../work/s5-design-system-v2/plan.md#동일-종-보유와-파티-교체)를 따른다.

`[스펙 미확정]` 교체에서 개체 ID와 친밀도·경험치·배고픔·버프를 각 개체에 유지하는 안을 제안한다. 파티 칸의 이전 개체 값을 새 개체에 복사하지 않는다.
교체 저장에 실패하면 두 개체의 이전 배치를 유지한다. [거래 실패 원칙](#구매사용보상-거래의-실패-원칙)을 따른다.
확정 전에는 배치를 유지한다. 취소하면 두 개체의 이전 배치를 유지한다. 근거는 [취소와 남은 저장 실패](../work/s5-design-system-v2/plan.md#확정-전-취소와-남은-저장-실패의-처리)를 따른다.
같은 종이라는 이유로 기존 개체에 합치지 않는다. 두 개체의 친밀도를 공유하는 모델로 바꾸지 않는다.
미정: 빈 칸이 여러 개일 때의 배치 순서, 부분 진행의 저장 구현, 교체의 저장 구현.

### 파티 칸 확장과 박스 수용

2026-09-21 사용자 결정: 새 게임은 파티 2칸으로 시작한다. 포인트 구매로 2칸을 추가한다. 업적으로 2칸을 추가한다. 최대 6칸이다.
박스 보유 개체 수는 제한하지 않는다. 파티가 가득 차면 새 개체를 박스에 보내는 계약을 유지한다.
근거는 [파티 칸과 박스 수용 정책](../work/s5-design-system-v2/plan.md#파티-칸과-박스-수용-정책)을 따른다.

| 파티 칸의 출처 | 수량 | 확정 범위 |
|---|---|---|
| 기본 제공 | 2칸 | 새 게임에서 사용 가능 |
| 상점 구매 | 2칸 | 포인트를 사용해 해금 |
| 업적 | 2칸 | 업적 조건을 충족해 해금 |

구매와 업적은 공통된 필수 순서 없이 진행할 수 있다. 첫 번째 업적은 파티의 두 마리를 실제로 화면에 꺼내면 달성한다. 업적창에서 보상을 받아야 한 칸을 추가한다. 목적은 참여 유도다.
`[스펙 미확정]` 두 번째 업적의 조건은 사용자가 구상 중이다. 구매 가능한 파티 칸 2개를 모두 구매하는 조건은 과거 후보이며 채택하지 않았다. 조건 확정은 보류한다.
`[스펙 미확정]` 구매 가격과 개별 구매 또는 묶음 구매 방식은 정하지 않았다. 업적 보상은 업적창에서 직접 수령한다.
후속 결정: 숨긴 채 두 칸에 배치한 것만으로 첫 업적을 달성하지 않는다. 두 마리를 꺼낸 뒤 업적 배너로 달성을 알린다. 달성 후 다시 숨겨도 달성을 유지한다.
같은 종의 서로 다른 두 개체도 첫 업적에서 인정한다. 근거는 [동일 종의 첫 업적 인정](../work/s5-design-system-v2/plan.md#동일-종의-첫-업적-인정)을 따른다.
각 업적의 보상은 한 번만 받는다. 수령을 완료한 업적은 다시 수령할 수 없다. 반복형 업적이 필요하면 별도 종류로 나중에 설계한다. 근거는 [1회 수령과 미수령 유지](../work/s5-design-system-v2/plan.md#업적-보상의-1회-수령과-미수령-유지)를 따른다.
출처와 참여 흐름은 [참여 유도 업적](../work/s5-design-system-v2/plan.md#참여-유도-업적)을 따른다.
출처별 칸 수를 3·4번은 구매, 5·6번은 업적이라는 고정 순서로 해석하지 않는다.
기존 저장의 파티 칸과 구매 권리를 새 게임 기본값으로 덮어쓰지 않는다. 저장 이식 계약은 별도로 정한다.
박스 수량 제한을 두지 않는다는 결정은 실제 저장 용량이나 성능이 무한하다는 보장이 아니다. 구현 시 대량 개체 목록의 조회·표시 방식을 설계한다.

### 첫 구매와 업적 보상 수령

2026-09-21 후속 결정: 두 번째 포켓몬의 직접 구매 튜토리얼을 랜덤알 구매와 부화 튜토리얼로 변경한다. 파티창에서 처음 숨겨진 포켓몬을 꺼내는 조작을 알려준다. 첫 업적 달성 후 업적창에서 보상을 받도록 알려준다. 2026-09-22 정정으로 이 부분은 업적 배너의 `바로가기`가 맡는다.
최신 근거는 [랜덤알 구매와 파티창 튜토리얼](../work/s5-design-system-v2/plan.md#랜덤알-구매와-파티창-안내)를 따른다.
출처는 [튜토리얼 구매와 업적 수령](../work/s5-design-system-v2/plan.md#튜토리얼-구매와-업적-수령)을 따른다.

| 단계 | 사용자 경험 | 상태 변화 |
|---|---|---|
| 포인트 지급 | 튜토리얼에서 구매할 포인트를 받는다. | 보유 포인트 증가. 지급량은 미정 |
| 첫 구매 | 상점에서 랜덤알을 구매한다. | 포인트 차감과 알 획득 |
| 부화 준비 | 돌봄 활동으로 대기시간을 단축한다. | 시간이 끝나면 부화 가능 상태 |
| 직접 부화 | 사용자가 부화 가능한 알을 직접 연다. | 새 개체 획득. 자동 부화하지 않음 |
| 파티 튜토리얼 | 처음 숨겨진 개체를 파티창에서 꺼낸다. | 파티의 숨김 상태에서 표시 상태로 변경 |
| 업적 배너 | 업적 달성 배너의 `바로가기`로 업적창에 이동한다. | 보상 미수령. 칸 수는 그대로 |
| 보상 수령 | 업적창에서 받기를 선택한다. | 수령 처리 후 파티 칸 1개 추가 |

업적 달성과 보상 수령은 다른 상태다. 달성 알림만으로 칸을 열지 않는다. 업적창은 관리 창 헤더의 업적창 아이콘으로 여는 모달이다. 알림 배너의 `바로가기`도 이 모달을 연다.
업적을 달성하면 화면 한쪽에 작은 배너를 잠깐 표시한다. 클릭하면 해당 업적의 보상 수령 화면으로 이동한다. 클릭하지 않으면 배너는 사라진다. 배너 표시와 클릭만으로 보상을 지급하지 않는다. 근거는 [진화와 업적의 배너 알림](../work/s5-design-system-v2/plan.md#진화와-업적의-배너-알림)을 따른다.
기본 2칸에서 다른 칸 해금 없이 이 흐름을 마치면 3칸이 된다. 두 번째 업적 후보를 채택해도 마지막 칸 구매만으로 업적 칸까지 자동으로 열지 않는다.
같은 업적의 완료된 수령 처리를 재시도해도 보상을 중복 지급하지 않는다. 수령 저장에 실패하면 칸과 수령 상태를 함께 이전 상태로 유지한다. 구매·사용·보상 거래의 실패 원칙을 따른다.
달성 후 보상을 받지 않았으면 미수령 상태로 유지한다. 업적창을 닫거나 앱을 다시 실행해도 유지한다. 수령 기한은 두지 않는다. 사용자는 업적창에서 언제든 받을 수 있다.
`[스펙 미확정]` 수령 전 파티 구성이 달라졌을 때의 처리는 정해야 한다.
튜토리얼을 건너뛰어도 시작 포인트를 동일하게 한 번 지급한다. 가이드북을 열어도 추가 지급하지 않는다. 건너뛰어도 상점 구매와 업적 달성이 가능해야 한다.
근거는 [시작 포인트와 튜토리얼 건너뛰기](../work/s5-design-system-v2/plan.md#시작-포인트와-튜토리얼-건너뛰기)를 따른다.
시작 포인트는 다른 상품에도 자유롭게 사용할 수 있다. 알 구매를 권하되 강제하지 않는다. 다른 상품을 구매해도 일반 게임 진행을 막지 않는다.
튜토리얼에서 부화한 포켓몬은 처음에 숨긴 파티 개체다. 박스 보관이 아니다. 숨김 중에도 적립·배고픔·버프 시간은 기존 파티 규칙대로 진행한다.
초반 숨김을 모든 신규 개체의 공통 규칙으로 확대하지 않는다. 일반 획득 개체의 표시 상태는 미정이다.
첫 칸 업적은 두 마리를 실제로 꺼낸 뒤 달성한다. 위 표는 알 구매를 먼저 진행한 예시다. 파티 튜토리얼의 시작 조건은 아래 계약을 따른다. 업적 튜토리얼은 제거했다. 첫 업적 달성은 업적 배너로 알린다.
다른 상품을 구매하면 관련 행동을 할 때까지 이후 튜토리얼을 기다린다. 나중에 알을 구매하면 그때 부화 튜토리얼을 시작한다. 시작 포인트를 재지급하지 않는다.
미정: 시작 포인트 지급량, 알 가격과 구체적인 결과 종 목록·확률, 활동별 부화 단축량, 부화 시 파티가 가득 찬 경우의 튜토리얼. 튜토리얼 다시보기와 다시보기 실습 보상은 설계 대상에서 제외한다.

### 행동에 따른 단계별 튜토리얼

2026-09-21 사용자 결정: 튜토리얼은 단계별로 나눈다. 사용자가 관련 행동을 했을 때 해당 튜토리얼을 시작한다. 처음 알 구매를 미뤘더라도 나중에 구매하면 부화 튜토리얼을 시작한다.
출처는 [행동에 따른 튜토리얼 분리](../work/s5-design-system-v2/plan.md#행동에-따른-튜토리얼-분리)를 따른다. 전체 튜토리얼을 순서대로 끝내야 일반 게임을 할 수 있는 구조로 만들지 않는다.

| 튜토리얼 | 시작 계기 | 알려줄 내용 |
|---|---|---|
| 첫 돌봄 | 첫 포켓몬을 만난 뒤. 세부 시점은 미정 | 밥 주기와 놀아주기 |
| 상점 | 시작 포인트를 받은 뒤. 세부 표시 시점은 미정 | 랜덤알 구매를 권하고 상점 이용 설명 |
| 부화 | 랜덤알을 구매했을 때 | 활동으로 대기시간 단축, 준비 완료 후 직접 열기 |
| 파티 | 첫 포켓몬과 다른 개체를 처음 획득했을 때 | 파티 이용과 개체 표시 방법 |
| 놀이공간 | 첫 돌봄 튜토리얼을 마치거나 스킵했을 때 | 변경 방법을 화면 설명으로 알려준다 |

파티 튜토리얼은 종이 아닌 개체 식별자를 기준으로 한다. 같은 종이어도 다른 개체를 처음 얻으면 시작한다. 알 구매 자체는 포켓몬 개체 획득이 아니다. 부화·직접 구매 등 획득 경로와 숨김 여부는 시작 조건이 아니다. 파티 만석으로 박스에 들어가도 획득 조건은 충족한다.
첫 개체를 숨겼다가 꺼내거나 박스에서 다시 배치한 것은 새 개체 획득이 아니다. 같은 개체의 진화도 별도 개체 획득으로 세지 않는다. 실제 식별자 필드명과 저장 방식은 아직 정하지 않았다.
파티 튜토리얼 시작 조건의 근거는 [파티와 업적 튜토리얼의 시작 조건](../work/s5-design-system-v2/plan.md#파티와-업적-안내의-시작-조건)을 따른다.
2026-09-22 정정: 업적 튜토리얼을 제거했다. 업적창 첫 진입과 첫 업적 달성은 튜토리얼 시작 계기가 아니다. 업적 이용은 업적 배너와 업적창으로 익힌다. 사용법은 가이드북에서 확인한다. 근거는 [업적 튜토리얼 제거](../work/s5-design-system-v2/plan.md#업적-배너의-바로가기와-업적-안내-제거)를 따른다.
`[스펙 미확정]` 이미 표시된 개체와 박스에 들어간 개체에 맞춰 파티 튜토리얼 내용을 달리하는 안이다.

`[스펙 미확정]` 튜토리얼별로 미시작·진행 중·건너뜀·완료를 구분하는 안을 제안한다. 포인트 지급과 업적 수령 상태는 튜토리얼 상태와 별도로 유지한다.
`[스펙 미확정]` 실제 게임 행동이 튜토리얼보다 앞서면 완료한 행동을 다시 요구하지 않는 안을 제안한다. 두 번째 구매로 부화 튜토리얼이 중복 실행되지 않도록 한다.
동시에 여러 튜토리얼 조건이 생기면 하나씩 보여준다. 진행 중인 튜토리얼을 마치거나 건너뛰면 다음 튜토리얼을 보여준다. 튜토리얼 때문에 화면을 강제로 전환하지 않는다.
2026-09-23 결정: 튜토리얼은 코치마크로 보여준다. 화면을 어둡게 하고 알려줄 대상만 밝게 남긴다. 대상 옆 말풍선 카드에 단계 수, 제목, 설명, `다음`, `✕`를 둔다. `✕`는 닫기이며 스킵으로 처리한다. 대상에는 강조 테두리를 두르지 않는다. 근거는 [빈 화면 제안 검토](../work/s5-design-system-v2/plan.md#빈-화면-제안-검토)를 따른다.
여러 튜토리얼 조건이 생기면 먼저 생긴 조건의 튜토리얼부터 보여준다. 같은 순간에 생긴 조건은 첫 돌봄 → 상점 → 부화 → 파티 → 놀이공간 순서로 보여준다. 기다리는 튜토리얼이 표시되기 전에 앱이 종료되면 미완료 상태로 남긴다. 재실행 후 같은 순서 규칙으로 보여준다. 대기만 한 튜토리얼을 스킵으로 처리하지 않는다. 근거는 [동시 튜토리얼 순서](../work/s5-design-system-v2/plan.md#동시-안내-조건의-발생-순서와-흐름-우선순위)를 따른다.
사용자가 튜토리얼을 닫으면 해당 튜토리얼을 스킵한 것으로 처리한다. 스킵한 튜토리얼은 관련 화면 재진입과 앱 재실행 후에도 다시 표시하지 않는다. 튜토리얼 다시보기는 제공하지 않는다. 이후 사용법은 가이드북에서 확인한다. 근거는 [튜토리얼 닫기와 가이드북](../work/s5-design-system-v2/plan.md#튜토리얼-닫기와-가이드북)을 따른다.
2026-09-23 결정: 가이드북은 설정 모달의 `가이드북` 행에서 연다. 모달을 겹치지 않고 설정 모달 안에서 화면이 바뀐다. `‹ 설정`으로 돌아간다. 구성은 돌봄, 상점과 알, 파티와 박스, 진화, 업적의 다섯 주제다. 근거는 [가이드북 진입과 구성](../work/s5-design-system-v2/plan.md#가이드북-진입과-구성-sr-02)을 따른다. 가이드북 열람은 게임 조작이나 보상 지급을 실행하지 않는다. 가이드북도 미발견 행동 조건의 정답을 숨기는 기존 정보 공개 원칙을 따른다. 개별 튜토리얼 스킵을 모든 기능 튜토리얼의 일괄 비활성화로 확대하지 않는다.
진행 중인 튜토리얼이 앱 종료로 끊기면 스킵으로 처리하지 않는다. 미완료 상태로 남긴다. 비정상 종료와 PC 종료도 같은 원칙을 따른다. 앱을 다시 실행하면 끊긴 튜토리얼을 처음부터 다시 보여준다. 끊긴 단계부터 이어가지 않는다. 다시 보여주는 튜토리얼도 순차 표시와 강제 전환 금지 규칙을 따른다. 근거는 [끊긴 튜토리얼의 재표시](../work/s5-design-system-v2/plan.md#앱-종료로-끊긴-안내의-처음부터-재표시)를 따른다.
`[스펙 미확정]` 대기 튜토리얼의 표시 위치, 튜토리얼 상태의 저장 구현, 재표시 시점에 해당 행동을 이미 완료한 경우의 튜토리얼 내용, 가이드북의 주제별 문구는 별도로 정한다.

### 활동에 따른 부화 대기시간 단축

랜덤알의 결과 종은 해금한 종 중 알 입수처와 경로별 조건을 만족한 종으로 제한한다. 진화 전용 종과 이후에도 기믹 입수만 허용한 종은 제외한다. 기믹으로 해금했어도 후속 알 입수를 허용한 종은 후보가 될 수 있다. 알 전용 종의 도감 해금 수 조건도 확인한다. 근거는 [종별 후속 입수처와 조건부 알 후보](../work/s5-design-system-v2/plan.md#종별-후속-입수처와-조건부-알-후보)를 따른다.
구매 후 새로 해금한 종은 이미 구매한 알의 결과에 포함하지 않는다. 후보 범위 제한과 실제 결과 추첨 시점은 구분한다. 근거는 [구매 후 해금 제외와 행동 부화 구상](../work/s5-design-system-v2/plan.md#구매-후-해금-제외와-행동-부화-구상)을 따른다.
구매 시 나올 수 있는 종 범위를 해당 알의 개체 식별자(sid)에 연결해 저장한다. 일반 알에 행동에 따른 결과 분기를 적용한다. 별도의 ‘특정 알’ 분류를 추가하지 않는다.
행동 조건에 대응한 포켓몬이 한 종이면 그 포켓몬으로 부화한다. 여러 종이면 그 포켓몬들 중 무작위로 부화한다. 충족한 조건이 없으면 해당 알에 저장한 종 범위에서 무작위로 부화한다. 행동 미충족 때문에 부화를 막지 않는다. 준비 시간과 직접 열기 원칙은 유지한다. 시간 단축 효과와 결과 지정 조건은 구분한다.
근거는 [일반 알의 행동 조건과 결과 분기](../work/s5-design-system-v2/plan.md#일반-알의-행동-조건과-결과-분기)를 따른다. 구매 당시 범위 밖의 종을 행동으로 추가하는 예외는 정하지 않았다.
행동 조건을 충족하지 않은 일반 무작위 부화에서는 종별 수집 난이도 1~5에 따른 공통 가중치를 사용한다. 종별 상세 확률을 직접 지정하지 않는다. 같은 난이도에는 같은 가중치 기준을 적용한다. 문서 표기는 1이 얻기 쉬운 쪽이고 5가 얻기 어려운 쪽이다. 난이도 값 자체를 추첨 가중치로 사용하지 않는다. 2026-09-23 결정: 난이도는 종족값 합계, 진화 단계, 전설·환상 여부로 자동 계산한다. 종별 예외는 `data/species.overrides.json`에서 덮어쓴다. 근거는 [콘텐츠 데이터 결정](../work/s5-design-system-v2/plan.md#콘텐츠-데이터-결정)을 따른다.
가중치는 해당 알의 허용 후보 범위 안에서 적용한다. 수집 난이도로 해금·입수처 조건을 우회하지 않는다. 이로치 여부는 별도의 동일한 낮은 확률로 추첨한다. 근거는 [수집 난이도와 부화 가중치](../work/s5-design-system-v2/plan.md#수집-난이도에-따른-일반-부화-가중치)를 따른다.
복수 행동 조건을 충족해 후보 종이 여러 개이면 그 후보들 사이에서도 같은 수집 난이도별 공통 가중치를 적용한다. 충족하지 않은 종은 섞지 않는다. 후보가 한 종이면 그 종으로 부화한다. 근거는 [복수 조건의 난이도 가중치](../work/s5-design-system-v2/plan.md#복수-행동-조건-후보의-난이도-가중치)를 따른다.
알 행동 조건을 지정하는 종에는 조건을 하나만 둔다. 같은 종에 여러 대체 조건을 배정하지 않는다. 동일 종의 복수 조건에 따른 가중치 합산은 설계 대상이 아니다. 서로 다른 종의 조건은 한 알에서 여러 개 충족할 수 있다. 근거는 [종별 단일 행동 조건](../work/s5-design-system-v2/plan.md#알-행동-조건의-종별-단일-지정)을 따른다.
`[스펙 미확정]` 단계별 실제 가중치, 종별 난이도 배정, 후보 구성에 따른 정규화, 구매 후 가중치 변경의 적용 시점은 별도로 정한다.
사용자가 직접 행동을 시도하며 조건을 발견하는 경험을 지향한다. 근거는 [복수 조건과 발견 경험](../work/s5-design-system-v2/plan.md#복수-행동-조건의-무작위-결과와-발견-경험)을 따른다. 알 입수처의 기본 조건 공개와 숨겨진 행동 결과 조건의 공개는 구분한다.
발견 전에는 정답 행동과 충족한 포켓몬 목록을 직접 표시하지 않는다. 알의 반응이나 작은 힌트로 탐색하게 한다. 기본 입수 조건을 공개하는 원칙은 유지한다. 근거는 [발견 전 알의 반응과 힌트](../work/s5-design-system-v2/plan.md#발견-전-알의-반응과-힌트)를 따른다.
알의 행동 조건을 실제로 충족하고 대응하는 포켓몬이 부화하면 해당 조건을 발견으로 기록한다. 조건 없이 일반 무작위 결과로 같은 종을 얻었으면 조건 발견으로 처리하지 않는다. A와 B 조건을 충족하고 A가 부화했으면 A 조건만 새로 발견한다.
발견한 행동 조건은 도감에 정확히 기록한다. 미발견 조건에는 기존 반응·힌트 원칙을 적용한다. 근거는 [알 행동 조건의 발견 판정과 기록](../work/s5-design-system-v2/plan.md#알-행동-조건의-발견-판정과-기록)을 따른다.
알 구매부터 직접 열기 직전까지 행동 조건을 알별로 누적한다. 준비 시간이 끝난 뒤에도 계속 돌보며 조건을 충족할 수 있다. 직접 열 때 그동안 충족한 조건으로 결과를 결정한다. 준비 완료만으로 결과를 확정하거나 탐색을 끝내지 않는다. 근거는 [직접 열기 전까지 알 행동 누적](../work/s5-design-system-v2/plan.md#직접-열기-전까지-알-행동-누적)을 따른다.
부화 저장 실패와 완료 부화의 재시도는 [거래 실패 원칙](#구매사용보상-거래의-실패-원칙)을 따른다. 발견 기록은 부화 거래에 포함한다. 부화에 실패하면 발견 기록도 추가하지 않는다.
`[스펙 미확정]` 행동과 대상 종, 행동·열기 동시 요청 처리 순서, 결과 저장 구현과 실제 가중치는 정하지 않았다. 실제 힌트·반응·도감 표현을 별도로 정한다. 동일 종의 복수 조건은 종별 단일 지정으로 발생하지 않는다. 단일 종을 먼저 획득해 알 후보가 소진되는 경우는 단일 종의 알 제외로 발생하지 않는다.

2026-09-21 사용자 결정: 부화 준비에 최대 5분을 잡는다. 돌봄 같은 활동으로 시간을 단축한다. 시간이 다 지나면 사용자가 직접 열어야 부화한다.
후속 결정: 구매 완료 직후 준비 타이머를 시작한다. 가방에서 별도로 부화 시작을 누를 필요는 없다. 근거는 [구매 직후 부화 준비 시작](../work/s5-design-system-v2/plan.md#구매-직후-부화-준비-시작)을 따른다.
부화 완료를 사용자가 놓치지 않도록 자동 부화를 하지 않는다. 출처는 [부화 준비와 직접 열기](../work/s5-design-system-v2/plan.md#부화-준비와-직접-열기)를 따른다.

| 상태 | 진행 | 다음 상태 |
|---|---|---|
| 준비 중 | 시간이 경과한다. 활동으로 남은 시간을 단축한다. | 남은 시간이 0이면 부화 가능 |
| 부화 가능 | 사용자의 열기 조작을 기다린다. 행동 조건을 계속 쌓을 수 있다. | 직접 열 때 누적한 조건으로 결과 결정 후 부화 완료 |
| 부화 완료 | 새 개체를 얻는다. | 기존 파티·박스 배치 계약 적용 |

최대 5분은 부화 가능 상태까지의 대기 기준이다. 사용자가 직접 열기까지 기다리는 시간은 포함하지 않는다.
PC 잠금·절전·앱 종료 중에는 부화 준비를 진행하지 않는다. 남은 시간을 보존하고 복귀 후 이어간다. 관리 창만 닫으면 준비 시간은 계속 진행한다.
`[스펙 미확정]` 인정된 돌봄이 완료되면 남은 시간에서 정해진 시간을 차감하는 안을 제안한다. 남은 시간은 0 아래로 내려가지 않게 한다. 버튼 클릭만으로 반복 차감하지 않는 안이다.
돌보미집에는 알을 최대 6개 배치한다. 각 알은 개별 타이머로 동시에 진행한다. 전체 알의 진행도를 하나로 합치지 않는다.
알 돌봄 행동은 쓰다듬기와 노래 들려주기다. 포켓몬에게 밥 주기·놀아주기를 하는 것과 구분한다. 상점의 부화시간 단축 도구는 채택하지 않는다. 기본 대기 시간을 늘리면 다시 검토한다. 근거는 [알 돌봄 행동](../work/s5-design-system-v2/plan.md#알-돌봄-행동의-채택)을 따른다. `[스펙 미확정]` 행동별 단축량, 반복 인정 간격, 행동 조건표에서 두 행동을 조합하는 방식은 정하지 않았다.
선택한 알을 돌보면 그 알의 남은 시간만 단축한다. 다른 알은 자기 타이머에 따라 진행한다.
알의 부화 준비가 완료되는 순간 작은 알림을 한 번 제공한다. 돌보미집 메뉴를 열지 않았어도 완료를 알 수 있어야 한다. 알림만으로 부화시키거나 결과 포켓몬을 공개하지 않는다. 근거는 [메뉴형 돌보미집과 알림](../work/s5-design-system-v2/plan.md#메뉴형-돌보미집과-부화-준비-완료-알림)을 따른다.
알림은 화면 한쪽의 작은 배너로 잠깐 표시한다. 배너의 `바로가기`를 누르면 돌보미집을 연다. 누르지 않으면 배너는 사라진다. 배너가 사라져도 준비된 알은 직접 부화할 때까지 유지한다. 근거는 [배너와 클릭 이동](../work/s5-design-system-v2/plan.md#부화-준비-완료-배너와-클릭-이동)을 따른다.
여러 알이 동시에 준비되면 알별 배너를 하나씩 순차 표시한다. 알림이 대기하는 동안에도 알은 부화 가능한 상태로 유지한다.
미정: 활동별 단축량, 최소 대기 여부. 배너의 정확한 위치·다중 화면 기준·표시 시간도 별도로 정한다. 동시 완료 순서는 [알림 배너의 개별 표시](#알림-배너의-개별-표시)를 따른다. 소리는 알림 배너 공통 계약을 따른다. 메뉴 진입 버튼의 준비된 알 개수 표시는 제안이다.
돌보미집의 알 칸과 포켓몬 파티 칸은 별개다. 파티 칸 구매·업적으로 돌보미집 칸이 늘어나는 것으로 해석하지 않는다.
돌보미집에 빈 칸이 있으면 구매한 알을 바로 배치하고 타이머를 시작한다. 6개가 차면 추가 알 구매를 막고 돌보미집을 안내한다.
부화 가능한 알도 직접 열기 전까지 칸을 차지한다. 직접 부화하면 칸이 비고 추가 구매가 가능해진다. 부화 결과 창은 태어난 개체와 들어간 자리(파티의 숨김 칸 또는 박스)를 보여주고 `확인`만 둔다. 닫으면 돌보미집에 머문다. 근거는 [빈 화면 제안 검토](../work/s5-design-system-v2/plan.md#빈-화면-제안-검토)를 따른다. 초과 구매한 알을 별도로 보관하는 경로는 제공하지 않는다.
구매 제한 시 포인트를 차감하지 않는다. 만석이라는 이유로 준비 중인 알을 자동 부화시키지 않는다. 근거는 [돌보미집 만석 처리](../work/s5-design-system-v2/plan.md#돌보미집-만석-처리)를 따른다.
후속 결정은 [돌보미집과 알 6개](../work/s5-design-system-v2/plan.md#돌보미집과-알-6개)를 따른다. 돌보미집은 메뉴로 구성한다. 알 배치는 메뉴 내부의 알 칸을 뜻한다. 데스크톱 놀이공간에는 알을 배치하지 않는다. 돌보미집은 박스 메뉴 안에 둔다. 박스 메뉴 안의 세부 배치는 아직 정하지 않았다.
근거와 제안은 [알별 진행과 상호작용 후보](../work/s5-design-system-v2/plan.md#알별-진행과-상호작용-후보)를 따른다.
두 마리를 꺼내면 첫 칸 업적을 달성한다. 첫 달성은 업적 배너로 알린다. 숨긴 파티 개체의 시간 진행 규칙은 유지한다. 업적 판정과 상태 시뮬레이션의 진행 조건은 별개다.

### 재검토할 기존 종 등록 기반 계약

아래 내용은 이전 계약을 보존한 것이다. 위의 개체별 육성·종별 입수처와 충돌하는 내용을 새 구현에 적용하지 않는다. 개체 획득과 보유 개체 배치를 나누는 후속 설계가 필요하다.

도감 전체 목록에는 등록·미등록 종을 모두 표시한다. 목록에 보인다는 이유로 소환할 수 있지는 않다.
'등록'은 해당 종을 획득하여 소환할 수 있는 상태다. 스타터 선택, 구매, 부화, 진화로 등록한다.
상점 구매 조건 충족과 도감 등록은 별개다. 조건 충족만으로 유료 상품을 무료 소환하지 않는다.
[스펙 미확정] 기존 해금 자료를 새 등록으로 옮기는 기준은 이식 설계에서 확정한다.

빈 칸을 누르면 해당 칸을 지정한 도감을 연다. 기본 필터는 '등록'이다.
지방과 검색으로 종을 고른다. 'N번 칸에 소환'을 누른다. 완료하면 파티로 돌아간다.
상세의 '교체'도 해당 칸을 지정한 도감을 연다. 기존 포켓몬은 확정 전까지 유지한다.
취소, Escape, 다른 탭 이동은 파티를 변경하지 않는다. 취소하면 선택한 목적지를 해제한다.
일반 도감에서도 파티 칸을 선택해 등록된 종을 소환할 수 있다.
미등록 종은 획득 방법만 안내한다. 미등록 종의 소환은 막는다. 잠긴 칸은 대상 목록에서 제외한다.

| 동작 | 파티 칸 | 도감과 육성 기록 |
|---|---|---|
| 소환 | 등록된 종을 빈 칸에 배치 | 등록 유지 |
| 교체 | 선택한 종으로 배치 변경 | 이전 종 등록과 기록 유지 |
| 소환 해제 | 빈 칸으로 변경 | 등록과 기록 유지 |
| 숨기기 / 다시 표시 | 배치 유지, 표시만 변경 | 육성 유지 |

교체는 한 번의 저장 작업으로 처리한다. 그림이나 저장이 실패하면 이전 배치를 유지한다.
파티 칸은 최대 여섯 칸이다. 도감 등록 수와 독립적이다.
파티가 가득 차도 구매·부화·진화로 새 종을 등록한다. 소환은 이후에 할 수 있다.
진화 전 종도 등록을 유지하므로 외형 선택이나 별도 보관 화면 없이 다시 소환할 수 있다.
놓아주기와 영구 삭제는 제공하지 않는다.

종별 기록을 친밀도의 소유자로 삼는 기존 제안은 개체별 관리 방향으로 대체한다. 재소환 시 같은 개체를 복원하는 방식과 새 개체를 획득하는 방식은 구분해서 설계해야 한다.
후속 사용자 결정으로 동일 종의 여러 개체 보유와 동시 파티 배치를 허용한다. 기존 HTML의 중복 제한은 새 계약의 기준으로 사용하지 않는다.
박스 보관과 숨김의 상태 변화는 [파티 표시와 박스 보관](#파티-표시와-박스-보관)으로 확정했다. 이전의 미소환 중 육성 제안보다 이 계약이 우선한다.
[스펙 미확정] 종별 기록과 기존 중복 개체의 이식은 구현 전에 확정한다. 진화 시 유지할 개체 정보는 아래의 후속 결정을 따른다.
겹침과 소환 순서는 유지한다. 교체한 포켓몬은 새로 소환한 순서로 표시한다.

## 4. 상세와 진화

진화 조건을 충족하면 진화 가능 상태를 알린다. 사용자가 원할 때 직접 진화시킨다. 구체적인 조건은 미정이다. 이미 알린 같은 진화 가능 상태는 반복해서 알리지 않는다.
선택 가능한 진화 분기의 조건을 둘 이상 충족하면 가능한 후보를 보여주고 사용자가 선택한다. 리전폼 진화 조건을 충족하면 해당 경로의 결과는 지정된 리전폼으로 정한다. 해당 조건이 없고 일반 진화 조건을 충족하면 일반 폼으로 정한다. 같은 경로에서 일반 폼과 리전폼을 자유 선택하게 하지 않는다. 조건 충족만으로 진화를 실행하지 않는다.
사용자 예시: 지정된 리전폼 조건에서는 히스이 대검귀로 진화한다. 일반 조건에서는 일반 대검귀로 진화한다. 구체적인 조건은 아직 정하지 않았다. 근거는 [분기 진화와 리전폼 조건](../work/s5-design-system-v2/plan.md#분기-진화-선택과-리전폼-조건)을 따른다.
가능한 진화 결과는 실제 실행 시점의 조건으로 판단한다. 진화 가능 알림 시점에 결과를 고정하지 않는다. 실행 전에 결과 종과 폼을 보여주고 사용자 확인을 받는다. 과거 알림만으로 현재 조건에 맞지 않는 진화를 실행하지 않는다. 근거는 [조건 판정과 결과 확인](../work/s5-design-system-v2/plan.md#진화-실행-시-조건-판정과-결과-확인)을 따른다.
진화 저장에 실패하면 진화 전 종과 도감 기록을 유지한다. 진화 완료로 표시하지 않는다. [거래 실패 원칙](#구매사용보상-거래의-실패-원칙)을 따른다.
`[스펙 미확정]` 결과 확인 중 조건이 바뀌는 경우, 여러 폼 조건의 중복 처리, 각 리전폼의 구체적인 도감 해금 조건은 별도로 정한다. 리전폼의 독립 항목과 별도 집계는 도감 해금 계약을 따른다.
한 번의 진화 선택은 다음 한 단계로만 진행한다. 여러 단계의 조건을 이미 충족했어도 중간 단계를 건너뛰지 않는다. 진화 후 다음 단계 조건도 충족했다면 다시 직접 선택해 진화할 수 있다. 다음 단계는 자동으로 실행하지 않는다. 사용자는 현재 단계에 머무를 수 있다. 근거는 [단계별 직접 진화](../work/s5-design-system-v2/plan.md#단계별-직접-진화)를 따른다.
진화 가능 알림은 화면 한쪽의 작은 배너로 잠깐 표시한다. `바로가기`를 누르면 해당 개체의 상세 화면으로 이동한다. 누르지 않으면 배너는 사라진다. 배너 표시와 `바로가기`만으로 진화시키지 않는다. 근거는 [진화와 업적의 배너 알림](../work/s5-design-system-v2/plan.md#진화와-업적의-배너-알림)을 따른다.
2026-09-21 결정: 일반 개체의 완료한 진화는 이전 단계로 되돌리지 않는다. 지정한 단일 포켓몬 진화 계열은 공유 `sid`와 박스 종 교체의 예외 계약을 따른다. 진화 확정 전 취소와 완료 후 되돌리기를 구분한다. 확정 전에 취소하면 개체와 진화 가능 상태를 그대로 유지한다.
진화 후에도 개체 ID, 친밀도, 성격, 레벨, 누적 경험치를 유지한다. 만복도, 남은 버프 시간, 먹이 쿨타임도 유지한다. 파티·박스 위치와 숨김 상태를 유지한다. 진화 자체로 이 값을 초기화하거나 회복하지 않는다. 근거는 [진화 시 개체 정보 유지](../work/s5-design-system-v2/plan.md#진화-시-개체-정보-유지)를 따른다.
일반 개체의 진화 전 종을 다시 표시하려면 그 종의 별도 개체가 필요하다. 획득 후 배치와 보유 개체 교체는 [개체 획득과 파티 교체](#개체-획득과-파티-교체)를 따른다. 종별 입수 조건은 별도로 정한다.

2026-09-21 재설계: 배고픔 표현은 [돌봄을 알리는 원칙](../design.md#돌봄을-알리는-원칙)을 따른다. 말풍선 반복 정책은 후속 결정으로 확정했다. 단계 수와 수치 경계, 행동 간격은 아직 정하지 않았다.
돌봄을 미뤘을 때의 효과는 [배고픔 상태 디버프](../design.md#배고픔-상태-디버프)를 따른다. 구체적인 적용 조건과 배율은 미정이다.

별명 입력과 모습 선택을 제공하지 않는다. 실제 종의 이름과 그림을 표시한다.
[스펙 미확정] 크기는 1~6단계로 제안한다. 조절을 완료하면 한 번 저장한다.
박스 개체의 상세에는 돌봄과 표시 항목을 두지 않는다. 성장·진화, 도구 사용, `파티에 배치` 또는 `교체`만 둔다. 가방 도구는 박스 개체에게도 쓸 수 있다. 상세 프로필의 경험치는 레벨 옆에 짧은 막대와 다음 레벨까지의 비율로 보여준다. 근거는 [빈 화면 제안 검토](../work/s5-design-system-v2/plan.md#빈-화면-제안-검토)를 따른다.
상세의 숨기기·다시 표시는 표시 여부만 바꾼다. 박스 보관과 구분한다. 숨김 몬스터볼 마커 자체를 버튼으로 확정하지 않는다.
성격 변경은 가방에서 민트를 사용하는 방식으로 제공한다. 확인하면 민트 1개를 소비한다. 현재와 같은 성격으로는 바꿀 수 없다. 근거는 [민트 유지](../work/s5-design-system-v2/plan.md#민트-유지)를 따른다. 이로치 색상 변경 약은 후속 계약으로 채택했다. 대상 지정 화면의 구성은 미정이다.
밥 주기와 놀아주기를 유지한다. 먹이는 [일상 사용 계약](#일상-사용-계약)을 따른다. 이전 효과 2배 안내는 새 버프의 확정 수치가 아니다.

기존 제안은 진화 전후 종을 등록하고 각각 도감에서 소환하는 방식이었다. 이 방식은 개체별 육성과 레벨 기반 해금 제안에 맞게 재검토한다. 도감에서 이전 종을 선택해 같은 개체의 진화를 되돌리지는 않는다.
별도 보관 개체를 복제하는 이전 설계는 폐기한다.
이로치 개체는 진화 후에도 이로치를 유지한다. 색상과 도감 기록은 아래의 이로치 계약을 따른다.
[스펙 미확정] 기존 하루 상한의 승계, 진화 연출 중 시간 처리, 종별 경험치 곡선의 호환은 정하지 않았다. 친밀도와 성격은 같은 개체에 유지한다.
[스펙 미확정] 이미 등록된 종으로의 반복 진화 규칙은 정하지 않았다.
[스펙 미확정] 반복 진화의 보상 정책은 정하지 않았다.
현재 Figma는 정적 화면이다. 실제 진화·기록 승계·소환 처리는 구현하지 않았다.
2026-09-22 결정: 진화의돌을 채택한다. 원작에서 돌로 진화하는 종은 진화의돌이 진화 조건이다. 2026-09-23 결정: 돌은 원작대로 종류를 나눈다. 불꽃·물·천둥·리프·얼음·달·태양 같은 돌을 각각 상품으로 둔다. 종별로 맞는 돌을 지정한다. 돌이 결과를 정하는 종은 고른 돌이 곧 결과다. 분기 선택 창은 돌 밖의 조건이 여럿 겹칠 때만 쓴다. 근거는 [콘텐츠 데이터 결정](../work/s5-design-system-v2/plan.md#콘텐츠-데이터-결정)을 따른다. 진화의돌은 상점에서 구매해 공통 가방에 보관한다. 가방에서 돌과 대상 개체를 고르면 진화 확인 화면으로 이어진다. 확인하면 진화하고 돌 1개를 소비한다. 취소하면 돌과 개체를 그대로 둔다. 돌 사용과 진화는 하나의 거래로 처리한다. 근거는 [진화의돌 채택](../work/s5-design-system-v2/plan.md#진화의돌-채택)을 따른다.
[스펙 미확정] 돌 종류, 대상 종, 가격, 친밀도·시간 조건과의 관계, 여러 분기가 있는 종의 확인 화면은 종별 진화 조건표와 함께 정한다.
모든 진화에 돌이 필요하다고 가정하지 않는다. 조건과 실패 이유를 표시한다. 메인 프로세스가 최종 검사한다.

## 5. 전체 도감과 지방

전국도감 전체 종을 표시한다. 상점 해금 규칙이 있는 종만으로 제한하지 않는다.
지방 선택과 이름·번호 검색을 함께 적용한다. 검색과 선택을 유지한다.
[스펙 미확정] 지방은 전체, 관동, 성도, 호연, 신오, 하나, 칼로스, 알로라, 가라르, 히스이, 팔데아로 제안한다.
[스펙 미확정] 분류는 최초 등장 지방 기준으로 제안한다. 여러 지역도감의 중복 등재와 구분한다.
리전폼은 일반 폼과 별도 도감 항목으로 표시한다. 일반 대검귀 `503`과 히스이 대검귀 `503-1`처럼 구분한다. 지방 폼을 번호 범위만으로 판정하지 않는다. 리전폼 이외 형태의 표시와 집계는 미정이다.
지역 정보가 없는 종도 '전체'에서 누락하지 않는다.
[확인 필요] 실제 매핑과 출처는 구현 전에 검증한다.
전체 목록 표시, 획득 조건 충족, 도감 등록, 소환 중 상태를 구분한다.
등록 필터는 전체 / 등록 / 미등록이다. 지방과 검색을 함께 적용한다.
획득 규칙이 없는 종에는 '획득 방법 준비 중'을 표시한다.
그림이 없으면 공통 자리표시자를 쓴다. 다른 종의 그림으로 대신하지 않는다.
보이는 카드와 선택한 종의 그림만 요청한다. 저작자와 라이선스를 표시한다.
이전 HTML의 도감은 일부 종 예시였다. 현재 v2의 도감 본문은 미제작이다. 전체 도감 데이터 연결은 구현 단계에서 처리한다.

## 6. 상점과 가방

### 상점 구매 창

상품 카드에는 가격과 `›`만 둔다. 카드마다 `구매` 버튼을 두지 않는다. 카드를 누르면 구매 창을 연다.
여러 개 사는 상품은 구매 창에서 수량을 고르고 합계와 보유 포인트를 확인한 뒤 산다. 알·포켓몬·파티 칸처럼 하나씩 사는 상품은 수량 줄 없이 확인만 한다. 0P 상품의 버튼은 `받기`다.
구매할 수 없으면 카드는 그대로 누를 수 있다. 구매 창에서 이유를 한 줄로 보여주고 `구매`를 비활성으로 둔다. 돌보미집이 가득 찼으면 `돌보미집 보기`로 박스 메뉴의 돌보미집으로 이동한다. 포인트는 차감하지 않는다. 근거는 [검수 P2 결정](../work/s5-design-system-v2/plan.md#검수-p2-결정-항목)을 따른다.
`[스펙 미확정]` 한 번에 살 수 있는 최대 수량과 구매 창 문구는 정하지 않았다.

### 구매·사용·보상 거래의 실패 원칙

구매·사용·보상 수령은 관련 상태를 하나의 거래로 변경한다. 성공하면 전부 반영한다. 실패하면 관련 상태 전부를 이전 상태로 유지한다. 구매 실패 시 포인트와 상품 재고를 함께 유지한다. 사용 실패 시 재고와 개체 상태를 함께 유지한다. 보상 수령 실패 시 보상과 수령 상태를 함께 유지한다. 이미 완료된 거래의 재시도는 보상을 중복 지급하거나 비용을 다시 차감하지 않는다. 처리 결과가 불명확하면 저장된 결과를 확인한 뒤 재시도한다. 근거는 [사용자 결정](../work/s5-design-system-v2/plan.md#거래-실패-원자성과-코스모움-분리안-재검토)을 따른다.

실패한 거래는 실패로 처리하고 끝낸다. 실패 복구용 선택값 보존과 다시 시도 버튼은 제공하지 않는다. 자동 재시도도 하지 않는다. 관련 상태의 이전 값 유지와 완료 거래의 중복 방지는 유지한다. 정상 구매·사용·수령 조작을 이후에도 할 수 있다. 근거는 [실패 처리 범위](../work/s5-design-system-v2/plan.md#거래-실패-처리만-제공)를 따른다.
부화·진화·교체에도 같은 거래 원칙과 실패 처리를 적용한다. 근거는 [복합 동작의 거래 적용](../work/s5-design-system-v2/plan.md#부화진화교체의-트랜잭션-적용)을 따른다.

| 동작 | 하나의 거래로 처리할 상태 | 실패 시 유지할 상태 |
|---|---|---|
| 부화 | 알 소비, 개체 생성, 파티 또는 박스 배치, 돌보미집 칸 비우기, 도감 획득·발견 기록 | 부화 가능한 알과 돌보미집 칸. 개체와 도감 기록은 추가하지 않음 |
| 진화 | 종 변경, 도감 해금·획득 기록 | 진화 전 종과 도감 기록 |
| 교체 | 파티 개체와 박스 개체의 위치 | 두 개체의 원래 위치 |

부화 저장에 실패하면 결과를 저장하지 않는다. 사용자가 같은 알을 다시 열면 그 시점까지 충족한 조건으로 결과를 새로 정한다. 이 조작은 정상 열기이며 실패 전용 재시도가 아니다.
완료된 부화를 재시도해도 개체를 더 만들지 않는다. 완료된 진화를 재시도해도 다음 단계로 한 번 더 진화하지 않는다. 완료된 교체를 재시도해도 위치를 되돌리지 않는다.
진화나 교체의 결과 확인 단계에서 취소하면 아무 상태도 바꾸지 않는다. 공유 `sid` 종 교체는 일반 교체와 같은 거래로 처리한다. 실패하면 현재 종과 파티 칸을 유지한다.
첫 선택을 확정하기 전에 창을 닫으면 개체를 만들지 않는다. 다음 실행 때 선택 화면을 다시 연다. 첫 선택 저장에 실패하면 선택 화면에 남는다. 다시 고르는 조작은 정상 선택이다. 확정한 첫 개체와 시작 포인트는 재실행해도 중복 지급하지 않는다.
놀이공간 변경을 취소하거나 저장에 실패하면 적용 전 영역을 유지한다. 근거는 [취소와 남은 저장 실패](../work/s5-design-system-v2/plan.md#확정-전-취소와-남은-저장-실패의-처리)를 따른다.
`[스펙 미확정]` 실패 문구와 표시 위치, 거래 식별·저장 방식, 결과 불명 상태의 내부 판정은 후속 설계한다.

### 이로치와 색상 변경 약

이로치는 같은 개체의 색상 상태다. 별도 도감 항목이나 해금 종 수를 추가하지 않는다. 해당 도감 항목에 이로치 획득 여부를 기록한다. 일반 폼과 리전폼의 독립 항목은 유지한다.
알에서는 낮은 확률로 이로치 개체를 얻는다. 사용자 예시 `1/1000`은 후보 확률이다. 상점의 `모습이 바뀌는 약`을 사용해 기존 개체를 이로치로 바꿀 수도 있다. 약은 비싸게 판매한다. 실제 가격은 미정이다. 단일 종은 알에서 얻을 수 없으므로 약으로 이로치가 된다.
상점의 `돌아오는 약`은 0원이다. 대상 개체를 일반색으로 되돌린다. 두 상품명은 가칭이다. 약으로 색상을 변경해도 새 개체를 만들지 않는다. 근거는 [이로치 기록과 획득 경로](../work/s5-design-system-v2/plan.md#이로치-기록과-알-및-약-획득-경로)를 따른다.
일반 개체는 최초 획득 시 일반색으로 시작한다. 알에서 이로치로 태어났거나 약으로 바꾼 개체는 숨김 해제·박스 복귀 후에도 이로치를 유지한다. 재배치로 색상을 초기화하지 않는다.
이로치 개체는 진화 후에도 이로치를 유지한다. 돌아오는 약으로 일반색으로 바꿔도 도감의 이로치 획득 기록은 보존한다. 현재 일반색인 개체와 과거 이로치 획득 여부를 구분한다. 근거는 [이로치 유지와 획득 기록 보존](../work/s5-design-system-v2/plan.md#진화-이로치-유지와-도감-획득-기록-보존)을 따른다.
알 행동 조건은 태어날 종에만 영향을 준다. 이로치 여부는 종 결정과 별도로 동일한 낮은 확률로 추첨한다. 일반 무작위 부화와 행동 조건으로 정해진 부화 모두 같은 이로치 확률을 적용한다. 행동 조건을 여러 개 충족해도 추첨 기회나 확률을 늘리지 않는다. 준비 시간을 단축하는 기존 돌봄 효과는 유지한다. 근거는 [알 행동과 이로치 추첨 분리](../work/s5-design-system-v2/plan.md#알-행동-조건과-이로치-추첨-분리)를 따른다.
`[스펙 미확정]` 알의 이로치 추첨 시점과 실제 확률, 아이템 소모 단위·대상 범위·실패 처리, 기존 구매 권리 이식은 별도로 정한다.

### 기존 상점과 가방 제안

후속 결정: 랜덤알 구매와 부화는 첫 구매 튜토리얼에 채택했다. 아래의 기존 상품·작업 부화 제안 전체를 채택한 것은 아니다. 최신 계약은 [첫 구매와 업적 보상 수령](#첫-구매와-업적-보상-수령)을 따른다.

[스펙 미확정] 아래 상품·효과·작업 부화는 기존 제안이다. 사용자 채택 근거를 확인하기 전까지 구현 지시와 인수 조건으로 사용하지 않는다. 상점·가방 탭의 존재는 개별 상품의 채택 근거가 아니다.

| 분류 | 구매 결과 | 대상 선택 |
|---|---|---|
| 포켓몬 | 도감에 해당 종 등록 | 없음 |
| 랜덤알 | 가방에 알 추가 | 없음 |
| 도구 | 가방 재고 증가 | 없음 |
| 파티 칸 | 다음 칸 해금 | 다음 칸 자동 지정 |

민트와 진화의돌은 채택했으며 상세와 진화 계약을 따른다. 현재 S4의 민트는 구매 즉시 적용된다. 새 설계는 가방에 보관한 뒤 사용한다. 이로치 약은 위의 후속 계약을 따른다. 프리미엄먹이 판매는 후속 사용자 결정이며 [일상 사용 계약](#일상-사용-계약)을 따른다.
상점에서는 사용할 포켓몬을 고르지 않는다. 대상과 옵션은 가방에서 고른다.
민트는 가방에서 대상과 성격을 고른다. 이로치 도구도 가방에서 대상을 고른다.
[스펙 미확정] 현재 소환한 포켓몬만 대상으로 삼는 기존 도구 제안은 상품별로 재검토한다. 경험사탕은 후속 결정에 따라 파티와 박스의 보유 개체 모두에게 사용할 수 있다. 파티가 비었다는 이유로 경험사탕 사용을 막지 않는다.
[스펙 미확정] 이로치 약의 소모 단위는 정하지 않았다. 일반색 전환은 후속 결정인 0원 돌아오는 약을 사용한다.
가격이 정해지지 않았으면 화면에 '미정'으로 표시한다. 이 경우 구매를 막는다. 기존 가격 예시는 새 경제 규칙 확정이 아니다.
파티가 비어 있어도 도구를 구매할 수 있다. 가방이 비면 상점 이동을 제공한다.
사용 시 소유, 수량, 대상, 적용 가능 여부를 검사한다.
성공한 경우에만 수량을 차감한다. 저장이 실패하면 수량과 개체 상태를 함께 복원한다.

[스펙 미확정] 기존 랜덤알 제안은 구매 → 가방 → 부화 시작 → 작업 → 도감 등록 흐름이다. 작업 부화 자체를 확정 기능으로 취급하지 않는다.
[스펙 미확정] 이미 등록된 종의 재구매는 막는 것으로 제안한다. 이전 HTML의 동작이며 현재 Figma나 런타임 구현 완료 항목이 아니다.
[스펙 미확정] 알 중복 결과의 보상·대체 정책은 별도로 확정한다.
[스펙 미확정] 가격, 획득 범위, 확률, 중복 정책, 필요 작업량, 동시 부화 수는 정하지 않았다.
[스펙 미확정] 작업량은 기존 에이전트 활동 감지를 재사용하는 방향으로 검토한다. 시간·토큰 환산 비율은 별도로 확정한다.
기존 작업량 전용 부화 제한은 새 대기시간 단축 계약에 적용하지 않는다. 최신 계약에서는 시간 경과로 준비가 진행되고 돌봄 활동으로 단축한다.
부화 결과는 저장한다. 재시도나 재실행으로 다시 추첨하지 않는다.
구매 전에 획득 범위, 확률, 부화 조건을 확인할 수 있어야 한다.

## 7. 설정과 연결

배너 알림 소리는 기본으로 켠다. 설정에서 무음으로 전환할 수 있다. 음원과 적용 범위는 [알림 배너 계약](#알림-배너의-개별-표시)을 따른다.

설정 내부에 '일반 / 연결'을 둔다. 독립 연결 탭은 제거한다.
일반은 포켓몬 표시, 클릭 통과, 다른 앱에서도 표시, 잠들기 기준, 언어, 로그인 시 시작이다.
놀이공간 항목은 `화면 전체 | 영역 지정` 전환 버튼 하나로 둔다. `영역 지정`을 고르면 `영역 그리기`가 나타난다. 영역을 그린 뒤에는 `다시 그리기`로 바뀐다. 근거는 [검수 P2 결정](../work/s5-design-system-v2/plan.md#검수-p2-결정-항목)을 따른다.
`로그인 시 시작`의 기본값은 켜짐이다. 사용자는 설정에서 끌 수 있다. 근거는 [로그인 시 시작 기본값](../work/s5-design-system-v2/plan.md#로그인-시-시작의-기본값-켜짐)을 따른다. `[스펙 미확정]` 기존 설치의 설정값 처리와 OS별 등록·실패 처리는 이식·구현 설계에서 정한다.
[스펙 미확정] 실행 환경 명칭은 '데스크톱에서 사용 / VS Code에서 사용 / 터미널 세션에서 사용'으로 제안한다.
내부 모드 식별자와 CLI 명령은 바꾸지 않는다. 모드를 고르는 새 옵션은 만들지 않는다.
데스크톱 실행은 항상 위 표시임을 설명한다. 세션 실행은 지원하는 표시 설정만 제공한다.
취침 대기 시간의 기본값은 5분이다. 선택지는 3분·5분·10분·15분·잠들지 않음이며 기존 제안이다. 성격·종 배율은 유지한다.
언어는 한국어와 영어다. 개발 실행의 자동 시작은 '앱 설치 후 사용 가능'으로 표시한다.

Claude Code, Codex CLI, Gemini CLI를 각각 연결·해제한다.
실제 훅 상태와 사용량 감지 상태를 구분한다. 화면 진입만으로 파일을 바꾸지 않는다.
미설치, 연결 안 됨, 연결됨, 확인 필요를 구분한다. 미설치 행에는 `다시 확인`을 둔다. 설치 후 누르면 다시 감지한다. 설치를 권하는 링크는 두지 않는다. 근거는 [검수 P2 결정](../work/s5-design-system-v2/plan.md#검수-p2-결정-항목)을 따른다.
토큰을 지원하지 않으면 '작업 시간으로 적립'으로 표시한다. 0으로 가장하지 않는다.
CLI별 친밀도를 추정하지 않는다. 오늘 날짜의 파티 전체 작업 적립만 표시한다.
훅 원본 누락은 `hook-source-missing`으로 구분한다. 사용자 훅과 백업 규칙을 유지한다.

## 8. 저장과 명령

2026-09-23 결정: 모듈 책임과 저장 구조는 [모듈 계약](modules.md)을 따른다. `SaveV3`를 새로 만들고 기존 `SaveV2`를 한 번 변환한다. 거래 실행기 모듈이 저장 쓰기와 중복 반영 방지를 맡는다. 근거는 [모듈 책임과 저장 구조 설계](../work/s5-design-system-v2/plan.md#모듈-책임과-저장-구조-설계)를 따른다. 아래는 이전 제안이다.

현재 [저장 타입](../../src/shared/types.ts)은 `SaveV2.party: Pet[]`에 개체를 직접 넣는다.
도감 해금과 개체별 육성 기록을 분리한다. 박스 보관 시 같은 개체의 기록을 보존한다. 기존 박스 제외 방향은 후속 보관 계약으로 대체했다.
[스펙 미확정] 저장 구조는 등록 종 목록, 개체별 육성 기록, 파티 배치, 도구 재고, 개별 알과 부화 상태로 제안한다.
[스펙 미확정] 저장 v2 유지 여부, 정확한 필드, 마이그레이션 절차는 구현 전에 확정한다.
기존 ID, 친밀도, 성격, 파티 순서, 표시, 포인트, 구매 권리를 보존한다.
별명과 `look`은 새 화면에서 쓰지 않는다. 원본 백업 없이 지우거나 별도 개체로 변환하지 않는다.
[스펙 미확정] 기존 먹이 재고와 이로치 권리의 보존·보상 정책은 정하지 않았다. 자동으로 폐기하지 않는다.

| 계약 제안 | 역할 |
|---|---|
| `settings:snapshot` | 등록 상태, 육성 기록, 파티 배치, 가방, 도감, 가격, 연결, 설정 조회 |
| `party.summon` / `party.dismiss` | 등록된 종 소환·교체 / 소환 해제 |
| `party.show` / `party.hide` | 표시 변경 |
| `bag.use` | 도구와 대상 검사, 적용과 차감 |
| `egg.*` | 세부 부화 규칙 확정 후 정의 |
| `shop.buy` | 구매 종류별 도감 등록·가방·칸 반영 |
| `feed` / `play` / `evolve` | 돌봄과 진화. 진화 기록 계약 재설계 |
| `pet.set` | 크기. 별명·모습 UI 추가 없음 |
| `agent.connect` / `agent.disconnect` | 개별 연결 |
| `settings.set` / `settings.open` | 환경 설정 변경 / 창 열기 |

[스펙 미확정] 명령 이름은 제안이다. 기존 처리기와 mailbox를 함께 갱신한다.
요청 ID로 중복을 막는다. 응답이 불확실하면 결과부터 조회한다. 자동으로 재구매하거나 재사용하지 않는다.
[스펙 미확정] writer 교체와 응답 유실에도 구매·사용·부화가 중복되지 않는 영속 정책이 필요하다. 구현 전에 확정해야 한다.
메인 프로세스가 잔액, 대상, 소유, 그림, 진화 조건을 검사한다. 렌더러는 저장을 직접 다루지 않는다.
환경 설정은 잠금 안에서 최신 값과 변경 키를 병합한다. 실패 시 이전 값으로 복원한다.
자동 시작과 훅은 실제 상태를 재조회한다. 통계 저장 실패를 등록 실패로 가장하지 않는다.

## 9. 창과 보안

런타임마다 설정창 하나를 둔다. 재요청은 기존 창을 앞으로 가져온다.
우클릭 메뉴는 개체 상세를 연다. 우클릭 메뉴는 포켓몬 위에서만 연다. 메뉴는 앱이 직접 그리며 구현 조건은 [UI 컴포넌트 계약](ui-components.md#c-21-포켓몬-위-우클릭-메뉴)을 따른다. 포켓몬이 없는 놀이공간 영역의 우클릭은 가로채지 않고 뒤의 앱으로 넘긴다. 메뉴는 이름·상태 / 밥 주기·놀아주기 / 설정의 세 묶음이며 종료는 두지 않는다. 이름·상태 줄은 만복도 구간 이름과 기분 구간 이름을 문구로 보여준다. 만복도 구간은 배고픔 말풍선의 구간과 같다. 근거는 [검수 P2 결정](../work/s5-design-system-v2/plan.md#검수-p2-결정-항목)을 따른다. 근거는 [포켓몬 위 우클릭 메뉴](../work/s5-design-system-v2/plan.md#포켓몬-위-우클릭-메뉴)를 따른다. 트레이 아이콘은 마지막 탭을 연다. `pokebuddy settings`는 실행 중인 앱의 창을 연다.
앱이 없으면 `pokebuddy companion` 실행을 안내한다. 임의로 새 게임을 만들지 않는다.
VS Code 확장 명령은 해당 창으로 요청한다. 숨김·클릭 통과에서도 복구 경로를 유지한다.
설정창을 닫아도 포켓몬과 육성은 계속 실행한다.
포켓몬 표시 창과 설정창은 별개다. 설정창은 클릭 통과와 항상 위 설정을 물려받지 않는다.
contextIsolation과 sandbox, 전용 preload, IPC sender·최상위 frame 검사를 유지한다.
키·값·명령을 검증한다. 그림 요청은 종·폼 키만 받는다. 임의 경로나 URL은 받지 않는다.
원격 탐색과 새 창을 막는다. 저작자 링크는 허용 목록에 있는 것만 외부로 연다.
보이는 동안 최대 초당 한 번 상태를 갱신한다. 성공 직후에는 즉시 갱신한다.
닫거나 숨기면 구독·타이머를 해제한다. 탭·검색·선택·스크롤·포커스를 유지한다.

## 10. 구현 순서와 위험

2026-09-21 설계 방향 논의: 사용자는 흐름과 와이어프레임에서 시작하는 컴포넌트 조립 방식을 요청했다. 향후 기능 추가를 고려한 모듈형 설계를 요청했다.
`[스펙 미확정]` 구체적인 단계와 책임 경계는 [설계 방향 초안](../work/s5-design-system-v2/plan.md#2026-09-21-설계-방향-후속-논의)을 따른다. 이 초안은 아래의 구현 순서를 변경하는 확정 결정이 아니다.

1. 미정 게임 규칙, 명칭, 저장 이식 정책을 확정한다.
2. 저장·명령 계약과 마이그레이션을 설계한다. 이를 검사한다.
3. 도감 등록·소환·가방 처리기, 거래 원자성, 중복 방지를 구현한다.
4. 전체 도감·지방 자료, 상품·알 규칙을 연결한다.
5. 설정창과 전용 preload를 만든다. 다섯 탭을 연결한다.
6. CLI·확장 진입, 메뉴, 언어, 연결을 검사한다.
7. 남은 JS를 TS로 이식한다. UI 검사와 이식 회귀는 나눈다. 설치 전 `postinstall.js`는 유지한다.

대상: [공유 타입](../../src/shared/types.ts), [저장](../../src/save/store.ts), [mailbox](../../src/save/mailbox.ts), [파티](../../src/main/party.ts), [상품](../../data/shop.json), [해금](../../data/unlocks.json), [종 자료](../../data/species.defaults.json), `src/renderer/`, `src/main/`.
위험: 저장 손실, 재소환·재등록으로 인한 보상 중복, 중복 소환, 사용 대상 경합, 중복 부화, 도감 성능, 지방 폼 오분류.

## 11. 인수 기준

2026-09-21 후속 설계: 최신 개체별 육성·박스·알·업적 흐름의 수용 조건은 [시나리오별 수용 확인](s5-scenarios.md)을 따른다. 아래 기존 항목 중 종 등록만으로 소환하는 계약과 종별 기록 계약은 재검토 대상이다. 충돌하는 과거 항목을 새 구현의 통과 기준으로 사용하지 않는다.

상품 사용·작업 부화 관련 기준은 해당 기능 채택 후에만 적용하는 기존 초안이다.

- 파티 0·1·6마리, 빈 칸·잠긴 칸·숨김을 구분한다. 숨김 마커와 표시 조작에 접근성 이름이 있다.
- 빈 칸에서 등록 도감을 연다. 해당 칸에 소환한다. 미등록 종·잠긴 칸의 소환을 막는다.
- 교체 취소와 저장 실패는 파티를 유지한다. 소환 해제와 재소환은 등록·육성 기록을 보존한다.
- 일반 도감 진입 시 이전에 취소한 목적지 칸을 재사용하지 않는다.
- 진화 전후 종을 모두 도감에서 소환한다. 반복 진화와 등록 보상 악용을 막는다.
- 파티가 가득 차도 구매·부화 결과를 도감에 등록한다.
- 별명, 모습, 독립 연결 탭이 없다. 박스 보관은 후속 계약에 따라 제공한다.
- 기본먹이와 프리미엄먹이를 구분한다. 프리미엄먹이의 확정하지 않은 수치를 구현 기준으로 사용하지 않는다.
- 지방과 검색을 함께 적용한다. 결과 0건을 안내한다. 없는 획득 규칙을 해금으로 표시하지 않는다.
- 구매 시 대상 선택이 없다. 사용 실패 시 재고와 개체 상태를 보존한다.
- [스펙 미확정] 작업 부화를 채택하면 재시도로 결과가 바뀌지 않는지 검사한다.
- 마이그레이션·실패 복원·writer 경합·중복 요청 검사를 통과한다.
- 키보드, 한영 문구, 720×780 기준 화면, 확정한 최소 창 크기, 100·125·150% 배율을 검사한다.
- 상세 하단의 성장·소환 해제에 스크롤로 도달한다. 모달에서 배경과 상태를 구분한다.
- 친밀도 값과 막대를 연결한다. 숫자별 variant를 만들지 않는다.
- 여섯 상태의 이동·복귀·실패 조작을 연결한다. 숨김 설명과 실제 종 타입을 일치시킨다.
- 실제 기능은 임시 HOME으로 검사한다. 실제 저장, 훅, 자동 시작은 변경하지 않는다.

## 12. 미정 사항과 검증 상태

[스펙 미확정] 종별 육성·재소환·보상 규칙, 진화 승계의 남은 항목, 진화의돌 종류·대상·가격, 알 경제·작업량, 이로치 약의 사용 세부 조건, 기존 권리 이식은 반드시 확정해야 한다. 확정한 개체 정보 유지 범위는 상세와 진화 계약을 따른다.
[스펙 미확정] 실행 환경 명칭과 지방 분류 기준은 제안 단계다. 동일 종의 동시 파티 배치와 박스 보관은 위의 계약을 따른다. 다섯 탭의 기존 시각 배치는 박스 UI 설계에서 재검토한다.
이번 단계는 설계까지만 진행한다. 기능 구현과 실제 저장 검증은 미시작이다.
이전 박스 시안 검수는 이전 버전에만 적용한다.
현재 시각 검증은 [v2 검수](../work/s5-design-system-v2/review.md)에 남긴다. 열린 디자인 항목 12개는 [v2 피드백](../work/s5-design-system-v2/feedback.md)을 따른다.
이전 HTML과 검수는 [과거 S5 자료](../archive/s5-legacy/README.md)에 보존한다. 현재 기능 구현의 근거로 사용하지 않는다.
