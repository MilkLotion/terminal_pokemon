# pokebuddy 설계 v2 — 무대 하나, 여섯 마리, 성격

> 2026-09-17 재설계. 1판(세션 펫 → 동반자)의 결정은 그대로 두고, 게임 구조를 "한 마리" 에서 **무대 하나에 최대 6마리, 마리마다 성격·상태** 로 바꾼다.
>
> 지금 어디까지 왔고 다음에 무엇을 하는지는 [`docs/progress.md`](progress.md).
> 코드는 기능별 모듈로 나눠 **TypeScript** 로 다시 짠다. 구현은 이 문서가 확정된 뒤에 시작한다.
> 지금 동작(0.3.0)의 설명은 [guide.md](guide.md). 1판 원문은 git 이력에.
> 숫자는 전부 자리표시자 — 규칙표 한 곳에 두고 써 보며 고친다.

> **S4 구현(2026-09-18):** 겹침 허용과 소환 순서 표시, 해금, 상점, 진화, 모습·색 선택을 구현했다. [계획](plan-s4.md)과 [검수 기록](review-s4.md)을 우선한다. S5 설정창 전까지 메뉴와 CLI로 조작한다. 기존 외부 Artifact는 이전 설계 기록으로 보존한다.

> **S3 초기값 확정(2026-09-17):** 중립 기준 켜 두기 시간당 친밀도 +6, 토큰 1천당 +1(토큰 미지원 작업 시간당 +12), 마리별 하루 친밀도 200, 토큰 1만당 포인트 +1·하루 50, 배고픔 시간당 +10·밥 −40, 활동성 +1이면 걷기 20% 증가. 사용자 승인 후 구현. 세부 규칙·검증·남은 실기는 [S3 검토 기록](review-s3.md). 원본 Artifact의 예시 숫자보다 이 확정값을 우선한다.

## 결정 (누적)

| 항목 | 결정 | 이유 · 비고 |
|---|---|---|
| 제품 형태 | 상주 Electron 앱 + 얇은 VS Code 확장 | 1판. 앱이 런타임·저장·설정창, 확장은 센서·런처·설정 미러 |
| 실행 형태 둘 | 독립 실행 `pokebuddy companion` 은 **항상 위**, 확장 기동은 **그 VS Code 창 안에서만** | 1판. M1 구현 완료 |
| 기존 세션 펫 | `!pokebuddy <종>` 은 샌드박스로 유지 — 게임 없음 | 1판 |
| **화면 구조** | **무대 하나** — 따라가는 창 크기의 투명 창 하나에 최대 6마리를 함께 그린다 | 마리마다 프로세스면 6마리도 수백 MB 에 창 추적 여섯 번. 무대 하나면 한 번이고, 펫들이 서로 보고 모일 수 있다 |
| **파티** | 최대 **6마리** — 원작 파티 여섯 칸 (처음 20 → 2026-09-17 5 → 같은 날 6). "다른 포켓몬으로 바꾸기" 는 없다 | 마리마다 크기·성격·표시를 따로 정한다 |
| **화면 참고** | 설정창은 **원작 게임 화면**을 따른다 — 파티 탭은 파티 화면(여섯 칸 카드, 빈 칸·잠긴 칸 포함), 카드를 누르면 요약 화면(상세), 도감 탭은 도감 목록, 상점 탭은 프렌들리숍 목록 | 사용자가 이미 아는 화면 문법. 스크롤 목록 대신 칸 (사용자 지시 2026-09-17: "포켓몬 게임 화면 레퍼런스로 참고 많이해") |
| **로고** | 픽셀 격자 로고 — 어두운 둥근 사각 배경 위 작은 터미널 창(신호등 점 셋 · `>` 프롬프트 · 초록 커서), 창 오른쪽 아래 모서리에 걸친 **민트색 여우 버디**(오리지널). 원본은 글자 지도 `assets/logo/src/logo.txt`, `npm run logo:build` 가 PNG(16~1024) · SVG · ICO · ICNS 를 만든다 | 실제 포켓몬·몬스터볼은 상표라 로고에 쓰지 않는다(2026-09-17 결정). 트레이는 첫 마리의 그림 그대로. 쓰이는 곳: mac 앱 번들(.icns) · Windows 창·설치(.ico) · VS Code 확장 카드(128) · README(svg) |
| **성격** | **원작 25개 성격 그대로.** 얻을 때 무작위, 상점의 민트로 바꿈. 성격 하나 = 다섯 축 값 | 사용자가 이미 아는 이름. "활발함" 설정을 대신한다 |
| **모듈** | 움직임 · 상태 · 도감/해금 · 상점 · 커맨드 처리기 · 저장 · 창 추적 · 화면 — 기능별로 나눈다 | 모듈은 자기 데이터만 소유, 커맨드 처리기만 여러 모듈을 잇는다 |
| **TypeScript** | `src/` 에 새 구조로 다시 짜며 TS. `tsc` 하나, 번들러 없음. `allowJs` 로 옛 JS 와 섞어 두고 하나씩 옮긴다 | 그대로 쓸 것(창 추적·판정·저장)은 옮기며 타입만, 다시 짤 것(움직임·상태·화면)은 TS 로 바로 |
| **종 프로필** | PokeAPI 데이터에서 1089종 기본값 + 손으로 다듬는 종만 override JSON | 1089종을 손으로 쓸 수 없다 |
| **메뉴 최소** | 우클릭 = 그 펫 한 줄 · 밥 주기 · 놀아주기 · 설정… · 종료. 트레이 = 설정… · 종료. **숨기기·고스트 모드·크기·성격은 전부 설정창** | "최대한 간단한 정보만, 설정에서 최대한 다" |
| **에이전트 연결** | 설정창 "연결" 탭에 Claude Code · Codex · Gemini **버튼을 따로**. 누르면 그 CLI 에 훅 등록, 옆에 연결 상태와 오늘 토큰 | 지금 `pokebuddy setup` 이 한 번에 하던 등록을 CLI 별로 |
| **토큰 → 친밀도·포인트** | 훅이 턴이 끝날 때 **토큰 사용량**을 읽어 적고, 상태 모듈이 친밀도와 포인트로 바꾼다. 토큰을 못 읽는 CLI 는 일한 시간으로 | "일한 양" 의 척도. CLI 별 읽기 가능성은 확인 필요 |
| **모습 선택** | 마리마다 **보이는 그림**을 도감에 해금된 모습(같은 진화 사슬 + 색) 중에서 고른다. 진화 단계·친밀도는 그대로 | 진화한 뒤 이전 모습을 원할 수 있다. 도감 해금이 곧 선택지 |
| 첫 화면 | 첫 실행만 스타터 선택 창, 이후엔 펫 + 트레이 | 1판 |
| 설정창 기술 | 바닐라 HTML · CSS (TS 로 컴파일한 ESM) | 1판. 빌드 도구는 tsc 뿐 |
| 친밀도 | 감소 없음. 기분·배고픔만 오르내림. 원천은 전부 — 켜 두기·밥·놀기·찌르기·에이전트가 일한 시간·턴 완료 | 1판 + 배고픔 추가 |
| 문구 | 호칭 없이 동사만 · 종료 · 담담한 설명 · 기분은 말로 · 안 되는 항목은 이유를 항목에 · 설정 라벨은 두세 글자 명사 · "게임 켬/끔" 없음 | 1판. `lib/i18n/<언어>.json`, 이름은 `lib/names.json` |
| 언어 | 기본 ko, en 준비 | 1판 |

## 한 문장

터미널 창 위의 투명한 무대에 내 포켓몬들이 각자 성격대로 돌아다니고, 내가 개발하는 동안 친해지고, 배고프면 밥을 달라 하고, 친밀도가 차면 진화하며, 조건을 채우면 새 종이 해금된다.

## 화면 — 무대 (stage)

| 항목 | 설계 |
|---|---|
| 창 | 투명 BrowserWindow 하나. 크기는 **따라가는 창 ∩ 그 창이 있는 디스플레이**. 창은 `setBounds` 로만 옮기고 마리는 무대 안에서 움직인다 — 옛 "창이 곧 펫" 의 이동 해석(자기 이동 구분·드래그 유예)이 전부 사라졌다. 독립 실행은 맨 앞 터미널 창을 따르고 항상 위, 창 모드는 그 VS Code 창 바로 위 — 1판 판정 그대로(`src/follow/`) |
| 그리기 | 캔버스 하나에 N마리(`src/renderer/stage.ts`). 메인이 40ms 마다 `StageFrame`(마리별 모습 · 배율 · 몸 좌상단 · 동작)을 보내고, 렌더러는 마리별 `Animator` 로 프레임만 진행한다. 시트는 모습(look)마다 한 번 보내 디코드해 둔다. 그림은 **PMD 전용**(2026-09-17 사용자 승인 — GIF·시트 그림 로더와 `art` · `gif` · `pos` · `fps` · `scale` 옵션은 지웠다. 남은 CLI 옵션은 `dot` · `buddy` · `keep` · `click`, 동반자는 `buddy` · `click` 만). 문서에 CSP(스크립트는 자기 파일, 그림은 data URL) |
| 클릭 | 창은 기본 클릭 통과. 메인이 40ms 마다 커서 자리를 묻고 렌더러가 시트 알파로 **마리 id 또는 없음**을 답한다(겹치면 위에 그린 것). id 면 통과를 끄고, 없으면 켠다. 들고 있는 동안은 끈다. 고스트 모드면 늘 통과 |
| 드래그 | 그 마리만 옮긴다. 놓은 자리는 마리의 `home` — **무대 오른쪽 아래 기준 몸 좌상단 오프셋**(옛 앵커 오프셋과 같은 뜻). 동반자·창 모드는 저장 v2, 세션 펫은 `config.json`. 무대 크기가 바뀌면 오른쪽 아래에 붙어 따라온다 |
| 겹침 | 겹침을 허용한다. 먼저 소환한 마리를 먼저 그린다. 나중에 소환한 마리가 앞에 보인다. 드래그·목록 갱신·모습 변경은 순서를 바꾸지 않는다. 숨긴 마리를 다시 소환하면 마지막에 그린다. 재시작 때는 저장된 파티 순서로 소환한다. |
| 표시 | 마리마다 "화면에 보임" 토글(설정창). 파티에는 있지만 안 보이는 마리도 상태는 흐른다. 나오는 마리 수는 `min(칸 수, SAVE_RULES.slots.max)` |
| 여러 창 | 창 모드에서 창이 여럿이면 무대도 여럿. **임시 규칙(S2)**: 각 무대에 파티 전원, 집 저장은 저장을 쥔(writer) 무대만 — 확정되면 `src/main/party.ts` 한 곳만 바꾼다 [스펙 미확정] |
| 실측 (mac, 2026-09-17) | 2560×1324 무대에 6마리 — CPU 메인 1.1% + GPU 4.6% + 렌더러 4.8% ≈ 10.5%. 목표선(10%)에 걸친다. 줄일 곳: 바뀐 프레임만 그리기는 이미 함, 다음 후보는 무대를 아래쪽 띠로 줄이기 · 프레임 변화분만 보내기 |

## 모듈 — `src/` 와 `data/`

```
src/
├─ shared/              공유 타입 — types.ts(저장·명령·상태) · stage.d.ts(메인↔preload↔렌더러 무대 계약, 타입만) · clock.ts
├─ main/                Electron 메인                                                     ✓ S2
│  ├─ app.ts            기동 순서 · 모드(session · window · companion) · 단일 인스턴스 · 종료 정리
│  ├─ anchor.ts         창 추적 상태기 — 헬퍼 질의 · 앵커 확정(2회) · 모드별 target · 보임 판정 · z-order 대상
│  ├─ stage-window.ts   무대 창 — setBounds · 보임/숨김 · z-order · 클릭 통과 · hover 질의 · IPC
│  ├─ stage.ts          무대 상태 + 25fps 틱 — 마리별 움직임 · 집 · 들기 · 소환 순서 · StageFrame · 포인터 처리
│  ├─ party.ts          마리 목록의 출처 — 저장 v2(writer/reader) 또는 세션 샌드박스 · 첫 실행 스타터 · v1 사본 · 집 저장
│  ├─ layout.ts         자리 계산(순수) — homeSpot · petSpot · roamBox · clampInStage · stageOf
│  ├─ art.ts · text.ts · paths.ts   옛 art/ · lib/i18n · lib/names · config.js 를 타입으로 감싼 층 (S5 에 이식)
│  ├─ commands.ts       커맨드 처리기 등록 — quit · settings.set · party.show/hide · pet.set(home) · poke · snapshot, writer 면 mailbox
│  ├─ menus.ts · tray.ts · shortcuts.ts · picker-window.ts · lifetime.ts(pid 파일 규약) · preload.ts
│  └─ settings-window.ts                                                                  S5
├─ follow/              창 추적 · 에이전트 판정 — state · front · line-helper · winbounds   ✓ S2 (옛 lib/ 에서 타입만 붙여)
├─ motion/              움직임 — brain(판단) · pet-motion(마리 하나) · rules · params(성격 배율 자리)   ✓ S2 (옛 buddy/ 1:1)
├─ renderer/            무대 캔버스 — stage · sprites(Animator) · hit · pointer, 스타터 화면 picker (ESM, tsconfig.renderer.json)   ✓ S2
├─ agents/              에이전트 연결 — usage(토큰 증분) · registry(연결·해제·상태)     ✓ S1
├─ save/                저장 v2 · 잠금 · mailbox · 규칙표                                 ✓ S1
├─ commands/            dispatcher                                                        ✓ S1
├─ dex/                 성격 · 종 프로필 · 진화 사슬 · 해금 판정                          ✓ S1
├─ hooks/               CLI 상태 훅 (설치 이름 pokebuddy-state.cjs)                       ✓ S1
├─ tools/               데이터·로고 빌드, 자체 확인 6벌, dev-save                          ✓
├─ state/               상태 — 마리별 배고픔 · 기분 · 친밀도 · 잠, 종 프로필 + 성격 축, 하루 상한   S3
├─ shop/                상점 — 포인트 · 가격표 · 구매 · 민트 · 칸                           S4
└─ cli/                 pokebuddy 명령                                                     S5 (지금 cli/*.js)
data/
├─ natures.json            25 성격 → 다섯 축
├─ species.defaults.json   PokeAPI 에서 뽑은 기본 프로필 (src/tools/build-species.ts, 빌드 산출물)
├─ species.overrides.json  손으로 다듬은 종 (스타터·자주 보는 종)
├─ unlocks.json            종별 해금 조건
├─ evo.json                진화 사슬 (PokeAPI, 빌드)
├─ shop.json               가격표·칸                                                        S4
└─ names.json · dex.json   (지금 lib/ — S5 에 옮김)
```

모듈 규칙
- 모듈은 자기 데이터만 소유한다. 다른 모듈의 데이터는 함수로만 읽고 쓴다
- 시각·파일·Electron 은 주입한다 — `motion` · `state` · `dex` · `shop` 은 node 만으로 시험한다 (지금 brain.js · economy.js 의 태도)
- 커맨드 처리기만 여러 모듈을 잇는다. 메뉴·설정창·CLI 는 처리기에 요청을 보내고 결과 코드로 문구를 만든다
- 숫자는 모듈마다 규칙표 하나(`RULES`)에

아직 옛 JS 로 남은 것 — `config.js` · `art/` · `lib/`(i18n · names · dex · zip · electron) · `cli/*.js` 는 S5 에 옮긴다(`src/main/` 이 타입 층으로 감싸 쓴다). `game/`(1판 한 마리 코어)는 S2 정리에서 지웠다 — S3 `state/` 가 바탕으로 삼을 규칙표·하루 상한·스트릭은 `git show da9ac04:game/economy.js` 로 본다. `pokebuddy status` 의 게임 줄은 저장 v2 파티 요약(칸 · 포인트 · 마리별 이름 · 성격 · 보임 · 친밀도)으로 바뀌었고 `pokebuddy-status` 별칭은 없앴다. **S2 동안 친밀도·기분·포인트 적립은 멈춘다**(사용자 확인 2026-09-17).

## 성격 — 25개를 다섯 축으로, 축은 배율

이 앱에 능력치는 없다. 원작에서 성격 25개가 "어느 능력치를 올리고 어느 것을 내리나" 로 정의된 것을 **25개 이름을 다섯 축에 나눠 넣는 규칙으로만** 빌린다 — 그래서 25개가 저절로 서로 다른 값이 된다. 앱 안에서 축의 값(+1 · 0 · −1)은 전부 **배율**로 작동한다. 아래 배율은 예시고 숫자는 규칙표(`natures.json`)에서 손본다.

| 축 | 원작에서 빌린 자리 | 오르면 (+1) | 내리면 (−1) |
|---|---|---|---|
| 활동성 | 스피드 | 더 자주·빨리 걷는다. 켜 두기 친밀도 ×1.2. 배고픔 ×1.2 | 오래 서 있다. 켜 두기 ×0.8. 배고픔 ×0.8 |
| 대담함 | 공격 | 커서를 쫓고 클릭에 크게 반응한다. 에이전트 실패에 기분 −2 | 커서를 피한다. 실패에 기분 −8 |
| 안정성 | 방어 | 기분 변동 ×0.7. 잠이 빨리 든다 | 기분 변동 ×1.3. 잠이 늦다 |
| 사교성 | 특수공격 | 다른 펫 옆으로 모인다. 함께 있으면 기분 +, 놀아주기 친밀도 ×1.3 | 거리를 둔다. 혼자여도 기분이 안 떨어진다. 놀아주기 ×0.8 |
| 인내 | 특수방어 | 배고픔 ×0.8. 방치에 기분이 늦게 떨어진다. 밥 친밀도 ×0.8 | 배고픔 ×1.3. 밥 친밀도 ×1.3 |

| 오르는 축 \ 내리는 축 | 대담함 | 안정성 | 활동성 | 사교성 | 인내 |
|---|---|---|---|---|---|
| **대담함** | 노력 | 외로움 | 용감 | 고집 | 개구쟁이 |
| **안정성** | 대담 | 온순 | 무사태평 | 장난꾸러기 | 촐랑 |
| **활동성** | 겁쟁이 | 성급 | 성실 | 명랑 | 천진 |
| **사교성** | 조심 | 온화 | 냉정 | 수줍음 | 덜렁 |
| **인내** | 차분 | 얌전 | 건방 | 신중 | 변덕 |

대각선 다섯(노력·온순·성실·수줍음·변덕)은 축이 0 인 중립 성격 — 변덕만 "가끔 아무 축이나 잠깐 튄다" 를 붙인다. 값은 오르는 축 +1, 내리는 축 −1, 나머지 0 을 기본으로 두고 `data/natures.json` 에 적는다. 얻을 때 무작위로 붙고, 상점의 **민트**로 바꾼다. 화면에는 성격 이름만 보인다(설정창 마리 카드).

## 상태 — 마리별

| 지표 | 범위 | 오르는 것 | 내리는 것 |
|---|---|---|---|
| 배고픔 | 0~100 (높으면 배고프다) | 시간 × 종의 `hungerRate` × (활동성 +, 인내 −) | 밥 −40 |
| 기분 | 0~100 | 밥·놀기·찌르기, 함께 있기(사교성), 에이전트 턴 완료 | 배고픔 70 넘음, 방치, 에이전트 실패(대담함이 낮으면 더), 혼자(사교성 높으면) |
| 친밀도 | 누적, 감소 없음 | 1판 원천 전부 — 켜 두기·밥·놀기·찌르기·**토큰(또는 일한 시간)**·턴 완료. 하루 상한은 마리별 | — |
| 잠 | 잠들기까지 남은 시간 | 입력 없음 × (안정성 +) | 입력·클릭·에이전트 일 시작 |

- 시간 원천(켜 두기·일한 시간·턴 완료)은 **무대에 나온 마리 전원**이 각자 받되 **마리마다 양이 다르다** — 종 프로필의 배율 × 성격 축의 배율 (사용자 결정 2026-09-17: "포켓몬마다 시간에 따른 친밀도가 다르니 그 시간마다 주면 된다"). 밥·놀기·찌르기는 그 마리에게
- **일한 양** — 연결된 에이전트가 토큰을 읽을 수 있으면 토큰(예: 1천 토큰에 친밀도 +1, 1만 토큰에 포인트 +1, 하루 상한), 못 읽으면 `running` 시간(1판). 훅이 `Stop`(턴 끝) 때 사용량을 상태 기록에 `usage: { in, out }` 로 적고, 상태 모듈이 tick 에서 증분을 읽는다. 무대의 전원이 마리별 배율로 받는다
- 배고픔이 높으면 펫이 밥그릇 쪽을 보거나 우클릭 첫 줄이 "배고픔" 을 먼저 말한다. 배고픔 자체는 친밀도를 깎지 않는다
- 종 프로필(`species.defaults.json`) — `affinityRate`(시간 원천 배율), `hungerRate`(체중↑·기본 스피드↑), `sleepiness`(체중↑), `moodBase` · `moodSwing`, `likes`(함께 일하기·놀기·모임 중 무엇에 더 반응하나 — 타입에서 기본값). 뽑는 규칙과 값은 **Claude 가 맡는다**(사용자 결정) — 1089종 기본값을 만들고 스타터 29종은 손으로 다듬어 다섯 종만 표본으로 보인다. 자주 보는 종은 `species.overrides.json` 에

## 도감 · 해금 — `data/unlocks.json`

```json
{
  "eevee":    { "starter": true },
  "raichu":   { "evolve": { "from": "pikachu", "affinity": 500 } },
  "umbreon":  { "evolve": { "from": "eevee", "affinity": 500, "when": "night" } },
  "snorlax":  { "shop": 800 },
  "ditto":    { "party": { "count": 3 } },
  "lapras":   { "work": { "hours": 100 } },
  "chansey":  { "streak": { "days": 14 } },
  "raichu-alola": { "bond": { "of": "pikachu", "affinity": 1500 }, "shop": 300 }
}
```

| 조건 | 뜻 | 판정에 필요한 것 |
|---|---|---|
| `starter` | 첫 실행 선택 화면에 나온다 | — |
| `evolve` | 그 종을 가진 마리의 친밀도가 임계에 닿으면 그 마리가 바뀐다. `when` 은 시간대 | 마리별 친밀도, 시각 |
| `shop` | 해금된 뒤 포인트로 산다 | 포인트 |
| `party` | 파티 마리 수 | 저장 |
| `work` | 에이전트와 함께 일한 누적 시간 | 통계 `totals.workMs` |
| `streak` | 연속 교감 일수 | 저장 |
| `bond` | 특정 종을 가진 마리의 친밀도 | 저장 |
| `time` · `event` | 시간대·날짜 | 시각 |

- 조건 종류마다 판정 함수 하나 `check(cond, world) → boolean`. `world` = 저장 + 누적 통계 + 시각. 여러 조건이 있으면 전부 만족
- **해금 ≠ 획득.** 해금되면 상점(`shop` 가격이 있으면)이나 무료 획득 목록에 나타난다. 진화 조건을 만족해도 종을 자동으로 바꾸지 않는다. `evolve` 명령으로 진화한다
- 진화는 `evo.json` 의 사슬을 따르고 분기는 사용자 선택(1판). 에버스톤은 그 마리에 붙는다
- 누적 통계 `totals` 를 저장에 둔다 — `workMs · presenceMs · turns · days · fed · played`

## 상점

| 항목 | 효과 | 비고 |
|---|---|---|
| 새 종 | 해금된 종을 빈 칸에 더한다 (성격 무작위) | 가격은 `unlocks.json` 의 `shop`. 빈 칸이 없으면 못 산다 |
| **파티 칸(슬롯)** | 파티 칸을 늘린다 — 처음 1, 최대 6 | 진행의 뼈대. 빈 칸이 없으면 새 종을 살 수 없다 — 칸을 먼저 산다 (원작에서 파티가 차면 못 받는 것과 같다). 무대에 안 내보내는 것은 마리별 "보임" 으로 |
| 민트 | 그 마리의 성격을 고른 것으로 | 원작 그대로 |
| 먹이 | 다음 밥의 효과 ×2 | |
| 에버스톤 | 그 마리의 진화 잠금 | |
| 색 | 그 마리 shiny. 일반색으로 되돌려도 구매 권리 유지 | 기본 종의 PMD 경로 `<도감번호>/0000/0001`. 그림 확인 성공 후 차감 |

포인트 원천 — 토큰, 진화, 단계 달성(진화 없는 종), 하루 첫 교감 스트릭, 조건 해금. S4 초기 가격과 보상은 `data/shop.json`에 있다.

## 커맨드 처리기 — `src/main/commands.ts`

입력 넷(우클릭 · 트레이 · 설정창 IPC · mailbox 파일 = CLI·확장)이 같은 모양 `{ cmd, target?, args, from }` 으로 들어와 모듈에 분배되고, 결과는 코드로 나간다. 문구는 표면(메뉴·설정창·CLI)이 언어 파일로 만든다.

| 명령 | 대상 | 모듈 |
|---|---|---|
| `feed` · `play` · `poke` | 마리 | state (+ motion 에 연출 요청) |
| `evolve` | 마리, 후보 | dex → state |
| `party.show` · `party.hide` · `party.remove` | 마리 | state · stage |
| `pet.set` (크기 · 성격은 민트로만 · 별명) | 마리 | state · stage |
| `pet.look` (모습 · 색 — 해금된 것 중) | 마리 | dex → stage |
| `agent.connect` · `agent.disconnect` | CLI 이름 | agents (훅 등록·해제) |
| `settings.set` (표시 · 고스트 모드 · 잠들기 · 언어 · 자동 시작) | 전역 | main |
| `shop.buy` | 항목, 마리 | shop → dex/state |
| `snapshot` | — | 전 모듈 읽기 |
| `quit` | — | main |

## 메뉴 — 최소

```
펫 우클릭 (그 마리)              트레이                 설정창 (탭 다섯)
├─ 피카츄 · 용감 · 배고픔 조금    ├─ pokebuddy           ├─ 파티   여섯 칸 카드(원작 파티 화면) → 카드를 누르면 상세(요약) 화면 — 아래 절
├─ 밥 주기                       ├─ 설정…               ├─ 도감   해금된 종 · 조건 · 진화 트리 · 그린 사람
├─ 놀아주기                      └─ 종료                ├─ 상점   포인트 · 슬롯 · 새 종 · 민트 · 먹이 · 에버스톤 · 색
├─ ───────                                              ├─ 연결   Claude Code [연결] · Codex [연결] · Gemini [연결] — 상태 · 오늘 토큰 · 오른 친밀도
├─ 설정…                                                └─ 설정   표시 · 고스트 모드 · 잠들기 · 언어 · 자동 시작
└─ 종료
```

- 우클릭 첫 줄은 그 마리의 이름 · 성격 · 지금 가장 말할 것(배고픔 / 기분 / 친밀도 다음 임계까지) 한 가지
- 진화하기는 그 마리가 준비됐을 때만 우클릭에 나타난다
- 첫 실행 스타터 선택 창은 그대로(1판) — 고르면 첫 칸에

### 설정창 — 파티 탭과 상세 화면

원작 파티 화면을 따른다. 스크롤 목록이 아니라 **여섯 칸**이 늘 보이고, 칸의 상태(마리 · 빈 칸 · 잠긴 칸)가 진행을 말한다.

```
┌ 파티 ───────────────────────┬─────────────────────────────┐
│ [이브이 그림]  이브이         │ [피카츄 그림]  피카츄         │
│   용감 · 기분 좋음            │   명랑 · 기분 보통            │
│   친밀도 ▓▓▓▓▓▓▓░░░ 340/500  │   친밀도 ▓▓░░░░░░░░ 120/500  │
│   배고픔 ▓▓▓░░░░░░░          │   배고픔 ▓▓▓▓▓▓░░░░          │
├─────────────────────────────┼─────────────────────────────┤
│   빈 칸 — 상점에서 새 종      │   🔒 칸 — 상점에서 (80)       │
├─────────────────────────────┼─────────────────────────────┤
│   🔒                         │   🔒                         │
└─────────────────────────────┴─────────────────────────────┘
  칸 2 / 6 · 포인트 120
```

| 칸 | 보이는 것 | 누르면 |
|---|---|---|
| 마리 | 그림(움직이는 스프라이트) · 이름(별명) · 성격 · 기분 말 · 친밀도 바(다음 진화까지) · 배고픔 바. 무대에 안 보이는 마리는 흐리게 | 상세 화면 |
| 빈 칸 | "빈 칸 — 상점에서 새 종" | 상점 탭(새 종) |
| 잠긴 칸 | 자물쇠 · 칸 가격 | 상점 탭(칸) |

상세 화면은 원작 요약 화면. 위에 `← 파티`, 큰 그림, 이름 · 종 · 성격 · 함께한 날. 마리별 값과 행동은 전부 여기에.

| 줄 | 내용 |
|---|---|
| 상태 | 친밀도 340/500 (진화까지 160) · 배고픔 · 기분 말 · 잠 |
| 조절 | 크기 ─●─ · 모습 ▾ (해금된 모습 중) · 보임 ☑ · 에버스톤 ☐ |
| 행동 | 밥 · 놀기 · 진화 (준비됐을 때만 살아난다, 아니면 조건을 회색으로) · 별명 |
| 진화 | 사슬(이브이 → 여덟 갈래)과 다음 조건 한 줄 — 도감 탭과 같은 자료 |

## 저장 v2 — `save.json`

```json
{
  "v": 2,
  "points": 120, "slots": 1,
  "party": [
    { "id": "p1", "species": "umbreon", "look": "eevee", "shiny": false, "nature": "jolly", "nick": null, "size": 3, "shown": true,
      "home": { "dx": -24, "dy": -60 },
      "hunger": 35, "mood": 72, "affinity": 340, "stage": 1, "everstone": false,
      "since": 1758000000000, "fedAt": 1758080000000, "playedAt": 1758070000000,
      "daily": { "gained": 80, "pokes": 3 } }
  ],
  "daily": { "date": "2026-09-17", "streak": 4, "interacted": true },
  "totals": { "workMs": 0, "presenceMs": 0, "tokens": 0, "turns": 0, "days": 1, "fed": 0, "played": 0 },
  "agents": { "claude": { "connected": true, "tokensToday": 48210, "tokensTotal": 913000 }, "codex": { "connected": false } },
  "unlocked": ["eevee", "pikachu"],
  "inventory": { "berry": 3, "mint": 0 },
  "acc": {},
  "log": []
}
```

`slots` 는 파티 칸 수(처음 1, 최대 6) 이고 `party` 길이는 `slots` 를 넘지 않는다 — 1판 저장을 옮길 때 마리가 더 많으면 마리 수만큼 칸을 준다. 시각은 전부 ms. 마리에 `id` 를 두어 종이 바뀌어도(진화) 같은 마리다. `species` 는 게임상의 종(진화 단계), `look` 은 보이는 그림 — 없으면 species 와 같다. 쓰는 프로세스 하나(잠금)·mailbox·파손 시 `.bak` 은 1판 그대로.

## TypeScript 전환

- `tsconfig.json` — 메인 · CLI 가 부르는 모듈 · 도구. `strict` · `noUncheckedIndexedAccess`, `module nodenext`(CJS 출력), `src/` → `dist/`, `src/renderer/**` 제외
- `tsconfig.renderer.json` — 렌더러(브라우저 ESM). `module es2022` · `moduleResolution bundler` · DOM lib · `verbatimModuleSyntax`, `src/renderer` → `dist/renderer`. 공유 계약은 선언 파일 `src/shared/stage.d.ts` 로 두 설정이 같이 읽는다(`.ts` 면 rootDir 밖이라 거부된다). HTML 은 `src/renderer/*.html` 에 두고 `../../dist/renderer/*.js` 를 부른다
- `package.json` — `main: dist/main/app.js`, `build` = 두 tsc, `check` = 두 tsc --noEmit, `prepare` = build(git clone 뒤 `npm install` 이 빌드까지), `start` = build + electron. 배포 `files` 에 `dist/` · `data/` · `src/renderer/*.html` · `assets/logo/out/`. 번들러 없음, 개발 의존성은 `typescript` · `@types/node`
- 옮긴 파일은 옛 자리에서 지운다 — 두 벌이 되면 진단이 거짓말을 한다(1판 원칙). S2 는 단위 넷을 병렬로 옮기는 동안 옛 앱이 돌게 두고, 통합 확인 뒤 한 번에 지웠다(`main.js` · `preload.js` · `buddy/` · `renderer/` · `lib/state` · `lib/follow` · `lib/line-helper`)

## 순서

| 단계 | 내용 | 끝나면 보이는 것 |
|---|---|---|
| **S0 설계 확정** | 이 문서. 미확정 항목 답 | — |
| **S1 뼈대** | 2026-09-17 완료 — TS 빌드(`tsconfig.json` · `npm run build` → `dist/`), `src/shared/types.ts` · `clock.ts`, `src/save/`(저장 v2 · v1 이전 · 잠금 · mailbox), `src/commands/dispatcher.ts`, `data/`(natures 25 · species.defaults 1089 · overrides 29 · evo 484간선 · unlocks 514종) + `src/dex/`(성격·프로필·진화 사슬·해금 판정), `src/agents/`(토큰 증분 · CLI 별 연결/해제/상태), 훅의 Claude 토큰 누적. 자체 확인 4벌 통과. 배럴(index.ts) 없이 직접 import | `node` 로 저장·명령·데이터·해금 판정이 돈다. 화면 변화 없음 |
| **S2 무대** | 2026-09-17 완료 — 투명 무대 창 하나(따라가는 창 ∩ 디스플레이)에 파티 최대 6마리, 마리별 히트 · 드래그 · 집 · 밀어내기, `src/main/` 16 · `src/follow/` · `src/motion/`(옛 brain 과 27만 틱 동치) · `src/renderer/`(ESM), 옛 JS 삭제(메인 · 렌더러 · 움직임 · 추적 · 1판 게임 코어 · GIF/시트 로더), 자체 확인 6벌. 세 모드 기동 · 따라가기 · 종료 실기 확인, 6마리 CPU ≈ 10.5%. 마우스 입력(드래그 · 클릭 · 우클릭 · 단축키 · 앱 전환 숨김 · 선택 창)은 사용자 실기 확인 필요 | 지금 펫이 무대 위에서 그대로 돈다. 두 마리를 띄워 서로 다른 자리에 |
| **S3 성격·움직임·상태** | `natures.json` → motion 파라미터, 마리별 brain, 배고픔·기분·잠, 밥·놀기 연출(열매로 걸어가 먹기 · 커서 쫓기), 모임 | 성격이 다른 두 마리가 다르게 움직인다. 배고프면 밥을 달라 한다 |
| **S4 도감·해금·상점·진화** | 2026-09-18 구현·자동 검수 완료. 해금·슬롯·상점·민트·먹이·에버스톤·색·진화 연출·분기·모습 선택. 메뉴와 `pokebuddy game` 연결 | 조건을 채우면 도감에 해금이 뜨고 상점에서 산 종이 무대에 나온다 |
| **S5 설정창·메뉴·연결** | 설정창 탭 다섯(파티 여섯 칸 → 상세 화면 · 도감 목록 · 상점 · 연결 · 설정), 최소 메뉴, 스타터 화면, 확장·CLI 연결, 옛 JS 삭제 | 사용자가 보는 모든 조작이 설정창에 |
| **S6 배포** | 1판 M5 | |

## 미확정 · 확인 필요

- [확인 필요] 따라가는 창 크기의 투명 창 — 클릭 통과·GPU (mac · Windows). 창 모드에서 창 위 z-order 유지
- [확인 필요] 6마리 × 25fps 성능 — mac 실측 ≈ 10.5%(메인 1.1 · GPU 4.6 · 렌더러 4.8). Windows 큰 투명 창(DWM)과 클릭 통과 전달은 아직
- [스펙 미확정] 성격 축의 배율 숫자 — 사용자가 생각한 배율이 있으면 그것으로
- 종 프로필 매핑 규칙(체중→배고픔 등)은 Claude 가 정해 표본으로 보인다 — 사용자 검토 항목 아님
- [스펙 미확정] 창 모드에서 창이 여럿일 때 파티 배분
- [확인 필요] **토큰 사용량 읽기** — Claude Code 는 됐다(S1): `Stop`·`StopFailure` 훅이 `transcript_path` 의 지난 바이트 뒤를 읽어 assistant 응답의 usage(in · out · cacheRead · cacheWrite)를 누적, `SessionStart` 에 기준점. Codex 는 `~/.codex/sessions/` 의 세션 기록에서 `session_id` 로 찾아야 할 것. Gemini 는 미확인. 못 읽는 CLI 는 일한 시간으로 대체
- [스펙 미확정] 토큰 → 친밀도·포인트 환율과 하루 상한
- [스펙 미확정] 숫자 전부
