# 화면 도트 스타일 — Galmuri 글꼴과 원작 창틀

모든 화면의 글꼴을 Galmuri로 바꾼다. 카드·대화상자·말풍선을 원작 3세대 창틀 모양으로 바꾼다.
작업 절차는 [문서 작업 절차](../../contributing/workflow.md)를 따른다.

| 작업 | 날짜 | 상태 |
|---|---|---|
| 조사와 설계 | 2026-09-26 | 설계 작성 |
| Figma 반영 | 2026-09-26 | 완료. 글꼴 Galmuri, 3세대 창틀은 되돌림, 파티 칸 B 배치, 페이지 정리 |
| 코드 반영 | 2026-09-26 | 완료. 검수 통과 |

## 설계

날짜: 2026-09-26. 상태: 설계. 기능 구현은 미시작이다.

### 사용자 결정

| 날짜 | 원문 | 뜻 |
|---|---|---|
| 2026-09-26 | "폰트랑 스타일관련도 저 어나더레드가 실제 포켓몬처럼 되어있어서 참고많이하고싶은데" | 팬게임 포켓몬스터 어나더 레드의 글꼴과 화면 모양을 참고한다. |
| 2026-09-26 | "Galmuri 랑 그거 사용하는거로 전체화면에 적용하자. figma 보면서 어디 수정해야할지 분석" | Galmuri 글꼴과 창틀을 모든 화면에 적용한다. "그거"는 직전 대화에서 설명한 창틀이다. |
| 2026-09-26 | "권장대로하고, 색은 원작 최대한 따라가고싶은데?" | 아래 크기 대응과 적용 대상을 제안대로 쓴다. 창틀 색은 원작 값을 따른다. |
| 2026-09-26 | "이제보인다. 작업진행. 이 프로젝트 다른세션에 작업할거 미리 공지하고 작업진행해." | 작업을 시작한다. 같은 날 다른 세션 `terminal-pokemon-18`에 범위를 공지했다. |

### 조사 결과

관측일은 2026-09-26이다.

- 어나더 레드는 Pokémon Essentials(RPG Maker XP)로 만들었다. 어나더 레드가 쓰는 한글 글꼴의 이름은 공개 자료에서 찾지 못했다. [나무위키](https://namu.wiki/w/%ED%8F%AC%EC%BC%93%EB%AA%AC%EC%8A%A4%ED%84%B0%20%EC%96%B4%EB%82%98%EB%8D%94%20%EB%A0%88%EB%93%9C)
- [Galmuri](https://github.com/quiple/galmuri)는 닌텐도 DS 글꼴을 참고한 도트 글꼴이다. 라이선스는 SIL OFL 1.1이다. 앱에 넣어 배포할 수 있다.
- Galmuri는 정해진 크기에서만 선명하다. Galmuri14는 15px, Galmuri11은 12px, Galmuri9는 10px, Galmuri7은 8px이다. 굵은 글꼴은 Galmuri11 Bold 하나다.
- 창틀 색은 PokéRogue 저장소의 3세대 창틀 그림 `images/ui/legacy/windows/window_1.png`~`window_3.png`에서 화소 값을 읽었다. 그림 파일은 쓰지 않는다. 색 값만 쓴다.

### 목표

- 모든 화면의 글자를 Galmuri로 표시한다.
- 컨테이너 컴포넌트 14개를 3세대 창틀 모양으로 표시한다.
- Figma와 코드의 글꼴·창틀 값을 같게 둔다.

### 범위

**Figma** (`MA3K41Y6omAi5mRu6YDFly`)
- `00 · Foundations`: 글자 스타일 `PB/Type/*` 9개의 크기와 줄 높이를 아래 표로 바꾼다. 창틀 색 변수와 창틀 효과 스타일을 추가한다.
- `02 · Molecules`, `03 · Organisms`: 아래 창틀 대상 14개에 창틀을 적용한다.
- `05 · Screens`: 화면 43개에서 글자 넘침과 스타일에 묶이지 않은 글자를 점검한다.

**코드**
- `assets/fonts/`: Galmuri11, Galmuri11-Bold, Galmuri14, Galmuri9의 woff2 파일과 OFL 전문을 둔다.
- `src/renderer/*.html` 6개: 글꼴, 글자 크기, 창틀 CSS를 바꾼다. CSP에 `font-src 'self'`를 더한다.
- `src/renderer/stage.ts`: 말풍선의 글꼴과 테두리를 바꾼다. 글꼴을 불러온 뒤에 그린다.

**문서**
- [현재 설계](../../design.md)와 [UI 컴포넌트 계약](../../specs/ui-components.md)의 공통 규칙에 글꼴과 창틀 예외를 적는다.

### 범위 밖

- `S5 · *` 페이지와 `S5/Type/*` 스타일 14개. 옛 자산이다.
- 도구 그림 출처 변경. 별도 작업이다.
- 버튼, 칩, 탭, 수치 막대, 배지, 박스 칸, 알 칸의 모양. 창틀을 넣지 않는다.
- `src/renderer/manage.ts`. 다른 세션이 같은 기간에 수정할 수 있다. 이 작업은 `.html`의 CSS만 바꾼다.

### 글자 스타일 대응

| 스타일 | 현재 | 바꿀 값 |
|---|---|---|
| `PB/Type/Title` | Inter Bold 24/32 | Galmuri11 Bold 24/32 |
| `PB/Type/Heading` | Inter Semi Bold 18/26 | Galmuri14 Regular 15/22 |
| `PB/Type/Body` | Inter Regular 14/20 | Galmuri11 Regular 12/18 |
| `PB/Type/Label/Regular` | Inter Regular 13/18 | Galmuri11 Regular 12/18 |
| `PB/Type/Label/Semibold` | Inter Semi Bold 13/18 | Galmuri11 Bold 12/18 |
| `PB/Type/Caption/Regular` | Inter Regular 12/16 | Galmuri11 Regular 12/16 |
| `PB/Type/Caption/Semibold` | Inter Semi Bold 12/16 | Galmuri11 Bold 12/16 |
| `PB/Type/Badge/Semibold` | Inter Semi Bold 11/16 | Galmuri9 Regular 10/14 |
| `PB/Type/Micro/Semibold` | Inter Semi Bold 10/14 | Galmuri9 Regular 10/14 |

Body와 Label은 같은 크기가 된다. 두 스타일은 글자색으로 구분한다.

### 창틀

2026-09-26 정정: 이 절의 3세대 창틀은 채택하지 않았다. 근거는 [방향 전환](#방향-전환--3세대-창틀에서-bw-전체-스타일로)이다. 현재 설계는 [재설계](#재설계--기존-스타일-위의-galmuri)를 따른다.

바깥에서 안쪽 순서다. 두께 합은 5px이다. 값은 `window_1.png`~`window_3.png`의 가운데 줄에서 읽었다.

| 층 | 두께 | 기본 | 빨강 | 초록 |
|---|---|---|---|---|
| 외곽선 | 1px | `#283030` | `#283030` | `#283030` |
| 밝은 테 | 1px | `#8888C8` | `#E0A028` | `#88D880` |
| 틀 | 2px | `#706880` | `#D04028` | `#48B840` |
| 안쪽 선 | 1px | `#D8D0D8` | `#D8D0D8` | `#D8D0D8` |
| 바탕 | — | `#F8F8F8` | `#F8F8F8` | `#F8F8F8` |

- 이번 적용은 기본 색만 쓴다. 빨강과 초록은 변수로만 둔다. 쓰임새는 `[스펙 미확정]`이다.
- 창틀 모서리는 4px이다. 그림의 모서리 모양에 가까운 값이다. `[스펙 미확정]`
- 글자색과 글자 그림자는 이번에 바꾸지 않는다. 원작 값은 근거를 찾은 뒤 정한다. `[스펙 미확정]`

**창틀 대상 14개**

| 페이지 | 컴포넌트 |
|---|---|
| `02 · Molecules` | `Notice` `133:1017`, `Empty State` `332:199`, `Daycare Strip` `334:239`, `Bag Item Card` `335:257`, `Product Card` `335:258`, `Species Card` `336:299`, `Achievement Row` `336:341`, `Candidate Card` `401:315` |
| `03 · Organisms` | `Party Slot Card` `194:317`, `Dialog` `299:3575`, `Notification Banner` `338:732`, `Speech Bubble` `338:733`, `Context Menu` `338:738`, `Coach Bubble` `338:764` |

**강조 금지 규칙과의 관계** — 사용자 선호 규칙은 둥근 요소를 컬러 테두리로 강조하지 않는다. 창틀은 선택이나 상태를 강조하지 않는다. 창틀은 모든 컨테이너에 같은 모양으로 붙는 틀이다. 그래서 이 규칙의 예외로 기록한다. 선택 표현은 기존대로 왼쪽 막대와 옅은 배경이다.

### 구현 방법

- Figma: 외곽선은 1px 안쪽 테두리로 둔다. 밝은 테·틀·안쪽 선은 안쪽 그림자 3개로 둔다. 오프셋과 흐림은 0이고 퍼짐은 1·3·4px이다.
- CSS: `border: 1px solid` 외곽선과 `box-shadow: inset 0 0 0 1px …, inset 0 0 0 3px …, inset 0 0 0 4px …`로 같은 모양을 만든다.
- 제약: Figma 원격 도구(`use_figma`)는 Galmuri를 불러오지 못한다. 2026-09-26 `figma.loadFontAsync({ family: "Galmuri11" })`가 "font family does not exist"로 실패했다. 그래서 스타일 9개의 글꼴 이름은 사용자가 Figma 앱에서 바꾼다. 크기와 줄 높이는 도구로 먼저 바꾼다.

### 위험

- Galmuri 한글 글자는 Inter보다 넓다. 좁은 카드의 이름이 넘칠 수 있다. 예: 상점의 "가라두구머리장식".
- 스타일에 묶이지 않은 글자는 스타일을 바꿔도 따라오지 않는다.
- 캔버스 말풍선은 글꼴을 불러오기 전에 그리면 기본 글꼴로 나온다.
- 소수 배율 화면에서 도트 글꼴이 흐려질 수 있다.

### 수용 조건

1. Figma의 `PB/Type/*` 9개가 위 표의 값이다.
2. 창틀 대상 14개가 창틀 효과 스타일과 창틀 변수를 쓴다.
3. 화면 43개 캡처에서 글자가 잘리거나 넘치지 않는다.
4. 앱의 모든 창에서 글자가 Galmuri로 표시된다. 말풍선도 포함한다.
5. CSP 변경은 `font-src 'self'` 추가만이다.
6. `npm run selftest`, `node scripts/check-docs.cjs`, `git diff --check`를 통과한다.

### 재설계 — 기존 스타일 위의 Galmuri

날짜: 2026-09-26. 상태: 설계. 사용자 지시 원문: "다시 해야하는것들 꼼꼼하게 다 확인하고 전체 어떻게 할지 설계 좀 해봐. 진짜 내가해야해? 너가 못해? 이거 만든거는 다 만들어놓고 일일이 시키는게 이상한데?"

**지금까지 확정된 것** (사용자 발언 기준)
- 글꼴은 Galmuri다. Figma 스타일 9개는 사용자가 바꿨다.
- 3세대 창틀은 쓰지 않는다. BW 어두운 배경도 쓰지 않는다. 기존 밝은 스타일에 녹인다.
- 타입 배지는 기존 알약 모양이다. 여백을 줄였다.
- 파티 칸 시안(`473:15064`): 타입·수치 막대에 테두리가 없다. 선택 막대(blade)가 없다. 초상은 96px이다. 레벨과 이름은 한 줄이다. info는 작게 한다.

**열린 결정** `[스펙 미확정]`
1. 카드 외곽: 시안의 2px 깎은 모서리를 쓸지, 기존 1px 둥근 테두리로 둘지.
2. 선택 표시: 파티 칸에서 blade를 뺀 것이 파티 칸만인지, 모든 카드인지. 현재 공통 규칙은 blade다([UI 컴포넌트 계약](../../specs/ui-components.md#공통-규칙)).
3. 긴 글자: 말줄임과 두 줄 허용 중 무엇을 쓸지. 대상은 상점 설명, 가방 이름이다.
4. 선택 탭 앞의 ▶ 커서를 쓸지.
5. `09 · BW 스타일 시안` 페이지와 쓰지 않는 창틀 변수 15개·효과 스타일 `PB/Frame/Window`를 지울지.

**도구 제약과 해결** — Galmuri 글자 노드는 `use_figma`로 고칠 수 없다. 서버에 글꼴이 없기 때문이다. Chrome 연동으로 사용자 브라우저에서 고치는 방법은 2026-09-26 "Claude in Chrome is turned off in your settings"로 막혔다. 방법은 둘이다.
- A. 사용자가 Claude in Chrome을 켠다. 브라우저 안에서는 Galmuri를 불러올 수 있다. 그러면 글자 수정도 이 세션이 한다. 가능 여부는 켠 뒤 확인한다. `[스펙 미확정]`
- B. 글자를 고칠 때 이 세션이 스타일 9개를 Inter로 잠시 바꾼다. 모든 글자 수정을 한 번에 끝낸다. 마지막에 사용자가 스타일 9개를 Galmuri로 한 번 되돌린다. 넘침 검사는 Galmuri 상태에서 한다. 읽기는 글꼴 없이도 된다.

**남은 작업**

| 순서 | 대상 | 작업 | 누가 |
|---|---|---|---|
| 1 | 결정 | 위 열린 결정 5개 | 사용자 |
| 2 | Figma 컴포넌트 | 파티 칸 시안을 `Party Slot Card` `194:317`에 옮긴다. 초상 96, 레벨·이름 한 줄, 테두리 없는 막대. 결정 1·2에 따라 카드 외곽과 선택 표시를 모든 카드에 맞춘다 | 이 세션 |
| 3 | Figma 글자 | 상점 설명·가방 이름(결정 3), `Picker / Party Slot` 대화상자 본문 줄바꿈 | 이 세션 (방법 A 또는 B) |
| 4 | Figma 화면 | 화면 43개 넘침 재검사와 캡처 확인 | 이 세션 |
| 5 | Figma 정리 | 결정 5 | 이 세션 |
| 6 | 코드 | 3세대 창틀 CSS를 지운다. 대상은 `manage.html` 창틀 블록·변수, `banner.html`·`menu.html`·`picker.html` 창틀, `stage.ts` 말풍선 창틀이다. 글꼴 적용은 남긴다. 확정된 Figma 값을 CSS에 옮긴다 | 이 세션 (Figma 확인 뒤) |
| 7 | 코드 확인 | 첫 선택 창(`picker.html`) 글자가 캡처에서 안 보이는 문제의 원인을 찾는다. 글꼴 로드는 `loaded`였다. 가방 수량 "×" 기호의 글꼴 대체를 확인한다 | 이 세션 |
| 8 | 문서 | [현재 설계](../../design.md)와 [UI 컴포넌트 계약](../../specs/ui-components.md)의 글꼴·선택 표시 규칙, `progress.md`, 월별 이력 | 이 세션 |
| 9 | 검수 | `npm run check`, `npm run selftest`, 개발 실행기 캡처, `node scripts/check-docs.cjs`, `git diff --check` | 이 세션 |

**사용자 결정** (2026-09-26, 선택지 응답)
- 수정 방식: "Chrome 연동 켜기". 방법 A다.
- 카드 외곽: "기존 1px 둥근 테두리". 결정 1을 닫는다.
- 선택 표시: "모든 카드에서 빼기". 결정 2를 닫는다. 선택은 옅은 배경만으로 표시한다. 공통 규칙을 바꾼다.
- 긴 글자: "말줄임(…)". 결정 3을 닫는다.
- 결정 4(▶ 커서)와 결정 5(정리)는 아직 열려 있다.

**현재 미커밋 파일 중 이 작업의 것** — `package.json`(`files`에 `assets/fonts/`), `src/renderer/*.html` 6개, `src/renderer/stage.ts`, `assets/fonts/`, `docs/work/ui-pixel-style/`. `Galmuri-v2.40.4/`와 zip은 사용자가 받은 원본이다. 커밋 대상이 아니다. 나머지 미커밋 파일은 다른 세션의 것이다.

## 작업

### Figma 반영

날짜: 2026-09-26. 도구: Figma 원격 도구 `use_figma`.

**변수와 스타일**
- `PB / Primitive`에 `window/*` 색 9개를 더했다. 값은 [창틀](#창틀) 표다.
- `PB / Color`에 `frame/outline`·`frame/fill`·`frame/highlight`·`frame/body`·`frame/inner`를 더했다. 모두 `window/*`의 별칭이다.
- `PB / Dimension`에 `radius/frame` = 4를 더했다.
- 효과 스타일 `PB/Frame/Window`를 만들었다. 안쪽 그림자 3개의 퍼짐은 5·4·2px이다. 1px 외곽선이 가장 바깥 1px을 덮으므로 설계의 1·3·4px에 1px씩 더했다.
- 글자 스타일 `PB/Type/*` 9개의 크기와 줄 높이를 [글자 스타일 대응](#글자-스타일-대응) 표로 바꿨다. 글꼴 이름은 아직 Inter다.

**창틀 대상 14개** (변형 포함 28개)
- 테두리를 `frame/outline` 1px 안쪽 테두리로 바꿨다. 모서리를 `radius/frame`에 묶었다. 흰 바탕(`bg/surface`)은 `frame/fill`로 바꿨다. 선택·잠김 같은 다른 바탕은 두었다.
- 효과 스타일 `PB/Frame/Window`를 붙였다.
- `clipsContent`를 켰다. 이 값이 꺼진 프레임에서는 Figma가 안쪽 그림자를 그리지 않았다. 2026-09-26 임시 프레임 시험으로 확인했다. 시험 노드는 지웠다.
- 안쪽 여백을 네 방향 모두 4px 늘렸다. 1px 테두리가 5px 창틀이 된 만큼이다.
- `Speech Bubble` `338:733`은 꼬리가 프레임 밖에 있다. 그래서 루트의 채움·테두리·효과를 지웠다. 뒤쪽 절대 배치 사각형 `window` `469:14461`이 창틀을 그린다. 꼬리는 `frame/fill`·`frame/outline`을 쓰고 아래 가장자리에 붙는다.

**스타일에 묶이지 않은 글자** — `05 · Screens` 글자 2,628개 중 290개가 스타일 없이 글꼴을 직접 가졌다.
- 원본 컴포넌트에서 묶었다: `App Brand` 로고 글자 → `Heading`, `Point Balance` → `Micro/Semibold`·`Label/Semibold`, `Candidate Card` 이름 → `Caption/Semibold`.
- 화면에 직접 놓인 앱 문구 11개를 묶었다. 첫 선택 제목 → `Title`, 안내 문구 → `Label/Regular`·`Caption/Regular`.
- 남긴 것: 창 조절 기호 `─`·`☐`·`✕` 99개, 캔버스 주석 "창 끝 · 780" 15개, 가짜 작업 앱 글자 8개. 앱 글꼴 대상이 아니다.

**관측된 문제**
- `Party / Approved UI` `217:1705`에서 "친밀도 80/100"의 오른쪽이 잘린다. 여백이 늘어 내용 폭이 줄었다. 글꼴을 바꾼 뒤 다시 판단한다.

### 방향 전환 — 3세대 창틀에서 BW 전체 스타일로

날짜: 2026-09-26.

**사용자 피드백** — 원문: "너무 심하게 어색해졌는데? 근데 왜 벌써 어플리케이션 검사해 figma부터 해야지". 어색한 점 원문: "창틀이 너무 두꺼움·많음", "기존 청록·둥근 UI와 충돌", "창틀색이 기존색과 어울리지않음. 두껍기도하고. 할거면 전체스타일링을 바꿔야지, 그리고 저 보라색이 원작스타일이 맞아?? 아닌거같은데".
- 코드 변경 처리: 사용자 선택 "그대로 두기". 앱 코드는 미커밋으로 남기고 Figma 확정 뒤 맞춘다.
- 기준 세대: 사용자 선택 "5세대 BW".

**보라색 창틀의 출처 확인** — Bulbagarden Archives의 원작 캡처 `File:Frame 1 FRLG.png`의 테두리 화소가 `#283030`·`#8888C8`·`#706880`·`#706880`·`#D8D0D8`이었다. 설계의 값과 같다. 파이어레드·리프그린의 창틀 "TYPE 1"이다. 원작 색은 맞다. 어색함의 원인은 창틀만 원작이고 나머지 화면은 기존 색이라는 점이다.

**BW 원작 색** — 관측 원본은 Bulbagarden Archives의 `File:BW Party.png`, `File:BW Menu.png`, `File:Options BW.png`, `File:Pokédex entry BW.png`다.

| 부분 | 값 |
|---|---|
| 배경 | `#101820` 위 격자선. 원작 격자선은 `#303030`·`#404040`이다. 시안은 `#202830`을 16px 간격으로 썼다 |
| 보통 칸 | 채움 `#084870`, 테두리 `#286888` |
| 고른 칸 | 채움 `#1070A0`, 테두리 `#08B0F8`, 가장자리 `#38C8D8` |
| 메뉴 타일 | 채움 `#383838`, 밝은 모서리 `#D0D0D0`·`#F8F8F8` |
| 대화 상자 | 바탕 `#F8F8F8`, 테두리 `#505050`·`#C0C0C0` |
| 글자 | 어두운 판 위 흰 글자 `#F8F8F8`와 회색 그림자 |

**Figma 시안** — 새 페이지 `09 · BW 스타일 시안` `471:121`.
- `BW / Party` `471:122`: 파티 화면 640 × 780. 모서리를 깎은 칸, BW 메뉴 타일 탭, 검정 헤더, HP 막대 모양 수치 막대. 초상 자리는 빈 창이다.
- `BW / Dialog & Bubble` `471:14611`: BW 대화창 모양의 대화상자와 말풍선.
- 글자는 `PB/Type/*` 스타일에 묶었다. 스타일의 글꼴 이름은 아직 Inter다.
- 기존 컴포넌트 14개의 3세대 창틀은 그대로다. BW 방향이 확정되면 다시 바꾼다. `[스펙 미확정]`

### 기존 스타일에 녹인 시안

날짜: 2026-09-26. 사용자 피드백 원문: "배경 좀 에반데, 흠... 기존스타일에 녹이는 방식으로 해봐." BW 시안의 어두운 격자 배경은 채택하지 않는다.

**Figma 시안** — 같은 페이지 `09 · BW 스타일 시안`.
- `Blend / Party` `473:15041`: 기존 색(`PB / Color`)과 배경·헤더·밑줄 탭을 유지한다. 바꾼 것은 모양이다.
  - 카드: 모서리를 6px 깎고 2px 중립 테두리 `#C9D0C8`을 쓴다.
  - 선택: 기존 규칙대로 왼쪽 막대와 옅은 배경을 쓴다. 선택 탭 앞에 ▶ 커서를 둔다.
  - 타입 배지: 알약 대신 네모와 1px 짙은 테두리를 쓴다.
  - 수치 막대: HP 막대처럼 짙은 1px 틀 안에 막대를 둔다.
- `Blend / Dialog & Bubble` `473:15120`: 흰 바탕, 2px `#4A6663` 테두리, 안쪽 1px `#DDE1DB` 선. BW 대화창의 이중 테두리 구조를 기존 색으로 옮겼다. 버튼은 3px 깎은 모서리와 아래 2px 짙은 턱을 쓴다.
- 채택 여부는 사용자 확인 대기다. `[스펙 미확정]`

**사용자 피드백 2** (2026-09-26) — 원문: "음 바꾼것들이 다 마음에 안드는데? 테두리한게 좀 조진느낌? 파티 화면 타입,친밀도 에 테두리 없애고, blade없애고, portrait 크기를 좀 더 키워봐." 파티 칸 방향은 사용자가 `473:15064`를 직접 고쳐 보였다. 원문: "info자체를 좀 더 작게해봐. 사이즈는 일단 안바꿨어."
- 사용자 수정 `473:15064`: 초상 96px. 레벨과 이름을 한 줄에 둔다.
- 반영: 파티 칸 2개(`473:15064`, `473:15085`)에서 타입 배지·수치 막대의 테두리를 지웠다. 막대 바탕은 `bg/subtle` `#DDE1DB`다. 선택 막대(blade)를 지웠다. 두 번째 칸도 레벨·이름 한 줄 구조로 맞췄다. info 폭은 172 → 140이다. 칸 폭은 294로 고정했다. 사용자 수정 뒤 첫 칸이 318로 늘어 옆 칸과 겹쳤기 때문이다.
- 사용자 지시 (2026-09-26) 원문: "타입 badge 는 기존거 써봐." 두 칸의 임시 네모 배지를 기존 `Type Badge` `118:134` 인스턴스로 바꿨다. 피카츄 `Type=Electric`, 파이리 `Type=Fire`다.
- 사용자 지시 (2026-09-26) 원문: "기존거는 여백이 많이 크네. 기존거를 좀 손봐야겠다. 알약스타일은 마음에들었거든". `Type Badge` `118:134`의 변형 18개를 고쳤다. 폭은 52 고정에서 글자 맞춤(좌우 여백 8)으로 바꿨다. 결과 폭은 두 글자 타입에서 35다. 높이는 22 → 18이다. 알약 모양과 색은 그대로다. 이 컴포넌트를 쓰는 모든 화면에 반영된다.

### 글꼴 교체 뒤 점검

날짜: 2026-09-26. 사용자가 `PB/Type/*` 9개의 글꼴을 Galmuri로 바꿨다. 같은 날 조회에서 9개 모두 [글자 스타일 대응](#글자-스타일-대응) 표와 같았다.

**3세대 창틀 되돌림** — 사용자는 두꺼운 창틀을 채택하지 않았다. 그래서 컴포넌트 14개(변형 포함 28개)를 원래 모양으로 되돌렸다. 테두리는 `border/default` 1px, 모서리는 `radius/lg`, 흰 바탕은 `bg/surface`다. 효과를 지웠다. `clipsContent`를 껐다. 여백을 4px씩 줄였다. 말풍선의 뒤쪽 사각형 `469:14461`을 지웠다. 창틀 변수와 효과 스타일 `PB/Frame/Window`는 남겼다. 쓰는 곳은 없다.
- 확인 필요: `Notice`의 Warning·Error 변형의 원래 테두리 색은 기록이 없다. 셋 모두 `border/default`로 두었다.

**글자 넘침** — `05 · Screens`에서 글자가 가장 가까운 틀 밖으로 나간 곳을 찾았다. 창틀을 되돌린 뒤 남은 곳은 4종류다.

| 원인 | 넘친 글자 | 화면 | 조치 |
|---|---|---|---|
| `Stat Meter` `Size=Narrow` | "80/100" +4px. 머리줄 폭 77, 글자 합 81 | 7 | 값 글자를 `Badge/Semibold`로 바꾼다. 사용자 작업 대기 |
| `Product Card` `335:258` | "만복도 최대 · 친밀도 ×2 2시간" +15px | 4 | 이름·설명 틀을 칸 폭 채우기로 바꿨다. 글자 말줄임은 사용자 작업 대기 |
| `Bag Item Card` `335:257` | "모습이 바뀌는 약" +22px | 1 | 이름 틀을 칸 폭 채우기로 바꿨다. 글자 말줄임은 사용자 작업 대기 |
| `Picker / Party Slot` 대화상자 본문 | 한 줄 +64px | 1 | 글자를 줄바꿈으로 바꾼다. 사용자 작업 대기 |

**제약** — 글꼴을 Galmuri로 바꾼 뒤에는 `use_figma`가 글자 노드를 고치지 못한다. 글자 노드를 고치려면 글꼴을 불러와야 하는데, 서버에 Galmuri가 없어 불러오기가 실패한다. 글자를 감싼 틀은 고칠 수 있다.

### 재설계 반영 — Figma

날짜: 2026-09-26. Chrome 연동은 2026-09-26 확장 쪽 오류 "Claude in Chrome is turned off in your settings"로 세 번 실패했다. 그래서 방법 B로 진행했다. 사용자 발언 원문: "다른세션에선 잘만했는데 짜증나네 내가 하기 귀찮으니까 다 시키려고 이러는거잖아".

- 스타일 9개를 작업용으로 Inter로 바꿨다. 크기·줄 높이는 그대로다. Galmuri11 Bold → Inter Bold, Galmuri11 Regular → Inter Regular, Galmuri14·Galmuri9 → Inter Semi Bold다. 사용자가 [글자 스타일 대응](#글자-스타일-대응) 표대로 다시 되돌려야 한다.
- 선택 막대(blade) 8개를 지웠다(결정 2): `Box Slot` 2개, `Target Row`, `Egg Slot`, `Bag Item Card`, `Species Card`, `Candidate Card`, `Select Option`. `Select`는 `Select Option` 인스턴스라 함께 없어졌다. 탭 밑줄 `selection-indicator`는 카드 선택 표시가 아니라서 두었다.
- 말줄임(결정 3): `Product Card`의 이름·설명, `Bag Item Card`·`Species Card`·`Candidate Card`의 이름. 글자 폭은 칸 채우기, 최대 1줄, 끝 말줄임이다.
- `Party Slot Card` `123:149`: 초상 80 → 96. 레벨과 이름을 한 줄에 둔다. 숨김 표시의 오른쪽 위 위치는 유지했다.
- `Picker / Party Slot`의 대화상자 본문 글자를 칸 폭 채우기와 자동 높이로 바꿨다.
- 남은 것: 스타일 9개를 Galmuri로 되돌린 뒤 넘침 재검사. 결정 4(▶ 커서)·결정 5(정리)는 열려 있다.

### Galmuri 복귀 뒤 점검

날짜: 2026-09-26. 사용자가 스타일 9개를 Galmuri로 되돌렸다. 조회 결과 9개 모두 표와 같았다.
- 넘침 재검사: 26곳. 원인은 둘이다.
  - `Party Slot Card`: 초상 96으로 글자 칸이 150px로 줄었다. 이름 줄(레벨+이름 93px)과 상태 배지 2개(62px)가 한 줄에 들어가지 않는다. 이름 +21px, 성격 +4px이다. 화면 6개다.
  - `Picker / Party Slot` 대화상자: 글꼴 문제가 아니었다. Body 높이 300 고정에 내용 364px이었다. Body와 대화상자를 내용 높이 맞춤으로 바꿨다. 대화상자는 460 → 524다.
- 상태 배지 위치: 사용자 지시 원문 "각 시안 figma로 먼저 만들어봐." `09 · BW 스타일 시안` 페이지의 `상태 배지 배치 시안` `489:214`에 셋을 만들었다. 인스턴스 안에서는 배치 방식을 바꿀 수 없어 detach 사본을 썼다.
  - A `489:264`: 초상 아래쪽에 겹친다.
  - B `489:336`: 카드 오른쪽 위 구석.
  - C `489:408`: 초상 88px, 좌우 여백 12. 배치는 그대로다.
- 사용자 지시 원문: "b에서 ... node-id=489-342 ... 는 줄여봐". B 시안의 `identity-copy` `489:342`가 152×40이었다. 가로·세로를 내용 맞춤으로 바꿔 93×22가 됐다. 카드 내용을 세로 가운데로 정렬했다. 상태 배지는 카드 오른쪽 위 구석에 다시 맞췄다.
- 사용자 수정 확인 (2026-09-26): 사용자가 B 카드의 상태 배지를 카드 바로 아래 그룹으로 옮겼다. 위치는 위 16, 오른쪽 여백 16이다. 사용자 지시 원문 "너가 조금씩 손봐바."에 따라 그룹을 자동 레이아웃 틀 `status` `492:229`로 바꿨다. 고정 방향은 오른쪽·위, 위치는 위 12다. 이름 줄과 5px 떨어진다.
- 정정: 친밀도·만복도 막대를 위아래로 쌓았다가 사용자 지시 원문 "저거 왜 위아래 배치한거야. 이전처럼해야지"에 따라 좌우 배치로 되돌렸다. 배치 변경은 요청 범위 밖이었다.

### B 배치 반영

날짜: 2026-09-26. 사용자 지시 원문: "저거로 진행."
- `Party Slot Card` `123:149`: `identity-copy`를 내용 맞춤으로 바꿨다. 상태 배지 2개를 오른쪽 위 고정 틀 `status` `494:774`로 옮겼다. 위치는 오른쪽 여백 16, 위 12다. 카드 내용을 세로 가운데로 정렬했다. 친밀도·만복도는 좌우 배치 그대로다.
- 같은 카드의 `traits` 간격을 8 → 6으로 줄였다. 폭 292 카드에서 타입 2개와 성격이 4px 넘쳤기 때문이다.
- `05 · Screens` 넘침 재검사: 0곳.
- 관측: `Party / Approved UI`의 폭 294 카드에서 "친밀도80/100"처럼 이름과 값 사이가 붙어 보인다. 수치 막대(Narrow) 폭이 70이라 여유가 없다. 같은 화면 둘째 줄 두 칸의 폭이 서로 다르다. 이 차이는 이번 작업 전부터 있던 화면 배치다.
- 사용자 지시 원문: "row2의 첫번째카드 개선. 2는 진행." `Party Layout` `196:189`의 `row/2`에서 두 칸이 308·276으로 갈려 있었다. 두 칸을 292 고정으로 맞췄다. 첫 칸은 내용이 줄지 않아 폭을 밀어냈다.
- 같은 폭이 되자 첫 칸도 "친밀도80/100"처럼 붙어 보였다. `Party Slot Card` 좌우 여백을 16 → 12로, 막대 사이 간격을 12 → 10으로 줄였다. 상태 배지 틀도 오른쪽 여백 12에 맞췄다. 292 칸에서 이름과 숫자 사이는 6px이다. 넘침 재검사는 0곳이다.

### Figma 전체 검수와 페이지 정리

날짜: 2026-09-26. 사용자 지시 원문: "피그마 페이지들 전체 검수 수행. 필요없는 페이지(Page 1 은 내가 작업용으로 둔거긴한데 지워도됨) 들 다 지우고, 테스트용 시안 페이지 따로 구성하고, atoms부터 screens까지 다 되어있는지 등 검사. 문서도 level 0 으로 올리고 페이지 위치도 올리고 말이야." 계획 승인 원문: "진행."

**검수 결과** (정리 전, 읽기 전용 조회)
- `00`~`05` 페이지는 `S5` 페이지·`S5 Tokens` 변수·`S5/Type/*` 글자 스타일을 쓰지 않았다. `S5` 페이지와 `Page 1`은 자기 안에서만 참조했다.
- 컴포넌트: Atoms 36, Molecules 31, Organisms 16, Templates 7. 화면 43개. 글자 넘침 0곳.
- 쓰이지 않는 컴포넌트: `Scrim` `141:125`(`Modal Scrim`이 대신 쓰임), `App Shell` `195:319`.
- 변수에 묶이지 않은 색: `Visibility Marker` 8곳(몬스터볼 그림 고유색이라 둔다), `App Header` 창 조절 기호 3곳. `Target Row` 8곳은 선택 막대를 지우며 없어졌다.
- 컴포넌트 없이 그린 틀: `Playground / Region Draw`의 `region-toolbar`, `First Run` 두 화면의 `head`·`candidates`. 가짜 작업 창 `mock · 작업 중인 창`은 배경 장치다. 이번에 바꾸지 않았다. `[스펙 미확정]`

**정리**
- 페이지 삭제: `Page 1`, `S5 · Design System`, `S5 · Components`, `S5 · Screens`.
- 변수·스타일 삭제: `S5 Tokens` 47개, `S5/Type/*` 14개, 창틀 변수 15개(`frame/*`·`window/*`·`radius/frame`), 효과 스타일 `PB/Frame/Window`.
- 컴포넌트 삭제: `Scrim`, `App Shell`.
- 색 연결: `App Header`의 `─`·`☐`·`✕`를 `text/secondary`에 묶었다.
- 페이지 이름·순서: `06` → `0.1 · 전체 설계·문서 트리` `236:121`, `07` → `0.2 · 와이어프레임` `252:121`. 둘을 맨 위로 옮겼다. 페이지 ID는 그대로라 문서 링크는 유지된다.
- 시안 통합: `09` 페이지 `471:121`을 `99 · 시안 (테스트)`로 바꿔 맨 끝에 뒀다. 섹션 셋에 시안을 모았다. `개체 상세 시안 (구 08)` `500:14436`, `화면 스타일 시안 (구 09)` `500:14437`, `공유 sid 시안 (구 10)` `500:14438`. 옮긴 프레임의 ID는 그대로다. 빈 `08`·`10` 페이지를 지웠다.
- `00 · Foundations` 안내 글에서 옛 `S5` 문장을 지우고 문서·시안 페이지 위치를 적었다.
- 최종 페이지: `0.1`, `0.2`, `00`~`05`, `99`.
- 문서: `progress.md`·`README.md`·`specs/ui-components.md`의 페이지 이름을 고쳤다. `game-runtime/record.md`의 `10` 페이지 표기는 그 기록을 쓰는 세션이 고친다.

### 코드 반영

날짜: 2026-09-26. 사용자 지시 원문: "진행". Figma 확정 뒤에 시작했다.
- 이보다 앞서 2026-09-26 Figma 확정 전에 코드를 먼저 바꾼 일이 있었다. 사용자 원문: "근데 왜 벌써 어플리케이션 검사해 figma부터 해야지". 그때 넣은 3세대 창틀은 이번에 모두 걷어냈다.

**글꼴** (`src/renderer/*.html` 6개)
- `assets/fonts/`에 Galmuri11·Galmuri11-Bold·Galmuri14·Galmuri9 woff2와 `OFL.txt`를 뒀다. `package.json`의 `files`에 `assets/fonts/`를 더했다.
- 창마다 `@font-face`와 `--font-body`·`--font-small`·`--font-large` 변수를 뒀다. `font-synthesis: none`으로 가짜 굵게를 막는다.
- CSP에 `font-src self`만 더했다.
- 글자 크기를 네 단계로 바꿨다. 10·11px → Galmuri9 10px. 12·13px → Galmuri11 12px. 14~18px → Galmuri14 15px. 20·22px → Galmuri11 24px. `stage.html`의 디버그 줄은 고정폭 글꼴로 뒀다.
- `stage.ts` 말풍선: Galmuri11 12px로 그린다. 글꼴이 도착하면 다시 그린다. 모양은 원래대로 흰 바탕·1px 테두리·반경 12다.

**Figma 확정값**
- 선택 막대(`::before`) 규칙을 지웠다: `manage.html`의 `.row-card`·`.select-opt`·`.cell`·`.dex-cell`, `picker.html`의 `.card`.
- 파티 칸(`manage.html`·`manage.ts`): 초상 96(`.slot > .portrait`), 여백 위아래 18·좌우 12, 초상과 글자 사이 12. 첫 줄은 "Lv.N 이름" 한 줄이다. 이름이 길면 말줄임한다. "다음 레벨까지 N%"는 칸의 `title`로 옮겼다. Figma에 없는 정보라 화면에서는 뺐다.
- 빈 칸: 점선과 투명 바탕을 지웠다. Figma `Party Slot Card` 빈 칸처럼 흰 바탕과 1px 실선이다.
- 타입 배지 `.type`: 높이 22 → 18, 최소 폭 52를 지웠다. 폭은 글자에 맞춘다.
- 말줄임: `.row-card .title`·`.row-card .note`(상점·가방 카드), `picker.html`의 `.name`.
- Figma 파티 칸의 오른쪽 위 상태 배지는 코드에 해당 데이터가 없어 넣지 않았다. `[스펙 미확정]`

**문서**
- [현재 설계](../../design.md)의 글꼴·선택 표현 문장, [UI 컴포넌트 계약](../../specs/ui-components.md)의 공통 규칙(선택 표현, 새 글꼴 행)과 C-09·C-17·C-26 선택 상태를 고쳤다.
- 루트 `README.md` 라이선스 절에 Galmuri(OFL 1.1) 출처를 더했다.

## 검수

날짜: 2026-09-26.

| 검사 | 결과 |
|---|---|
| `npm run check` | 통과 |
| `npm run selftest` | 통과. 종료 코드 0 |
| 개발 실행기 캡처 | 관리 창 파티·상점·가방·도감·개체 상세, 우클릭 메뉴, 알림 띠, 첫 선택 창을 찍었다. 글자는 모두 Galmuri다. 창틀과 선택 막대는 없다. 증거는 [evidence](evidence/) |
| 첫 선택 창 글자 | 창을 띄운 뒤 0.3·0.8·1.5·2.5초 캡처에서 모두 보였다 |
| Figma 넘침 검사 | `05 · Screens` 43개에서 0곳 |
| `node scripts/check-docs.cjs` | 통과 |
| `git diff --check` | 통과 |

의미 검수: 이번에 바꾼 문장(설계·계약·README·이 기록)을 [작성 원칙](../../contributing/writing.md) 목록으로 읽었다. 사용자 원문과 관측을 나눴다. 미확정 항목에 `[스펙 미확정]`을 붙였다.

## 피드백과 수정

- 첫 선택 창 글자가 캡처에서 안 보이던 현상: 창틀 CSS가 있던 때 1.5·3초 캡처에서 관측했다. 창틀을 걷어낸 뒤에는 재현되지 않았다. 원인은 확인하지 못했다.
- 가방 수량 "×1"의 × 기호가 다른 글꼴처럼 가늘게 보인다. 원인은 확인하지 않았다. `[스펙 미확정]`
- Figma에서 컴포넌트 없이 그린 틀 3곳(`region-toolbar`, `First Run`의 `head`·`candidates`)은 그대로다.
- Galmuri 글자 노드는 `use_figma`로 고칠 수 없다. Claude in Chrome 연동은 "Claude in Chrome is turned off in your settings"로 막혔다. 새 `claude --chrome` 세션도 같았다. 다음 글자 수정도 스타일을 Inter로 잠시 바꾸는 방식을 써야 한다.
- 커밋: 사용자 지시 원문 "커밋"(2026-09-26). 이 작업의 파일만 올렸다.
