# S5 디자인 시스템 v2 검수와 문서 동기화

날짜: 2026-09-21. 상태: 00~05 검수와 문서 동기화 완료. 디자인 수정 항목은 열림. 앱 기능 구현 미시작.

## 목표와 범위

- Figma `00 · Foundations`부터 `04 · Templates`까지 현재 구조와 스타일을 확인한다.
- 공유 대화의 작업 기록을 현재 Figma 속성과 대조한다.
- 기존 S5 자산과 PB v2 자산의 기준을 구분한다.
- 추가 사용자 요청으로 `05 · Screens` 6개와 모달 Template 변형을 검수 범위에 포함했다.
- 이번 변경 대상은 `docs/`다. Figma 자산과 앱 코드는 편집하지 않는다.

## 근거와 SSOT

- 현재 디자인: [Figma 00 · Foundations](https://www.figma.com/design/MA3K41Y6omAi5mRu6YDFly/pokebuddy?node-id=113-245).
- 이전 작업 설명: [공유 대화](https://chatgpt.com/share/6aaffb44-3534-83ee-874e-9a63888658eb).
- 추가 작업 설명: [두 번째 공유 대화](https://chatgpt.com/share/6aaffe73-d0e0-83ee-8d3c-8a674cbcb916).
- 결정: [design.md](../../design.md). 현황: [progress.md](../../progress.md).
- 제품 계약: [S5 계획](../../specs/s5.md). 사용자 명칭: [terms.md](../../terms.md).
- 이전 자산: [공통 목록](../../archive/s5-legacy/figma-s5-inventory.json), [상세 목록](../../archive/s5-legacy/figma-s5-detail-inventory.json).
- 공유 대화의 완료 주장은 당시 기록이다. 현재값은 Figma를 직접 읽어 확인한다.
- Figma 시안은 런타임 구현의 증거로 사용하지 않는다.

## 위험과 수용 검사

- 이전 S5 완료 기록을 v2 전체 완료로 해석하지 않는다.
- 페이지별 자산 ID와 직접 의존 계층을 기록한다.
- 색상 변수, 글꼴 스타일, 대표 간격과 치수를 기록한다.
- 내비게이션 하단 테두리 1px와 선택 밑줄 2px를 각각 확인한다.
- 상세 템플릿 전체 높이와 실행 화면의 스크롤 높이를 구분한다.
- 하드코딩 색상, 누락 참조, 상위 노드 겹침의 검사 범위를 명시한다.
- 대표 화면을 캡처로 확인한다. 미확정 디자인은 열린 항목으로 남긴다.
- 추가 6개 화면의 표시·숨김·성공·확인·오류 상태를 캡처와 속성으로 확인한다.
- 본문 스크롤, 화면 이동, 모달 불투명도, 친밀도 막대 비율과 종 데이터를 확인한다.
- 문서 상대 링크, JSON 구문, `git diff --check`를 확인한다.
- 런타임 코드 변경이 없으므로 빌드·타입 검사·자체 검사는 실행하지 않는다.

## 결과

- 현재 스타일·컴포넌트·내비게이션·Template 기준은 [검수 기록](review.md)에 있다.
- PB 자산과 관측값은 [v2 자산 목록](evidence/inventory.json)에 있다.
- 6개 화면의 문자·상태·스크롤·모달·막대 근거는 [화면 자산 목록](evidence/screens-inventory.json)에 있다.
- 디자인 수정 항목은 [피드백](feedback.md)에 남겼다.
- 기존 S5 기록에 Legacy 적용 범위를 추가했다. 앱 구현은 시작하지 않았다.

## 2026-09-21 설계 방향 후속 논의

상태: 설계 방향 초안. 기능 구현 미시작.
출처: 사용자는 `42f03e4cd575e60feea17a807440301f83d82aa0`을 제시했다. 플로우차트나 와이어프레임으로 설계부터 진행하기를 요청했다. 컴포넌트를 만들고 조립하는 방식을 요청했다. 향후 기능 추가를 고려한 모듈형 설계를 요청했다.
아래의 구체적인 순서와 경계는 `[스펙 미확정]` 제안이다. 사용자 요청을 개별 구조의 채택 근거로 확대하지 않는다.

### 목표와 범위

목표: 사용자 행동부터 화면 상태와 기능 계약까지 추적할 수 있는 설계 방식을 정한다.
범위: S5 흐름 설계, 와이어프레임, 컴포넌트 계약, 모듈 책임, 단계별 수용 조건.
제외: 이번 논의에서는 Figma 편집과 런타임 구현을 시작하지 않는다. 상품 채택과 저장 구조를 확정하지 않는다.
SSOT는 위의 [근거와 SSOT](#근거와-ssot)를 따른다.

### 기존 작업에서 이어받을 내용

- 지정 커밋은 기존 실행 흐름의 오류를 수정했다. S5의 다음 산출물로 흐름별 계약 표를 제안했다. 근거는 [실행 흐름 기록](../runtime-e2e/record.md)에 있다.
- 기존 Figma 관측에는 Foundations부터 Screens까지의 계층이 있다. 파티와 상세의 정적 상태 6개가 있다. 근거는 [v2 검수](review.md)에 있다.
- 당시 관측에는 프로토타입 연결이 없었다. 스크롤과 오류 복귀에도 열린 항목이 있다. 항목은 [피드백](feedback.md)에서 관리한다.
- 이번 논의에서는 Figma를 다시 관측하지 않았다. 과거 관측값을 현재 원격 파일의 상태로 단정하지 않는다.
- 현재 코드는 [명령 처리기](../../../src/main/commands.ts)가 돌봄과 구매 등의 모듈을 연결한다. 기존 경계를 S5 설계의 출발점으로 사용한다.

### 제안하는 설계 순서

아래 표 전체는 `[스펙 미확정]` 제안이다. 한 사용자 흐름에 순서대로 적용한다. 확인 중에 계약 누락을 찾으면 해당 단계로 돌아간다.

| 순서 | 산출물 | 다음 단계로 넘어갈 조건 |
|---|---|---|
| 1. 기능 범위 | 확정 기능과 미정 기능 목록 | 해당 흐름에 필요한 제품 규칙을 식별한다. |
| 2. 사용자 흐름 | 진입점과 분기를 담은 플로우차트 | 성공, 취소, 실패, 재시도의 도착점을 설명한다. |
| 3. 화면 구조 | 상태별 저해상도 와이어프레임 | 정보 순서, 조작 위치, 뒤로 가기, 스크롤을 확인한다. |
| 4. 계약 | 화면 입력·이벤트와 명령·저장 책임 표 | 각 상태의 소유자와 변경 시점을 정한다. |
| 5. 컴포넌트 | 재사용할 구성요소와 상태 목록 | 입력과 이벤트만으로 대표 상태를 재현한다. |
| 6. 화면 조립 | 컴포넌트 인스턴스로 만든 화면과 프로토타입 | 플로우차트의 분기를 화면에서 따라갈 수 있다. |
| 7. 기능 연결 | 흐름 하나의 구현과 수용 검사 | 임시 저장에서 성공과 실패 복원을 확인한다. |

전체 탭의 관계는 먼저 개략적으로 정한다. 세부 설계와 구현은 흐름 하나씩 진행한다.
기존 컴포넌트는 와이어프레임의 요구와 대조한다. 요구를 충족하면 재사용한다. 부족한 상태만 추가한다.
색과 간격은 기존 디자인 토큰을 출발점으로 사용한다. 저해상도 와이어프레임에서는 정보 구조와 행동을 먼저 확인한다.

### UI 구성과 기능 모듈의 경계

아래 표 전체는 `[스펙 미확정]` 제안이다. 항목은 책임 구분이며 새 디렉터리 확정안이 아니다.

| 경계 | 소유할 책임 | 외부와의 계약 |
|---|---|---|
| 공통 UI | 버튼, 탭, 대화상자, 상태 안내의 표시와 조작 | 표시 입력과 사용자 이벤트. 게임 저장과 IPC에 직접 접근하지 않는다. |
| 기능 UI | 파티 카드, 돌봄 조작, 도감 검색처럼 기능에 속하는 표현 | 해당 기능의 표시 데이터와 행동 이벤트. 다른 기능의 내부 상태를 직접 변경하지 않는다. |
| 화면 조립 | 탭, 상세 진입, 검색, 선택, 포커스, 스크롤 | 기능 UI를 조합한다. 이동 시 유지할 임시 상태를 정한다. |
| 기능 연결 | 명령 요청, 처리 중 상태, 결과 조회, 오류의 화면 변환 | 전용 preload의 허용된 계약을 사용한다. 게임 상태의 원본은 메인에 둔다. |
| 게임 규칙 | 파티 배치, 돌봄 조건, 도감 등록, 거래 검증 | 기존 명령 처리 경로를 사용한다. 여러 데이터가 바뀌는 작업의 책임자를 정한다. |
| 저장과 외부 연결 | 저장, 실패 복원, mailbox, 활동 감지 | 기존 보안 경계와 writer 잠금을 유지한다. |

Figma의 Atoms부터 Screens까지는 시각 구성 계층으로 사용한다. 코드 디렉터리를 같은 이름으로 나눌 필요는 없다.
현재의 바닐라 HTML·CSS·TypeScript ESM 결정을 유지한다. UI 컴포넌트의 입력과 이벤트 계약은 이 기술 안에서 설계한다.
여러 모듈의 데이터를 변경하는 작업은 명령 처리 경로에서 조정한다. 렌더러가 저장 작업을 나누어 실행하지 않는다.

### 컴포넌트 계약과 확장 원칙

`[스펙 미확정]` 각 컴포넌트에는 다음 내용을 기록한다.

- 목적과 사용 화면.
- 표시 입력과 사용자 이벤트.
- 기본, 비활성, 처리 중, 오류 등 필요한 상태.
- 키보드 조작과 접근성 이름.
- 긴 번역 문구와 데이터 누락 시의 표시.
- Figma 구성요소와 코드 구현의 대응 위치. 구현 전에는 코드 위치를 미정으로 둔다.

`[스펙 미확정]` 공통화 여부는 표시 모양뿐 아니라 행동과 변경 이유로 판단한다. 구매 확인과 소환 해제 확인은 대화상자 틀을 공유할 수 있다. 각 작업의 검증과 저장 규칙은 해당 기능에 둔다.
`[스펙 미확정]` 새 기능은 기존 화면을 조립하는 지점에서 연결한다. 공통 버튼에 상품 종류나 포켓몬 규칙을 추가하지 않는다.
`[스펙 미확정]` 실제 두 번째 사용처가 생기면 기능 UI의 공통화를 검토한다. 미래 기능을 이유로 범용 플러그인 체계부터 만들지 않는다.
숫자와 이름은 콘텐츠로 다룬다는 기존 결정을 유지한다. 상태와 시각 역할을 variant로 구분한다.

### 첫 적용 흐름과 수용 조건

`[스펙 미확정]` 첫 대상은 파티에서 상세를 열고 숨기기 또는 다시 표시를 수행한 뒤 파티로 돌아오는 흐름이다.
선정 이유: 기존 `party.hide`와 `party.show` 명령을 활용할 수 있다. 미정인 상품과 재소환 정책에 대한 의존이 적다.
이 흐름에서 카드, 상세, 상태 안내와 명령 결과 처리의 경계를 확인한다.

| 확인할 상황 | 설계에 표현할 내용 |
|---|---|
| 정상 처리 | 저장 성공 후 상세와 파티 카드가 같은 표시 상태를 나타낸다. |
| 처리 중 | 같은 조작의 중복 요청을 제어한다. 제어 방식은 계약에 기록한다. |
| 저장 실패 | 기존 표시 상태를 유지한다. 오류에서 돌아갈 위치를 정한다. |
| 대상 소실 | 외부 명령으로 대상이 사라진 경우의 안내와 복귀 위치를 정한다. |
| 화면 복귀 | 요청 전 뒤로 가기는 상태를 변경하지 않는다. 요청 후 이동 시 결과를 반영할 위치를 정한다. |
| 재진입 | 최신 게임 상태를 조회한다. 이전 화면의 임시 상태와 구분한다. |

설계 수용 조건: 같은 흐름 식별자로 플로우차트, 와이어프레임, 컴포넌트 계약, 명령, 수용 검사를 연결한다.
위험: 시각적으로 같은 요소의 업무 규칙까지 합치면 기능 간 의존이 커진다. 미정인 저장 정책을 화면 예시로 확정하면 기존 기록을 손상할 수 있다.
다음 산출물 제안: 전체 탭의 관계와 첫 흐름의 상태별 와이어프레임. 실제 차트와 와이어프레임 제작은 아직 시작하지 않았다.

### 설계 문서 확인

의미 검수 범위: 이 후속 설계 절과 연결한 S5 계약·진행 현황 문장.
의미 검수 결과: 사용자 요청, 과거 관측, 구체적인 제안을 구분했다. 기능 구현과 시각 산출물 제작을 미시작으로 표시했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 393개, JSON 5개를 통과했다. `git diff --check`도 통과했다.
설계 전용 작업이므로 빌드와 런타임 검사는 미실행이다.

### 제품 목적부터 재검토

날짜: 2026-09-21. 상태: 제품 목적 확인. 시나리오 설계 진행 중. 기능 구현 미시작.
사용자 정정: 기존 설계의 적절성부터 의문이라고 설명했다. 가장 기초부터 플로우와 시나리오를 다시 설계하기를 요청했다.
따라서 앞의 파티·상세 흐름을 첫 대상으로 삼는 제안은 보류한다. 기존 컴포넌트의 재사용 여부도 목적과 시나리오를 검토한 뒤 판단한다.

사용자 원문:

> 컴퓨터 켜자마자 켜고, pc에서 자유롭게 노는 포켓몬들을 구경하도록 하는게 목적. 보이는 방치형게임 같은 느낌을 주고싶은거임. 자동기능은 없고, 조금씩 돌봐주는 느낌의 게임이야. 단순히 일하다가 한번의 리프레시를 위한 게임이야.

확인한 제품 목적은 [현재 설계](../../design.md#제품-목적)에 기록한다.
이번 범위는 제품 목적과 일상 사용 시나리오의 초안이다. 기존 기능의 유지·수정·보류 판단은 후속 설계에 포함한다.

아래 표는 `[스펙 미확정]` 일상 사용 시나리오 초안이다. 버튼, 화면 수, 시간 수치와 보상을 확정하지 않는다.

| 상황 | 사용자 목적 | 설계에서 확인할 결과 |
|---|---|---|
| PC를 켜고 앱을 시작한다. | 포켓몬과 하루를 시작한다. | 평소 사용에서 포켓몬을 구경하기까지 필요한 절차를 정한다. 첫 사용은 별도 시나리오로 설계한다. |
| PC에서 일을 한다. | 자유롭게 노는 포켓몬을 가끔 구경한다. | 포켓몬의 활동 범위와 입력 방해를 제어할 방법을 정한다. |
| 잠시 쉬고 싶다. | 포켓몬을 조금 돌본다. | 짧은 조작과 포켓몬의 반응으로 경험을 마칠 수 있게 한다. 구체적인 돌봄 종류는 별도로 검토한다. |
| 다시 일한다. | 포켓몬을 곁에 둔 채 작업을 계속한다. | 돌봄 화면에서 빠져나오는 절차와 이후 알림 정책을 정한다. |
| 앱을 종료한 뒤 다시 켠다. | 이전에 함께한 포켓몬을 다시 만난다. | 유지할 상태와 앱이 꺼진 동안의 변화 규칙을 정한다. |

`[스펙 미확정]` 설계 판단 기준: 구경하는 재미, 짧은 돌봄의 반응, 작업을 방해하지 않는 정도를 기준으로 기존 기능을 검토한다.
`[스펙 미확정]` 성장·수집·상점·작업량 연동의 역할은 아직 정하지 않았다. 기존 구현만으로 핵심 경험이라고 간주하지 않는다.
후속 답변에서 돌보지 않는 동안 배고픔 같은 상태가 변하는 방향을 확인했다. 아래의 작업과 짧은 돌봄 기록을 따른다. 기존 배고픔 수치는 새로 승인한 값이 아니다.
`[스펙 미확정]` “자동기능은 없고”의 세부 범위를 확인해야 한다. 자동 실행, 시간에 따른 성장, 돌봄 대행을 일괄 허용하거나 제외하지 않는다.

위험: 기존 화면 구조를 먼저 채택하면 제품 목적의 재검토 범위가 좁아진다. 방치형이라는 표현만으로 자동 보상과 오프라인 진행을 추가하면 사용자 의도를 벗어날 수 있다.
수용 조건: 제품 목적과 제안한 게임 규칙을 구분한다. 시나리오마다 시작 조건과 기대 결과를 정한다. 기존 기능의 판단 이유를 해당 시나리오에 연결한다.
다음 확인 항목은 후속 답변에 따라 상태 변화를 알아차리는 방식으로 변경했다. 이후 자유롭게 노는 행동과 돌봄의 반응을 구체화한다.

의미 검수: 이번 절과 현재 설계·진행 현황·S5 계약의 연결 문장을 검토했다. 직접 발언과 시나리오 제안을 구분했다. 자동기능의 범위를 추정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 397개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 이번 변경은 설계 문서에 한정하므로 빌드와 런타임 검사는 미실행이다.

### 작업과 짧은 돌봄

날짜: 2026-09-21. 상태: 사용 리듬 설계 중. 기능 구현 미시작.
사용자 원문:

> 배고픔 같은 변화가 생겨 나중에 조금 챙겨주게. 사람이 일할 때, 1시간중 50분일하고 10분정도는 쉬어야한다라고 하잖아. 그런 짤막하게 리프레시 할 수 있도록 하고 싶어.

확인한 방향: 돌보지 않는 동안 상태가 변한다. 사용자는 나중에 조금 돌보며 짧게 리프레시한다.
50분 작업과 10분 휴식은 사용자가 설명한 사용 리듬의 예시다. 이 기록은 건강 지침의 타당성을 판단하지 않는다.
고정 알림, 휴식 타이머, 10분 분량의 필수 조작을 채택한 근거로 사용하지 않는다.

`[스펙 미확정]` 일상 시나리오 제안:

1. 사용자는 앱을 켠 뒤 PC에서 일을 한다.
2. 포켓몬은 화면에서 활동한다. 시간이 지나면 배고픔 같은 상태가 변한다.
3. 사용자는 잠깐 쉬려 할 때 포켓몬의 상태를 알아차린다.
4. 사용자는 필요한 돌봄을 한다. 포켓몬은 알아볼 수 있는 반응을 보인다.
5. 사용자는 반응을 구경한 뒤 작업으로 돌아간다.

`[스펙 미확정]` 상태 표현은 행동이나 작은 표시로 전달하는 안을 제안한다. 시간 알림의 사용 여부는 별도로 정한다.
`[스펙 미확정]` 돌봄을 미뤘을 때의 불이익, 변화 속도, 상한, 오프라인 변화는 미정이다. 상태 변화 자체의 채택과 구분한다.
후속 답변에서 높은 배고픔 구간의 표현 방향을 확인했다. 아래의 배고픔 표현 기록을 따른다.
수용 조건: 사용자의 짧은 휴식 의도와 고정 시간 규칙을 구분한다. 돌봄 전후에 사용자가 알아볼 수 있는 변화를 설계한다.
의미 검수: 이번 절과 현재 설계·진행 현황의 변경 문장을 검토했다. 사용자 발언, 시나리오 제안, 미정 수치를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 397개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 배고픔 표현과 선택적인 돌봄

날짜: 2026-09-21. 상태: 설계 진행 중. 기능 구현 미시작.
사용자 원문:

> 배고프면 포켓몬이 작은 말풍선이나 행동으로 먼저 표현하면 좋긴한데, 강제성은 없애고 싶어.
> 예를 들어 배고픔을 5단계로 구분했을 때, 4,5 단계 정도에는 알림. 나머지는 사용자가 보다가 어 챙겨줘야지 하고 쉬도록.

확인한 원칙은 [현재 설계](../../design.md#돌봄을-알리는-원칙)에 기록했다. 5단계는 사용자가 제시한 예시다. 단계 수와 수치 경계의 확정으로 확대하지 않는다.
이번 설계 범위는 배고픔 표현과 사용자의 선택이다. 알림 UI 제작과 상태 계산 구현은 시작하지 않는다.

아래는 `[스펙 미확정]` 동작 제안이다.

| 상황 | 포켓몬의 표현 | 사용자 선택 |
|---|---|---|
| 낮거나 중간인 배고픔 구간 | 먼저 돌봄을 요청하는 알림은 표시하지 않는다. | 구경하다가 원할 때 돌본다. |
| 높은 배고픔 구간에 진입 | 작은 말풍선 또는 행동을 잠시 보여준다. | 바로 돌보거나 작업을 계속한다. |
| 사용자가 알림을 지나침 | 표현을 마친 뒤에도 화면 활동을 이어간다. | 알림을 닫거나 응답할 의무가 없다. |
| 사용자가 나중에 돌봄 | 돌봄에 대한 반응을 보여준다. | 반응을 구경한 뒤 작업으로 돌아간다. |

`[스펙 미확정]` 비강제성을 위한 제안: 포커스를 빼앗지 않는다. 확인창을 띄우지 않는다. 돌봄 전까지 알림을 계속 표시하지 않는다.
`[스펙 미확정]` 반복 여부와 간격은 별도로 정한다. 여러 포켓몬이 높은 구간에 들어간 경우의 표현 순서도 정해야 한다.
`[스펙 미확정]` 낮은 구간의 상태를 사용자가 알아보는 방법은 아직 정하지 않았다. 상태 조회와 먼저 요청하는 알림을 구분한다.
`[스펙 미확정]` 돌봄을 미뤘을 때의 추가 불이익은 아직 정하지 않았다. 알림이 선택적이라는 이유로 불이익 없음까지 확정하지 않는다.
위험: 반복 표현이나 불이익이 강하면 선택적인 알림도 돌봄 압박으로 느껴질 수 있다.
수용 조건 제안: 알림을 지나쳐도 작업을 계속할 수 있다. 낮은 구간에서도 사용자가 돌봄을 시작할 수 있다. 알림 후에도 포켓몬의 현재 상태를 확인할 수 있다.
후속 답변에서 친밀도 증가량을 줄이는 방향을 확인했다. 아래의 배고픔 상태 디버프 기록을 따른다.
의미 검수: 이번 절과 현재 설계·S5 계약·진행 현황의 변경 문장을 검토했다. 사용자 원칙과 5단계 예시를 구분했다. 비강제성을 위한 구체적인 동작은 제안으로 표시했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 400개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 배고픔 상태 디버프

날짜: 2026-09-21. 상태: 효과의 방향 확인. 세부 계약 설계 중. 기능 구현 미시작.
사용자 원문:

> 맞아. 오래 돌보지 않더라도 친밀도 하락보다는 친밀도 오르는 수치가 기존 1배에서 0.8배가 된다던지(예시수치임), 살짝의 디버프 개념인거야. 이게 배고픔상태 디버프에 들어가는거고.

확인한 결정은 [현재 설계](../../design.md#배고픔-상태-디버프)에 기록했다. 이전의 추가 불이익 미정 항목은 이 효과에 한해 구체화했다.
앞선 대화에서 제안한 추가 불이익 없음은 채택된 계약으로 취급하지 않는다. 사용자는 친밀도 증가량 감소라는 효과를 명시했다.
사용자의 0.8배는 예시다. 실제 밸런스 수치로 사용하지 않는다.

이번 범위는 배고픔 상태 디버프의 의미다. 기존 상태 계산과 게임 데이터는 변경하지 않는다.
`[스펙 미확정]` 적용 시작 구간, 단계별 배율, 해제 조건, 소수 처리와 하루 상한의 관계는 정하지 않았다.
`[스펙 미확정]` 시간 경과, 작업 활동, 직접 돌봄 중 어떤 친밀도 획득 경로에 적용할지 정하지 않았다.
`[스펙 미확정]` 배고픔이 회복되면 이후 친밀도 획득부터 기본 배율로 돌아가는 안을 제안한다. 이전에 덜 얻은 친밀도를 보충하는지 여부는 정하지 않았다.
알림 조건과 디버프 조건은 별도로 설계한다. 4·5단계 알림 예시를 디버프 구간의 채택 근거로 사용하지 않는다.
위험: 직접 돌봄의 친밀도 증가량까지 줄이면 돌봄 반응을 약하게 느낄 수 있다. 적용 경로를 정할 때 짧은 리프레시 목적과 비교한다.
수용 조건: 디버프 때문에 누적 친밀도가 감소하지 않는다. 예시 배율을 확정 수치로 기록하지 않는다. 적용·해제 조건에서의 증가량을 설명할 수 있어야 한다.
다음 확인 항목: 디버프를 어떤 친밀도 획득 경로에 적용할지 정한다. 이후 돌봄에 따른 배고픔 회복과 반응을 정의한다.
의미 검수: 이번 절과 현재 설계·S5 계약·진행 현황·명칭 사전의 변경 문장을 검토했다. 누적 친밀도와 증가량을 구분했다. 효과의 채택과 예시 수치를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 404개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 친밀도 획득과 에이전트 보상

날짜: 2026-09-21. 상태: 기본 획득 경로 확인. 보상 균형 검토 중. 기능 구현 미시작.
사용자 답변: 함께 켜 두는 시간에 친밀도를 얻는 방식에 동의했다. 밥 주기와 놀아주기로 추가 친밀도를 얻는다고 설명했다.
사용자 구상: 던전 같은 탐험 요소를 생각하고 있다. 구현을 요청하거나 규칙을 확정하지 않았다.
사용자 우려: 현재 GPT(Codex)·Claude 같은 에이전트의 토큰 사용량에 따른 이득이 사용자에게 불리하게 느껴질 수 있는지 질문했다.
이번 범위는 기존 보상 코드의 관측과 밸런스 방향 제안이다. 기능이나 보상 수치는 변경하지 않는다.

현재 코드 관측:

- [상태 규칙](../../../src/state/rules.ts)의 기본 켜 두기 친밀도는 시간당 6이다. 토큰 친밀도는 1,000토큰당 1이다.
- [상태 처리](../../../src/state/core.ts)는 표시 중인 개체에 켜 두기와 작업 친밀도를 적립한다. 실제 획득량에는 종과 성격 등의 보정 및 하루 상한이 적용된다.
- 토큰을 읽지 못하는 에이전트가 실행 중이면 시간당 12의 작업 친밀도를 적용한다. 일반 비사용자에게 같은 작업 보상을 주는 조건은 아니다.
- 표시 중인 개체가 있으면 토큰 보상으로 10,000토큰당 1포인트를 지급한다. 해당 토큰 포인트의 하루 상한은 50이다.
- 기본 밥 주기 친밀도는 8이다. 기본 놀아주기 친밀도는 6이다. 두 행동의 재사용 대기시간은 각각 10분이다. 실제 보정은 상태 처리를 따른다.

관측은 코드 읽기 결과다. 런타임 검사를 실행한 결과나 이번 재설계의 승인 수치가 아니다.
단순 비교: 다른 보정과 상한을 제외하면 6,000토큰의 친밀도는 켜 두기 1시간의 친밀도와 같다. 큰 토큰 사용량이 기본 돌봄 경험을 앞설 가능성을 검토해야 한다.
판단: 사용자가 실제로 불공평함을 느끼는지는 확인하지 않았다. 성장 속도와 구매력의 차이가 함께 생기는 구조는 비사용자의 불리함을 느끼게 할 위험이 있다.

아래는 `[스펙 미확정]` 설계 제안이다.

| 선택지 | 효과 | 검토할 점 |
|---|---|---|
| 토큰량에 비례한 보상 유지 | 사용량이 많을수록 빠르게 성장한다. | 수익 감소 구간과 상한을 두어도 비사용자와의 격차가 남는다. |
| 누구나 얻는 활동 보너스의 대체 경로 | 에이전트 사용도 같은 보너스를 채우는 방법이 된다. | 비사용자의 획득 방법과 필요한 시간을 먼저 정해야 한다. |
| 성장과 분리한 연출·기록 | 연결 반응과 사용 기록으로 에이전트 연동을 표현한다. | 성장 보상을 기대하던 기존 경험의 변경 여부를 검토해야 한다. |

당시 추천 제안: 에이전트 없이 켜 두기와 가벼운 돌봄만으로 만족할 성장 속도를 먼저 정한다. 에이전트 보상은 공통 보너스의 대체 경로로 검토한다. 후속 사용자 답변에서 공통 한도를 공유하는 안은 거부됐다. 아래의 추가 이득 결정이 우선한다.
같은 상한만 설정해도 획득 난이도는 다를 수 있다. 비사용자에게 반복 조작을 요구하면 짧은 리프레시 목적과 충돌한다.
포인트와 친밀도는 별도로 비교한다. 친밀도 상한만 같게 해도 구매력 격차는 남을 수 있다.
탐험을 채택하면 비사용자도 접근할 수 있는지 검토한다. 탐험을 필수 반복 과제로 확정하지 않는다.

수용 조건 제안: 같은 이용 시간과 돌봄 횟수에서 비사용자·적은 토큰 사용자·많은 토큰 사용자의 친밀도와 포인트를 비교한다. 각 경로가 상한에 도달하는 시간과 필수 조작 횟수도 비교한다.
후속 답변에서 에이전트 사용을 별도의 추가 이득으로 정했다. 추가 보상의 계산 방식은 다음 설계에서 검토한다.
의미 검수: 이번 절과 현재 설계·진행 현황·S5 계약의 변경 문장을 검토했다. 사용자 결정, 구상 중인 탐험, 코드 관측, 밸런스 제안을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 408개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 에이전트 사용의 추가 이득 결정

날짜: 2026-09-21. 상태: 보상 역할 결정. 계산 방식 미정. 기능 구현 미시작.
사용자 원문:

> 같은 보너스 한도가 있으면 그거만하고 끄더라고. 그런 제한없게 전자가 맞는거같아. 추가이득

사용자는 공통 보너스 한도를 공유하는 제안을 거부했다. 한도를 채운 뒤 앱을 끄게 되는 경험을 이유로 들었다. 이 설명은 사용자의 설계 판단이며 모든 사용자의 행동을 검증한 결과는 아니다.
확인한 결정: 에이전트 사용은 기본 성장에 더해 별도의 추가 이득을 제공한다. 공통 보상을 대신 채우는 방식으로 설계하지 않는다.
현재 결정은 [친밀도 획득과 확장 후보](../../design.md#친밀도-획득과-확장-후보)에 반영했다.

범위: 보상 역할과 이전 제안의 채택 상태를 정정한다. 기존 보상 코드와 수치는 변경하지 않는다.
`[스펙 미확정]` 추가 보상의 대상, 토큰량에 따른 계산식, 기본 성장 대비 크기는 아직 정하지 않았다.
`[스펙 미확정]` 기존 친밀도와 포인트 상한 각각의 처리 여부는 아직 정하지 않았다. 공통 보너스 한도에 대한 거부를 기존 상한 전체의 삭제 지시로 확대하지 않는다.
위험: 추가 이득이 기본 성장을 압도하면 앞서 우려한 비사용자의 불리함이 커질 수 있다. 이 문제를 해결하기 위해 거부된 공통 한도를 다시 도입하지 않는다.
수용 조건: 같은 이용 시간과 돌봄 횟수에서 에이전트 활동은 추가 이득을 제공한다. 기본 보상을 얻었다는 이유로 공유 한도에서 에이전트 보상이 소진되는 구조를 사용하지 않는다.
다음 설계에서는 기본 성장과 추가 이득의 크기를 비교한다. 탐험은 구상 단계로 유지한다.
의미 검수: 이번 절과 현재 설계·진행 현황·S5 계약의 변경 문장을 검토했다. 거부된 제안과 새 결정을 구분했다. 기존 상한 변경을 승인받은 것으로 기록하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 409개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 놀이공간 돌봄 재개 결정

날짜: 2026-09-21. 상태: 일상 사용 방향 확인. 세부 시나리오 설계 중. 기능 구현 미시작.
사용자 답변:

> 1. 화면 전체를 자유롭게 돌아다니도록 구상하긴했는데, 이거는 옵션으로 했음좋겠어. 사용자가 놀이공간(문서에는 무대라고 되어있을거야. 무대라는 말이 좀 거시기해) 을 지정할 수 있도록. 영역 드래그로 지정하는것도 좋아보여.
> 2.클릭 한 번으로 반응 구경하기 +  평소에는 한 번, 원하면 더 놀아주기.  먹이는 기본먹이가 있고, 상점에서 구매하는 프리미엄먹이(규토리?) 같은거로 일정시간동안 친밀도 상승 1.1배 버프를 준다던지 하고 싶어.
> 3. 종료한 상태 그대로 이어가기

목표: 실제 일상 사용을 기준으로 활동 영역과 돌봄 경험을 정의한다. 재실행했을 때의 상태를 정한다.
범위: 놀이공간, 돌봄 조작, 먹이의 역할, 종료 중 상태 변화. 화면 제작과 코드 변경은 포함하지 않는다.
SSOT: 결정은 [현재 설계](../../design.md#놀이공간과-직접-돌봄)에 기록했다. 상세 계약은 [일상 사용 계약](../../specs/s5.md#일상-사용-계약)에 기록했다. 용어는 [명칭 사전](../../terms.md)을 따른다.

정정: 기존 S5의 먹이 판매 제거 방향을 기본먹이와 프리미엄먹이 제공으로 대체한다. 기존 상점 구현의 가격이나 효과를 새 계약에 자동 적용하지 않는다.
사용자가 사용한 놀이공간을 설계 용어로 반영한다. 내부 `stage` 파일과 식별자는 변경하지 않는다.
영역 드래그는 사용자가 긍정적으로 제시한 조작 후보로 기록한다. 확정·취소와 경계 규칙은 아직 정하지 않았다.
프리미엄먹이의 기간형 친밀도 증가 버프는 설계 방향으로 반영한다. `규토리`와 `1.1배`는 확정 이름이나 수치가 아니다.
종료한 상태 그대로 재개한다는 답변을 반영한다. 절전, 시스템 잠금, 앱 숨김을 종료와 같은 것으로 가정하지 않는다.

`[스펙 미확정]` 기간형 버프도 종료 중 시간을 소모하지 않는 안을 제안한다. 종료 상태 유지 원칙을 버프에 적용한 해석이며 별도 답변을 받은 항목은 아니다.
`[스펙 미확정]` 반복 놀아주기의 애니메이션과 보상 지급을 구분하는 안을 제안한다. 한 번 반응을 본 뒤 더 놀아주려면 어떤 행동을 할지 정해야 한다.
`[스펙 미확정]` 놀이공간 지정 초안은 영역 선택 시작, 드래그, 미리보기, 확정 또는 취소 순서다. 확정 전에는 기존 영역을 유지하는 안을 제안한다.

위험: 모니터 연결이 바뀌면 저장한 놀이공간이 화면 밖에 놓일 수 있다. 반복 클릭 보상이 기본 성장을 압도할 수 있다. 버프와 디버프의 적용 순서가 불명확하면 같은 행동의 보상이 달라질 수 있다.
수용 조건 제안: 놀이공간 선택을 취소하면 기존 영역을 유지한다. 한 번의 돌봄으로 반응을 볼 수 있다. 종료 후 다시 켜도 배고픔과 친밀도가 종료 중 시간 때문에 변하지 않는다. 먹이 효과의 예시를 확정 수치로 사용하지 않는다.
후속 답변에서 첫 사용의 놀이공간과 먹이 버프 대상을 확인했다. 추가 놀이의 후보는 아래 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황의 변경 문장을 검토했다. 이전 먹이 판매 제외 계약을 정정했다. 확정 방향, 이름 후보, 예시 수치와 해석을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 417개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 첫 사용과 추가 놀이

날짜: 2026-09-21. 상태: 첫 사용 방향 확인. 튜토리얼 세부 설계 전. 기능 구현 미시작.
사용자 답변:

> 1. 처음엔 전체, 영역바꿀 수 있다 가이드 같은거 하면 좋겠음. 그래서 튜토리얼 있음 좋을듯.
> 2. 다양한 반응 보게만 생각했고, 오래 놀아주면 추가버프를 준다던지를 좀 생각했는데, 장난감으로 추가액션이나 놀이로 버프 주는것도 좋아보임.
> 3. 먹이준 한마리만

목표: 첫 사용의 진입 부담을 낮춘다. 추가 놀이의 후보와 먹이 효과의 대상을 구분한다.
범위: 첫 놀이공간, 튜토리얼 방향, 놀이 반응, 프리미엄먹이 버프 대상. UI 제작과 보상 구현은 미시작이다.
결정은 [현재 설계](../../design.md#놀이공간과-직접-돌봄)에 기록했다. 상세 항목은 [일상 사용 계약](../../specs/s5.md#일상-사용-계약)을 따른다.

확인한 방향: 첫 사용은 화면 전체에서 시작한다. 튜토리얼에서 놀이공간 변경을 안내한다. 반복 놀아주기는 다양한 반응을 보는 경험이다. 먹이 버프는 먹이를 준 한 마리에게만 적용한다.
구상 후보: 오래 놀아주면 추가 버프를 얻는 방식. 장난감으로 추가 행동이나 놀이를 제공하는 방식. 두 후보의 채택과 지급 조건은 아직 정하지 않았다.
장난감을 상점 상품으로 확정하지 않는다. 오래 놀아주기의 판단 기준을 클릭 횟수나 체류 시간으로 임의 결정하지 않는다.

`[스펙 미확정]` 첫 사용 시나리오 초안: 첫 포켓몬을 만난다. 화면 전체에서 노는 모습을 본다. 짧은 안내로 돌봄과 놀이공간 변경을 알아본다. 구경하며 작업하는 일상으로 넘어간다.
당시 제안: 튜토리얼은 건너뛰기와 다시 보기를 제공한다. 안내 도중 영역을 반드시 지정하게 하지는 않는다. 후속 답변에서 채택했다. 아래의 튜토리얼 결정을 따른다.
기존 스타터 선택 구현이 첫 포켓몬을 만나는 새 시나리오의 승인 근거는 아니다. 첫 만남 방식은 다음 질문에서 확인한다.
위험: 튜토리얼에 후보 기능까지 넣으면 첫 사용이 길어진다. 오래 놀아주기 보상이 강하면 짧은 휴식이 반복 조작 의무로 바뀔 수 있다.
수용 조건: 처음에 영역 지정을 요구하지 않는다. 영역 변경 가능성을 안내한다. 한 마리에게 준 먹이 버프가 다른 개체에게 적용되지 않는다. 놀이 후보를 구현 확정 기능으로 표시하지 않는다.
다음 확인 항목: 첫 포켓몬을 만나는 방식, 튜토리얼의 참여 방식, 함께 활동하는 포켓몬 사이의 상호작용.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 사용자가 정한 기본값과 긍정적으로 검토한 후보를 구분했다. 버프 대상과 적용 획득 경로를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 420개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 튜토리얼 결정과 첫 포켓몬 비교

날짜: 2026-09-21. 상태: 튜토리얼 참여 방식 결정. 첫 포켓몬 방식 미정. 기능 구현 미시작.
사용자 답변:

> 1. 원래는 스타팅에서 고르게 했는데, 랜덤으로 할까 싶기도하고, 원하는한마리로 할까싶기도하고 고민중이야.
> 2. 너가 제안하는 방향으로 진행. 놀이공간 변경은 화면으로 설명만해줘도 될듯.

승인 대상 제안: 밥 주기와 놀아주기를 짧게 직접 해본다. 건너뛰기와 다시 보기를 제공한다. 놀이공간 변경은 가능하다는 안내만 제공한다.
사용자 보충: 놀이공간 변경은 화면으로 설명한다. 실제 영역 변경을 튜토리얼 완료 조건으로 두지 않는다.
목표와 범위: 튜토리얼의 참여 방식을 확정한다. 첫 포켓몬 방식의 판단 근거를 정리한다. 구현과 Figma 편집은 시작하지 않는다.
현재 계약은 [일상 사용 계약](../../specs/s5.md#일상-사용-계약)을 따른다. 첫 포켓몬 선택에 관한 기존 구현은 새 방식의 채택 근거로 사용하지 않는다.

아래 표는 `[스펙 미확정]` 선택지 비교다. 사용자 반응을 측정한 결과가 아니다.

| 방식 | 기대하는 경험 | 검토할 위험 |
|---|---|---|
| 스타터 후보 중 선택 | 작은 목록에서 첫 동반자를 고른다. | 좋아하는 포켓몬이 목록에 없을 수 있다. |
| 랜덤으로 만남 | 뜻밖의 포켓몬과 인연을 시작한다. | 원하는 결과를 얻으려 재시작하거나 이탈할 수 있다. |
| 원하는 한 마리 선택 | 좋아하는 포켓몬과 바로 생활한다. | 후보가 많으면 선택이 길어진다. 이후 수집과 진화의 역할을 함께 정해야 한다. |

`[스펙 미확정]` 추천: 원하는 한 마리 선택을 기본으로 검토한다. 선택이 어려운 사용자를 위한 랜덤 선택을 보조 경로로 검토한다. 선택 가능한 종 범위는 별도로 정한다.
추천 이유: 사용자가 곁에 두고 구경하고 싶은 포켓몬을 만나는 경험이 제품 목적과 맞는다. 모든 종을 첫 선택 대상으로 확정한다는 뜻은 아니다.
`[스펙 미확정]` 튜토리얼 순서 초안: 첫 포켓몬 등장, 기본먹이로 밥 주기, 놀아주기, 놀이공간 변경 화면 설명, 일상 사용 시작. 건너뛰기 이후 다시 볼 수 있다.
미정: 튜토리얼 실습의 저장·재고·친밀도 반영, 중간 종료 후 재개, 다시 보기에서 보상 처리.
위험: 다시 보기에서 보상을 반복 지급하면 성장 설계와 충돌할 수 있다. 첫 선택의 범위에 따라 이후 획득과 진화의 의미가 바뀔 수 있다.
수용 조건: 건너뛰어도 일상 사용에 진입할 수 있다. 안내를 다시 볼 수 있다. 실제 영역 변경 없이 튜토리얼을 마칠 수 있다. 첫 포켓몬 후보를 확정된 규칙으로 표시하지 않는다.
후속 답변에서 원하는 포켓몬을 기준으로 고르는 방향을 확인했다. 시작 형태와 이후 획득은 아래 기록을 따른다. 선택 가능한 종의 정확한 범위는 미정이다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 사용자 승인과 비교 중인 선택지를 구분했다. 튜토리얼 세부 순서는 제안으로 남겼다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 422개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 첫 포켓몬과 이후 획득

날짜: 2026-09-21. 상태: 첫 선택과 성장 방향 결정. 세부 획득 규칙 미정. 기능 구현 미시작.
사용자 답변:

> 그렇긴해. 원하는 포켓몬있으면 그거부터 해주고 이후는 랜덤이나 구매나 해금이나 하면 좋을거같애.
> 파이리부터 시작하게 할거야.

답변 맥락: 리자몽을 원하면 처음부터 리자몽을 제공할지, 파이리부터 키우게 할지 질문했다.
확인한 방향: 첫 포켓몬은 원하는 포켓몬을 기준으로 고른다. 진화 계열의 시작 형태부터 키우는 과정을 남긴다. 이후에는 랜덤·구매·해금 경로로 획득한다.
목표: 원하는 포켓몬과의 첫 만남에 사용자의 선택을 반영한다. 이후 성장과 수집의 역할을 정한다.
범위: 첫 선택 원칙과 이후 획득 방향. 종별 규칙, 경제 수치, UI 제작과 구현은 포함하지 않는다.
현재 결정은 [현재 설계](../../design.md#놀이공간과-직접-돌봄)에 기록했다. 상세 상태는 [일상 사용 계약](../../specs/s5.md#일상-사용-계약)을 따른다.

미정 사항: 진화하지 않는 종, 특수한 진화 계열, 폼, 희귀한 종의 첫 선택 허용 범위. 첫 선택에서 랜덤 보조 기능을 제공할지도 아직 정하지 않았다.
랜덤 획득은 알이나 작업 부화의 확정이 아니다. 구매 가격과 해금 조건은 아직 정하지 않았다. 해금이 무료 지급인지 구매 가능 상태인지도 정하지 않았다.
`[스펙 미확정]` 선택 화면에서는 실제로 시작할 포켓몬과 진화 정보를 함께 보여주는 안을 제안한다. 원하는 진화형과 실제 지급할 종을 혼동하지 않게 한다.
당시 제안: 진화 조건을 충족하면 가능 상태를 알리고 사용자가 원할 때 진화시킨다. 후속 답변에서 사용자가 채택했다. 아래의 진화와 친밀도 기록을 따른다.
위험: 실제로 받을 종을 선택 화면에서 설명하지 않으면 사용자가 잘못 지급됐다고 느낄 수 있다. 기존 상품과 알 제안을 새 획득 경로에 자동 포함하면 미확정 규칙이 확정된 것으로 보일 수 있다.
수용 조건: 리자몽을 원하는 첫 선택 사례는 파이리로 시작한다. 확정되지 않은 종 범위와 획득 조건을 구현 계약으로 사용하지 않는다. 사용자가 예상할 시작 형태를 설명한다.
후속 답변에서 직접 진화를 선택하는 방향을 확인했다. 다음 논의는 진화 후 이전 종을 선택할 때의 친밀도 소유다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 사용자 예시와 일반 원칙의 적용 범위를 구분했다. 랜덤 획득을 부화 기능 승인으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 426개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 진화와 친밀도의 소유

날짜: 2026-09-21. 상태: 직접 진화 결정. 친밀도 소유 모델 재검토. 기능 구현 미시작.
사용자 답변:

> 조건을 채우면 진화 가능 상태를 알려주고, 사용자가 원할 때 직접 진화시키는 방식으로 할거긴해.
> 아 근데 막상 생각해보니 해금은 리자몽까지 다 했는데, 파이리를 보이게 싶고하면 도감에서 고르게 하라고 했었는데, 친밀도를 어떻게 하지가 애매해졌네 흠....

확정: 진화 가능 상태를 알린 뒤 사용자가 직접 진화시킨다.
열린 문제: 도감에서 파이리를 다시 선택할 때 같은 동반자의 단계가 바뀌는지, 별도 종의 기록을 불러오는지 정해야 한다.
목표: 진화의 달성 기록과 친밀도의 소유를 구분한다. 도감 선택으로 기존 육성의 의미가 달라지지 않게 한다.
범위: 기록 모델 선택지와 사용자 경험. 저장 구조 변경과 구현은 시작하지 않는다.
기존 계약은 [도감 등록과 파티 소환](../../specs/s5.md#3-도감-등록과-파티-소환)을 따른다. 종별 기록은 제안 상태이며 채택 근거로 사용하지 않는다.

아래 표는 `[스펙 미확정]` 대안 비교다.

| 친밀도 소유 | 파이리를 다시 선택한 결과 | 결정할 문제 |
|---|---|---|
| 개별 동반자 | 같은 개체가 이미 도달한 단계 중 파이리로 바뀐다. 친밀도는 그대로 유지한다. | 단계 변경을 허용해야 한다. 다른 개체의 추가 획득 규칙은 별도다. |
| 종별 기록 | 파이리에 남겨 둔 친밀도를 불러온다. 리자몽 기록은 별도다. | 진화 시 친밀도를 복사할지 옮길지 정해야 한다. 이전 종으로 돌아갔을 때 성장 손실처럼 보일 수 있다. |
| 진화 계열 공통 기록 | 파이리와 리자몽이 같은 계열 친밀도를 사용한다. | 동시 소환의 중복 적립과 분기 진화 간 공유 범위를 정해야 한다. |

당시 추천: 개별 동반자가 친밀도를 소유하고 도감에서 이미 도달한 진화 단계를 고르는 방식이었다. 후속 사용자는 개체별 육성 방향을 선택했지만 진화는 되돌릴 수 없도록 정했다. 따라서 단계 변경 제안은 철회한다. 아래의 후속 결정이 우선한다.
예시 수치는 설명용이다. 파이리로 친밀도 100을 쌓고 리자몽 상태에서 200이 됐다면, 같은 개체를 파이리 단계로 바꿔도 200을 유지한다.
이 안은 기존 S5의 모습 선택 제외·종별 재소환 제안과 다른 모델이다. 사용자가 선택하기 전에는 기존 계약을 확정적으로 대체하지 않는다. `look` 그림만 바꾸는 기존 기능의 재사용 여부도 정하지 않았다.
`[스펙 미확정]` 같은 개체의 단계 변경이면 성격·배고픔·먹이 버프도 해당 개체에 유지하는 안을 제안한다. 전환으로 디버프를 초기화하거나 한 마리의 버프를 복제하지 않게 한다.
위험: 같은 개체를 두 칸에 동시에 배치하면 친밀도와 기간형 버프가 중복 처리될 수 있다. 새 종으로 복사하는 모델은 반복 진화 보상 문제를 만들 수 있다.
수용 조건 제안: 같은 예시에서 파이리 재선택 후의 친밀도를 설명할 수 있다. 리자몽과 파이리의 동시 배치가 가능한지 설명할 수 있다. 선택 전환으로 기록이 소실되거나 보상이 복제되지 않는다.
다음 확인 항목: 사용자가 키운 한 마리를 파이리로 바꾸고 싶은지, 파이리와 리자몽을 별개로 함께 두고 싶은지 정한다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 직접 진화의 승인과 친밀도 모델 제안을 구분했다. 기존 설계와의 충돌을 명시했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 429개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 개체별 육성과 되돌릴 수 없는 진화

날짜: 2026-09-21. 상태: 육성 소유 방향 결정. 도감 해금 제안 검토 중. 기능 구현 미시작.
사용자 답변:

> 개체별로 하는게 맞는거같긴하네. 진화해야만 도감해금되는게 아니라 도감은 레벨에 따라 자동해금되게 하는거면 좀 나을거같긴해. 대신 진화는 철회못하게.

확인한 방향: 친밀도는 개체별로 관리한다. 완료한 진화는 되돌리지 않는다.
사용자 제안: 도감은 진화 여부에만 의존하지 않고 레벨에 따라 자동 해금한다. 어떤 레벨인지와 해금 후 가능한 행동은 아직 설명하지 않았다.
목표: 진화한 개체의 육성과 다른 종을 표시하려는 선택을 분리한다.
범위: 기록 소유와 진화 방향의 정정. 레벨·획득 경제와 저장 구조는 확정하지 않는다.
현재 결정은 [도감과 해금](../../design.md#도감--해금)에 반영했다. 상세 재검토 범위는 [S5 도감 계약](../../specs/s5.md#3-도감-등록과-파티-소환)에 표시했다.

이전 제안 정정: 도감에서 같은 개체를 파이리와 리자몽 사이로 전환하는 안은 철회한다. 종별 친밀도와 진화 계열 공통 친밀도도 현재 선택한 방향이 아니다.
`[스펙 미확정]` 예시 제안: 개체 A가 파이리에서 리자몽으로 진화해도 A의 친밀도를 유지한다. 파이리를 다시 원하면 별도 개체 B를 획득한다. B의 초기 친밀도는 별도로 정한다. A의 기록을 B에 복사하지 않는 안이다.
별도 파이리 개체가 필요하다는 사실만으로 박스 UI를 추가하지 않는다. 기존 박스 제외와 소환 해제 시 기록 보존 요구를 개체 모델에서 다시 검토한다.
`[스펙 미확정]` 도감 해금은 종을 획득할 수 있는 조건으로 분리하는 안을 제안한다. 해금 즉시 개체를 지급할지, 구매 등을 허용할지는 사용자 확인이 필요하다.
미정: 레벨이 사용자 전체에 속하는지 개별 포켓몬에 속하는지, 경험치 획득 방식, 종별 해금 기준, 해금과 최초 선택 예외의 관계.
위험: 도감 해금과 개체 보유를 같은 상태로 저장하면 친밀도 소유가 다시 모호해질 수 있다. 파티에서 빠진 개체의 기록을 어떻게 유지할지 정하지 않으면 육성 손실이 생길 수 있다.
수용 조건 제안: 진화한 개체를 도감 선택으로 이전 단계로 되돌릴 수 없다. 새 개체의 획득과 기존 개체의 재배치를 구분한다. 진화 실패 복원과 완료 후 되돌리기 금지를 구분한다.
후속 답변에서 개별 포켓몬 레벨과 종별 입수처를 확인했다. 아래의 후속 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 개체별 육성과 가역적인 단계 변경을 구분했다. 레벨 기반 해금은 제안으로 표시했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 432개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 개체 레벨과 종별 입수처

날짜: 2026-09-21. 상태: 레벨 소유자와 입수처 구분 결정. 경험치 규칙 미정. 기능 구현 미시작.
사용자 답변:

> 1. 포켓몬 각각의 레벨로 생각하긴했어. 예를들어 파이리 16렙을 찍으면 리저드가 해금된다던지. 이때 리저드는 상점구매가 아닌 파이리에서 진화해야만 가질 수 있도록 포켓몬별로 입수처 구분이 필요.
> 2. 상점구매 항목에 추가되는거로 생각하긴 했어.

확인한 결정: 해금에 사용하는 레벨은 개별 포켓몬의 레벨이다. 종마다 입수처를 구분한다. 구매 가능한 종은 해금 후 상점에 추가한다. 진화 전용인 리저드는 파이리를 진화시켜서만 얻는다.
두 답변의 적용 범위를 구분한다. 상점 추가를 모든 해금 종에 적용하지 않는다. 사용자가 직접 지정한 진화 전용 조건을 유지한다.
목표: 레벨 조건 달성과 개체를 실제로 얻는 행동을 구분한다.
범위: 레벨의 소유, 해금, 종별 입수처의 계약. 경험치 계산과 전체 종 조건표 및 구현은 포함하지 않는다.
SSOT: [현재 설계](../../design.md#도감--해금), [개체 레벨과 입수처](../../specs/s5.md#개체-레벨과-입수처), [용어](../../terms.md).

사용자 예시 시나리오: 파이리가 레벨 16에 도달하면 리저드가 해금된다. 사용자는 파이리의 진화를 선택한다. 진화를 완료하면 같은 개체가 리저드가 된다. 상점에서 리저드를 구매하지 않는다.
진화를 선택하지 않으면 파이리로 지낼 수 있다. 레벨 16은 사용자가 든 예시로 보존한다. 전체 진화 조건표의 확정 근거로 사용하지 않는다.

`[스펙 미확정]` 해금 조건·입수처·획득 실행 조건을 종별 계약에서 구분하는 안을 제안한다. 같은 종에 복수 입수처를 허용할지는 별도로 정한다.
`[스펙 미확정]` 사용자 전체의 도감 해금 기록과 각 개체의 진화 조건을 별도로 관리하는 안을 제안한다. 해금만으로 다른 저레벨 개체가 바로 진화하지 않게 한다.
미정: 레벨과 친밀도의 관계, 경험치의 획득과 승계, 해금 기록의 유지, 상점 재구매, 새 개체 초기 상태, 랜덤 획득 대상.
위험: 친밀도와 경험치를 별도 수치로 두면 기존 돌봄·배고픔 디버프·먹이 버프의 적용 대상이 모호해질 수 있다. 반대로 친밀도를 레벨로 환산하면 두 용어가 같은 성장을 중복 설명할 수 있다.
수용 조건: 해금만으로 개체를 지급하지 않는다. 진화 전용 종을 상점에 추가하지 않는다. 사용자는 각 종의 실제 입수 방법을 확인할 수 있어야 한다.
후속 답변에서 친밀도와 경험치를 별도로 관리하는 방향을 확인했다. 아래의 분리 결정을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 일반 상점 추가와 진화 전용 예외를 구분했다. 기존 등록 기반 계약을 재검토 대상으로 명시했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 436개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 친밀도와 경험치 분리

날짜: 2026-09-21. 상태: 성장 경로 결정. 포인트 산정 방식 미정. 기능 구현 미시작.
사용자 답변:

> 친밀도랑 경험치랑 별개로 해야겠네. 지금은 친밀도로 포인트를 구할 수 있고, 그 포인트로 상점에서 경험사탕 구매해서 레벨업하는 식으로 설계.
> 추후 던전탐험 같은거 추가하면 그때 경험사탕과 경험치 얻도록 하자.

확인한 결정: 친밀도와 경험치는 별도다. 친밀도를 통해 포인트를 얻는다. 포인트로 경험사탕을 구매해 레벨을 올린다.
향후 방향: 던전 탐험을 추가할 때 경험사탕과 경험치를 얻도록 한다. 탐험의 즉시 구현 지시로 확대하지 않는다.
목표: 돌봄으로 쌓는 친밀도와 레벨 성장의 역할을 구분한다.
범위: 성장 경로와 상품 역할. 포인트 계산, 경험치 곡선, 가격 및 구현은 포함하지 않는다.
SSOT: [현재 설계](../../design.md#도감--해금), [친밀도와 경험치의 역할](../../specs/s5.md#친밀도와-경험치의-역할), [명칭 사전](../../terms.md).

미정: 친밀도를 교환하여 소모하는지, 증가량에 따라 포인트를 얻는지, 누적 친밀도 구간 보상인지. 자동 적립 여부와 수령 조작도 정하지 않았다.
당시 추천은 새로 얻은 친밀도를 기준으로 포인트를 지급하는 방식이었다. 후속 사용자 설명은 현재 친밀도에 따른 시간 적립이다. 아래의 정정이 우선한다.
사용자가 앞서 말한 자동기능 제외를 자동 포인트 적립의 승인으로 해석하지 않는다. 포인트 수령 방식은 별도로 확인한다.
`[스펙 미확정]` 경험사탕을 구매한 뒤 가방에서 대상 개체를 선택하는 안을 제안한다. 포인트를 얻은 개체와 사탕을 받을 개체를 같게 제한할지는 정하지 않았다.
배고픔 디버프와 프리미엄먹이 버프는 친밀도 증가량에 적용하는 기존 결정을 유지한다. 경험치 배율로 임의 전환하지 않는다.
위험: 친밀도 버프가 포인트 수입에도 영향을 주면 프리미엄먹이 가격과 효과가 경제에 함께 영향을 준다. 에이전트 추가 이득도 포인트 수입과 경험사탕 구매력에서 비교해야 한다.
수용 조건: 친밀도와 경험치를 같은 값으로 저장하지 않는다. 경험사탕 구매·사용과 레벨 상승을 설명할 수 있다. 친밀도 소비를 사용자 확인 없이 도입하지 않는다. 탐험 보상을 현재 구현 기능으로 표시하지 않는다.
후속 답변에서 현재 친밀도에 따른 시간 적립 방식을 확인했다. 아래의 후속 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 친밀도와 경험치의 역할을 구분했다. 현재 설계와 향후 확장을 구분했다. 포인트 산정 방식은 제안으로 남겼다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 442개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 현재 친밀도에 따른 포인트 적립

날짜: 2026-09-21. 상태: 적립 원리 결정. 다개체 합산과 계산식 미정. 기능 구현 미시작.
사용자 답변:

> 예를들어, 친밀도 100을 기준으로 100초마다 1포인트가 오른다고 치면 90일땐 110초마다 1포인트 오르고. 그런거지. 친밀도는 유지한 채로 양에 따라 포인트 얻는 첫번째 방식을 생각했어.

확인한 결정: 현재 친밀도에 따라 포인트 적립 간격이 달라진다. 친밀도가 높으면 간격이 짧아진다. 친밀도를 소모하지 않는다.
앞선 질문의 첫 번째 선택지를 그대로 복사하지 않는다. 사용자가 구체적으로 설명한 시간 적립 원리가 우선한다. 친밀도 증가량에 비례한 일회성 지급은 채택된 규칙이 아니다.
목표: 현재 친밀도와 포인트 생산의 관계를 명확히 한다.
범위: 적립 원리와 문서 정정. 수치 조정과 구현은 포함하지 않는다.
SSOT: [현재 설계](../../design.md#도감--해금), [친밀도와 경험치의 역할](../../specs/s5.md#친밀도와-경험치의-역할), [명칭 사전](../../terms.md).

100·100초·1포인트와 90·110초·1포인트는 예시다. 단순 선형식이나 역비례식으로 확정하지 않는다. 높은 친밀도에서도 적립 간격이 0 이하가 되지 않는 계산 규칙이 필요하다.
친밀도가 새로 오르지 않아도 현재 값에 따라 포인트를 적립한다. 종료 상태 그대로 재개한다는 결정에 따라 종료 중에는 적립하지 않는다.
정정: 이전 자동기능 제외 발언을 이 시간 적립의 금지로 해석하지 않는다. 이번 답변은 포인트의 시간 적립을 구체적으로 설명한 근거다. 자동 돌봄 등 다른 기능은 별도다.

해석: 기존 배고픔 디버프는 친밀도 증가량을 줄인다. 현재 친밀도 자체는 낮추지 않으므로, 이 효과만으로 현재 적립 속도를 즉시 낮추지는 않는다.
해석: 프리미엄먹이 버프는 친밀도 증가를 빠르게 한다. 포인트 적립에 별도의 배율을 곱하는 효과까지 승인된 것은 아니다.
`[스펙 미확정]` 적립 대상 개체, 다개체 합산, 적립 도중 친밀도 변경 처리, 종료 시 부분 진행 저장, 절전과 앱 숨김의 처리는 정하지 않았다.
위험: 모든 보유 개체가 적립하면 개체 수가 성장 속도를 지배할 수 있다. 표시 개체만 적립하면 숨기기나 작업 방해 회피에 경제적 불이익이 생길 수 있다.
수용 조건: 포인트 적립 후 친밀도가 유지된다. 친밀도 증가 이벤트 없이도 시간 적립을 설명할 수 있다. 예시를 최종 계산식으로 사용하지 않는다. 종료 중 포인트를 소급 적립하지 않는다.
후속 답변에서 파티 개체별 적립과 숨김 시 유지 방향을 채택했다. 아래의 파티 적립 결정을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 현재 친밀도와 증가량을 구분했다. 예시 수치와 확정 원리를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 447개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 파티 개체의 포인트 적립

날짜: 2026-09-21. 상태: 적립 대상과 숨김 처리 결정. 기능 구현 미시작.
사용자 답변: “그렇게 진행”.
승인 대상 제안: 파티에 배치한 개체가 각자 적립한다. 잠깐 숨겨도 적립을 유지한다. 모든 보유 개체가 적립하는 대안과 비교한 뒤 채택했다.
목표: 포인트 적립 대상을 정한다. 작업을 위해 포켓몬을 숨겨도 적립을 유지한다.
범위: 적립 대상과 개체별 결과의 합산. 적립 계산식과 파티 밖 개체의 다른 상태 처리는 확정하지 않는다.
SSOT: [현재 설계](../../design.md#도감--해금), [친밀도와 경험치의 역할](../../specs/s5.md#친밀도와-경험치의-역할), [명칭 사전](../../terms.md).

확인한 계약: 파티 개체는 자신의 친밀도에 따라 각각 적립한다. 결과는 보유 포인트에 합산한다. 숨김은 파티 배치를 유지하므로 적립을 멈추지 않는다. 파티 밖 개체는 포인트를 적립하지 않는다.
앱 종료 중에는 적립하지 않는 기존 결정을 유지한다. 숨김과 종료를 같은 상태로 취급하지 않는다.
미정: 파티에서 뺐다가 다시 배치할 때의 적립 진행 시간. 파티 밖 개체의 친밀도·배고픔·버프 시간. 보유 개체를 보관하고 다시 선택하는 UI.
당시 제안: 파티 밖 개체는 상태를 보존하고 재배치 시 이어간다. 후속 사용자는 박스 보관의 경우 이 방향에 동의했다. 숨긴 파티 개체의 상태는 계속 변해야 한다고 구분했다. 아래의 후속 결정이 우선한다.
위험: 배치 변경으로 적립 시간이 복제되거나 초기 지급이 반복되면 보상 악용이 가능하다. 파티 개체 수가 수입에 영향을 주므로 칸 해금과 상품 가격을 함께 검토해야 한다.
수용 조건: 같은 파티와 같은 친밀도라면 숨김 여부로 적립량이 달라지지 않는다. 파티 밖 개체는 적립하지 않는다. 여러 파티 개체의 적립 결과는 각각 한 번만 반영한다.
다음 확인 항목: 파티 밖 개체를 보유하고 다시 배치하는 경험과 상태 보존 방식.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 사용자의 짧은 승인과 승인 대상 제안을 연결했다. 포인트 적립과 다른 상태 변화를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 452개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 숨김과 박스 보관의 구분

날짜: 2026-09-21. 상태: 표시·숨김·보관의 상태 변화 결정. 기능 구현 미시작.
사용자 정정:

> 파티에서 빠졌다는게, 박스 보관일때를 얘기하는거면 그 방향이 맞아보여.
> 대신, 숨김 포켓몬은 배고픔이나 그런게 같이 닳아야해. 안그러면 친밀도 올린채로 볼에 넣으면 배고픔도 안닳으면서 포인트 벌 수 있는 악용사례가 있어보여.

승인 대상 제안: 박스 보관 중 친밀도·배고픔·남은 버프 시간을 보존한다. 포인트 적립은 중단한다. 다시 파티에 배치하면 이어간다.
추가 요구: 숨김은 파티에 남아 있는 상태다. 표시 상태와 동일하게 포인트 적립과 배고픔 등 상태 변화를 진행한다.
사용자가 말한 배고픔이 닳는다는 표현은 계속 배고파지는 의미로 기록한다. 기존 수치 방향이나 계산식을 변경하라는 지시로 해석하지 않는다.
목표: 숨김으로 돌봄 필요를 멈추면서 포인트만 얻는 경로를 막는다. 보관과 숨김의 의미를 분리한다.
범위: 상태 변화 계약과 기존 박스 제외 문서의 정정. 박스 UI 제작과 런타임 수정은 미시작이다.
SSOT: [상태 표](../../specs/s5.md#파티-표시와-박스-보관), [현재 설계](../../design.md#도감--해금), [용어](../../terms.md).

정정: 기존 박스 제외 방향을 보관 계약으로 대체한다. 보관 개체를 복제하는 구조나 별도 창을 자동 채택하지 않는다.
확인한 경계: 숨김은 배치를 유지한다. 박스 보관은 배치를 해제한다. 닫힌 몬스터볼 마커만으로 두 상태를 같은 것으로 취급하지 않는다.
숨긴 파티 개체도 친밀도 성장과 배고픔 변화가 진행된다. 버프 시간도 경과한다. 박스 개체는 포인트를 적립하지 않으며 보존한 상태로 재배치한다.
미정: 이동 시 진행 중인 적립 시간, 박스 수용량, 화면 구성, 개체 선택과 교체 조작.
위험: 표시 여부를 전체 시뮬레이션의 진행 조건으로 쓰면 숨김에서 상태 변화가 멈출 수 있다. 적립과 돌봄 상태의 진행 조건을 함께 검사해야 한다.
수용 조건: 같은 개체를 표시하거나 숨겨도 같은 시간 동안 배고픔 변화와 적립 결과가 같다. 숨김에서 버프가 정지하지 않는다. 박스에서는 적립 없이 상태를 보존한다. 보관과 재배치로 배고픔이나 버프를 초기화하지 않는다.
다음 확인 항목: 파티와 박스 사이의 이동 및 보유 개체 선택 흐름.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 숨김과 박스 보관을 구분했다. 기존 박스 제외 계약을 정정했다. 수치 방향을 사용자 표현만으로 변경하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 458개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 동일 종 보유와 파티 교체

날짜: 2026-09-21. 상태: 보유·자동 배치·교체 방향 결정. 기능 구현 미시작.
사용자 답변:

> 1. 가능.
> 2. 파티에 빈자리가 있으면 바로 배치하고, 가득 차면 박스로 보내기
> 3. 맞바꾸기. 실제포켓몬 게임처럼

질문 맥락: 첫 질문은 같은 종을 여러 개체 보유하고 동시에 파티에 둘 수 있는지였다. 세 번째 질문은 파티에서 교체를 선택하고 박스 개체를 고르면 한 번에 맞바꾸는 방식이었다.
확인한 결정: 동일 종의 다개체 보유와 동시 배치를 허용한다. 새 개체는 빈 파티 칸으로 들어간다. 파티가 가득 차면 박스로 들어간다. 교체는 선택한 두 개체를 한 번에 맞바꾼다.
목표: 새 개체의 도착 위치와 파티 교체 절차를 명확히 한다.
범위: 보유와 배치 계약. 원작의 박스 수·소지품·교환 등 언급하지 않은 규칙은 채택하지 않는다. 구현과 화면 제작은 미시작이다.
SSOT: [개체 획득과 파티 교체](../../specs/s5.md#개체-획득과-파티-교체), [현재 설계](../../design.md#도감--해금), [명칭 사전](../../terms.md).

같은 종의 파이리 A와 B는 다른 개체다. A만 진화시키고 B는 파이리로 남길 수 있다. 이는 개체별 육성과 직접 진화 결정에 따른 결과다.
사용자가 맞바꾸기를 선택했다고 해서 그냥 박스로 보내는 별도 조작까지 확정하지 않는다. 해당 조작의 위치와 제공 여부는 후속 화면 설계에서 정한다.
`[스펙 미확정]` 맞바꾸기에서는 각 개체의 기록을 유지하는 안을 제안한다. 저장 실패 시 두 개체의 이전 배치를 유지한다. 중간 상태를 저장하지 않는 안이다.
미정: 파티 초기 칸 수와 확장 조건, 박스 수용량, 박스도 가득 찬 경우, 빈 칸 자동 선택 순서, 맞바꾸기 후 표시 상태와 적립 진행 시간.
위험: 개체 대신 종 식별자로 교체하면 같은 종의 다른 개체를 잘못 이동할 수 있다. 파티 개체 수는 포인트 수입에 영향을 주므로 칸 확장과 경제를 함께 검토해야 한다.
수용 조건: 같은 종의 두 개체를 별도로 보유하고 파티에 함께 배치할 수 있다. 빈 칸 유무에 따라 새 개체의 도착 위치가 달라진다. 한 번의 맞바꾸기로 양쪽 개체의 위치가 교환된다.
후속 답변에서 기본 2칸, 구매 2칸, 업적 2칸과 박스 수량 제한 없음을 확인했다. 아래의 수용 정책 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 짧은 승인과 질문 범위를 연결했다. 원작 전체 규칙을 채택한 것으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 463개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 파티 칸과 박스 수용 정책

날짜: 2026-09-21. 상태: 기본 칸과 확장 출처 결정. 가격·업적 조건 미정. 기능 구현 미시작.
사용자 답변:

> 1. 처음엔 2칸. 상점에서 포인트로 구매 및 업적해금으로. 2개는 구매, 2개는 업적
> 2. 제한없이.

확인한 결정: 새 게임의 파티는 기본 2칸이다. 포인트 구매로 2칸을 추가한다. 업적으로 2칸을 추가한다. 총 6칸이다. 박스에는 보유 개체 수 제한을 두지 않는다.
목표: 파티 확장 경로와 보관 수용 범위를 정한다.
범위: 칸 수와 확장 출처. 가격, 업적 조건, 저장 이식과 구현은 포함하지 않는다.
SSOT: [파티 칸 확장과 박스 수용](../../specs/s5.md#파티-칸-확장과-박스-수용), [현재 설계](../../design.md#도감--해금), [명칭 사전](../../terms.md).

구매 칸과 업적 칸의 선후 관계는 미정이다. 구매 2칸을 먼저 확보해야 업적 칸을 얻는 것으로 가정하지 않는다.
두 구매 칸의 가격과 구매 단위는 미정이다. 두 업적의 조건과 보상 수령 방식도 미정이다.
기존 저장에 열린 칸을 회수하라는 지시가 아니다. 기존 구매 권리 보존은 저장 이식에서 다룬다.
위험: 파티 확장은 포인트 수입을 늘리므로 경험사탕·먹이 구매와 경쟁하는 선택이 된다. 업적에 에이전트 사용을 필수로 두면 앞선 비사용자 불리함 우려와 충돌할 수 있다.
`[스펙 미확정]` 업적은 일반 돌봄과 성장·수집으로 달성하는 안을 제안한다. 구체적인 활동과 횟수는 아직 정하지 않았다.
수용 조건: 새 게임에서 2칸을 사용할 수 있다. 구매와 업적으로 각각 2칸만 추가한다. 최대 6칸을 넘기지 않는다. 파티가 가득 차면 박스에 보관한다. 박스 수량 제한으로 획득을 거부하지 않는다.
후속 답변에서 공통 필수 순서 없이 진행하는 방향과 첫 업적을 확인했다. 아래의 참여 유도 업적 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 기본 칸과 확장 수량을 구분했다. 순서·가격·업적 조건을 임의로 채택하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 468개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 참여 유도 업적

날짜: 2026-09-21. 상태: 첫 칸 업적 결정. 두 번째 칸 업적 후보 검토 중. 기능 구현 미시작.
사용자 답변:

> 1. 독립적으로
> 2. 참여유도. 첫번째 업적은 칸 2칸에 포켓몬 소환하기로 한칸 열게 해줄거야. 2번째업적은 아직 고민중이긴한데 아마 상점에서 칸 모두 구매 이런느낌으로 할거같은데?

확인한 결정: 구매와 업적에 공통된 필수 해금 순서를 두지 않는다. 첫 업적은 파티 두 칸에 포켓몬을 배치하면 한 칸을 연다. 목적은 참여 유도다.
사용자 후보: 두 번째 업적은 상점에서 구매할 수 있는 파티 칸을 모두 구매하는 조건이다. 아직 고민 중이라고 명시했으므로 확정하지 않는다.
목표: 첫 파티 구성에서 추가 칸을 얻는 참여 흐름을 만든다.
범위: 첫 업적과 두 번째 후보의 구분. 가격과 보상 수령 UI 및 구현은 포함하지 않는다.
SSOT: [파티 칸 확장과 박스 수용](../../specs/s5.md#파티-칸-확장과-박스-수용), [현재 설계](../../design.md#도감--해금).

두 번째 후보는 구매에 의존한다. 독립 진행이라는 답변을 근거로 이 의존성을 감추지 않는다. 공통 강제 순서 없음과 개별 업적의 조건을 구분해 기록한다.
`[스펙 미확정]` 첫 업적의 판정은 현재 파티에 두 개체가 배치된 상태로 제안한다. 같은 종이어도 개체가 다르면 센다. 숨겨도 파티에 있으면 센다. 개체 하나를 두 칸으로 옮긴 이력을 중복해서 세지 않는다.
`[스펙 미확정]` 한 번 해금한 칸은 이후 개체를 박스로 보내도 유지하는 안을 제안한다. 같은 조건을 다시 충족해도 추가 칸을 반복 지급하지 않는다.
미정: 보상 자동 반영 또는 수령 버튼, 두 번째 개체의 초기 획득 경로, 업적 안내 시점, 두 번째 업적의 최종 조건.
위험: 두 번째 개체를 얻기까지 오래 걸리면 첫 참여 업적이 늦어진다. 구매 완료 업적을 채택하면 마지막 구매가 구매 칸과 업적 칸을 함께 여는 경우가 생긴다.
수용 조건: 구매를 먼저 하지 않아도 첫 업적을 달성할 수 있다. 두 칸에 배치한 결과로 추가 한 칸을 얻는다. 두 번째 업적은 후보로 표시한다. 총 구매 2칸·업적 2칸의 범위를 유지한다.
후속 답변에서 튜토리얼 포인트로 직접 구매하고 업적창에서 보상을 받는 흐름을 채택했다. 아래의 후속 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 독립 진행과 구매 완료 조건의 관계를 명시했다. 첫 업적의 확정과 두 번째 후보를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 472개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 튜토리얼 구매와 업적 수령

날짜: 2026-09-21. 상태: 첫 구매와 업적 수령 흐름 결정. 예외 흐름 설계 전. 기능 구현 미시작.
사용자 답변:

> 1. 튜토리얼 포인트 지급 → 직접 구매 로 하자. 그런다음 업적클리어로 업적창으로 가서 보상받기 하는 그런 자연스러운 업적창 유도방식이 좋아보여.
> 2. 나는 업적창에서 받아야 열리게 하고싶어.

확인한 결정: 튜토리얼에서 포인트를 지급한다. 사용자가 상점에서 두 번째 포켓몬을 직접 구매한다. 파티 두 칸이 채워져 업적을 달성하면 업적창으로 안내한다. 사용자가 보상을 받아야 추가 칸을 연다.
이전 추천 정정: 달성 시 자동으로 칸을 여는 안은 채택하지 않는다. 두 번째 업적 후보에서도 구매 완료와 업적 보상 수령을 구분한다.
목표: 첫 구매로 상점을 익힌다. 첫 업적의 보상 수령으로 업적창을 익힌다.
범위: 튜토리얼 구매·업적 진입·직접 수령의 연결. 지급량·상품 구성·시각 설계와 구현은 포함하지 않는다.
SSOT: [첫 구매와 업적 보상 수령](../../specs/s5.md#첫-구매와-업적-보상-수령), [현재 설계](../../design.md#놀이공간과-직접-돌봄), [업적 용어](../../terms.md#구매와-사용).

사용자 경험의 순서는 포인트 지급, 직접 구매, 빈 칸 자동 배치, 업적 달성 안내, 업적창에서 수령이다. 기본 2칸인 사용자는 수령 후 3칸을 사용한다.
달성 알림을 봤거나 업적창을 열었다는 이유만으로 보상을 지급하지 않는다. 보상 수령 조작을 구분한다.
`[스펙 미확정]` 달성 후 미수령 상태를 저장하는 안을 제안한다. 창을 닫거나 앱을 종료해도 나중에 받을 수 있도록 한다. 이미 받은 보상은 다시 지급하지 않는다.
미정: 튜토리얼 건너뛰기 시 포인트 지급, 다시 보기에서 중복 지급 방지, 구매할 수 있는 종, 지급량, 다른 상품에 사용 가능한지 여부, 수령 전 파티를 바꾼 경우.
위험: 지급 포인트를 다른 곳에 써서 두 번째 포켓몬을 구매할 수 없으면 안내가 막힐 수 있다. 다시 보기로 포인트를 반복 지급하면 경제를 우회할 수 있다.
수용 조건: 직접 구매 없이 두 번째 개체를 자동 지급하지 않는다. 업적 달성만으로 칸을 열지 않는다. 업적창에서 수령한 뒤 추가 칸을 사용할 수 있다. 두 번째 업적은 여전히 조건 후보로 남긴다.
후속 답변에서 건너뛰어도 동일하게 한 번 지급하고 다시 보기에서는 추가 지급하지 않는 정책을 채택했다. 첫 구매 선택 범위는 미정이다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 달성·안내·수령을 구분했다. 업적창의 실제 UI 형태는 확정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 477개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 시작 포인트와 튜토리얼 건너뛰기

날짜: 2026-09-21. 상태: 시작 포인트 지급 정책 결정. 기능 구현 미시작.
사용자 답변: “그렇게하자.”
승인 대상 제안: 튜토리얼을 건너뛰어도 시작 포인트는 동일하게 한 번 지급한다. 다시 보기에서는 추가 지급하지 않는다. 설명을 생략해도 직접 구매와 업적 달성이 가능하게 한다.
목표: 안내 참여 여부로 시작 자원에 차이가 생기지 않게 한다. 다시 보기로 시작 포인트를 반복 지급하지 않는다.
범위: 시작 포인트 지급 정책. 지급량과 첫 구매 상품 및 구현은 포함하지 않는다.
SSOT: [첫 구매와 업적 보상 수령](../../specs/s5.md#첫-구매와-업적-보상-수령), [현재 설계](../../design.md#놀이공간과-직접-돌봄).

확인한 계약: 튜토리얼을 진행하거나 건너뛰는 두 경로 모두 같은 시작 포인트를 한 번 받는다. 이미 지급했다면 다시 보기에서 추가 지급하지 않는다.
`[스펙 미확정]` 지급 여부를 튜토리얼 완료 여부와 별도로 저장하는 안을 제안한다. 재실행이나 중간 건너뛰기로 지급이 반복되지 않게 한다.
미정: 지급 시점과 양, 첫 구매 대상, 다른 상품 구매 허용, 다시 보기 실습의 일반 돌봄 보상.
위험: 안내 단계를 다시 시작할 때 지급 상태도 초기화하면 중복 지급이 생길 수 있다.
수용 조건: 진행과 건너뛰기의 시작 포인트가 같다. 다시 보기로 포인트가 늘지 않는다. 지급 후 재실행해도 시작 포인트를 다시 지급하지 않는다.
후속 답변에서 랜덤알 구매를 권하는 방향과 시작 포인트의 자유 사용을 확인했다. 아래의 랜덤알 안내와 단계별 튜토리얼 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 짧은 승인과 승인 대상을 연결했다. 시작 포인트와 실습의 일반 보상을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 480개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 랜덤알 구매와 파티창 안내

날짜: 2026-09-21. 상태: 랜덤알과 파티 안내 흐름 결정. 부화 방식 미정. 기능 구현 미시작.
사용자 답변:

> 1. 시작하면 포인트를 주고, 상점에서 랜덤알 구매 (자연스럽게 상점 이용 방법 설명 유도용) - 알 부화 - 파티창에서 꺼내기 (처음에는 숨어있음(볼에있음) + 파티창 사용방법 설명 유도용)
> 2. 이건 자유롭게. 최대한 알 구매 유도를 할거긴하지만 말안들으면 쩔수지 뭐

확인한 결정: 두 번째 포켓몬의 직접 구매 안내를 랜덤알 구매로 대체한다. 구매로 상점을 익힌다. 부화한 포켓몬은 처음 숨겨져 있다. 파티창에서 꺼내며 표시 조작을 익힌다. 시작 포인트는 다른 상품에도 자유롭게 쓸 수 있다.
목표: 획득 행동을 상점과 파티창 학습으로 연결한다. 안내를 따르지 않아도 일반 게임을 계속할 수 있게 한다.
범위: 랜덤알 채택과 초반 숨김·꺼내기 흐름. 부화 계산과 결과 확률, UI 제작과 구현은 포함하지 않는다.
SSOT: [첫 구매와 업적 보상 수령](../../specs/s5.md#첫-구매와-업적-보상-수령), [현재 설계](../../design.md#놀이공간과-직접-돌봄), [명칭 사전](../../terms.md).

기존 상품 기록 정정: 랜덤알 구매와 부화는 이번 답변에서 채택했다. 작업량 기반 부화와 기존 알의 수치까지 채택한 것은 아니다.
숨김은 파티의 표시 상태다. 박스 보관과 구분한다. 앞서 정한 숨김 중 적립·배고픔·버프 시간 경과를 유지한다.
초반 숨김을 모든 새 개체의 공통 규칙으로 확대하지 않는다. 파티가 가득 찬 경우에는 기존 박스 이동 계약이 적용되며 안내 시점은 추가 설계가 필요하다.
미정: 부화 방식과 시간, 랜덤 결과 종, 가격, 첫 업적 판정 시점. 기존 두 칸 배치 조건만으로는 숨긴 상태에서 업적이 먼저 달성될 수 있다.
위험: 파티창을 배우기 전에 업적 안내가 뜨면 의도한 학습 순서가 달라질 수 있다. 포인트를 다른 곳에 쓴 사용자를 안내 화면에 가두면 자유 사용 결정과 충돌한다.
수용 조건: 상점에서 알을 구매할 수 있다. 부화 후 숨김 상태와 표시 상태를 구분한다. 다른 상품에 시작 포인트를 써도 게임 진행을 막지 않는다.

### 행동에 따른 튜토리얼 분리

날짜: 2026-09-21. 상태: 안내 시작 원리 결정. 세부 예외 설계 전. 기능 구현 미시작.
후속 사용자 답변:

> 그 단계별로 튜토리얼을 따로 만들면 되겠네. 만약 포인트를 다른곳에 썼다가 다시 모아서 구매하면 부화관련 튜토리얼 시작하는등의

확인한 결정: 각 단계의 튜토리얼을 분리한다. 사용자의 실제 행동에 따라 관련 안내를 시작한다. 알 구매가 늦어져도 구매한 시점에 부화 안내를 시작한다.
목표: 사용자의 행동 순서를 강제하지 않으면서 필요한 시점에 사용법을 설명한다.
범위: 안내의 시작 계기와 분리 원칙. 구체적인 상태 저장 필드와 화면 구현은 미시작이다.
SSOT: [행동에 따른 단계별 안내](../../specs/s5.md#행동에-따른-단계별-튜토리얼), [진행 현황](../../progress.md).

`[스펙 미확정]` 안내별 진행 상태를 저장하는 안을 제안한다. 시작 포인트 지급 여부와 업적 보상 수령 여부는 별도 기록으로 유지한다.
`[스펙 미확정]` 이미 끝낸 행동을 다시 요구하지 않는 안을 제안한다. 여러 안내가 동시에 시작할 때의 우선순위와 중간 종료 후 복귀는 별도로 설계한다.
수용 조건: 시작 포인트를 다른 상품에 쓴 뒤 포인트를 다시 모아 알을 구매해도 부화 안내를 시작할 수 있다. 그동안 다른 기능을 사용할 수 있다. 안내 재개 때문에 시작 포인트를 다시 지급하지 않는다.
다음 확인 항목: 알 부화 방식과 첫 업적 판정·파티 안내의 순서. 개별 안내 건너뛰기와 다시 보기의 적용 범위도 정해야 한다.
의미 검수: 위 두 후속 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 첫 구매 흐름의 정정을 반영했다. 랜덤알 채택과 작업 부화 미정을 구분했다. 실제 행동과 안내 상태를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 488개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 부화 준비와 직접 열기

날짜: 2026-09-21. 상태: 부화 준비와 완료 조작 결정. 세부 단축 규칙 미정. 기능 구현 미시작.
사용자 답변:

> 1. 돌봄같은 활동으로 진행도를 쌓는데, 최대5분을 잡고, 시간가속시키는 느낌의 그건데, 아 무슨알고리즘이라고 해야하지 시간가속말곤 용어가 생각안나네. 암튼 부화완료 조건 시간이 있고, 그 시간지난후에 직접 열기해야 부화되게. 자동부화하면 언제 깨어났지? 할거같애.
> 2. 두 마리 꺼내기 → 업적 안내

확인한 결정: 부화 준비에 최대 5분을 잡는다. 돌봄 같은 활동으로 시간을 단축한다. 준비 시간이 끝나도 자동 부화하지 않는다. 사용자가 직접 열어야 개체를 얻는다.
이유: 자동 부화하면 사용자가 태어나는 순간을 놓칠 수 있다.
첫 업적 정정: 두 마리를 실제로 꺼낸 뒤 업적을 안내한다. 숨긴 채 두 칸에 배치한 것만으로 달성하는 이전 제안은 대체한다. 승인 대상 제안에 따라 달성 후 다시 숨겨도 유지한다.
목표: 돌봄 참여로 기다림을 줄인다. 부화 순간을 사용자 조작에 연결한다.
범위: 부화 준비·가능·완료의 구분과 첫 업적 조건. 수치 조정·알 결과 확률·구현은 포함하지 않는다.
SSOT: [활동에 따른 부화 대기시간 단축](../../specs/s5.md#활동에-따른-부화-대기시간-단축), [현재 설계](../../design.md#상점), [용어](../../terms.md#구매와-사용).

문서 설명에는 활동으로 대기시간을 단축하는 부화 타이머라는 표현을 사용한다. 특정 표준 알고리즘 이름이라고 주장하지 않는다.
최대 5분은 부화 가능해질 때까지의 대기 기준으로 기록한다. 직접 열기를 늦춘 시간까지 포함한 강제 부화 기한으로 해석하지 않는다.
`[스펙 미확정]` 성공한 돌봄마다 남은 대기시간을 차감하는 구현 원리를 제안한다. 타이머 속도를 일시적으로 높이는 방식과의 선택은 미정이다. 단축량은 정하지 않았다.
앱 종료 중 상태 보존 원칙을 적용한다. 실제 시간으로 5분 뒤 오프라인 부화하는 규칙으로 확대하지 않는다.
미정: 타이머 시작 시점, 인정 활동과 단축량, 동시 알 수, 부화 가능 알림, 열기 화면, 결과를 확정하는 저장 시점.
위험: 클릭 횟수만으로 단축하면 반복 입력을 유도할 수 있다. 열기 재시도마다 결과를 다시 추첨하거나 중복 개체를 지급하지 않는 계약이 필요하다.
수용 조건: 준비 시간이 끝나도 개체를 자동 지급하지 않는다. 부화 가능 상태에서 사용자가 직접 열어 얻는다. 숨긴 두 번째 개체를 꺼내기 전에는 첫 업적을 달성하지 않는다. 달성 후에는 업적창에서 보상을 받아야 칸이 열린다.
후속 답변에서 알 구매 직후 준비 타이머를 시작하는 방향을 채택했다. 활동별 단축 방식은 후속 설계에서 구체화한다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 부화 가능과 완료를 구분했다. 첫 업적의 기존 배치 조건을 정정했다. 활동별 단축 알고리즘은 제안으로 남겼다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 494개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 구매 직후 부화 준비 시작

날짜: 2026-09-21. 상태: 준비 타이머 시작 계기 결정. 기능 구현 미시작.
사용자 답변: “구매후 바로”.
질문 맥락: 구매 직후 시작과 가방에서 별도 시작 조작 중 선택을 요청했다.
확인한 결정: 알 구매 완료 직후 준비 타이머를 시작한다. 별도 부화 시작 조작을 요구하지 않는다. 부화 가능 상태에서 직접 열어야 한다는 기존 결정은 유지한다.
목표와 범위: 구매부터 준비 진행까지의 조작을 줄인다. 활동별 단축량과 동시 알 수 및 구현은 포함하지 않는다.
SSOT: [활동에 따른 부화 대기시간 단축](../../specs/s5.md#활동에-따른-부화-대기시간-단축), [현재 설계](../../design.md#상점).
`[스펙 미확정]` 구매와 알 준비 상태를 함께 저장하는 안을 제안한다. 구매 실패 시 알이나 타이머만 생성하지 않는다. 재실행에서 같은 알의 시간을 초기화하지 않는 안이다.
수용 조건: 구매 완료 후 추가 입력 없이 준비가 진행된다. 준비 완료만으로 자동 부화하지 않는다. 튜토리얼 표시 여부가 타이머 시작 조건이 되지 않는다.
후속 답변에서 알별 진행과 알 상호작용 후보를 제시했다. 아래의 후보 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 자동 준비 시작과 사용자가 직접 수행하는 부화를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 497개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 알별 진행과 상호작용 후보

날짜: 2026-09-21. 상태: 알별 진행 방향 확인. 단축 수단 검토 중. 기능 구현 미시작.
사용자 답변:

> 1. 알이라서 뭐 쓰다듬기, 노래들려주기 같은걸 생각하긴했었는데, 아님 상점의 부화시간단축도구 그런거?
> 2. 개별로.

질문 맥락: 포켓몬의 밥 주기·놀아주기를 단축 활동으로 할지 물었다. 여러 알의 동시 또는 순차 진행과 전체 알에 대한 단축 적용도 함께 질문했다.
확인한 방향: 알별로 진행도를 관리한다. “개별로”는 개별 진행의 근거다. 동시 처리 수나 순차 대기열의 승인으로 확대하지 않는다.
사용자 후보: 알 쓰다듬기, 노래 들려주기, 상점의 부화시간 단축 도구. 세 가지 모두 채택했다는 뜻으로 기록하지 않는다.
목표와 범위: 알 자체의 상호작용과 진행 단위를 정리한다. 가격·단축량·조작 UI와 구현은 포함하지 않는다.
SSOT: [활동에 따른 부화 대기시간 단축](../../specs/s5.md#활동에-따른-부화-대기시간-단축), [현재 설계](../../design.md#상점).

`[스펙 미확정]` 추천: 쓰다듬기와 노래 들려주기를 무료 알 돌봄으로 검토한다. 선택한 알의 반응을 본 뒤 해당 알의 남은 시간만 단축한다. 상점 도구는 필요성을 확인한 뒤 추가하는 후보로 둔다.
추천 이유: 짧은 돌봄과 반응을 즐기는 제품 목적에 맞는다. 최대 5분의 대기에서 별도 유료 도구의 역할은 가격과 단축량을 정할 때 검토할 수 있다.
노래 들려주기를 마이크 입력이나 실제 노래 녹음 기능으로 가정하지 않는다. 구체적인 조작과 소리 사용은 미정이다.
위험: 무료 돌봄과 도구를 함께 설계하면 각각의 단축량이 겹칠 수 있다. 단순 반복 클릭을 최적 행동으로 만들면 짧은 리프레시 목적과 어긋날 수 있다.
수용 조건 제안: 알 A와 B의 준비 상태를 구분한다. A를 대상으로 한 단축이 B에게 자동 적용되지 않는다. 준비 완료 후 직접 열기는 유지한다.
후속 답변에서 알별 동시 진행과 개별 돌봄 및 돌보미집 최대 6개 배치를 확인했다. 활동과 도구의 채택 조합은 미정이다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 사용자 후보와 채택한 방향을 구분했다. 짧은 답변에서 동시 진행 제한을 추정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 500개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 돌보미집과 알 6개

날짜: 2026-09-21. 상태: 배치 공간과 개별 진행 확정. 초과 구매 정책 미정. 기능 구현 미시작.
사용자 답변:

> 그렇지. 개별로 부화타이머와 돌봄을 해야하는거야. 돌보미집에 알이 최대 6개씩 배치될 수 있게.

질문 맥락: 여러 알이 각자의 타이머로 동시에 진행하고 돌봄은 선택한 알에만 적용하는지 확인했다.
확인한 결정: 돌보미집에 최대 6개의 알을 배치한다. 개별 타이머로 동시에 진행한다. 돌봄은 선택한 알에 개별 적용한다.
목표와 범위: 알 준비 공간과 개별 돌봄의 경계를 정한다. 가격·칸 확장·UI 제작과 구현은 포함하지 않는다.
SSOT: [활동에 따른 부화 대기시간 단축](../../specs/s5.md#활동에-따른-부화-대기시간-단축), [현재 설계](../../design.md#상점), [명칭 사전](../../terms.md).

파티 칸과 돌보미집의 알 칸은 다른 개념이다. 파티의 기본 2칸·구매 2칸·업적 2칸 규칙을 돌보미집에 적용하지 않는다.
이번 답변은 개별 진행을 명확히 했다. 앞서 함께 질문한 무료 활동·도구 보류 조합은 별도 채택 근거로 확대하지 않는다.
`[스펙 미확정]` 빈 칸이 있을 때 구매한 알을 바로 배치하는 안을 제안한다. 돌보미집이 가득 차면 추가 알 구매를 제한하는 안을 제안한다. 구매 직후 진행 원칙과 맞추기 위한 안이다.
`[스펙 미확정]` 준비가 끝나도 직접 열기 전까지 알이 칸을 차지하는 안을 제안한다. 직접 부화하면 칸을 비우고 개체를 파티 또는 박스로 보낸다.
미정: 처음 사용 가능한 알 칸 수, 가득 찼을 때 구매와 초과 알 보관, 알 이동, 돌보미집 진입 위치, 활동의 채택 조합.
위험: 초과 알을 구매하게 하면서 준비 위치를 정하지 않으면 구매 직후 타이머 원칙과 최대 6개 규칙이 충돌한다.
수용 조건: 돌보미집에 동시에 배치되는 알은 6개 이하다. 알 A의 돌봄은 B의 남은 시간을 단축하지 않는다. 각 알은 따로 부화 가능 상태가 되고 사용자가 직접 연다.
후속 답변에서 만석일 때 추가 구매를 제한하는 안을 채택했다. 아래의 만석 처리를 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 최대 배치 수와 초기 칸 수를 구분했다. 파티 공간과 알 공간을 구분했다. 도구 채택을 추정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 505개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 돌보미집 만석 처리

날짜: 2026-09-21. 상태: 추가 구매 제한 결정. 기능 구현 미시작.
사용자 답변: “그렇게 진행.”
승인 대상 제안: 6개의 알로 가득 차면 추가 알 구매를 막는다. 부화 가능한 알도 직접 열기 전까지 자리를 차지한다. 기존 알을 부화시켜 빈 칸을 만든 뒤 다시 구매한다.
목표와 범위: 구매 즉시 준비 시작과 최대 6개 배치를 일치시킨다. 알 가격·결과 범위·구현은 포함하지 않는다.
SSOT: [활동에 따른 부화 대기시간 단축](../../specs/s5.md#활동에-따른-부화-대기시간-단축), [현재 설계](../../design.md#상점).

확인한 계약: 만석이면 알을 추가 구매할 수 없다. 구매 제한을 안내하고 돌보미집으로 이동할 수 있게 한다. 초과 알을 먼저 구매해 보관하는 대안은 채택하지 않는다.
부화 준비가 끝났어도 직접 열기 전에는 점유 수를 줄이지 않는다. 직접 부화로 개체를 얻으면 해당 칸을 비운다.
`[스펙 미확정]` 구매 시 최신 점유 수를 검사하고 알 생성과 포인트 차감을 함께 처리하는 안을 제안한다. 부화 완료와 개체 획득 및 칸 비우기도 함께 저장하는 안이다.
위험: 구매창의 이전 점유 수만 사용하면 동시 구매로 6개를 넘길 수 있다. 직접 열기가 실패했는데 칸만 비우면 알이 유실될 수 있다.
수용 조건: 6개 점유 중에는 포인트 차감 없이 구매를 거부한다. 부화 가능 알도 점유 수에 포함한다. 하나를 정상 부화시킨 뒤 하나를 구매할 수 있다. 수용 제한을 해결하려고 자동 부화하지 않는다.
후속 답변에서 해금한 종만 랜덤알 결과로 제공하는 방향을 확인했다. 아래의 해금 범위 기록을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 승인 대상과 대안을 구분했다. 부화 가능 상태와 빈 칸을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 509개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 랜덤알의 해금 범위

날짜: 2026-09-21. 상태: 결과 범위 원칙 결정. 확률·판정 시점 미정. 기능 구현 미시작.
사용자 답변: “해금한종에서만”.
질문 맥락: 해금 종만 나오는 방식과 미해금 종도 나오는 방식을 비교했다. 어느 경우든 진화 전용 종은 제외한다는 기존 입수처 조건을 함께 안내했다.
확인한 결정: 해금한 종 중 알 입수처가 허용된 종만 나온다. 해금했어도 진화 전용 종은 제외한다.
목표와 범위: 도감 해금과 알 결과의 관계를 정한다. 전체 종 목록·확률·가격과 구현은 포함하지 않는다.
SSOT: [활동에 따른 부화 대기시간 단축](../../specs/s5.md#활동에-따른-부화-대기시간-단축), [현재 설계](../../design.md#상점), [명칭 사전](../../terms.md).
미정: 해금 판정 시점, 결과 확정 시점, 결과 목록과 확률, 초기 해금 목록, 알과 종 지정 구매의 가격 차이.
위험: 첫 알의 적격 종이 없으면 튜토리얼 구매가 성립하지 않는다. 구매 후 해금 상태가 바뀌면 구매 시 안내한 결과 범위와 달라질 수 있다.
수용 조건: 미해금 종을 결과로 제공하지 않는다. 진화 전용 종을 결과로 제공하지 않는다. 해금과 알 입수 가능 여부를 별도로 검사한다.
후속 답변에서 가격·확정성의 역할 구분과 종별 전용 입수처를 확인했다. 결과 확정 시점과 초기 결과 목록은 미정이다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 해금과 보유를 혼동하지 않았다. 진화 전용 예외와 미정인 판정 시점을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 513개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 종별 입수처 지정

날짜: 2026-09-21. 상태: 입수처 분류 방향 결정. 종별 배정 미정. 기능 구현 미시작.
사용자 답변:

> 그렇게 구상하긴했어. 대신 랜덤알에서만 얻을수 있는 종도 있고 그런식. 알에서도 못얻고 특수기믹으로만 얻는 종도 있고 다 따로 지정할거야.

질문 맥락: 저렴한 랜덤알과 더 비싼 종 지정 구매의 구분 및 전용 입수처 여부를 물었다.
확인한 방향: 가격과 확정성의 구분을 유지한다. 종마다 입수처를 지정한다. 알 전용 종이 있다. 알에서도 얻지 못하는 특수 기믹 전용 종도 있다.
목표: 종의 해금과 실제 입수 방법을 별도 계약으로 관리한다.
범위: 입수처 분류와 배정 원칙. 구체적인 종 목록·가격·확률·특수 기믹·구현은 포함하지 않는다.
SSOT: [개체 레벨과 입수처](../../specs/s5.md#개체-레벨과-입수처), [현재 설계](../../design.md#상점), [명칭 사전](../../terms.md).

알 전용도 앞서 정한 해금 종 제한을 따른다. 특수 기믹 전용 종은 해금됐다는 이유로 상점 구매나 알 결과에 추가하지 않는다. 진화 전용도 기존 제한을 유지한다.
해금한 종 전체가 같은 입수처를 갖는 모델은 사용하지 않는다. 같은 종의 복수 입수처와 전용 입수처를 구분할 수 있어야 한다.
`[스펙 미확정]` 종별 계약에서 해금 조건·허용 입수처·경로별 실행 조건·안내 문구를 분리하는 안을 제안한다. 입수처를 새로 추가할 때 기존 돌봄과 파티 모듈에 조건을 흩어 넣지 않는 방향이다.
미정: 종별 배정, 특별한 종의 첫 선택 가능 여부, 특수 기믹과 해금의 관계, 도감에 공개할 조건과 힌트, 알 가격과 직접 구매 가격의 차이.
위험: 입수처만 다르고 제한 검사가 공통으로 적용되지 않으면 전용 종을 다른 경로로 얻을 수 있다. 특수 기믹 정보를 모두 숨기면 사용자가 획득 가능 여부를 알기 어려울 수 있다.
수용 조건: 알 전용 종은 종 지정 구매에 나타나지 않는다. 특수 기믹 전용과 진화 전용 종은 알 추첨 대상에 들어가지 않는다. 미해금 종도 알 대상에서 제외한다. 종별 배정은 사용자 확인 전에 임의로 확정하지 않는다.
후속 답변에서 일반 입수 조건 공개와 특수 기믹의 발견 전 힌트·발견 후 방법 공개를 채택했다. 아래의 공개 정책을 따른다.
의미 검수: 이번 절과 현재 설계·기능 계약·명칭 사전·진행 현황을 검토했다. 경로의 일반 역할과 전용 입수처를 구분했다. 개별 종과 기믹을 임의 생성하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 519개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 도감 입수 방법 공개

날짜: 2026-09-21. 상태: 공개 원칙 결정. 발견 판정 미정. 기능 구현 미시작.
사용자 답변:

> 구매·알·진화는 조건을 명확히 표시, 특수 기믹은 처음에는 힌트만 표시하고, 발견한 뒤 방법 공개 이렇게 생각하긴했어.

확인한 결정: 구매·알·진화의 조건은 도감에 명확히 표시한다. 특수 기믹은 발견 전 힌트만 표시한다. 발견 후에는 정확한 입수 방법을 공개한다.
목표와 범위: 도감의 입수처 정보 공개 단계를 정한다. 실제 힌트·기믹 내용과 구현은 포함하지 않는다.
SSOT: [개체 레벨과 입수처](../../specs/s5.md#개체-레벨과-입수처), [현재 설계](../../design.md#상점).
`[스펙 미확정]` 기믹을 수행해 해당 종을 처음 획득하면 발견으로 기록하는 안을 제안한다. 공개된 방법은 현재 보유 여부와 관계없이 유지하는 안이다.
미정: 발견의 정확한 판정 시점, 미해금 상태의 이름·그림 공개 정도, 힌트 문구, 첫 선택에서 전용 입수처를 가진 종을 허용할지 여부.
위험: 미발견 종의 다른 화면에서 정확한 조건을 노출하면 힌트 정책이 무의미해질 수 있다. 발견 기록을 현재 보유 여부와 묶으면 안내가 다시 숨겨질 수 있다.
수용 조건: 일반 입수처의 실행 조건을 도감에서 알 수 있다. 미발견 특수 기믹에는 힌트를 표시한다. 발견한 뒤 정확한 방법을 표시한다. 세부 판정은 제안으로 남긴다.
후속 답변에서 첫 선택 후보를 별도로 지정하는 방향을 채택했다. 실제 후보 종과 이후 입수처의 관계는 미정이다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 공개 정책과 미정인 발견 판정을 구분했다. 종별 기믹을 임의로 추가하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 523개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 첫 선택 후보 목록

날짜: 2026-09-21. 상태: 첫 선택 범위의 관리 방식 결정. 실제 목록 미정. 기능 구현 미시작.
사용자 답변:

> 첫 선택 후보를 별도로 지정하고, 그 안에서는 원하는 진화 전 포켓몬을 자유롭게 선택

확인한 결정: 첫 선택 후보를 별도 목록으로 지정한다. 목록 안에서 원하는 진화 전 포켓몬을 자유롭게 선택한다.
목표와 범위: 첫 동반자 선택과 이후 수집 경로를 구분한다. 실제 후보 종과 화면 제작 및 구현은 포함하지 않는다.
SSOT: [일상 사용 계약](../../specs/s5.md#일상-사용-계약), [현재 설계](../../design.md#놀이공간과-직접-돌봄).
전체 종이나 일반 상점·알 목록을 첫 선택 후보로 자동 사용하지 않는다. 첫 선택에서 모든 전용 입수처를 우회할 수 있는 구조로 해석하지 않는다.
미정: 실제 후보 종, 진화하지 않는 종의 포함 여부, 후보별 이후 입수처, 최초 해금 상태, 첫 선택 취소·중간 종료 후 재개.
`[스펙 미확정]` 후보 목록은 종 데이터의 별도 자격으로 관리하는 안을 제안한다. 선택 가능한 시작 형태와 진화 정보를 함께 표시하는 안이다.
위험: 첫 선택 후보와 초기 알 결과가 맞물리지 않으면 첫 구매 튜토리얼의 결과 범위가 비거나 지나치게 좁을 수 있다. 두 목록의 초기 상태를 함께 검토해야 한다.
수용 조건: 첫 선택에서 지정 후보만 고를 수 있다. 후보 안에서는 원하는 개체의 시작 종을 직접 선택한다. 후보에 없는 종을 자동 포함하지 않는다. 실제 종 목록은 미정으로 남긴다.
다음 작업: 지금까지 정한 사용자 시나리오를 연결해 전환과 예외를 검토한다. 후보 종과 수치 설계는 별도로 유지한다.
의미 검수: 이번 절과 현재 설계·기능 계약·진행 현황을 검토했다. 후보 목록 방식과 실제 종 배정을 구분했다. 특정 종이나 전용 입수처 예외를 임의 채택하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 45개, 파일 링크 527개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 통합 시나리오 설계

날짜: 2026-09-21. 사용자 요청: “시나리오 설계”. 상태: 시나리오 초안 작성 완료. 사용자 검토 전. 기능 구현 미시작.
목표: 확정한 제품 경험을 첫 실행부터 일상 사용과 재실행까지 연결한다. 전환·취소·실패·중단에서 지킬 상태를 식별한다.
범위: 사용자 시나리오, 단계별 안내, 수용 조건, 화면 역할과 모듈 책임 초안. 가격·확률·종 목록의 확정과 Figma 편집·런타임 구현은 포함하지 않는다.
SSOT: 제품 결정은 [현재 설계](../../design.md)를 따른다. 기능 규칙은 [S5 계약](../../specs/s5.md)을 따른다. 명칭은 [명칭 사전](../../terms.md)을 따른다.
산출물: 반복 참조할 흐름 계약은 `docs/specs/s5-scenarios.md`에 둔다. 작업 근거는 이 기록에 유지한다. 과거 시나리오 제안과 최신 사용자 결정을 구분한다.
위험: 숨김과 보관 혼동, 부화 가능과 완료 혼동, 업적 달성과 수령 혼동, 재실행에 따른 보상 중복, 오래된 종별 소환 계약의 재사용.
수용 조건: 각 시나리오에 진입 조건·사용자 목적·정상 흐름·분기·종료 상태·확인 항목이 있다. 기존 미정 사항을 사용자 결정으로 바꾸지 않는다. 흐름 ID로 후속 와이어프레임과 검사를 연결할 수 있다.

작성 결과: [시나리오 계약](../../specs/s5-scenarios.md)에 SC-01부터 SC-11까지 연결했다. 정상 흐름과 수용 확인을 기록했다. 모듈 책임과 예외 처리는 필요한 부분을 제안으로 표시했다.
상태 정리: `progress.md`에서 대화 결정의 반복 나열을 제거했다. 해당 사실은 현재 설계와 기능 계약에 유지한다. 진행 현황은 시나리오 검토와 다음 산출물을 안내한다.
문서 충돌 검토: 기존 S5 인수 기준의 종 등록 기반 소환을 최신 개체 계약의 통과 기준으로 사용하지 않도록 적용 범위를 표시했다. 과거 Figma 검수 결과는 변경하지 않았다.
의미 검수: 시나리오 전체와 README·현재 설계·진행 현황·기능 계약의 연결 문장을 읽었다. 숨김과 보관, 준비와 부화, 달성과 수령을 구분했다. 거래 실패·중간 종료·버프 시간 보존의 새 제안은 사용자 승인으로 표시하지 않았다. 기존 결정과 구현 완료를 구분했다.
남은 설계 판단: Q-01부터 Q-08까지 기록했다. 초기 알 목록, 알 결과 확정 시점, 실패·재개 계약을 우선 검토한다. 열려 있는 제품 판단은 이번 문서 작성의 구현 누락으로 처리하지 않는다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 557개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 런타임 빌드와 기능 검사는 미실행이다. 설계 전용 단계에서 종료한다.

### 통합 시나리오 후속 검토

날짜: 2026-09-21. 사용자 요청: “검토 진행.” 기능 구현 미시작.
목표: SC-01부터 SC-11까지 사용자 결정과의 일치 및 분기 연결을 검토한다.
범위: 첫 구매 성립, 안내의 독립성, 표시 개체가 없는 상태, 제안의 확정 여부, 장기 돌봄 동기. 런타임과 Figma는 검토 범위 밖이다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`. 직접 발언은 이 기록의 앞선 결정 절을 따른다.
수용 조건: 발견 사항마다 근거와 발생 조건을 기록한다. 사용자 결정이 필요한 문제를 임의로 해결하지 않는다. 기존 Figma 문제와 새 시나리오 문제를 구분한다.
결과: [검토 기록](review.md#시나리오-설계-검토)에 열린 항목 5개와 밸런스 관측 1개를 기록했다. [피드백](feedback.md#시나리오-설계-후속-항목)에 후속 작업을 연결했다.
변경 범위: 검토 기록, 후속 목록, 시나리오 상태와 진행 현황이다. 게임 규칙과 시나리오 본문은 변경하지 않았다.
검사 결과와 의미 검수는 검토 기록에 둔다. 설계 단계만 진행했으므로 기능 구현과 화면 제작은 미시작이다.

### 초기 해금과 첫 알 구매

날짜: 2026-09-21. 사용자 답변: “제안대로 그렇게 진행.” 상태: 초기 구성 원칙 결정. 기능 구현 미시작.
승인 대상: 첫 선택 후보 전체를 초기 도감에 해금한다. 그중 알 입수가 허용된 종만 초기 알 후보로 둔다. 시작 포인트는 랜덤알 하나를 구매할 수 있는 금액으로 지급한다. 사용처는 자유롭게 유지한다.
목표: SR-01의 첫 알 구매 성립 조건을 보완한다. 범위는 초기 해금·후보 필터·구매 가능 금액 원칙이다. 실제 종 목록·가격·확률과 동일 종 업적 인정은 이번 승인에 포함하지 않는다.
SSOT: [제품 결정](../../design.md#놀이공간과-직접-돌봄), [입수처 계약](../../specs/s5.md#개체-레벨과-입수처), [시나리오](../../specs/s5-scenarios.md).
변경: SC-01·03과 Q-01에 결정 범위를 반영했다. SR-01은 일부 보완으로 남겼다. 앞선 검토 기록의 초기 구성 미정 판정은 이번 결정 이전 상태다.
위험: 후보 전체 해금을 모든 개체 지급으로 오해할 수 있다. 초기 알 필터가 비어 있을 수 있다. 초기 후보를 이후의 모든 알 결과 제한으로 오해할 수 있다.
수용 조건: 실제 종 목록에서 초기 알 후보가 하나 이상이다. 초기 지급액은 알 하나의 가격 이상이다. 해금만으로 개체를 지급하지 않는다. 종별 전용 입수처는 유지한다.
의미 검수: 변경한 제품 결정·계약·시나리오·진행 상태·피드백·이 기록을 읽었다. 도감 해금과 개체 지급을 구분했다. 확정한 구성 원칙과 미정인 데이터 및 업적 조건을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 580개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 동일 종의 첫 업적 인정

날짜: 2026-09-21. 사용자 답변: “인정하도록 진행.” 상태: 업적 조건 보완. 기능 구현 미시작.
질문 맥락: 파이리 두 마리를 꺼내도 첫 업적을 달성하도록 제안했다. 업적의 목적은 서로 다른 종 수집이 아니라 파티 표시 조작을 익히는 것이다.
확인한 결정: 같은 종의 서로 다른 두 개체도 첫 업적에서 인정한다. 화면에 두 개체를 실제로 꺼내야 한다. 보상은 기존 계약대로 업적창에서 직접 받는다.
목표와 범위: SR-01의 동일 종 결과 분기를 보완한다. 알 확률, 실제 종 목록, SR-02의 안내 시작 조건은 이번 승인에 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 파티 칸 확장 계약, `docs/specs/s5-scenarios.md`의 SC-05.
위험: 서로 다른 개체 수 대신 종 수나 표시 조작 횟수를 셀 수 있다.
수용 조건: 파이리 두 개체를 실제로 표시하면 달성한다. 한 개체의 반복 표시만으로 달성하지 않는다. 숨긴 두 개체의 파티 점유만으로 달성하지 않는다.
변경: 제안 상태였던 동일 종 인정을 확정으로 바꿨다. SC-05와 후속 상태를 갱신했다. SR-01의 실제 종 목록·가격 검증은 남아 있다.
의미 검수: 변경한 결정·계약·시나리오·진행 현황·피드백과 이 기록을 읽었다. 종과 개체를 구분했다. 달성과 보상 수령을 구분했다. 기존 검토 당시의 미정 기록은 과거 근거로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 582개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 파티와 업적 안내의 시작 조건

날짜: 2026-09-21. 상태: 시작 조건 정정. 기능 구현 미시작.
사용자 정정: 파티 안내는 처음 숨겨진 개체가 아니라 첫 포켓몬과 다른 개체를 처음 얻었을 때 시작한다. 같은 종이어도 개체 식별자로 구분한다. 업적 안내는 업적창 첫 진입이나 어느 업적이든 첫 조건 달성으로 시작한다.
목표와 범위: SR-02의 획득 경로 의존성을 제거한다. 파티 안내와 업적 안내의 시작 조건을 별도로 기록한다. UI 제작과 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 단계별 안내, `docs/specs/s5-scenarios.md`의 SC-05.
이유: 새로운 동반자 획득이 파티 이용을 설명할 계기다. 숨김은 개체의 표시 상태다. 업적 이용 안내를 두 마리 표시 업적에만 묶지 않는다.
확정 범위: 첫 포켓몬과 다른 개체를 처음 획득하면 파티 안내를 시작한다. 숨김과 획득 경로는 조건이 아니다. 업적창 첫 진입 또는 임의 업적 첫 달성 중 먼저 발생한 계기로 업적 안내를 시작한다.
식별자: 사용자가 언급한 `sid`는 개체 식별의 예시다. 실제 저장 필드명은 확정하지 않는다. 종 ID로 개체를 구분하지 않는다.
수용 조건: 동일 종의 새 개체 획득도 파티 안내를 시작한다. 알 구매만으로 시작하지 않는다. 기존 개체의 표시 변경·재배치·진화를 새로운 개체 획득으로 세지 않는다. 업적 미달성 상태에서 창을 열어도 안내를 시작한다. 안내만으로 보상이나 파티 칸을 지급하지 않는다.
위험과 미정: 새 개체가 이미 표시되거나 박스에 들어간 경우에는 안내 내용이 달라져야 한다. 파티 안내와 업적 안내가 동시에 시작될 수 있다. 대상 유지·중복 시작 방지·표시 순서·재개는 제안으로 남긴다.
변경: SC-05의 진입 조건과 분기를 수정했다. 알 구매 예시를 안내의 필수 순서로 사용하지 않게 했다. SR-02는 일부 보완으로 표시했다.
의미 검수: 변경한 결정·기능 계약·시나리오·진행 현황·피드백·이 기록을 읽었다. 획득과 표시 상태를 구분했다. 안내 시작과 달성·수령을 구분했다. 식별자 구현과 동시 안내 정책을 승인으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 584개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했으므로 빌드와 런타임 검사는 미실행이다.

### 안내 순서와 닫기 후 재개

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 상태: 안내 표시 원칙 결정. 기능 구현 미시작.
승인 대상: 진행 중인 안내를 마치거나 건너뛰면 다음 안내를 보여준다. 화면을 강제로 전환하지 않는다. 사용자가 닫으면 다음에 관련 화면을 열 때 이어서 안내한다.
목표와 범위: SR-02에서 안내가 겹칠 때의 처리와 닫기 후 재개를 보완한다. 실제 화면과 저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 단계별 안내, `docs/specs/s5-scenarios.md`의 SC-05·10.
조건 구분: 완료와 건너뛰기는 다음 안내로 진행하는 계기다. 닫기는 현재 안내를 중단하는 계기다. 다음 안내 때문에 사용자의 화면을 바꾸지 않는다.
미정: 진행 중인 안내가 없을 때 동시에 생긴 조건의 우선순위, 대기 안내의 표시 위치, 앱 종료를 포함한 저장 방식, 상태별 안내 내용. 기존 미정 사항을 이번 승인으로 확정하지 않는다.
위험: 닫기를 건너뛰기로 처리하면 다음 진입에서 안내를 잃는다. 대기 안내를 위해 자동으로 화면을 열면 강제 전환 금지와 충돌한다.
수용 조건: 파티 안내 도중 업적 조건이 충족되어도 진행 중인 안내를 덮지 않는다. 완료·건너뛰기 뒤 다음 안내를 보여준다. 닫은 안내는 관련 화면 재진입 시 이어간다. 이 과정에서 화면을 강제로 전환하지 않는다.
변경: 단계별 안내와 SC-05·10을 보완했다. Q-07에서 재개 동작과 저장 방식의 확정 범위를 구분했다. SR-02는 일부 보완 상태를 유지한다.
의미 검수: 변경한 결정·계약·시나리오·진행 상태·피드백·이 기록을 읽었다. 완료·건너뛰기·닫기의 결과를 구분했다. 화면 재진입과 앱 종료 후 저장 복원을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 586개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 표시 개체가 없을 때 복귀

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 상태: SR-03 설계 보완 완료. 기능 구현 미시작.
승인 대상: 트레이에서 관리 화면을 열고 파티로 이동한다. 모두 숨겼으면 원하는 개체를 다시 표시한다. 파티가 비었으면 박스에서 개체를 골라 배치한다.
목표와 범위: 표시 개체를 클릭하지 않고 게임 관리로 복귀하는 경로를 연결한다. 화면 제작과 런타임 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 파티 표시와 박스 보관, `docs/specs/s5-scenarios.md`의 SC-07.
기존 계약 연결: 트레이는 관리 화면의 마지막 탭을 연다. 사용자가 파티로 이동한다. 트레이 조작만으로 모든 개체를 꺼내는 동작으로 확대하지 않는다.
위험: 전원 숨김과 빈 파티를 같은 상태로 처리하면 적립 조건을 잘못 적용할 수 있다. 박스 배치 후 표시 상태는 이번 승인에서 정하지 않았다.
수용 조건: 전원 숨김과 전원 박스 보관 각각에서 트레이로 파티 화면에 접근한다. 숨긴 개체를 다시 표시할 수 있다. 빈 파티에는 박스의 보유 개체를 배치할 수 있다. 화면 진입만으로 개체를 자동 배치하지 않는다.
변경: SC-07에 복귀 절차와 확인 조건을 추가했다. SR-03은 설계 보완 완료로 변경했다. 이전 검토 기록은 수정 전 관측으로 유지한다.
의미 검수: 변경한 결정·계약·시나리오·진행 현황·피드백·이 기록을 읽었다. 숨김과 박스 및 수동 선택을 구분했다. 설계 보완 완료를 앱 구현 완료로 표시하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 588개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 특수 기믹의 선행 해금 제외

날짜: 2026-09-21. 사용자 답변: “선행해금형은 필요없어보여”. 기능 구현 미시작.
질문 맥락: 선행 해금 후 기믹 수행 방식과 기믹 수행으로 해금·획득하는 방식을 종별로 지정하는 안을 제안했다. 사용자는 선행 해금형을 제외했다.
목표와 범위: SR-04의 공통 선행 해금 조건을 정정한다. 특수 기믹 전용 종은 기믹을 수행하면 도감 해금과 개체 획득이 함께 일어나는 방식으로 설계한다. 구체적인 기믹·재획득·저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 개체 레벨과 입수처, `docs/specs/s5-scenarios.md`의 SC-09.
유지 조건: 발견 전에는 힌트만 표시한다. 발견 후에는 정확한 입수 방법을 공개한다. 특수 기믹 전용 종은 해금 후에도 일반 구매나 알에서 나오지 않는다.
위험: 모든 입수처에 도감 해금을 먼저 요구하면 특수 기믹 획득을 막는다. 선행 해금 제외를 모든 기믹 실행 조건의 삭제로 확대하지 않는다.
수용 조건: 미해금 특수 기믹 종도 해당 기믹을 수행해 해금과 획득을 진행할 수 있다. 구매·알·진화의 기존 입수 조건은 유지한다. 해금 후 전용 입수처를 우회하지 않는다.
미정: 실제 종과 기믹 내용, 힌트, 발견 연출과 저장 시점, 재획득 가능 여부. 이 항목은 이번 결정으로 확정하지 않는다.
변경: SC-09의 공통 선행 해금 조건을 입수처별로 분리했다. SR-04는 설계 보완 완료로 표시했다. 이전 검토는 수정 전 관측으로 유지한다.
의미 검수: 변경한 결정·계약·시나리오·진행 현황·피드백·이 기록을 읽었다. 선행 해금과 기믹 실행 조건을 구분했다. 해금과 일반 입수처 허용을 구분했다. 저장·반복 획득의 미정 범위를 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 590개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 종별 후속 입수처와 조건부 알 후보

날짜: 2026-09-21. 사용자 정정: 해금 후 상점 또는 알에서 얻는 종과 단일 포켓몬을 각각 지정한다. 알 전용 종도 도감 100종 이상 해금 후 무작위 획득 가능 같은 조건을 지정한다.
정정 이유: 직전 답변의 ‘기믹으로 얻은 종은 해금 후에도 상점이나 알에서 나오지 않는다’는 일반화가 사용자 의도보다 좁았다. 최초 획득 경로만으로 이후 경로를 고정하지 않는다.
목표와 범위: 종별로 해금 조건, 최초 획득, 이후 허용 입수처, 경로별 조건과 획득 제한을 구분한다. 실제 종 배정·수치·확률·구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 개체 레벨과 입수처, `docs/specs/s5-scenarios.md`의 SC-04·09.
확정 범위: 기믹으로 해금한 뒤 상점이나 알로 추가 획득할 수 있는 종을 지정할 수 있다. 계속 기믹으로만 얻는 종도 지정할 수 있다. 알 전용 종에는 도감 해금 수 같은 획득 가능 조건을 지정할 수 있다. 조건 충족은 알의 무작위 획득 기회를 열며 즉시 개체 지급을 뜻하지 않는다.
예시와 미정: 100종은 사용자 예시이며 확정 수치가 아니다. 도감 해금 수와 보유 개체 수를 혼동하지 않는다. ‘단일 포켓몬’이 저장별 총 획득 1회인지 동시 보유 1마리인지는 확인이 필요하다. 알 조건 달성과 대상 종 해금의 관계도 세부 설계에서 구분해야 한다.
위험: 최초 기믹 획득을 영구적인 알 제외 조건으로 쓰면 허용된 후속 획득을 막는다. 같은 종의 알 여러 개를 준비한 경우 단일 제한을 중복 지급할 수 있다. 결과 확정 시점과 제한 검사 시점은 별도 결정한다.
수용 조건: 해금 후 상점·알 허용 종과 불허 종을 각각 구분한다. 알의 경로 조건을 충족하지 않으면 후보에서 제외한다. 조건 충족만으로 개체를 지급하지 않는다. 단일 제한의 의미를 임의로 확정하지 않는다.
변경: 최초 획득과 이후 입수처를 구분했다. SC-04·09의 영구 제외 표현을 수정했다. 이전 선행 해금 제외 기록은 그 당시 판단으로 남기고 이번 정정을 우선한다.
의미 검수: 변경한 결정·계약·시나리오·진행 현황·피드백·이 기록을 읽었다. 기믹 최초 획득과 영구 전용 입수처를 구분했다. 조건 충족과 실제 지급을 구분했다. 단일 제한과 해금 시점의 미확정 범위를 명시했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 591개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 단일 포켓몬의 일회 획득

날짜: 2026-09-21. 사용자 답변: “딱 한 번만 획득 가능하게 할거야. 전설의포켓몬이나 환상의포켓몬이 그 예시가 되겠지.” 기능 구현 미시작.
질문 맥락: 해당 저장에서 총 획득 1회와 동시 보유 1마리를 비교했다. 사용자는 총 획득 1회를 선택했다.
목표와 범위: 단일 포켓몬의 제한 의미를 확정한다. 실제 적용 종, 알 결과 예약, 저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 개체 레벨과 입수처, `docs/specs/s5-scenarios.md`의 SC-09, `docs/terms.md`.
확정 범위: 단일 포켓몬으로 지정한 종은 해당 저장에서 딱 한 번만 획득할 수 있다. 현재 보유 수가 아니라 획득 이력을 기준으로 한다. 허용 입수처가 여러 개여도 총 획득 제한은 공유한다.
예시: 전설의 포켓몬과 환상의 포켓몬은 적용 예시다. 이 분류 전체에 자동 적용하는 것으로 확대하지 않는다. 실제 제한 여부는 종별로 지정한다.
위험: 현재 파티나 박스 수만 검사하면 일회 제한을 우회할 수 있다. 여러 알이나 서로 다른 획득 경로에서 같은 종을 동시에 지급할 수 있다.
수용 조건: 단일 종은 최초 획득 뒤 다른 입수처로도 다시 지급하지 않는다. 숨김·보관·재배치로 획득 이력을 초기화하지 않는다. 일반 종의 복수 개체 허용은 유지한다.
미정: 알 구매 시 결과를 미리 정하는 경우의 예약과 중복 방지, 실패·재시도 시 획득 이력 확정 시점, 실제 종 목록. 방생·교환 등 새로운 개체 제거 기능은 추가하지 않는다.
변경: 결정·입수 계약·SC-09·용어사전과 후속 상태를 갱신했다. 일반 종의 복수 개체 허용과 단일 종의 예외를 구분했다.
의미 검수: 변경한 문장을 읽었다. 총 획득 횟수와 현재 보유 수를 구분했다. 전설·환상을 적용 예시로 유지했다. 저장별 제한과 미정인 구현 방식을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 593개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 사용자 흐름 연결 재점검

날짜: 2026-09-21. 사용자 요청: “시나리오 플로우 다시 한 번 점검해봐. 놓친부분이 있을지.” 기능 구현 미시작.
목표: 후속 결정이 반영된 SC-01부터 SC-11까지 연결해 수집 중단, 중복 획득, 안내 중단과 일상 복귀의 누락을 확인한다.
범위: 현재 설계·S5 계약·시나리오와 대화 결정의 대조다. 종 목록이 없는 상태에서 조건부 반례를 사용한다. 실제 앱·Figma·성능은 검사하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`. 발견 근거는 기존 `review.md`에 추가하고 후속 상태는 `feedback.md`에 둔다.
위험: 실제 데이터가 없는 가상 사례를 발생한 결함으로 오해할 수 있다. 이미 기록한 미정 사항을 새 발견으로 중복 집계할 수 있다.
수용 조건: 확정한 정상 경로와 열린 경계 조건을 구분한다. 각 발견에 구체적인 사용자 상황과 보완 확인을 적는다. 검토만으로 새 게임 규칙을 승인하지 않는다.
결과: [재점검 기록](review.md#사용자-흐름-연결-재점검)에 경로 표와 SR-06부터 SR-10까지를 추가했다. 기존 SR-02의 중단 분기도 구체화했다. 현재 열린 항목은 8개다.
문구 정정: SC-05의 개요에서 안내만 본 경우와 보상을 받은 경우를 구분했다. SC-06에서 경험사탕 사용과 실제 레벨 상승을 구분했다. 신규 규칙은 채택하지 않았다.
의미 검수와 실행 검사 결과는 재점검 기록에 둔다. 설계 단계만 진행했으며 구현은 미시작이다.

### 조건 충족에 따른 해금과 입수처 활성화

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 기능 구현 미시작.
승인 대상: 조건 충족 후 해당 종을 도감에 해금한다. 허용된 입수처를 활성화한 뒤 사용자가 실제로 획득한다. 해금 수는 보유 여부와 관계없이 해금된 서로 다른 종의 수다.
목표와 범위: SR-06의 조건부 알과 도감 해금 순서를 보완한다. 실제 종 배정·조건 수치·연쇄 평가 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 입수처 계약, `docs/specs/s5-scenarios.md`의 SC-09, `docs/terms.md`.
집계 기준: 초기 해금한 첫 선택 후보도 포함한다. 레벨 조건으로 해금했지만 진화하지 않은 종도 포함한다. 같은 종의 개체를 여러 마리 얻어도 해금 수는 늘지 않는다.
예시: 도감 100종 해금이 알 전용 종 A의 조건이면 조건 충족 시 A를 해금하고 알 후보에 추가한다. 부화하면 실제로 A를 획득한다. 100종은 확정 수치가 아니다.
유지 범위: 특수 기믹의 최초 수행으로 해금과 획득이 함께 일어나는 계약은 유지한다. 이번 결정은 기믹 전에 별도 도감 해금을 요구하는 뜻이 아니다. 기존 구매 알의 후보 갱신은 결과 확정 시점과 함께 별도로 정한다.
위험: 보유 종 수와 해금 종 수를 혼동할 수 있다. 실제 조건표에 도달 불가능한 순환이 남을 수 있다.
수용 조건: 조건부 알 종은 최초 부화 이전에 해금된다. 초기 후보와 미진화 해금 종을 집계한다. 중복 획득은 종 수를 늘리지 않는다. 도감 해금만으로 개체를 지급하지 않는다.
미정: 실제 조건표의 도달 가능성, 여러 종의 연쇄 해금 평가 방식, 알 구매 후 새 해금의 적용 시점. SR-06은 원칙 보완 후 데이터 검증을 남긴다.
변경: 제품 결정·입수 계약·SC-09·용어사전과 후속 상태를 갱신했다. 해금 순서와 종 수 집계를 확정했다. SR-06은 일부 보완 상태다.
의미 검수: 변경한 문장을 읽었다. 도감 해금과 개체 획득을 구분했다. 초기 해금·미진화 종의 집계를 확인했다. 기믹 최초 획득의 기존 예외와 알 결과 시점의 미정 범위를 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 601개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 구매 후 해금 제외와 행동 부화 구상

날짜: 2026-09-21. 사용자 답변: “그건 막았으면 좋겠긴해. 대신 알에 특정한 행동을 해야 부화하는 그런 조건도 넣고싶긴해.” 기능 구현 미시작.
맥락: 개봉 시점 추첨이면 구매 후 새로 해금한 종이 기존 알에서 나올 수 있다고 설명했다. 사용자는 그 동작을 제외하고 행동을 요구하는 부화 조건을 구상했다.
목표와 범위: 기존 알의 결과 범위 확장 금지를 반영한다. 행동 부화의 적용 범위를 확인할 준비를 한다. 최종 추첨 시점·실제 행동·수치·구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 부화 계약, `docs/specs/s5-scenarios.md`의 SC-03·04와 Q-02·04.
확정 범위: 구매 후 새로 해금한 종은 이미 구매한 알에서 나오지 않는다. 구매 시 결과 확정과 후보 범위 확정은 다른 결정이다. 이번 답변만으로 최종 추첨 시점을 확정하지 않는다.
사용자 구상: 특정 행동을 수행해야 부화할 수 있는 조건을 추가하고 싶다. 모든 알에 적용할지 일부 알에만 적용할지는 미정이다. 시간 단축 행동과 필수 부화 행동은 구분한다.
권장안: `[스펙 미확정]` 구매 당시 후보 범위를 저장하고 직접 열 때 그 범위 안에서 결과를 정한다. 일반 알은 기존 시간 조건을 유지하고 일부 알에 별도 행동 조건을 두는 안이다. 행동 조건 충족 후에도 직접 열기 원칙을 유지한다.
위험: 필수 행동 조건을 모든 알에 적용하면 돌보지 않아도 최대 5분 뒤 준비 완료라는 기존 계약이 바뀐다. 단일 종을 다른 경로로 먼저 얻으면 저장한 후보가 줄거나 없어질 수 있다.
수용 조건: 구매 당시 미해금인 종은 해당 알의 결과가 되지 않는다. 미정인 행동 조건을 일반 알에 임의 적용하지 않는다. 구매 후보 범위와 결과 확정 시점을 구분한다.
변경과 의미 검수: 결정·부화 계약·SC-03·04·Q-02·04와 진행 현황을 갱신했다. 확정한 후보 확장 금지와 제안인 개봉 시 추첨을 구분했다. 행동 부화의 적용 범위를 승인으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 603개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 일반 알의 행동 조건과 결과 분기

날짜: 2026-09-21. 사용자 정정: 일반 알에 적용한다. 구매할 때 가능한 종 범위를 해당 알 sid에 지정한다. 특정 행동을 하면 해당 포켓몬이 부화하고, 조건을 충족하지 못하면 랜덤 범위에서 부화한다.
정정 이유: 별도 ‘특정 알’ 분류와 필수 행동을 해야만 부화하는 해석은 어시스턴트의 제안이었다. 사용자 의도는 일반 알에서 행동 조건으로 결과를 정하는 것이다. 앞선 제안은 채택하지 않는다.
목표와 범위: 알별 후보 범위와 행동 조건 충족·미충족 결과를 확정한다. 실제 행동·복수 조건 우선순위·저장 구현은 포함하지 않는다. 기능 구현 미시작.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 부화 계약, `docs/specs/s5-scenarios.md`의 SC-03·04와 Q-02·04.
확정 범위: 구매 시 해당 알의 개체 식별자(sid)에 가능한 종 범위를 연결한다. 행동 조건을 충족하면 그 조건에 대응한 포켓몬으로 부화한다. 미충족이면 저장한 범위에서 무작위로 부화한다. 조건 미충족 때문에 부화를 막지 않는다.
유지 조건: 구매 후 새 해금 종을 기존 알에 추가하지 않는다. 기존 준비 시간과 직접 열기 계약을 유지한다. 행동으로 결과를 정하는 것과 준비 시간을 단축하는 것은 구분한다.
위험: 결과 조건을 부화 가능 조건으로 구현하면 돌봄을 강제하게 된다. 여러 조건이 충족되거나 대응 종이 이미 획득한 단일 종이면 결과 규칙이 필요하다.
수용 조건: 알 A와 B의 범위를 개별 식별자로 구분한다. 조건 충족 시 대응 결과를 사용한다. 미충족 시 저장 범위에서 무작위 결과를 사용한다. 행동하지 않아도 준비 후 직접 부화할 수 있다.
미정: 실제 행동과 대상 종의 배정, 조건 인정 시점·지속 여부, 여러 조건 동시 충족, 단일 종 충돌·후보 소진, 최종 결과 저장 시점. 구매 당시 후보 범위 밖 종을 행동으로 허용하는 예외는 이번 결정에서 추가하지 않는다.
변경과 의미 검수: 결정·부화 계약·SC-03·04·Q-02·04와 진행 현황을 정정했다. ‘특정 알’은 채택하지 않은 어시스턴트 제안으로 과거 기록에만 남겼다. 일반 알의 결과 지정과 필수 부화 조건을 구분했다. 실제 코드 필드는 구현하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 604개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 복수 행동 조건의 무작위 결과와 발견 경험

날짜: 2026-09-21. 사용자 답변: “여러개 충족했으면 음... 난 그 조건포켓몬들 중 랜덤으로 하고싶은데, 최대한 모험을통해알아가게 하고싶어.” 기능 구현 미시작.
목표와 범위: 일반 알에서 여러 행동 조건을 충족한 경우의 결과를 정한다. 직접 시도하며 조건을 알아가는 경험을 설계 방향으로 기록한다. 던전 기능 추가와 실제 행동·확률·공개 화면 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 부화 계약, `docs/specs/s5-scenarios.md`의 SC-04·09와 Q-04.
확정 범위: 조건에 대응한 포켓몬이 여러 종이면 그 포켓몬들 중 무작위로 부화한다. 임의의 조건 우선순위로 하나를 고르지 않는다. 충족한 조건이 없으면 해당 알에 저장된 일반 범위에서 무작위로 부화한다.
경험 방향: 사용자가 행동을 시도하며 결과와 조건의 관계를 알아가게 한다. ‘모험’을 새로운 던전 구현 요청으로 확대하지 않는다.
미정: 동일 확률인지 가중치를 둘지, 여러 조건이 같은 종에 대응할 때의 처리, 조건 충족·후보 목록의 공개 정도, 힌트 방식, 발견 판정과 발견 후 기록. 기존 특수 기믹의 발견 후 방법 공개를 알 행동 조건 전체에 자동 적용하지 않는다.
위험: 충족 후보 목록이나 정답 조건을 처음부터 표시하면 발견 경험을 줄일 수 있다. 무작위 부화만으로 어떤 조건을 알아냈는지 확정하면 실제로 발견하지 않은 정보를 공개할 수 있다.
수용 조건: 두 종의 조건을 충족하면 결과는 그 두 종 중 하나다. 충족하지 않은 종을 일반 후보에서 섞지 않는다. 조건 미충족이면 기존 범위에서 무작위 부화한다. 구매 후 새 해금 종 제외와 단일 종 제한은 유지한다.
변경과 의미 검수: 제품 결정·부화 계약·SC-04·09·Q-04와 진행 현황을 갱신했다. 우선순위 제안을 무작위 결정으로 대체했다. 실제 확률과 공개 정책은 미정으로 유지했다. 기본 입수 조건과 발견할 행동 조건을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 606개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 발견 전 알의 반응과 힌트

날짜: 2026-09-21. 사용자 답변: “추천한 방식대로 진행.” 기능 구현 미시작.
승인 대상: 처음부터 정답 행동이나 충족한 포켓몬 목록을 보여주지 않는다. 알의 반응이나 작은 힌트를 통해 사용자가 탐색하도록 한다.
목표와 범위: 일반 알의 행동 조건을 발견하기 전 정보 공개 원칙을 정한다. 실제 반응·힌트 문구·발견 후 기록은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 부화 계약, `docs/specs/s5-scenarios.md`의 SC-04·09.
해석 범위: 직전 답변에서 명시적으로 추천한 반응·힌트 방식을 채택했다. 발견 후 정확한 조건 기록과 힌트만 유지하는 방식은 선택 질문으로 제시했으며 추천하지 않았다. 이번 답변만으로 그 선택을 확정하지 않는다.
위험: 반응이나 힌트가 충족한 종 목록을 그대로 공개하면 탐색 경험을 없앨 수 있다. 알의 기본 입수 조건까지 숨기면 상점 안내와 충돌한다.
수용 조건: 미발견 행동의 정답과 현재 충족한 종 목록을 직접 표시하지 않는다. 사용자는 알의 반응과 힌트를 통해 탐색할 수 있다. 기본 입수 조건 공개와 행동 조건의 발견을 구분한다.
미정: 실제 힌트와 반응, 발견 판정, 발견 후 정보 공개 범위. 부화 결과·확률·단일 종 제한은 이번 결정으로 바꾸지 않는다.
변경과 의미 검수: 제품 결정·부화 계약·SC-04·09와 진행 현황을 갱신했다. 발견 전 정보 공개와 발견 후 기록을 구분했다. 기본 입수 조건까지 숨기는 것으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 608개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 알 행동 조건의 발견 판정과 기록

날짜: 2026-09-21. 사용자 답변: “추천한대로 진행”. 기능 구현 미시작.
승인 대상: 행동 조건을 실제로 충족하고 그 조건의 포켓몬이 부화했을 때 해당 조건을 발견으로 기록한다. 조건 없이 일반 무작위 결과로 얻은 경우에는 발견하지 않는다. 발견한 조건은 도감에 정확히 기록한다.
목표와 범위: 일반 알의 행동 조건 발견 시점과 발견 후 공개 범위를 확정한다. 특수 기믹 전체의 발견 판정·실제 조건·화면·저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 부화 계약, `docs/specs/s5-scenarios.md`의 SC-04·09.
확인 예시: A와 B 조건을 충족하고 A가 부화하면 A 조건만 발견한다. B 조건은 그 부화로 발견 처리하지 않는다. 이전에 이미 발견한 B 기록을 지운다는 뜻은 아니다.
이유: 종 획득과 행동 조건 발견은 다르다. 결과로 확인한 조건을 기록하면 사용자가 다음 알에서 같은 방법을 시도할 수 있다.
위험: 종 획득만 검사하면 일반 무작위 부화로 정답을 공개한다. 충족한 조건 전체를 발견하면 부화하지 않은 종의 조건까지 공개한다.
수용 조건: 조건 충족과 대응 종 부화를 모두 확인한다. 발견한 조건은 도감에 정확히 표시한다. 미발견 조건은 기존 반응·힌트 원칙을 유지한다. 부화하지 않은 다른 종의 조건을 새로 공개하지 않는다.
미정: 같은 종에 여러 행동 조건이 대응하는 경우의 발견 단위, 부화 중 저장 실패·재실행 처리, 실제 도감 표현. 이번 결정은 단일 종의 재획득을 허용하지 않는다.
변경과 의미 검수: 제품 결정·부화 계약·SC-04·09·Q-04와 진행 현황을 갱신했다. 일반 무작위 부화와 조건에 따른 부화를 구분했다. 다른 종의 미발견 조건을 공개하지 않도록 확인했다. 실제 저장 검사는 실행하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 610개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 직접 열기 전까지 알 행동 누적

날짜: 2026-09-21. 사용자 답변: “그렇게 진행해”. 기능 구현 미시작.
승인 대상: 알 구매부터 직접 열기 직전까지 행동 조건을 쌓을 수 있다. 준비 시간이 끝났어도 열지 않고 계속 돌보며 조건을 탐색할 수 있다. 직접 열 때 그동안 충족한 조건으로 결과를 결정한다.
목표와 범위: 알의 행동 인정 기간과 부화 가능 이후 상호작용을 확정한다. 실제 조건·확률·거래 저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 부화 계약, `docs/specs/s5-scenarios.md`의 SC-04와 Q-02·04.
이유: 준비 시간은 직접 부화할 수 있는 시점을 정한다. 결과 탐색을 끝내는 시점은 사용자가 직접 열기를 선택할 때다.
수용 조건: 구매 후 행동을 알별로 누적한다. 준비 완료 후에도 행동 조건을 충족할 수 있다. 직접 열 때까지 충족한 조건 집합으로 결과를 결정한다. 구매 후 해금된 종을 기존 후보에 추가하지 않는다. 준비 완료 알은 직접 열기 전까지 돌보미집 칸을 차지한다.
위험: 시간이 0이 될 때 행동 입력을 막거나 결과를 확정하면 추가 탐색이 불가능하다. 열기와 행동이 동시에 들어오면 결과 판정 경계를 명확히 해야 한다.
미정: 동시에 들어온 행동·열기 처리 순서, 저장 실패·재시도, 실제 행동 조건표, 단일 종 충돌·후보 소진. 준비 완료 상태의 행동은 남은 시간을 음수로 만들지 않는 기존 제안을 유지한다.
변경과 의미 검수: 제품 결정·부화 계약·SC-04·Q-02·04와 진행 현황을 갱신했다. 준비 완료와 행동 누적 종료를 구분했다. 결과 결정 시점의 승인과 저장 구현의 미정을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 612개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 단일 포켓몬의 알 입수 제외

날짜: 2026-09-21. 사용자 답변: “단일포켓몬은 알에서는 못구하게 할거라서 신경쓰지마.” 기능 구현 미시작.
목표와 범위: 단일 포켓몬을 알 입수처에서 제외한다. 단일 종과 미개봉 알의 획득 충돌을 검토하던 SR-07을 적용 대상에서 제외한다. 다른 입수처의 종류나 구현은 이번 결정에 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 입수처·부화 계약, `docs/specs/s5-scenarios.md`, `docs/terms.md`.
확정 범위: 단일 포켓몬은 알에서 획득할 수 없다. 알 구매 시 후보 범위에 넣지 않는다. 행동 조건으로 결과를 정하는 경우에도 단일 종을 제공하지 않는다. 저장별 총 획득 1회 규칙은 다른 허용 입수처에서 유지한다.
정정: 단일 종을 알 결과로 예약하거나 다른 경로에서 먼저 획득해 알 후보가 소진되는 경우는 현재 설계 대상이 아니다. 앞선 기록은 이 입수 제한이 명확해지기 전의 조건부 검토로 유지한다.
수용 조건: 일반 무작위와 행동 조건 결과 모두 단일 종을 제외한다. 알용 단일 종 예약이나 대체 결과 정책을 추가하지 않는다. 일반 알의 저장 실패·재시도와 초기 후보 목록 검증은 별도로 유지한다.
위험: 단일 종 제외를 최초 기믹 획득 종 전체의 알 제외로 확대하면 종별 후속 입수처 결정과 충돌한다. 단일 여부를 기준으로 제외한다.
변경과 의미 검수: 입수처·부화 계약·SC-03·04·09·Q-02·용어사전과 후속 상태를 정정했다. SR-07을 해당 없음으로 변경했다. 이전 검토는 당시 전제의 기록으로 유지한다. 단일 종 제외와 일반 알 저장·재시도 문제를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 614개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 튜토리얼 닫기와 가이드북

날짜: 2026-09-21. 사용자 정정: 파티 안내를 스킵하거나 닫으면 다음부터 보여주지 않는다. 튜토리얼 다시보기를 제공하지 않고 가이드북으로 사용법을 제공한다. 기능 구현 미시작.
목표와 범위: 닫기 후 재개와 튜토리얼 다시보기 계약을 철회한다. 안내 종료와 가이드북의 역할을 정한다. 가이드북 화면·실제 콘텐츠·저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 단계별 안내, `docs/specs/s5-scenarios.md`의 SC-01·05·10, `docs/terms.md`.
확정 범위: 안내를 닫으면 해당 안내를 스킵한 것으로 처리한다. 스킵한 안내는 관련 화면 재진입이나 앱 재실행 때 다시 보여주지 않는다. 튜토리얼 다시보기 기능은 제공하지 않는다. 이후 사용법은 가이드북에서 확인한다.
적용 범위: 특정 안내를 스킵했다고 아직 시작하지 않은 다른 기능의 안내까지 모두 끈 것으로 해석하지 않는다. 개별 안내의 기존 시작 조건을 유지한다. 안내 닫기와 앱 종료·비정상 종료는 구분한다.
철회: 이전의 닫기 후 관련 화면에서 이어보기, 건너뛴 안내 다시보기, 다시보기 실습 보상 설계는 현재 계약에서 제거한다. 과거 결정 기록은 이번 정정을 연결해 보존한다.
위험: 가이드북을 열면서 게임 조작이나 시작 보상을 다시 실행하면 다시보기 제거 취지와 충돌한다. 가이드북에 미발견 행동의 정답을 공개하면 기존 발견 경험과 충돌한다.
수용 조건: 파티 안내를 닫은 뒤 다시 파티창을 열어도 안내가 나오지 않는다. 앱 재실행 후에도 스킵 상태를 유지한다. 다시보기 버튼을 제공하지 않는다. 가이드북 열람은 보상 지급이나 게임 행동을 실행하지 않는다. 가이드북도 기존 미발견 정보 공개 원칙을 따른다.
미정: 가이드북 진입 위치·구성·문구, 앱 종료나 비정상 종료로 끊긴 진행 중 안내의 처리, 동시 시작 안내 순서. 명시적 스킵 상태의 보존과 저장 구현은 구분한다.
변경과 의미 검수: 제품 결정·일상 사용 및 단계별 안내 계약·SC-01·05·10·Q-07·용어사전·진행 현황·피드백을 갱신했다. 닫기와 스킵을 동일하게 처리했다. 개별 안내 종료와 앱 종료를 구분했다. 기존 다시보기 보상 문구를 제거했다. 과거 기록의 재개 결정은 후속 정정 이전의 이력으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 614개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 현재 기준 문서에서 이전 다시보기 제공·닫기 후 자동 재개 표현이 남지 않았는지 검색했다. 설계 문서만 변경했으며 빌드와 런타임 검사는 미실행이다.

### PC 잠금과 절전의 시간 정지

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 기능 구현 미시작.
승인 대상: 관리 창만 닫거나 포켓몬을 숨기면 계속 진행한다. PC 잠금·절전·앱 종료 중에는 배고픔·친밀도·포인트·버프 시간·알 준비 시간을 멈춘다. 돌아오면 멈춘 상태에서 이어간다.
목표와 범위: SR-09의 실행 상태별 시간 정책을 확정한다. 플랫폼 이벤트 감지·저장 구현과 경제 수치 설계는 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 일상 사용 계약과 실행 상태별 시간, `docs/specs/s5-scenarios.md`의 SC-10·Q-05.
이유: 자리를 비운 동안 돌봄 부담을 늘리지 않는다. 화면 표시 상태와 실제 게임 시간 정지 상태를 구분한다.
수용 조건: 잠금·절전·종료 중에는 다섯 상태의 시간 진행을 멈춘다. 남은 버프·알 준비 시간과 적립 중인 진행량을 보존한다. 복귀 때 중단 기간을 소급 계산하지 않는다. 관리 창 닫기나 숨김은 파티 시간 진행을 멈추지 않는다. 박스 개체의 기존 정지 계약은 유지한다.
위험: 관리 창 닫기와 앱 종료를 혼동할 수 있다. 절전에서 복귀했어도 PC가 잠겨 있으면 여전히 정지해야 한다. 종료 중 버프만 소모하면 통일한 시간 정책과 충돌한다.
미정: 플랫폼 감지·저장 단위·실패 복원, 친밀도 변경 중 포인트 간격 재계산, 박스 이동 시 적립 부분 진행 처리. SR-09의 정책 보완과 나머지 시간 계약을 구분한다.
변경과 의미 검수: 실행 상태 표를 기능 계약에 추가했다. 결정·SC-10·Q-05·진행 현황·피드백을 갱신했다. PC 잠금과 저장 코드의 잠금을 구분했다. 기존 버프 시간 미확정 문구를 정정했다. 실행 상태의 시간 정지와 박스의 개체별 정지를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 616개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 만복도와 프리미엄먹이 버프 갱신

날짜: 2026-09-21. 사용자 답변: “갱신하는 방식. 거기다가 만복도가 이제 먹이랑 관련된 수치인데, 이게 다 차있으면 못먹게 말이야. 프리미엄먹이는 만복도를 다 채우는걸 전제로 생각하긴했어.” 기능 구현 미시작.
목표와 범위: 먹이 사용 조건과 같은 프리미엄먹이의 재사용을 정한다. 실제 수치·상품 종류·서로 다른 버프 간 처리·런타임 저장 변환은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`.
확정 범위: 같은 프리미엄먹이를 다시 주면 기존 잔여 시간에 더하지 않고 해당 먹이의 기본 지속시간으로 갱신한다. 효과 배율을 누적하는 결정은 아니다. 만복도가 최대이면 먹이를 줄 수 없다. 기본먹이와 프리미엄먹이에 동일하게 적용한다. 프리미엄먹이는 대상 개체의 만복도를 최대까지 채우는 기준으로 설계한다.
기존 규칙 연결: 버프는 먹이를 준 개체에게만 적용한다. 먹일 수 없는 상태에서 버프만 갱신하지 않는다. 이미 정한 숨김·박스·잠금·절전·종료의 시간 정책은 만복도에도 적용한다.
용어: 만복도는 높을수록 배부른 먹이 관련 수치다. 이전 문서의 배고픔 표현과 구분한다. 배고픔과 만복도를 독립적인 두 자원으로 추가하지 않는다. 기존 런타임 수치와 새 만복도의 변환식은 미정이다.
위험: 버프가 끝났지만 만복도가 최대인 경우에는 먹이로 즉시 재적용할 수 없다. 만복도가 최대보다 조금 낮으면 다시 먹일 수 있으므로 지속시간·감소 속도·가격을 함께 조정해야 한다.
수용 조건: 최대 만복도에서는 먹이를 소비하지 않고 사용을 거부한다. 버프도 갱신하지 않는다. 최대 미만에서 프리미엄먹이를 사용하면 만복도를 최대까지 채운다. 버프 잔여 시간은 기본 지속시간으로 갱신한다. 기존 잔여 시간은 더하지 않는다.
미정: 기본먹이 회복량·제공 방식, 만복도 상한·감소 속도, 디버프 구간과 해제 조건, 서로 다른 먹이 버프의 관계. 기존 재사용 제한에 추가 조건을 임의 도입하지 않는다.
변경과 의미 검수: 제품 결정·먹이 계약·SC-02·용어사전·진행 현황을 갱신했다. 시간 갱신과 연장을 구분했다. 만복도 최대 상태의 사용 차단과 버프만 갱신하는 동작을 구분했다. 기존 배고픔 구현 설명은 유지하고 새 수치의 저장 변환을 미정으로 남겼다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 618개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 기본먹이 일정량 회복과 밥 주기 쿨타임

날짜: 2026-09-21. 사용자 답변: 기본먹이 일정량 회복으로 진행하되 밥 주기 자체에 쿨타임을 둔다. 기능 구현 미시작.
이유: 기본먹이를 한 번에 연속 사용해 만복도를 가득 채우면 프리미엄먹이의 완전 회복과 구분이 약해진다.
목표와 범위: 기본먹이 회복 방식과 밥 주기 재사용 대기를 채택한다. 실제 회복량·대기시간·범위·저장 구현은 포함하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`.
확정 범위: 기본먹이는 만복도를 일정량 회복한다. 실제 양은 미정이다. 밥 주기 자체에 쿨타임을 적용한다. 만복도가 최대이거나 쿨타임 중이면 먹이를 줄 수 없다. 프리미엄먹이의 최대 회복과 버프 시간 갱신은 유지한다.
권장안: `[스펙 미확정]` 쿨타임은 개체별로 두고 기본먹이·프리미엄먹이가 공유한다. 한 개체에게 먹여도 다른 개체는 별도로 돌볼 수 있게 한다. 먹이 사용 성공 때만 쿨타임을 시작하는 안이다.
위험: 먹이별 타이머를 따로 두면 먹이를 바꿔 연속 사용 제한을 우회할 수 있다. 파티 공통 타이머이면 여러 개체를 한 번의 휴식에 돌보기 어렵다.
수용 조건: 기본먹이를 연속 사용해 한 번에 최대 회복하는 흐름을 제한한다. 최대 만복도에서는 쿨타임이 끝나도 먹일 수 없다. 사용 거부 시 먹이와 만복도·버프를 바꾸지 않는다.
미정: 개체별 적용과 먹이 간 공유의 확정, 쿨타임 길이, 잠금·절전·종료·박스 보관 중 쿨타임 처리, 실패·재시도와 기본먹이 제공 방식.
변경과 의미 검수: 제품 결정·먹이 계약·SC-02·용어사전·진행 현황을 갱신했다. 일정량 회복의 채택과 실제 수치의 미정을 구분했다. 쿨타임 추가와 개체별 공유 제안을 구분했다. 만복도 제한과 재사용 대기 조건을 함께 확인했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 620개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 개체별 먹이 공통 쿨타임

날짜: 2026-09-21. 사용자 답변: “너말대로 먹이 쿨타임은 개별개체별로 진행.” 앞선 개체별 적용과 기본먹이·프리미엄먹이 공유 제안을 채택했다. 기능 구현 미시작.
목표와 범위: 밥 주기 쿨타임의 적용 대상을 확정한다. 실제 대기시간과 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`. 다음 행동은 `docs/progress.md`에 기록한다.
확정 범위: 쿨타임은 개체별로 적용한다. 같은 개체의 기본먹이와 프리미엄먹이가 쿨타임을 공유한다. 다른 개체의 밥 주기는 막지 않는다.
위험과 수용 조건: 같은 종의 서로 다른 개체도 대기시간을 공유하지 않는다. 먹이 종류를 바꿔도 해당 개체의 쿨타임을 우회할 수 없다.
미정: 쿨타임 길이, 성공·실패 때의 타이머 처리, PC 잠금·절전·종료·박스 보관 중의 처리.
변경과 의미 검수: 제품 결정, 먹이 계약, SC-02, 용어사전, 진행 현황의 변경 문장을 읽었다. 개체별 공유의 확정과 타이머 처리의 미정을 구분했다. 같은 종의 다른 개체와 먹이 종류 변경의 결과를 확인했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 622개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 기본먹이만으로 점진적 회복

날짜: 2026-09-21. 사용자 답변: “추천한대로 진행”. 직전 제안인 기본먹이만으로 여러 번 돌봐 배고픔에서 서서히 벗어나는 방향을 채택했다. 기능 구현 미시작.
목표와 범위: 낮은 회복 효율과 점진적 회복 가능성을 함께 설계 조건으로 둔다. 실제 수치와 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02, `docs/progress.md`.
확정 범위: 기본먹이는 무료·무제한으로 제공한다. 회복량은 적게 유지한다. 쿨타임을 지키며 여러 번 먹이면 기본먹이만으로도 배고픔에서 서서히 회복할 수 있다. 프리미엄먹이의 최대 회복과 친밀도 버프는 유지한다.
위험과 수용 조건: 자연적인 만복도 감소를 함께 계산해도 반복 돌봄으로 배고픔 구간에서 벗어날 수 있어야 한다. 감소 속도만 늦추고 회복은 불가능한 수치 조합을 허용하지 않는다. 모든 사용 간격에서 회복을 보장하거나 정확히 쿨타임마다 돌봐야 한다고 확정한 것은 아니다.
미정: 실제 회복량, 쿨타임, 자연 감소 속도, 회복에 필요한 횟수와 돌봄 간격. 기본먹이만으로 최대 만복도까지 도달 가능한지는 별도로 정한다.
변경과 의미 검수: 제품 결정、일상 사용 계약、SC-02、진행 현황의 변경 문장을 검토했다. 점진적 회복 가능성과 실제 수치의 미정을 구분했다. 자연 감소를 포함한 회복 조건을 확인했다. 기능 구현은 미시작으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 628개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 빌드와 런타임 검사는 미실행이다.

### 기본먹이 무제한 제공과 낮은 회복 효율

날짜: 2026-09-21. 사용자 답변: “기본먹이는 무제한 제공. 대신 효율이 매우 낮게.” 직전 무료·무제한 제공 제안을 채택하고 효율 조건을 추가했다. 기능 구현 미시작.
목표와 범위: 기본먹이의 제공 방식과 회복 효율 방향을 확정한다. 수치 조정과 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`, `docs/progress.md`.
확정 범위: 기본먹이는 무료로 무제한 제공한다. 만복도는 일정량 회복한다. 만복도 회복 효율은 매우 낮게 설정한다. 기존 개체별 쿨타임과 최대 만복도 사용 제한은 유지한다.
범위 해석: 직전 논의의 만복도 회복 효율을 대상으로 기록한다. 친밀도 보상량까지 낮추기로 확정한 것은 아니다.
위험: 회복량과 쿨타임 사이의 만복도 감소량에 따라 기본먹이만으로 회복 가능한 범위가 달라진다.
수용 조건: 포인트와 먹이 재고가 없어도 기본먹이를 사용할 수 있다. 단, 최대 만복도와 쿨타임 조건은 충족해야 한다. 무제한 제공을 연속 사용 허용으로 처리하지 않는다.
미정: 실제 회복량, 쿨타임 길이, 만복도 감소 속도와의 관계, 기본먹이만으로 회복 가능한 범위.
변경과 의미 검수: 제품 결정, 먹이 계약, SC-02, 용어사전, 진행 현황의 변경 문장을 검토했다. 무제한 제공과 쿨타임 제한을 구분했다. 낮은 회복 효율의 채택과 실제 수치의 미정을 구분했다. 친밀도 보상에 대한 결정을 추가하지 않았다.
문서 검사: 첫 편집 명령은 입력 인코딩 오류로 변경 전에 실패했다. 패치로 문서를 반영한 뒤 `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 626개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 숨김 개체의 배고픔 상태 표시

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 숨김 개체의 배고픔은 파티창 상태 표시로만 확인하는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 표시 상태에 따른 배고픔 표현 경로를 구분한다. 다른 종류의 알림과 UI 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-02. 현황은 `docs/progress.md`와 기존 `feedback.md`의 SR-10에 반영한다.
확정 범위: 화면에 보이는 포켓몬은 배고픔 알림 구간에서 작은 말풍선이나 행동으로 표현한다. 숨겨진 포켓몬의 배고픔은 파티창 상태 표시로만 확인한다. 배고픔을 이유로 숨김을 해제하거나 창을 자동으로 열지 않는다. 숨김 상태의 만복도 감소는 기존 규칙대로 유지한다.
위험과 수용 조건: 숨김 상태에서도 배고픔이 변한다. 파티창을 직접 열면 현재 상태를 확인할 수 있어야 한다. 배고픔 때문에 포켓몬이나 창이 자동으로 나타나면 안 된다.
미정: 말풍선 반복과 동시 표현, 부화·진화·업적 알림, 파티창 상태 표시의 구체적인 모양. SR-10은 일부 보완으로 남긴다.
변경과 의미 검수: 제품 결정, 일상 사용 계약, SC-02, 진행 현황, SR-10의 변경 문장을 검토했다. 숨김 상태의 배고픔 표현과 시간 진행을 구분했다. 부화·진화·업적 알림으로 승인 범위를 확대하지 않았다.
문서 검사: 첫 편집 명령은 입력 인코딩 오류로 변경 전에 실패했다. 패치 적용 후 `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 631개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 기능 구현은 미시작이다. 빌드와 런타임 검사는 미실행이다.

### 배고픔 말풍선과 행동의 반복 정책

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 알림 구간 진입 시 말풍선 1회와 이후 간헐적인 행동 표현을 채택했다. 기능 구현 미시작.
목표와 범위: 보이는 포켓몬의 배고픔 표현 반복 방식을 정한다. 수치와 애니메이션 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 배고픔 상태 표시, `docs/specs/s5-scenarios.md`의 SC-02. 다음 행동과 SR-10은 기존 현황 문서에 기록한다.
확정 범위: 배고픔 알림 구간에 진입하면 말풍선을 한 번 표시한다. 같은 구간에 머무르면 말풍선을 반복하지 않는다. 이후에는 가끔 배고픈 행동을 한다. 더 심한 배고픔 구간에 진입하면 말풍선을 다시 한 번 표시한다. 숨김 개체의 상태 확인 방식은 유지한다.
위험과 수용 조건: 같은 구간에서 시간만 지나도 말풍선이 반복되면 안 된다. 더 심한 구간 진입은 새 알림으로 구분한다. 이 정책을 부화·진화·업적 알림에 자동 적용하지 않는다.
미정: 행동 간격, 말풍선 표시 시간, 여러 개체의 동시 표현. 회복 후 재진입과 숨김 해제·재실행 때의 알림 이력 처리는 별도 설계가 필요하다. 단계 수와 수치 경계는 확정하지 않았다.
변경과 의미 검수: 제품 결정, 배고픔 상태 표시 계약, SC-02, 진행 현황, SR-10을 검토했다. 구간 진입 때의 말풍선과 같은 구간의 간헐적인 행동을 구분했다. 숨김 정책과 미정 수치를 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 634개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 메뉴형 돌보미집과 부화 준비 완료 알림

날짜: 2026-09-21. 사용자 답변: “준비되는 순간에도 작은 알림을 한 번 받고 싶어. 왜냐하면 화면에 배치되는 그런게 아니라 메뉴로 구성할거라서.” 기능 구현 미시작.
목표와 범위: 돌보미집의 메뉴 구성과 부화 준비 완료 시점의 알림을 확정한다. 실제 화면과 알림 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알 계약, `docs/specs/s5-scenarios.md`의 SC-04, `docs/terms.md`. 진행 현황과 SR-10도 갱신한다.
확정 범위: 돌보미집은 메뉴로 구성한다. 알을 데스크톱 놀이공간에 배치하는 방식이 아니다. 알의 부화 준비가 완료되는 순간 작은 알림을 한 번 제공한다. 준비 완료와 직접 부화는 구분한다. 알림만으로 부화시키지 않는다.
정정 맥락: 기존 최대 6개 배치는 돌보미집 내부의 알 칸을 뜻한다. 메뉴 구성은 개별 타이머와 돌봄 정책을 바꾸지 않는다.
위험과 수용 조건: 돌보미집 메뉴를 열지 않은 상태에서도 준비 완료를 알아차릴 수 있어야 한다. 준비된 알은 사용자가 직접 열 때까지 유지한다. 알림이 결과 포켓몬을 먼저 공개하면 안 된다.
미정: 알림 위치와 구현 방식, 클릭 동작, 소리, 표시 시간, 여러 알의 동시 완료 처리. 메뉴 진입 버튼의 준비된 알 개수 표시는 앞선 제안으로 남긴다. 메뉴의 구체적인 위치도 미정이다.
변경과 의미 검수: 제품 결정, 알 계약, SC-04, 용어사전, 진행 현황, SR-10을 검토했다. 메뉴 내부 배치와 데스크톱 배치를 구분했다. 준비 완료 알림과 직접 부화를 구분했다. 알림의 위치와 동작을 사용자 승인으로 추정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 637개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 부화 준비 완료 배너와 클릭 이동

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 화면 한쪽의 작은 배너와 클릭 시 돌보미집 이동을 채택했다. 기능 구현 미시작.
목표와 범위: 부화 준비 완료 알림의 표현과 사용자 행동을 정한다. UI 구현과 알림 수치는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알 계약, `docs/specs/s5-scenarios.md`의 SC-04. 진행 현황과 SR-10도 갱신한다.
확정 범위: 준비 완료 순간 화면 한쪽에 작은 배너를 잠깐 표시한다. 배너를 클릭하면 돌보미집을 연다. 클릭하지 않으면 배너가 사라진다. 메뉴를 닫고 작업 중이어도 배너를 확인할 수 있다. 알은 직접 부화할 때까지 유지한다.
위험과 수용 조건: 배너가 사라져도 부화 기회를 잃지 않는다. 배너 클릭은 돌보미집을 여는 동작이다. 클릭만으로 알을 부화하거나 결과 포켓몬을 공개하지 않는다.
미정: 정확한 위치, 다중 화면 기준, 소리, 표시 시간, 동시 완료 처리, 준비된 알 개수 표시. 진화·업적 알림으로 이 결정을 확대하지 않는다.
변경과 의미 검수: 제품 결정, 알 계약, SC-04, 진행 현황, SR-10의 변경 문장을 검토했다. 배너 클릭과 직접 부화를 구분했다. 배너 소멸 후에도 준비된 알이 유지되는지 확인했다. 수치와 다른 알림의 정책은 미정으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 640개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 진화와 업적의 배너 알림

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 진화 가능·업적 달성 알림의 배너 통일과 클릭 이동을 채택했다. 기능 구현 미시작.
목표와 범위: 알림 표현과 이동 목적지를 정한다. 배너 구현과 진화·보상 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-05·SC-06. 진행 현황과 SR-10도 갱신한다.
확정 범위: 진화 가능과 업적 달성도 화면 한쪽의 작은 배너로 잠깐 알린다. 클릭하지 않으면 배너는 사라진다. 진화 배너를 클릭하면 해당 개체의 상세 화면으로 이동한다. 업적 배너를 클릭하면 해당 업적의 보상 수령 화면으로 이동한다. 진화와 보상 수령은 사용자가 별도로 직접 진행한다.
위험과 수용 조건: 배너 표시와 클릭만으로 진화하거나 보상을 지급하지 않는다. 배너가 사라진 것을 진화 취소나 업적 취소로 처리하지 않는다. 기존 튜토리얼 시작 조건과 보상 수령 조건은 유지한다.
미정: 여러 알림의 동시 발생 처리, 반복과 재실행 이력, 소리와 표시 시간, 이동 대상이 이미 처리된 경우의 동작. 해당 업적을 보여주는 구체적인 화면 구성은 미정이다.
변경과 의미 검수: 제품 결정, 업적·진화 계약, SC-05·SC-06, 진행 현황, SR-10을 검토했다. 알림 클릭의 이동과 실제 진화·보상 수령을 구분했다. 검토 중 발견한 배고픔 반복 정책의 미정 문구를 기존 확정 내용에 맞췄다. 튜토리얼 시작 조건과 보상 조건은 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 644개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 알림 배너 개별 표시 정정

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직후 “아니다. 개별로.”라고 정정했다. 같은 종류를 묶는 제안은 취소한다. 기능 구현 미시작.
목표와 범위: 부화·진화·업적 배너의 개별 표시를 확정한다. 알림 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`. 진행 현황과 SR-10도 갱신한다.
확정 범위: 알별 부화 준비 완료, 개체별 진화 가능, 업적별 달성을 각각 별도 배너로 표시한다. 같은 종류도 개수 요약으로 묶지 않는다. 한 번에 배너 하나씩 보여주는 직전 제안은 유지한다. 대기한 배너는 순차 표시한다.
클릭 동작: 부화 배너는 돌보미집을 연다. 진화 배너는 해당 개체 상세를 연다. 업적 배너는 해당 업적의 보상 수령 화면을 연다. 묶인 목록으로 이동하는 제안은 취소한다.
위험과 수용 조건: 알 3개가 동시에 준비되면 개별 배너 3개로 알린다. 같은 종류라는 이유로 알림을 합치거나 누락하지 않는다. 클릭만으로 부화·진화·보상 수령을 실행하지 않는다.
미정: 동시 발생 시 우선순위, 표시 중 새 알림 처리, 대기 중 이미 처리된 대상, 배너 표시 시간과 소리, 재실행 때 대기열 처리. 순차 표시 때문에 대기하는 알림은 완료 순간 화면에 모두 나타나는 것으로 해석하지 않는다.
변경과 의미 검수: 제품 결정, 배너 계약, SC-04, 진행 현황, SR-10을 검토했다. 직전 동의보다 후속 정정을 우선했다. 같은 종류의 개별 표시와 한 번에 하나씩 표시하는 정책을 구분했다. 기존 개별 배너의 이동 목적지는 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 648개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 기본 알림 소리와 OS 시스템 알림음

날짜: 2026-09-21. 사용자 최종 정정: “아니다. 기본소리 있고, 설정에서 무음할 수 있게. 대신 시스템알림소리로 진행. os별 기본 시스템알림소리있잖아.” 기능 구현 미시작.
목표와 범위: 부화·진화·업적 배너의 소리 기본값과 음원을 정한다. OS별 재생 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알림 배너 계약과 설정, `docs/specs/s5-scenarios.md`. 진행 현황과 SR-10도 갱신한다.
정정: 기본 무음 제안과 이에 대한 앞선 동의는 최종 정정으로 대체한다. 시스템 알림음과 비슷한 게임 효과음을 만드는 요청이 아니다.
확정 범위: 배너 알림 소리는 기본으로 켠다. 앱 설정에서 무음으로 바꿀 수 있다. 사용하는 OS의 기본 시스템 알림음을 사용한다. 무음 설정은 배너 표시와 클릭 이동을 끄지 않는다.
위험과 수용 조건: 자체 효과음으로 대체한 뒤 시스템 알림음이라고 표시하면 안 된다. 앱 무음에서는 알림음을 재생하지 않는다. 소리 선택만으로 배너 UI를 OS 기본 알림창으로 변경하기로 확정한 것은 아니다.
미정: OS별 재생 방법과 지원 확인, OS 무음·집중 모드와의 연동, 재생 실패 처리, 대기열의 소리 재생 시점. 배고픔 행동이나 다른 게임 효과음에는 이번 결정을 확대하지 않는다.
변경과 의미 검수: 제품 결정, 배너·설정 계약, SC-04, 진행 현황, SR-10을 검토했다. 최종 정정인 기본 소리 켜짐을 적용했다. OS 기본 시스템 알림음 사용과 유사 효과음 제작을 구분했다. 무음 전환과 배너 표시를 구분했다. OS별 구현 검증은 미실행으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 651개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 표시한 상태 알림의 반복 금지

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 이미 알린 부화·진화·업적 상태를 재알림하지 않는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 표시한 배너와 미처리 상태의 수명을 구분한다. 저장 구현과 미표시 대기열 처리는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알림 배너 계약, `docs/specs/s5-scenarios.md`의 SC-10. 진행 현황과 SR-10도 갱신한다.
확정 범위: 같은 상태를 한 번 배너로 표시했으면 처리하지 않아도 반복하지 않는다. 앱을 다시 켜도 반복하지 않는다. 아직 처리하지 않은 부화 가능·진화 가능·업적 미수령 상태는 해당 메뉴에 유지한다. 배너를 클릭하지 않은 경우에도 이 규칙을 적용한다.
위험과 수용 조건: 배너를 보았는지와 행동을 완료했는지는 별도 상태다. 재실행 후 이미 표시한 배너가 다시 나오지 않아야 한다. 해당 메뉴에서는 가능한 행동을 계속 확인할 수 있어야 한다. 다른 알·개체·업적의 새 알림까지 차단하지 않는다.
미정: 아직 표시하지 않은 대기 배너의 재실행 처리, 표시 중 종료와 저장 실패 처리. 같은 개체가 이후 진화 단계에 도달한 경우 등 새 사건의 식별은 상세 설계에서 정한다. 배고픔 말풍선에는 이 결정을 적용하지 않는다.
변경과 의미 검수: 제품 결정, 배너·진화 계약, SC-10, 진행 현황, SR-10을 검토했다. 표시한 배너와 미표시 대기를 구분했다. 알림 표시 이력과 사용자 행동의 완료 상태를 구분했다. 새 대상의 알림까지 차단하는 규칙을 추가하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 654개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 진화 시 개체 정보 유지

날짜: 2026-09-21. 사용자 답변: “맞지. 이어가야지”. 직전 제안인 진화 전후 개체 정보의 유지를 채택했다. 기능 구현 미시작.
목표와 범위: 진화를 같은 개체의 성장으로 정의하고 유지할 상태를 확정한다. 저장 이식과 진화 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 상세와 진화, `docs/specs/s5-scenarios.md`의 SC-06·Q-06, `docs/progress.md`.
확정 범위: 진화 후에도 개체 ID, 친밀도, 성격, 레벨, 누적 경험치를 유지한다. 만복도, 남은 버프 시간, 먹이 쿨타임도 유지한다. 파티·박스 위치와 숨김 상태를 유지한다. 완료한 진화를 되돌릴 수 없다는 기존 결정은 유지한다.
위험과 수용 조건: 배고픈 개체를 진화해도 만복도가 회복되지 않는다. 진화로 쿨타임이 초기화되거나 버프 시간이 갱신되지 않는다. 숨긴 개체가 진화했다는 이유로 표시되거나 박스 개체가 파티로 이동하지 않는다. 경험치와 친밀도를 초기화하지 않는다.
미정: 진화 연출 중 시간 처리, 이로치와 기존 하루 상한의 승계, 종별 경험치 곡선의 호환, 포인트 부분 진행의 세부 저장 계약. 단일 종 획득 이력은 이번 결정과 별개다.
변경과 의미 검수: 제품 결정, 경험치·진화 계약, SC-06·Q-06, 진행 현황을 검토했다. 확정한 상태 유지와 미정인 저장·연출을 구분했다. 이전의 승계 전체 미정 문구를 확정 범위에 맞췄다. 실제 진화 구현 완료로 표시하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 656개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 단계별 직접 진화

날짜: 2026-09-21. 사용자 답변: “그렇게 진행. 단계별로 해야지.” 직전 제안인 한 단계씩 직접 선택하는 진화 흐름을 채택했다. 기능 구현 미시작.
목표와 범위: 여러 진화 단계의 조건을 이미 충족한 개체의 진행 방식을 정한다. 종별 조건표와 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 상세와 진화, `docs/specs/s5-scenarios.md`의 SC-06, `docs/progress.md`.
확정 범위: 사용자가 한 번 진화를 선택하면 다음 한 단계로만 진화한다. 중간 단계를 건너뛰지 않는다. 진화 후 다음 단계 조건도 충족했다면 사용자가 다시 선택해 진화할 수 있다. 자동 연속 진화는 하지 않는다. 현재 단계에 머무를 수 있다.
수용 조건: 여러 단계 조건을 갖춘 파이리도 먼저 리저드로 진화한다. 리자몽으로 진화하려면 별도로 다시 선택한다. 첫 진화의 완료만으로 다음 진화를 실행하지 않는다. 각 단계에서도 기존 개체 정보 유지와 진화 철회 불가 원칙을 적용한다.
위험과 미정: 진화 완료 후의 중복 입력 처리와 다음 단계 알림의 식별은 구현 전에 정한다. 분기 진화에서 여러 조건을 충족한 경우의 선택 방식은 다음 질문으로 남긴다. 예시는 종별 레벨 수치를 확정하지 않는다.
변경과 의미 검수: 제품 결정, 진화 계약, SC-06, 진행 현황을 검토했다. 다음 진화가 가능하다는 조건과 사용자가 다시 선택해야 한다는 조건을 구분했다. 단계별 진행이 개체 상태 초기화를 뜻하지 않도록 확인했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 658개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 분기 진화 선택과 리전폼 조건

날짜: 2026-09-21. 사용자 답변: 분기 진화 후보 선택으로 진행하되 히스이 대검귀 같은 리전폼은 특정 조건에서 해당 폼으로, 일반 조건에서는 일반 대검귀로 진화해야 한다. 기능 구현 미시작.
목표와 범위: 선택 가능한 분기와 조건으로 정해지는 폼을 구분한다. 종별 실제 조건과 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 상세와 진화, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 선택 가능한 진화 분기의 조건을 둘 이상 충족하면 가능한 후보 중 사용자가 선택한다. 리전폼으로 진화하도록 지정한 조건을 충족하면 해당 진화 경로의 결과는 리전폼이다. 해당 조건이 없고 일반 진화 조건을 충족하면 일반 폼으로 진화한다. 같은 경로의 일반 폼과 리전폼을 임의로 고르는 선택지를 주지 않는다.
사용자 예시: 지정한 리전폼 조건에서는 히스이 대검귀가 결과다. 일반 조건에서는 일반 대검귀가 결과다. 원작의 진화 조건을 이 게임에 자동 적용하는 결정은 아니다.
위험과 수용 조건: 일반 조건도 충족했다는 이유로 리전폼 조건을 무시하고 일반 폼을 선택할 수 없어야 한다. 폼 조건은 결과를 정하며 진화를 자동 실행하지 않는다. 사용자는 진화를 보류할 수 있다. 단계별 진화와 개체 상태 유지 원칙은 유지한다.
미정: 구체적인 리전폼 조건, 조건을 판정하는 시점, 여러 폼 조건이 겹치는 경우, 폼별 도감 해금과 집계 기준. 특정 폼 조건이 다른 독립 진화 분기까지 제거한다고 일반화하지 않는다.
변경과 의미 검수: 제품 결정, 진화 계약, SC-06, 용어사전, 진행 현황을 검토했다. 분기 선택과 조건에 따른 폼 결정을 구분했다. 특정 조건을 원작 규칙으로 추정하지 않았다. 용어사전의 진화 설명도 앞서 확정한 개체 정보 유지에 맞췄다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 661개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 진화 실행 시 조건 판정과 결과 확인

날짜: 2026-09-21. 사용자 답변: “그렇게 하자.” 직전 제안인 실제 진화 시점의 조건 판정과 실행 전 결과 확인을 채택했다. 기능 구현 미시작.
목표와 범위: 조건 판정 시점과 결과 확인 흐름을 정한다. 종별 조건표와 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 상세와 진화, `docs/specs/s5-scenarios.md`의 SC-06, `docs/progress.md`.
확정 범위: 실제로 진화할 때의 조건으로 가능한 결과를 판단한다. 진화 가능 알림을 표시한 시점의 결과로 고정하지 않는다. 실행 전 결과 종과 폼을 보여주고 사용자가 확인한다. 조건에 따라 리전폼 또는 일반 폼으로 이어지는 기존 규칙을 적용한다.
위험과 수용 조건: 알림 시점과 실행 시점의 조건이 다르면 실행 시점의 조건을 사용한다. 과거에 받은 알림만으로 현재 불가능한 진화를 실행하지 않는다. 사용자는 실행 전에 실제 결과를 확인할 수 있어야 한다.
미정: 결과 확인 화면을 연 뒤 조건이 바뀌는 경우의 처리, 조건 판정과 저장의 원자성, 결과 확인 화면 구성. 실행 후 연출 중 조건 변화는 별도 시간 계약에서 정한다.
변경과 의미 검수: 제품 결정, 진화 계약, SC-06, 진행 현황을 검토했다. 알림 시점과 실행 시점을 구분했다. 결과 확인의 채택과 확인 중 조건 변화 처리의 미정을 구분했다. 확인 화면의 상세 동작을 임의로 확정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 663개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 리전폼의 독립 도감 항목과 집계

날짜: 2026-09-21. 사용자 답변: 같은 도감번호를 쓰더라도 일반 대검귀는 `503`, 히스이 대검귀는 `503-1`로 구분하고 도감에서 다른 종으로 인식하게 한다. 따로 집계하는 방향을 선택했다. 기능 구현 미시작.
목표와 범위: 리전폼을 독립 도감 항목과 해금 수 집계 단위로 정한다. 데이터 이식과 UI 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 도감·해금 계약, `docs/specs/s5-scenarios.md`의 SC-09, `docs/terms.md`, `docs/progress.md`.
정정: 일반 폼과 리전폼을 합쳐 1종으로 세자는 직전 제안은 채택하지 않는다. 기존 폼을 종 아래에 묶는 설계는 리전폼의 독립 항목 원칙으로 대체한다.
확정 범위: 기본 도감번호가 같아도 일반 폼과 리전폼은 별도 도감 항목이다. 사용자 예시의 항목 표기는 일반 대검귀 `503`, 히스이 대검귀 `503-1`이다. 각 항목의 해금 상태를 독립 관리한다. 둘 다 해금하면 도감 해금 수에 2종으로 센다. 같은 항목의 중복 개체는 추가 집계하지 않는다.
위험과 수용 조건: 일반 폼 해금만으로 리전폼도 해금된 것으로 처리하지 않는다. 기본 도감번호만으로 중복 제거해 두 항목을 합치지 않는다. 도감 100종 같은 조건에도 항목별 집계를 적용한다. 이는 실제 획득 개체 수와 다르다.
미정: 다른 종의 접미 번호 배정, 데이터 내부 키와 저장 변환, 각 리전폼의 해금 조건. 이로치와 리전폼 이외 형태의 집계는 이번 결정에 포함하지 않는다.
변경과 의미 검수: 제품 결정, 도감·진화 계약, SC-09, 용어사전, 진행 현황을 검토했다. 기본 도감번호와 독립 도감 항목을 구분했다. 중복 개체와 리전폼의 별도 집계를 구분했다. 기존 폼 묶음 문구를 정정했다. 다른 형태와 내부 저장 키를 임의로 확정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 665개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 이로치 기록과 알 및 약 획득 경로

날짜: 2026-09-21. 사용자 결정: 이로치는 같은 개체로 취급한다. 도감에 이로치 획득 여부를 추가한다. 알에서 낮은 확률로 얻거나 비싼 모습이 바뀌는 약을 사용한다. 돌아오는 약은 0원이며 일반색으로 되돌린다. 단일 종의 이로치는 약으로 얻는다. 기능 구현 미시작.
목표와 범위: 이로치의 도감 집계와 획득 경로, 색상 변경 상품을 확정한다. 실제 가격·확률과 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 이로치 계약과 상점, `docs/specs/s5-scenarios.md`, `docs/terms.md`, `docs/progress.md`.
확정 범위: 이로치는 별도 도감 종 수를 늘리지 않는다. 해당 도감 항목에 이로치 획득 여부를 기록한다. 약은 기존 개체의 색상을 변경하며 새 개체를 만들지 않는다. 알 부화의 낮은 확률과 상점 아이템 사용을 이로치 획득 경로로 둔다. 단일 종의 알 입수 제외는 유지한다.
상품: `모습이 바뀌는 약`은 가칭이다. 상점에서 비싸게 구매해 대상 개체를 이로치로 바꾼다. 실제 가격은 미정이다. `돌아오는 약`은 일반색으로 되돌리는 0원 상품이다. 최종 상품명과 아이템 소모 단위는 미정이다.
확률: 사용자 표현 `1/1000정도?`는 후보 수치다. 확정 확률로 사용하지 않는다. 알 결과 종 결정과 이로치 추첨의 저장 시점은 별도 설계가 필요하다.
추가 확인: 사용자는 “소환은 무조건 일반으로”의 범위를 묻는 질문에 “이로치 개체는 다시 꺼내도 유지”라고 답했다. 일반 개체의 최초 획득은 일반색이다. 알의 이로치 추첨과 약 사용은 예외 경로다. 이미 이로치인 개체는 숨김 해제·박스 복귀 후에도 이로치를 유지한다.
위험과 수용 조건: 이로치 약으로 단일 종의 새 개체를 추가 지급하지 않는다. 리전폼별 독립 도감 항목은 유지한다. 이로치 때문에 해금 종 수가 증가하지 않는다. 이로치 종 수와 개체의 현재 색상, 도감 획득 기록을 구분한다.
미정: 진화 시 이로치 유지, 일반색 복귀 후 도감 획득 기록 유지, 약의 대상 범위와 실패 처리, 알 추첨 시점·확률, 기존 이로치 권리 이식. 이 기록은 이전 색상 변경 기능 채택 미정 문구를 대체한다.
변경과 의미 검수: 제품 결정, 이로치·상점 계약, SC-07·SC-09, 용어사전, 진행 현황을 검토했다. 개체 색상과 도감 항목·획득 기록을 구분했다. 낮은 확률은 확정 방향으로, 1/1000은 후보로 기록했다. 추가 답변으로 재배치 후 이로치 유지 범위를 확정했다. 약의 효과와 사용 세부 조건을 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 669개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 진화 이로치 유지와 도감 획득 기록 보존

날짜: 2026-09-21. 사용자 답변: “그렇게 유지.” 직전 제안인 진화 시 이로치 유지와 일반색 복귀 후 도감 획득 기록 보존을 채택했다. 기능 구현 미시작.
목표와 범위: 개체의 현재 색상과 도감의 과거 획득 기록을 구분한다. 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 진화·이로치 계약, `docs/specs/s5-scenarios.md`의 SC-06·SC-09·Q-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 이로치 개체는 진화 후에도 이로치를 유지한다. 돌아오는 약으로 일반색으로 바꿔도 도감의 이로치 획득 기록은 남는다. 도감 기록은 현재 색상을 뜻하지 않는다. 기존 개체 정보 유지와 리전폼별 도감 항목 분리는 유지한다.
위험과 수용 조건: 진화로 이로치가 일반색으로 초기화되지 않는다. 일반색 복귀 시 해당 도감 항목의 이로치 획득 여부가 취소되지 않는다. 이 기록 때문에 해금 종 수가 추가 증가하지 않는다.
미정: 알 행동 조건과 이로치 추첨의 관계, 실제 확률과 추첨 시점, 약의 사용 세부 조건, 기존 구매 권리의 이식.
변경과 의미 검수: 제품 결정, 진화·이로치 계약, SC-06·SC-09·Q-06, 용어사전, 진행 현황을 검토했다. 현재 개체 색상과 도감 획득 이력을 구분했다. 이로치 승계 미정 문구를 확정 내용으로 바꿨다. 알 추첨과 아이템 세부 규칙은 미정으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 671개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 알 행동 조건과 이로치 추첨 분리

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 종 결정과 별도의 동일한 낮은 확률로 이로치를 추첨하는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 알 행동 조건이 종 결정과 이로치 판정에 미치는 영향을 구분한다. 수치와 추첨 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알·이로치 계약, `docs/specs/s5-scenarios.md`의 SC-04, `docs/progress.md`.
확정 범위: 행동 조건은 태어날 종에만 영향을 준다. 이로치 여부는 종 결정과 별도로 동일한 낮은 확률로 추첨한다. 행동 조건으로 결과 종이 정해진 알도 이로치가 될 수 있다. 조건을 더 충족하거나 돌봄을 반복해도 이로치 확률을 높이지 않는다. 기존 돌봄의 준비 시간 단축 효과는 유지한다.
위험과 수용 조건: 일반 무작위 종 결과와 행동 조건 종 결과에 같은 이로치 확률을 적용한다. 여러 행동 조건을 충족했다는 이유로 이로치 추첨 기회를 여러 번 주지 않는다. 이로치 여부가 허용된 종 후보 범위를 바꾸지 않는다.
미정: 실제 이로치 확률, 추첨 시점과 저장·재시도 처리, 종별 결과 확률. 사용자 예시 1/1000을 확정 수치로 적용하지 않는다.
변경과 의미 검수: 제품 결정, 이로치 계약, SC-04, 진행 현황을 검토했다. 종 결정과 이로치 판정을 구분했다. 행동 조건 충족으로 추첨 기회가 늘어나지 않는지 확인했다. 기존 준비 시간 단축 효과와 실제 확률 미정을 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 673개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 수집 난이도에 따른 일반 부화 가중치

날짜: 2026-09-21. 사용자 정정: “따로 지정하는데, 막 상세하게 수치로 하는게 아니라 수집난이도를 1~5로 해서 가중치로 하는 방식”. 기능 구현 미시작.
목표와 범위: 행동 조건을 충족하지 않은 일반 무작위 부화의 종별 추첨 기준을 정한다. 실제 가중치와 데이터 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알 계약, `docs/specs/s5-scenarios.md`의 SC-04, `docs/terms.md`, `docs/progress.md`.
확정 범위: 종마다 수집 난이도 1~5를 지정한다. 종별 상세 확률을 직접 입력하는 대신 난이도별 공통 가중치를 사용한다. 추첨 대상은 해당 알에 저장된 허용 후보 범위를 따른다. 난이도는 해금과 입수처 제한을 대신하지 않는다. 이로치 추첨은 기존의 별도 동일 확률을 유지한다.
표기 기준: 문서에서는 1을 얻기 쉬운 쪽, 5를 얻기 어려운 쪽으로 정리한다. 구체적인 가중치 비율과 실제 종의 난이도 배정은 미정이다.
위험과 수용 조건: 난이도 값 자체를 그대로 추첨 가중치로 사용해 어려운 종이 더 자주 나오게 하면 안 된다. 같은 난이도의 종은 같은 가중치 기준을 적용한다. 가중치가 있어도 허용 후보 밖의 종은 추첨하지 않는다.
미정: 단계별 실제 가중치, 후보 구성에 따른 정규화와 난이도별 총확률, 구매 후 가중치 변경의 적용 시점. 여러 행동 조건을 충족한 후보들에도 같은 가중치를 적용할지는 후속 질문으로 남긴다.
변경과 의미 검수: 제품 결정, 알 계약, SC-04, 용어사전, 진행 현황을 검토했다. 사용자 정정에 따라 종별 확률 입력과 난이도별 공통 가중치를 구분했다. 난이도 단계와 실제 가중치 수치를 구분했다. 이로치 확률과 허용 후보 범위는 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 675개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 복수 행동 조건 후보의 난이도 가중치

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 복수 행동 조건 후보에도 수집 난이도 가중치를 적용하는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 일반 무작위 부화와 복수 행동 조건 부화에 같은 난이도별 가중치 기준을 적용한다. 실제 비율과 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알 계약, `docs/specs/s5-scenarios.md`의 SC-04·Q-04, `docs/terms.md`, `docs/progress.md`.
확정 범위: 충족한 행동 조건에 대응하는 후보 종이 여러 개이면 그 후보들 사이에서 수집 난이도 1~5의 공통 가중치로 추첨한다. 후보가 한 종이면 그 종이 확정된다. 충족하지 않은 종은 일반 후보 범위에서 섞지 않는다. 이로치 여부는 별도의 동일 확률로 판정한다.
위험과 수용 조건: A·B 조건만 충족하면 A·B에 대해서만 가중치 추첨한다. C는 원래 알 범위에 있어도 포함하지 않는다. 후보가 A 하나이면 수집 난이도가 높아도 A가 부화한다.
미정: 실제 가중치, 같은 종에 여러 행동 조건이 대응할 때의 중복 처리와 발견 기록 단위, 추첨과 저장 구현.
변경과 의미 검수: 제품 결정, 알 계약, SC-04·Q-04, 용어사전, 진행 현황을 검토했다. 후보 범위 제한과 가중치 적용을 구분했다. 한 종 확정과 복수 종 추첨을 구분했다. 실제 가중치와 같은 종의 조건 중복 처리는 미정으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 677개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 알 행동 조건의 종별 단일 지정

날짜: 2026-09-21. 사용자 답변: “그런 조건들이 하나씩만 있을거라 이런 케이스는 없을거같아.” 같은 종을 서로 다른 행동 조건으로 후보에 넣는 예시를 설명한 뒤 받은 정정이다. 기능 구현 미시작.
목표와 범위: 알 행동 조건의 종별 중복 사례를 설계 대상에서 제외한다. 구체적인 행동과 데이터 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 알 계약, `docs/specs/s5-scenarios.md`의 SC-04·Q-04, `docs/progress.md`.
확정 범위: 알 행동 조건을 지정하는 종에는 조건을 하나만 둔다. 같은 종에 여러 대체 행동 조건을 두지 않는다. 따라서 동일 종의 조건 중복에 따른 가중치 합산과 발견 단위 문제는 해당하지 않는다.
유지 범위: 서로 다른 종의 조건은 한 알에서 여러 개 충족할 수 있다. 이 경우 충족한 종들 사이에서 수집 난이도 가중치로 추첨한다. 후보가 한 종이면 확정한다. 조건을 충족하고 해당 종이 부화했을 때 조건 발견으로 기록하는 원칙은 유지한다.
위험과 수용 조건: 같은 종에 여러 조건을 배정한 데이터를 정상 사례로 취급하지 않는다. 종별 조건 하나라는 결정을 알 전체가 조건 하나만 충족할 수 있다는 제한으로 확대하지 않는다. 모든 알 후보 종에 행동 조건이 반드시 있다는 결정으로도 확대하지 않는다.
미정: 실제 행동 조건표, 행동 횟수·조합 등 한 조건의 구성, 가중치 수치, 저장·재시도 처리.
변경과 의미 검수: 제품 결정, 알 계약, SC-04·Q-04, 진행 현황을 검토했다. 같은 종의 복수 조건 미정 문구를 제거했다. 종별 조건 수와 한 알이 충족할 수 있는 서로 다른 종의 조건 수를 구분했다. 사용자에게 제시했던 중복 예시를 실제 요구사항으로 남기지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 679개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 새 개체 획득 시 숨김 배치

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 첫 포켓몬 이후 모든 획득 경로의 숨김 배치를 채택했다. 기능 구현 미시작.
목표와 범위: 새 개체의 초기 표시 상태를 통일한다. 첫 포켓몬과 기존 보유 개체의 박스 복귀 동작은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 개체 획득과 파티 교체, `docs/specs/s5-scenarios.md`의 SC-04·SC-05·SC-09, `docs/progress.md`.
확정 범위: 첫 포켓몬 이후 구매·부화·특수 획득으로 새 개체를 얻으면 사용 가능한 빈 파티 칸에 숨김 상태로 배치한다. 파티가 가득 차면 박스로 보낸다. 숨긴 파티 개체는 사용자가 직접 표시한다. 같은 종의 새 개체에도 적용한다.
위험과 수용 조건: 파티 배치와 화면 표시를 구분한다. 새 개체 획득만으로 화면에 포켓몬을 추가 표시하지 않는다. 숨긴 파티 개체도 기존 시간 규칙에 따라 만복도·성장·적립이 진행된다. 획득만으로 두 마리 실제 표시 업적을 달성하지 않는다.
유지 범위: 첫 포켓몬의 첫 만남은 기존 흐름을 따른다. 진화는 새 개체 획득이 아니며 표시 상태를 유지한다. 부화 결과를 보여주는 연출과 놀이공간의 상시 표시는 구분한다.
미정: 박스 개체를 빈 칸에 배치하거나 맞바꿀 때의 표시 상태, 빈 칸이 여러 개일 때의 배치 순서, 실제 UI와 저장 구현.
변경과 의미 검수: 제품 결정, 획득·파티 계약, SC-04·SC-05·SC-09, 진행 현황을 검토했다. 새 획득과 기존 개체의 복귀를 구분했다. 첫 포켓몬과 진화의 기존 표시 규칙을 유지했다. 부화 연출과 놀이공간의 상시 표시를 구분했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 681개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 박스 복귀와 교체 후 숨김 유지

날짜: 2026-09-21. 사용자 답변: “그래도 볼에 있는 포켓몬은 꺼낸다 라는 동작은 유지하게 하고싶어. 숨김처리.” 박스에서 파티로 직접 배치할 때 바로 표시하자는 제안을 거절했다. 기능 구현 미시작.
목표와 범위: 박스 복귀와 맞바꾸기의 표시 상태를 정한다. 배치 UI와 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 개체 획득과 파티 교체, `docs/specs/s5-scenarios.md`의 SC-07, `docs/terms.md`, `docs/progress.md`.
확정 범위: 박스 개체를 빈 파티 칸에 넣으면 숨김 상태로 배치한다. 맞바꾸기로 들어온 박스 개체도 숨김 상태다. 사용자가 별도로 꺼내기를 해야 화면에 나타난다. 교체 전 파티 칸의 표시 상태를 새로 들어온 개체에 적용하지 않는다.
위험과 수용 조건: 보이는 파티 개체와 박스 개체를 맞바꿔도 들어온 개체는 자동 표시되지 않는다. 빈 파티에서 복귀할 때도 배치 후 꺼내기를 별도로 한다. 파티 배치 순간부터는 숨김 파티의 기존 시간·적립 규칙을 적용한다. 색상과 성장 기록은 이번 표시 정책으로 초기화하지 않는다.
유지 범위: 첫 포켓몬의 첫 만남과 기존 파티 개체의 진화는 이번 결정 대상이 아니다. 새 획득 개체의 숨김 배치 원칙은 유지한다.
미정: 꺼내기 버튼의 최종 문구와 조작 위치, 빈 칸 선택 순서, 포인트 부분 진행 보존, 교체 저장과 실패 처리.
변경과 의미 검수: 제품 결정, 파티 계약, SC-07, 용어사전, 진행 현황을 검토했다. 파티 배치와 꺼내기를 구분했다. 기존 즉시 표시 제안을 사용자 정정으로 대체했다. 첫 포켓몬과 진화의 표시 정책은 확대 변경하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 683개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 박스 보관 시 포인트 부분 진행 보존

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 박스 보관 전 포인트 적립 진행도를 보존하고 복귀 후 이어가는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 박스 이동 시 포인트 부분 진행의 수명을 정한다. 친밀도 변경 중 간격 재계산과 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 포인트·파티 계약, `docs/specs/s5-scenarios.md`의 SC-07·Q-05. 진행 현황과 SR-09도 갱신한다.
확정 범위: 박스 보관 시 해당 개체의 포인트 부분 진행을 보존한다. 박스 안에서는 진행하지 않는다. 파티 복귀 후 보존한 지점부터 이어간다. 숨김 상태로 복귀해도 적립은 재개한다. 맞바꾸기에서도 진행은 각 개체에 귀속한다.
수용 조건: 친밀도와 적립 간격이 같다면 1포인트까지 30초 남은 개체를 박스에 넣었다 파티로 복귀시켜도 30초가 남는다. 보관·복귀로 진행을 초기화하거나 보관 시간을 적립하지 않는다. 한 개체의 진행을 파티 칸의 다음 개체에 넘기지 않는다. 30초는 설명용 예시다.
미정: 친밀도 변경 중 간격 재계산, 부분 진행 저장 단위와 실패 복원, 시간 경계에서 이동과 지급의 처리 순서. SR-09는 일부 보완으로 유지한다.
변경과 의미 검수: 제품 결정, 포인트·파티 계약, SC-07·Q-05, 진행 현황, SR-09를 검토했다. 박스 정지와 숨김 파티의 진행을 구분했다. 개체별 진행 보존을 확인했다. 남은 시간 예시는 적립 간격이 같은 경우로 한정했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 686개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 친밀도 변화 시 포인트 진행 비율 유지

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 진행 비율 유지와 남은 부분의 새 적립 속도 적용을 채택했다. 기능 구현 미시작.
목표와 범위: 친밀도가 올라 적립 간격이 짧아질 때 진행을 재계산하는 규칙을 정한다. 실제 속도 공식과 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 포인트 계약, `docs/specs/s5-scenarios.md`의 SC-02·Q-05. 진행 현황과 SR-09도 갱신한다.
확정 범위: 친밀도가 올라 적립 간격이 바뀌면 이미 쌓인 포인트 진행 비율을 유지한다. 남은 부분부터 새 적립 속도로 진행한다. 진행을 초기화하거나 이전 경과 시간 전체에 새 속도를 소급 적용하지 않는다.
수용 조건: 50% 진행 상태에서 적립 간격이 100초에서 80초로 바뀌면 진행은 50%이고 남은 시간은 40초다. 친밀도가 다시 바뀌기 전을 기준으로 한 설명용 예시다. 실제 간격 수치를 확정한 것은 아니다. 박스·잠금·절전·종료의 진행 정지는 유지한다.
미정: 친밀도별 적립 간격 공식, 진행 비율 저장 정밀도와 반올림, 지급과 상태 변경이 같은 시점인 경우의 처리, 저장 실패 복원. SR-09의 사용자 시간 정책은 보완했고 구현 계약은 남긴다.
변경과 의미 검수: 제품 결정, 포인트 계약, SC-02·Q-05, 진행 현황, SR-09를 검토했다. 진행 비율 유지와 과거 시간의 소급 재계산을 구분했다. 예시 수치를 실제 밸런스 값으로 확정하지 않았다. 박스와 앱 정지 상태의 기존 규칙을 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 689개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 경험사탕의 파티와 박스 대상 허용

날짜: 2026-09-21. 사용자 답변: “진행.” 직전 제안인 경험사탕을 파티와 박스의 보유 개체 모두에게 직접 사용하는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 경험사탕의 사용 대상 범위를 확정한다. 다른 도구의 대상 범위와 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 경험사탕 계약, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 파티와 박스의 보유 개체 모두 경험사탕 대상으로 선택할 수 있다. 숨김 상태인 파티 개체에도 사용할 수 있다. 박스에서 파티로 옮기거나 화면에 표시할 필요가 없다. 박스의 시간에 따른 성장 정지는 유지한다. 사용자가 직접 주는 경험사탕으로는 박스 개체도 경험치를 얻고 레벨을 올릴 수 있다.
위험과 수용 조건: 파티가 비어 있어도 박스에 대상 개체가 있으면 경험사탕을 사용할 수 있다. 사용 때문에 대상의 파티·박스 위치나 숨김 상태를 바꾸지 않는다. 레벨 상승만으로 자동 진화하지 않는다. 기존 소환한 개체만 도구 대상으로 삼는 제안을 경험사탕에 적용하지 않는다.
미정: 사탕 종류·가격·효과량, 사용 수량과 결과 미리보기, 구체적인 화면, 최대 레벨 처리와 저장 실패 복원. 다른 약과 먹이의 박스 사용을 이번 결정으로 확대하지 않는다.
변경과 의미 검수: 제품 결정, 경험사탕·도구 계약, SC-06, 용어사전, 진행 현황을 검토했다. 박스의 시간에 따른 성장 정지와 직접 사탕 사용을 구분했다. 대상 범위 확정과 사용 화면 제안을 구분했다. 기존 파티 전용 도구 제안이 경험사탕과 충돌하지 않도록 수정했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 691개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 경험사탕 수량 선택과 결과 미리보기

날짜: 2026-09-21. 사용자 답변: “그렇게 진행”. 직전 제안인 대상 한 개체와 사용 수량 선택, 획득 경험치와 예상 레벨 확인 후 사용을 채택했다. 기능 구현 미시작.
목표와 범위: 경험사탕의 여러 개 사용과 결과 확인 흐름을 정한다. 화면 배치와 실제 수치·구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 경험사탕 계약, `docs/specs/s5-scenarios.md`의 SC-06, `docs/progress.md`.
확정 범위: 한 개체를 선택한 뒤 사용할 경험사탕 개수를 지정한다. 여러 개를 한 번에 사용할 수 있다. 사용 전에 획득 경험치와 예상 레벨을 보여준다. 사용자가 확인한 뒤 사용한다. 파티·박스와 숨김 여부에 따른 기존 대상 범위는 유지한다.
위험과 수용 조건: 미리보기만으로 아이템이나 경험치를 바꾸지 않는다. 선택한 수량의 결과를 한 대상에 적용한다. 예상 레벨 표시를 자동 진화 예고로 취급하지 않는다. 레벨 상승 후 진화는 기존 단계별 직접 선택 규칙을 따른다.
미정: 최대 레벨, 상한을 넘는 수량과 초과 경험치 처리, 종류별 효과량과 가격, 수량 조작 UI, 미리보기 이후 상태 변경과 저장 실패 처리. 가방 진입점과 최종 화면 구성은 아직 제안이다.
변경과 의미 검수: 제품 결정, 경험사탕 계약, SC-06, 진행 현황을 검토했다. 대상·수량·결과 확인의 확정과 화면 구성의 미정을 구분했다. 미리보기와 실제 사용을 구분했다. 여러 개 사용을 여러 대상 일괄 사용으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 693개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 개체 레벨 상한 100

날짜: 2026-09-21. 사용자 답변: “100으로 제한으로 진행”. 직전 제안의 개체 최대 레벨 100을 채택했다. 기능 구현 미시작.
목표와 범위: 개체 레벨 상한을 확정한다. 경험치 곡선과 사탕의 상한 처리 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 경험치 계약, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 개체의 최대 레벨은 100이다. 레벨 100에 도달해도 친밀도 성장과 포인트 적립은 기존 규칙대로 계속한다. 레벨 상한을 친밀도 상한이나 적립 종료 조건으로 적용하지 않는다.
위험과 수용 조건: 레벨 101 이상으로 성장하지 않는다. 레벨 100의 숨김 파티 개체도 기존 파티 시간 규칙을 따른다. 박스·잠금·절전·종료의 정지 규칙은 그대로 유지한다.
미정: 레벨별 필요 경험치, 100레벨에서 경험사탕 사용 차단, 상한 도달에 필요한 수량 제한과 마지막 사탕의 초과 경험치 처리, UI와 저장 구현.
변경과 의미 검수: 제품 결정, 경험치 계약, SC-06, 용어사전, 진행 현황을 검토했다. 레벨 상한과 친밀도·포인트의 지속을 구분했다. 기존 시간 정지 규칙을 유지했다. 레벨 상한 확정을 사탕 낭비 처리의 승인으로 확대하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 695개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 경험사탕 상한 사용과 초과 경험치

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 100레벨 사용 차단, 필요 수량 제한, 마지막 사탕의 초과 경험치 소멸과 사전 표시를 채택했다. 기능 구현 미시작.
목표와 범위: 최대 레벨에서 경험사탕을 낭비하는 사용을 제한한다. 실제 효과량과 UI·저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 경험사탕 계약, `docs/specs/s5-scenarios.md`의 SC-06, `docs/progress.md`.
확정 범위: 이미 100레벨인 개체에는 경험사탕을 사용할 수 없다. 그 전에는 선택한 사탕으로 100레벨에 도달하는 데 필요한 최소 개수까지만 선택할 수 있다. 마지막 사탕에서 상한을 넘는 경험치는 소멸한다. 사용 전에 초과하여 소멸할 경험치 양을 표시한다.
수용 조건: 50 경험치가 남고 사탕 하나가 100 경험치를 주는 예시에서는 한 개를 사용할 수 있다. 사용 전 실제 적용 50과 초과 50을 구분한다. 사용 후 100레벨이 되고 초과 경험치는 보관하지 않는다. 두 개 이상은 선택할 수 없다. 이미 100레벨이면 사탕을 소비하지 않는다. 예시의 효과량은 실제 수치가 아니다.
위험과 미정: 재고 제한, 사용 직전 상태 변화와 미리보기 재계산, 저장 실패 복원은 구현 계약에서 정한다. 경험사탕 이외의 향후 경험치 경로로 이번 초과 처리 규칙을 자동 확대하지 않는다.
변경과 의미 검수: 제품 결정, 경험사탕 계약, SC-06, 진행 현황을 검토했다. 필요한 마지막 한 개의 초과분과 불필요한 추가 사탕 사용을 구분했다. 미리보기의 실제 적용량과 소멸량을 구분했다. 설명용 수치를 실제 상품 효과로 확정하지 않았다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 697개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 경험사탕 5종과 이상한사탕 및 이미지 조사

날짜: 2026-09-21. 사용자 결정: 경험사탕은 XS·S·M·L·XL 기본 5종으로 구성한다. 레벨을 올리는 이상한사탕도 추가한다. 이미지를 수집해 참고하고 실제 포켓몬 도구의 공개 자료를 조사한다. 기능 구현 미시작.
목표와 범위: 사탕 구성을 확정하고 공개 데이터·이미지의 출처를 확인한다. 게임 코드와 상점 UI 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 경험사탕 계약, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`. 수집 자료는 기존 작업의 `evidence/candy-references/`에 보관한다.
확정 범위: 경험사탕 XS·S·M·L·XL 5종과 이상한사탕을 사용한다. 이상한사탕은 고정 경험치량 대신 사용한 개체의 레벨을 1 올리는 도구다. 개체 레벨 상한 100은 유지한다. 앞선 소·중·대 3종 제안은 채택하지 않는다.
위험과 수용 조건: 원작 자료의 효과량·가격을 제품 확정 값으로 자동 복사하지 않는다. API 코드 라이선스와 원작 이미지의 권리를 구분한다. 수집한 파일에 URL과 식별자를 남긴다. 이미지 참고와 앱에 실제 탑재한 상태를 구분한다.
`[스펙 미확정]` 경험사탕 5종의 실제 효과량과 가격, 이상한사탕의 가격·입수처·대상 범위·수량 사용 UI, 사용 후 레벨 내 경험치 진행 처리. 100레벨에서 이상한사탕 사용 차단의 세부 처리는 후속 설계한다.
조사 결과: [사탕 참고 이미지](evidence/candy-references/README.md)에 원본 PNG 6개와 출처를 보관했다. PokéAPI XS 응답의 기본 이미지 링크는 비어 있었다. 경험사탕 5종은 PokéSprite에서 수집했다. 이상한사탕은 PokeAPI/sprites에서 수집했다. Python 요청의 네트워크 오류와 HTTP 403 이후 curl로 수집했다. PokéAPI 기본 경로와 gen8 경로의 XS 이미지 요청은 HTTP 404였다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 용어사전, 진행 현황과 출처 문서를 검토했다. 사용자 결정과 외부 자료 관측을 구분했다. 이상한사탕의 입수처와 경험치 처리를 미정으로 유지했다. 한 문장에 한 사실을 적는 원칙과 용어 일치를 확인했다. 기능 구현은 미시작이다.
검증: PNG 6개의 형식과 크기를 확인했다. 이미지 6개를 열어 확인했다. `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 709개, JSON 7개. `git diff --check` 통과. 문서 전용 작업이므로 빌드와 런타임 검사는 실행하지 않았다.

### 이상한사탕의 다음 레벨 경험치 초기화

날짜: 2026-09-21. 사용자 정정: “아니. 이상한사탕은 무조건 다음레벨 경험치0이야.” 기능 구현 미시작.
목표와 범위: 이상한사탕 사용 후 레벨 내 경험치 처리를 확정한다. 경험치 진행 비율을 유지하는 직전 제안은 채택하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 이상한사탕을 사용하면 레벨이 1 오른다. 오른 레벨의 경험치 진행은 0부터 시작한다. 예를 들어 15레벨의 경험치 진행이 70%여도 사용 후에는 16레벨의 0%다. 레벨 상한 100은 유지한다.
위험과 수용 조건: 경험치 0은 새 레벨 안의 진행을 뜻한다. 누적 경험치 전체를 0으로 만드는 뜻이 아니다. 사용 전 진행 비율이나 남은 경험치를 새 레벨에 이월하지 않는다. 경험사탕의 경험치 지급 규칙은 바꾸지 않는다.
`[스펙 미확정]` 이상한사탕의 가격·입수처·대상 범위·수량 사용 UI와 100레벨에서 사용 차단 처리는 후속 설계한다.
변경과 의미 검수: 제품 결정、기능 계약、SC-06、용어사전과 진행 현황의 변경 문장을 확인했다. 사용자 정정과 이전 제안을 구분했다. 레벨 내 진행과 누적 경험치를 구분했다. 용어와 조건 표현을 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 711개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 이상한사탕의 100레벨 사용 차단

날짜: 2026-09-21. 사용자 답변: “진행.” 직전 제안인 100레벨에서 이상한사탕 사용 차단과 미소비를 채택했다. 기능 구현 미시작.
목표와 범위: 이상한사탕의 최대 레벨 사용 규칙을 확정한다. 가격과 사용 화면 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 대상 개체가 100레벨이면 이상한사탕을 사용할 수 없다. 사용을 거부할 때는 사탕을 소비하지 않는다.
위험과 수용 조건: 99레벨에서는 한 개로 100레벨에 도달한다. 이때 새 레벨의 경험치 진행은 0이다. 이미 100레벨인 개체에 사용을 시도해도 개체 상태와 재고가 바뀌지 않는다.
`[스펙 미확정]` 이상한사탕의 대상 범위, 수량 사용 UI, 가격과 입수처는 별도로 정한다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 용어사전과 진행 현황의 변경 문장을 확인했다. 99레벨에서의 정상 사용과 100레벨에서의 거부를 구분했다. 거부 시 재고 보존을 확인했다. 사용자 결정과 미정 사항을 구분했다. 조건과 용어 표현을 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 713개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 이상한사탕의 파티와 박스 대상 허용

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 이상한사탕의 파티·박스 보유 개체 사용과 숨김 여부 제한 없음을 채택했다. 기능 구현 미시작.
목표와 범위: 이상한사탕의 사용 대상 범위를 확정한다. 가격과 수량 사용 화면 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 파티와 박스의 모든 보유 개체를 이상한사탕 대상으로 선택할 수 있다. 숨긴 파티 개체도 대상이다. 기존 100레벨 사용 차단은 유지한다.
위험과 수용 조건: 파티가 비어 있어도 박스 개체에게 사용할 수 있다. 사용 때문에 파티·박스 위치나 숨김 상태를 바꾸지 않는다. 박스의 시간에 따른 성장 정지는 유지한다. 직접 사용한 사탕의 효과만 적용한다.
`[스펙 미확정]` 이상한사탕의 수량 사용 UI, 가격과 입수처는 별도로 정한다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 용어사전과 진행 현황을 확인했다. 대상 범위 허용과 100레벨 사용 차단을 구분했다. 박스의 시간 정지와 직접 사탕 사용을 구분했다. 조건 표현과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 715개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 이상한사탕 수량 선택과 세션 인계

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 한 개체에 여러 개 사용, 예상 레벨 확인과 수량 한도를 채택했다. 다른 세션에서 이어갈 수 있도록 진행 상태 정리도 요청했다. 기능 구현 미시작.
목표와 범위: 이상한사탕 수량 계약을 확정한다. 진행 문서의 재개 안내와 남은 작업을 현행화한다. 앱 구현과 커밋·푸시는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/progress.md`. 기존 결정과 정정 근거는 이 기록을 유지한다.
확정 범위: 이상한사탕도 한 개체와 사용 수량을 선택한다. 예상 레벨을 확인한 뒤 여러 개를 한 번에 사용할 수 있다. 최대 선택 수량은 보유량과 `100 - 현재 레벨` 중 작은 값이다. 사용 후 레벨 내 경험치 진행은 0이다.
위험과 수용 조건: 98레벨에서 5개를 보유해도 최대 2개만 선택한다. 미리보기만으로 개체 상태와 재고를 바꾸지 않는다. 여러 개 사용을 여러 대상 일괄 사용으로 확대하지 않는다. 사용 직전 상태 변경과 저장 실패 처리는 미정으로 유지한다.
인계 범위: 같은 작업 폴더의 새 세션은 로컬 문서에서 재개한다. 다른 PC는 미추적 참고 자료를 포함한 변경 파일을 별도로 전달해야 한다. 로컬 파일 저장을 원격 동기화 완료로 표시하지 않는다.
`[스펙 미확정]` 이상한사탕의 입수처와 가격, 경험사탕 효과량과 성장 곡선, 사용 화면의 세부 조작은 남아 있다. 다음 사용자 질문은 이상한사탕 입수처다.
변경과 의미 검수: 이상한사탕 계약과 SC-06의 대상·수량·미리보기·상한을 확인했다. 진행 문서의 오래된 다음 질문을 제거했다. 누적 결정의 긴 나열을 현재 단계와 잔여 작업 표로 교체했다. 확정 결정은 기능 계약과 기존 기록으로 연결했다. 같은 폴더 재개와 다른 PC 전달 조건을 구분했다. 시나리오 초안, 설계 완료, 앱 구현 완료를 구분했다. 문장별 조건과 용어 일치를 확인했다.
검증: `git branch --show-current` 결과는 `main`이다. `git status --short`에서 추적 문서 변경과 미추적 시나리오·참고 자료를 확인했다. `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 720개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다. 커밋·푸시는 하지 않았다.

### 사탕 공통 입수 경로

날짜: 2026-09-21. 사용자 결정: “상점구매도 하고, 업적탐험도 하고. 다른 경험사탕도 똑같아.” 기능 구현 미시작.
목표와 범위: 경험사탕 5종과 이상한사탕의 공통 입수 경로를 확정한다. 가격과 개별 보상표는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 경험사탕 XS·S·M·L·XL과 이상한사탕은 모두 상점 구매, 업적 보상, 탐험 보상으로 얻을 수 있다. 탐험은 기존 결정대로 향후 확장이다.
위험과 수용 조건: 보상 경로 허용을 모든 업적·탐험에서 사탕 전종을 지급한다는 뜻으로 확대하지 않는다. 사탕 사용 대상·효과·수량·레벨 상한은 기존 계약을 유지한다. 상점에서 처음부터 모든 종류를 판매하는지는 아직 정하지 않았다.
`[스펙 미확정]` 종류별 가격과 판매 조건, 업적·탐험별 보상 종류와 수량, 획득 후 가방 보관과 사용 진입점은 별도로 정한다. 다음 질문은 공통 가방 보관과 직접 사용 흐름이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 용어사전과 재개 안내를 확인했다. 사탕 6종의 공통 경로와 개별 보상표를 구분했다. 탐험의 향후 확장 상태를 유지했다. 조건과 용어를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 722개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 사탕의 공통 가방 보관과 직접 사용

날짜: 2026-09-21. 사용자 답변: “그렇게 진행.” 직전 제안인 획득 경로와 관계없는 공통 가방 보관과 사탕→대상 개체→수량 선택을 채택했다. 기능 구현 미시작.
목표와 범위: 사탕 6종의 보관과 사용 흐름을 확정한다. 다른 도구의 보관 규칙과 화면 배치는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 경험사탕 XS·S·M·L·XL과 이상한사탕은 획득 경로와 관계없이 공통 가방에 종류별로 모아둔다. 사용자는 가방에서 사탕을 선택한다. 이어서 대상 개체와 수량을 선택한다. 기존 미리보기 계약에 따라 결과를 확인한 뒤 직접 사용한다.
위험과 수용 조건: 상점 구매와 업적 보상으로 얻은 같은 사탕은 같은 종류의 재고로 보관한다. 향후 탐험 보상도 같은 규칙을 따른다. 획득 자체로 개체를 성장시키지 않는다. 가방 진입점 확정을 최상위 탭 배치 확정으로 확대하지 않는다. 알은 기존 돌보미집 구매 직후 배치 규칙을 유지한다.
`[스펙 미확정]` 가방 용량과 종류별 보유 수량 상한, 수량 조작 화면, 사용 직전 상태 변경과 저장 실패 처리는 남아 있다. 다음 질문은 가방 용량 제한 여부다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 가방 용어와 재개 안내를 확인했다. 보관과 직접 사용을 구분했다. 알을 가방에 보관한다는 기존 요약을 돌보미집 배치 계약에 맞게 정정했다. 가방 사용 흐름과 최상위 화면 배치를 구분했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 724개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 가방 보관 제한 없음

날짜: 2026-09-21. 사용자 답변: “제한없이.” 직전 질문의 가방 칸 제한과 종류별 보유 수량 제한을 두지 않는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 가방의 보관 용량 규칙을 확정한다. 구매 조건과 화면 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
확정 범위: 가방에 칸 제한을 두지 않는다. 종류별 보유 수량도 제한하지 않는다. 같은 종류의 사탕은 획득 경로와 관계없이 수량을 합산한다.
위험과 수용 조건: 보관 제한 없음과 한 번에 사용하는 수량 한도를 구분한다. 사탕 사용은 보유량과 레벨 상한에 따른 기존 제한을 유지한다. 가방 정책을 돌보미집의 알 최대 6개 제한에 확대하지 않는다.
`[스펙 미확정]` 큰 수량의 표시·저장 방식과 사탕 종류별 판매 조건은 남아 있다. 다음 질문은 사탕의 상점 판매 시작 조건이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 가방 용어와 재개 안내를 확인했다. 보관 수량과 사용 수량을 구분했다. 돌보미집의 배치 제한을 유지했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 726개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 사탕 전종의 초기 상점 판매

날짜: 2026-09-21. 사용자 답변: “가격이 비싸면 자연스럽게 구매하게될거니까 그렇게 진행.” 직전 제안인 사탕 6종의 초기 판매와 가격을 통한 구매 시점 조절을 채택했다. 기능 구현 미시작.
목표와 범위: 사탕의 판매 시작 조건을 확정한다. 실제 가격과 경제 밸런스 계산은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/progress.md`.
확정 범위: 경험사탕 XS·S·M·L·XL과 이상한사탕을 처음부터 상점에서 판매한다. 사탕 종류별로 별도 해금 조건을 두지 않는다. 가격으로 구매 시점을 조절한다.
위험과 수용 조건: 큰 경험사탕과 이상한사탕도 처음부터 판매 항목에 포함한다. 필요한 포인트를 보유했다면 별도 레벨·업적 조건 때문에 구매를 막지 않는다. 이 결정을 포켓몬 등 다른 상품의 해금 규칙으로 확대하지 않는다.
`[스펙 미확정]` 실제 가격, 경험사탕 효과량, 성장 곡선과 보상표는 경제 설계에서 정한다. 다음 질문은 SR-08의 단일 포켓몬 진화 허용 여부다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06과 재개 안내를 확인했다. 판매 시작 조건 확정과 가격 미정을 구분했다. 사탕의 해금 조건 제거를 다른 상품으로 확대하지 않았다. 조건과 용어 일치를 확인했다.
검증: 문서 교체용 Python 명령은 인코딩 오류로 실행되지 않았다. 패치로 수정한 뒤 `node scripts/check-docs.cjs`를 통과했다. 결과는 문서 47개, 파일 링크 728개, JSON 7개다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 단일 포켓몬 진화 계열 제공과 종별 스탯

날짜: 2026-09-21. 사용자 결정: 단일 포켓몬 진화의 재획득 불가 경고를 원하지 않는다. 루나아라·솔가레오와 아고용을 예로 들며 관련 진화 계열을 모두 제공하도록 정정했다. “전설,환상,준전설 등 진화를 하면 기존이랑 친밀도,레벨등 전체 다 똑같은 진화한 개체를 가지도록 하자.” 스탯은 포켓몬별로 다르게 적용하도록 명시했다. 기능 구현 미시작.
목표와 범위: 단일 포켓몬 진화의 예외 방향과 진행 상태·종별 스탯의 구분을 기록한다. 던전 구현과 실제 종 목록 확정은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 위 대상의 진화 계열을 모두 가질 수 있도록 한다. 진화한 개체의 친밀도·레벨 등 진행 상태는 기존 개체와 동일하게 제공한다. 결과 종의 스탯은 해당 종의 기준을 적용한다. 기존 종의 스탯을 그대로 복사하지 않는다. 단일 포켓몬 전용 재획득 불가 경고 제안은 채택하지 않는다.
위험과 수용 조건: 일반 진화의 기존 개체 ID 유지 규칙과 이번 예외를 구분한다. 일반 진화까지 추가 개체를 지급하는 것으로 확대하지 않는다. 진행 상태 복제를 지속적인 상태 동기화로 확정하지 않는다. 모든 분기 결과를 한 번에 지급하는지는 아직 정하지 않았다. 사용자의 종 예시를 전체 진화 종에 대한 조사 결과로 기록하지 않는다.
`[스펙 미확정]` 원본 개체를 유지하고 별도 개체 ID로 결과를 추가하는지 먼저 확인한다. 이후 개체별 독립 성장, 분기 결과별 지급 시점, 종별 1회 획득 이력과 배치 방식을 정한다. 일반 진화의 결과 확인 화면까지 제거하는 것은 이번 결정에 포함하지 않는다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, SR-08과 재개 안내를 확인했다. 진행 상태와 종별 스탯을 구분했다. 사용자가 확정한 제공 방향과 개체 보존 방식의 해석을 구분했다. 일반 진화에 예외를 확대하지 않았다. 조건과 용어 일치를 확인했다.
검증: 문서 교체용 Python 명령은 인코딩 오류로 실행되지 않았다. 패치로 수정한 뒤 `node scripts/check-docs.cjs`를 통과했다. 결과는 문서 47개, 파일 링크 731개, JSON 7개다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 단일 포켓몬 진화의 원본 보존과 신규 개체 ID

날짜: 2026-09-21. 사용자 답변: “그렇게 새 개체id발급.” 직전 확인인 진화 전 개체 보존과 진화한 개체의 새 ID 발급을 승인했다. 기능 구현 미시작.
목표와 범위: 지정한 단일 포켓몬 진화 예외의 개체 생성 방식을 확정한다. 일반 진화와 던전 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 진화 전 개체는 그대로 남긴다. 진화한 개체를 새 개체 ID로 추가 지급한다. 기존 결정에 따라 친밀도·레벨 등 진행 상태는 동일하게 제공한다. 스탯은 결과 종의 기준을 적용한다.
위험과 수용 조건: 원본과 결과의 개체 ID가 달라야 한다. 원본을 삭제하거나 결과 종으로 바꾸지 않는다. 일반 진화의 같은 ID 유지 규칙은 그대로 둔다. 반복 진화로 단일 종의 획득 제한을 우회하도록 해석하지 않는다.
`[스펙 미확정]` 지급 후 독립 성장 여부, 분기 결과별 지급 시점, 중복 요청과 획득 이력 처리, 새 개체 배치 방식은 남아 있다. 다음 질문은 지급 시점에만 상태를 복사하고 이후에는 각자 성장하는 방식이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, SR-08과 재개 안내를 확인했다. 원본 보존과 결과 생성의 확정을 기록했다. 일반 진화의 ID 유지와 예외를 구분했다. 이후 독립 성장은 미정으로 남겼다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 734개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 단일 포켓몬 진화 계열의 공유 sid 정정

날짜: 2026-09-21. 사용자 정정: “그냥 같은 sid쓰는거로 공유하고, 소환을 베베놈을 했으면 아고용은 못하고 그런 느낌으로 가자. 단일개체니까.” 앞선 새 개체 ID 발급과 이후 독립 개체 방향을 철회했다. 기능 구현 미시작.
목표와 범위: 지정한 단일 포켓몬 진화 계열을 하나의 개체로 관리한다. 일반 포켓몬 진화와 화면 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 해당 진화 계열은 같은 `sid`를 사용한다. 별도의 진화 개체를 추가 생성하지 않는다. 친밀도·레벨·경험치·만복도·버프·쿨타임 등 진행 상태는 한 개체의 상태로 공유한다. 선택 가능한 종 중 하나만 꺼낼 수 있다. 베베놈을 꺼낸 동안 아고용을 동시에 꺼낼 수 없다. 스탯은 현재 선택한 종의 기준을 적용한다.
위험과 수용 조건: 종을 바꿔도 진행 상태를 초기화하거나 과거 상태로 되돌리지 않는다. 같은 `sid`를 여러 개체로 취급해 포인트나 시간 진행을 중복 계산하지 않는다. 도감의 서로 다른 종 항목과 개체 ID를 구분한다. 일반 진화의 철회 불가 규칙을 이 예외 때문에 변경하지 않는다. 단일 포켓몬 전용 재획득 불가 경고는 추가하지 않는다.
`[스펙 미확정]` 종 전환 화면과 조작, 분기별 선택 가능 시점, 도감 획득 이력은 후속 설계한다. 다음 질문은 파티에서 한 개체로 표시하고 상세 화면에서 사용할 종을 고르는 방식이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, 용어사전, SR-08과 재개 안내를 확인했다. 이전 새 ID 결정은 역사 기록에 보존하고 현재 계약에서 철회했다. 상태 공유와 종별 스탯을 구분했다. 일반 진화와 지정 계열의 예외를 구분했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 731개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 공유 sid 진화 계열의 박스 교체 흐름

날짜: 2026-09-21. 사용자 답변: “박스에서 교체하는 느낌으로.” 상세 화면에서 종을 선택하는 직전 제안 대신 박스에서 교체하는 흐름을 선택했다. 기능 구현 미시작.
목표와 범위: 지정한 단일 포켓몬 진화 계열의 종 전환 진입점을 정한다. 화면 배치와 데이터 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06·SC-07, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 해당 계열의 종은 박스에서 교체하는 흐름으로 선택한다. 같은 `sid`와 진행 상태 공유를 유지한다. 계열의 서로 다른 종을 동시에 꺼내지 않는다. 스탯은 선택한 종의 기준을 따른다.
위험과 수용 조건: 박스의 선택 대상을 서로 다른 독립 개체로 생성하지 않는다. 선택하지 않은 종 때문에 별도의 타이머나 포인트 적립을 만들지 않는다. 일반 개체끼리의 맞바꾸기와 공유 `sid`의 종 선택을 구분한다.
`[스펙 미확정]` 박스의 종별 표시 방식과 파티 칸 교체 세부 조작을 정한다. 다음 질문은 파티에 빈칸이 있어도 같은 `sid`의 현재 종이 차지한 칸을 맞바꾸는 방식이다. 분기별 선택 가능 시점과 도감 획득 이력도 남아 있다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06·SC-07, SR-08과 재개 안내를 확인했다. 박스 진입점 확정과 화면 배치 미정을 구분했다. 공유 개체의 종 선택과 독립 개체의 맞바꾸기를 구분했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 734개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 공유 sid 종 교체의 기존 파티 칸 유지

날짜: 2026-09-21. 사용자 답변: “그방식으로 진행.” 박스에서 같은 `sid`의 다른 종을 선택하면 빈칸이 있어도 기존 파티 칸을 교체하는 방식을 채택했다. 기능 구현 미시작.
목표와 범위: 공유 개체의 종 교체와 파티 칸 점유 규칙을 확정한다. 박스 화면 배치와 분기 조건은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-07, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 해당 `sid`가 파티에 있으면 박스에서 선택한 종으로 기존 파티 칸을 교체한다. 다른 빈 파티 칸이 있어도 추가 배치하지 않는다. 같은 `sid`는 두 파티 칸을 차지하지 않는다.
위험과 수용 조건: 파티의 베베놈 대신 아고용을 선택하면 기존 베베놈 칸이 아고용 칸이 된다. 빈칸이 있거나 파티가 가득 찼어도 같은 원칙을 적용한다. 공유 진행 상태를 초기화하지 않는다. 일반 개체끼리의 맞바꾸기 규칙은 유지한다.
기존 계약 적용: 박스에서 교체한 뒤에는 숨김 상태로 둔다. 사용자가 별도로 꺼내기를 한다. 박스 교체 뒤 자동 표시하지 않는 앞선 결정을 따른다.
`[스펙 미확정]` 분기별 선택 가능 시점과 도감 획득 이력은 후속 설계한다. 다음 질문은 분기별 조건을 충족하고 직접 진화한 종만 박스의 교체 대상으로 여는 방식이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06·SC-07, SR-08과 재개 안내를 확인했다. 기존 칸 교체와 빈칸 추가 배치를 구분했다. 박스 교체 뒤 숨김은 기존 결정의 적용으로 기록했다. 분기별 선택 가능 시점은 미정으로 유지했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 737개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 솔가레오와 루나아라의 동시 제공 정정

날짜: 2026-09-21. 사용자 정정: “루나아라랑 솔가레오는 진화하면 같이 제공받을거야.” 각 분기의 조건을 따로 충족하고 직접 진화해야 교체 가능하다는 직전 제안을 거절했다. 기능 구현 미시작.
목표와 범위: 코스모움 진화 시 솔가레오와 루나아라의 제공 시점을 확정한다. 다른 분기 진화의 제공 묶음과 실제 진화 조건은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 코스모움에서 진화를 한 번 실행하면 솔가레오와 루나아라를 함께 제공한다. 두 종 모두 같은 `sid`의 교체 가능한 종으로 열린다. 다른 한 종을 위해 별도 진화를 다시 할 필요는 없다. 별도 개체를 생성하지 않는다. 진행 상태 공유와 동시 꺼내기 금지는 유지한다.
위험과 수용 조건: 함께 제공한다는 것은 두 종을 동시에 화면에 꺼낸다는 뜻이 아니다. 도감 해금만으로 지급하는 것으로 확대하지 않는다. 솔가레오·루나아라 동시 제공을 일반 분기 진화 전체에 적용하지 않는다. 이미 선택 가능한 진화 전 종도 기존 박스 교체 흐름을 유지한다.
`[스펙 미확정]` 진화 직후 먼저 사용할 종의 선택 방식, 도감 획득 기록과 다른 대상의 제공 묶음은 후속 설계한다. 다음 질문은 진화할 때 먼저 사용할 종을 사용자가 선택하는 방식이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, SR-08과 재개 안내를 확인했다. 종별 별도 진화 제안을 철회했다. 두 종의 동시 제공과 동시 꺼내기 금지를 구분했다. 일반 분기 진화로 범위를 확대하지 않았다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 740개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 게임 시간에 따른 솔가레오와 루나아라 진화 결과

날짜: 2026-09-21. 사용자 정정: 진화 직후 종을 따로 선택하지 않는다. 게임 시간의 낮에 진화하면 솔가레오, 밤에 진화하면 루나아라가 된다. 게임 시간 기능은 미구현이며 차후 구현 예정이다. 네크로즈마는 별개의 포켓몬이라고 명시했다. 기능 구현 미시작.
목표와 범위: 두 종의 동시 제공과 진화 시 현재 종 판정을 구분한다. 게임 시간 구현과 네크로즈마의 별도 기믹은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 코스모움이 진화할 때 게임 시간의 낮이면 현재 종은 솔가레오가 된다. 밤이면 루나아라가 된다. 두 종을 함께 제공하는 직전 결정은 유지한다. 둘 다 같은 `sid`의 박스 교체 대상이 된다. 진화 직후 먼저 사용할 종을 사용자에게 고르게 하는 제안은 철회한다. 네크로즈마는 이 공유 `sid` 계열에 포함하지 않는 별개의 포켓몬이다.
위험과 수용 조건: 진화 시 현재 종 판정과 교체 대상 제공을 구분한다. 낮에 진화해도 루나아라를 얻기 위해 다시 진화하지 않는다. 밤에 진화해도 같은 원칙을 따른다. 게임 시간을 실제 OS 시간으로 임의 대체하지 않는다. 낮·밤 변화만으로 현재 종이 자동 전환된다고 해석하지 않는다. 종별 스탯과 공유 진행 상태 규칙을 유지한다.
`[스펙 미확정]` 게임 시간의 낮·밤 경계와 흐르는 속도는 해당 기능을 설계할 때 정한다. 도감 획득 이력과 다른 대상의 제공 묶음도 남아 있다. 다음 질문은 함께 제공된 두 종의 도감 획득 기록을 동시에 남기는 방식이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, SR-08과 재개 안내를 확인했다. 현재 종의 조건 판정과 두 종의 동시 제공을 구분했다. 수동 결과 선택 제안을 철회했다. 게임 시간 구현과 네크로즈마의 별개 취급 범위를 확인했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 743개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 솔가레오와 루나아라의 도감 동시 획득 기록

날짜: 2026-09-21. 사용자 답변: “둘 다 획득한거로 기록.” 두 종을 함께 제공하는 순간 도감에 모두 획득한 것으로 기록하는 직전 질문을 승인했다. 기능 구현 미시작.
목표와 범위: 동시 제공되는 두 종의 도감 획득 기록 시점을 확정한다. 다른 종의 조건표와 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 SC-06, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`.
확정 범위: 코스모움 진화로 솔가레오와 루나아라를 함께 제공하면 도감에 두 종 모두 획득한 것으로 기록한다. 현재 종이 솔가레오인지 루나아라인지는 획득 기록을 제한하지 않는다. 아직 박스에서 교체하거나 화면에 꺼내지 않은 종도 획득 기록에 포함한다.
위험과 수용 조건: 공유 `sid`는 하나지만 도감 획득 기록은 두 종이다. 별도 개체를 생성하지 않는다. 교체할 때 획득 기록을 지우거나 다시 지급하지 않는다. 도감 선행 해금과 실제 획득은 기존 구분을 유지한다.
`[스펙 미확정]` 다른 대상의 제공 묶음, 공유 계열의 박스 표시 방식과 게임 시간 구현은 남아 있다. 다음 질문은 Q-03의 거래·보상 처리 실패 시 상태 보존과 재시도 중복 방지 원칙이다.
변경과 의미 검수: 제품 결정, 기능 계약, SC-06, SR-08과 재개 안내를 확인했다. 하나의 공유 개체와 두 종의 도감 획득 기록을 구분했다. 도감 획득에 별도 꺼내기를 요구하지 않음을 확인했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 746개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 거래 실패 원자성과 코스모움 분리안 재검토

날짜: 2026-09-21. 사용자 결정: “이전상태 유지되도록 트랜잭션처럼 동작하게 설계.” 직전 질문의 구매·사용·보상 실패 시 이전 상태 유지와 완료 처리 재시도의 중복 지급 금지 원칙을 채택했다. 기능 구현 미시작.
목표와 범위: 거래의 실패 원칙을 확정한다. 코스모움 두 개체 구상은 재검토 안으로 기록한다. 저장 기술 선택과 게임 코드 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 Q-03, `docs/progress.md`.
확정 범위: 구매·사용·보상 수령은 관련 상태 변경을 하나의 거래로 처리한다. 성공하면 전부 반영한다. 실패하면 관련 상태 전부를 이전 상태로 유지한다. 이미 완료된 거래를 재시도해도 상품이나 보상을 중복 지급하거나 비용을 다시 차감하지 않는다.
수용 조건: 구매 실패 시 포인트만 줄거나 상품만 늘지 않는다. 사탕 사용 실패 시 재고와 개체 상태 모두 이전 상태를 유지한다. 보상 수령 실패 시 보상 재고와 수령 상태 모두 이전 상태를 유지한다. 처리 결과를 알 수 없는 중단은 저장된 거래 결과를 확인한 뒤 재시도한다.
`[스펙 미확정]` 거래 식별과 저장 방식, 결과 불명 상태의 화면·재시도 조작, 교체·부화·진화 등 다른 복합 동작의 세부 계약은 후속 설계한다.
사용자 구상: “솔가레오로 진화하는 코스모움과 루나라아로 진화하는 코스모움 2가지를 따로 구하게할까”라고 고민을 제시했다. 두 개체 구상은 미확정이다. 솔가레오·루나아라 동시 제공과 공유 `sid`의 기존 결정은 새 결정 전까지 유지한다. 분리안을 채택하면 종별 1회 획득과 게임 시간 진화 조건도 함께 재검토해야 한다.
다음 질문: 기존 공유 개체에서 두 종을 함께 얻는 방식과, 서로 다른 두 개체가 각자 한 종으로 진화하는 방식 중 수집 경험을 선택한다.

### 코스모움 기존 공유 개체안 유지

날짜: 2026-09-21. 사용자 답변: “아니다. 그냥 기존안대로 진행.” 직전 코스모움 두 개체 획득 구상을 채택하지 않고 기존안을 유지했다. 기능 구현 미시작.
목표와 범위: 코스모움 재검토 상태를 해소한다. 거래 원자성 결정은 유지한다.
확정 범위: 코스모움이 한 번 진화하면 솔가레오와 루나아라를 함께 제공한다. 게임 시간의 낮에는 솔가레오, 밤에는 루나아라가 현재 종이 된다. 두 종은 같은 `sid`로 진행 상태를 공유한다. 박스에서 기존 파티 칸을 교체하며 동시에 꺼내지 않는다. 도감에는 두 종 모두 획득한 것으로 기록한다. 네크로즈마는 별개의 포켓몬이다.
수용 조건: 코스모움 두 개체 획득 구상을 현재 계약으로 적용하지 않는다. 구매·사용·보상 수령의 전부 반영 또는 이전 상태 유지 원칙과 완료 거래 재시도 중복 방지는 철회하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/work/s5-design-system-v2/feedback.md`의 SR-08, `docs/progress.md`. 다음 질문은 구매·사용·보상 처리 실패 후 재시도 조작이다.
변경과 의미 검수: 이번 거래 원칙과 코스모움 재검토·유지 기록, 제품 결정, 기능 계약, SC-03·05·06과 Q-03, SR-08, 재개 안내를 확인했다. 거래 원칙의 확정과 저장 구현 미정을 구분했다. 코스모움 재검토를 해소하면서 거래 결정을 유지했다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 751개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 거래 실패 처리만 제공

날짜: 2026-09-21. 사용자 답변: “그냥 실패 만 처리.” 선택값을 보존하고 실패 이유와 다시 시도 버튼을 표시하는 직전 제안 대신 실패 처리만 하도록 지시했다. 기능 구현 미시작.
목표와 범위: 구매·사용·보상 실패 후의 전용 재시도 흐름을 제외한다. 실패 문구의 상세 표현과 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 Q-03, `docs/progress.md`.
확정 범위: 실패한 거래는 실패로 처리하고 끝낸다. 앞서 정한 트랜잭션 원칙에 따라 관련 상태를 이전 상태로 유지한다. 실패 복구용 선택값 보존과 다시 시도 버튼 제안은 채택하지 않는다. 자동 재시도도 추가하지 않는다.
위험과 수용 조건: 전용 재시도 UI를 제외해도 완료 거래의 중복 지급·차감 방지는 유지한다. 정상 구매·사용·수령 조작을 영구적으로 막는 뜻이 아니다. 저장 결과가 불명확한 경우의 내부 판정 계약은 별도로 정한다. 실패 처리 결정을 모든 화면의 오류 UI 제거로 확대하지 않는다.
`[스펙 미확정]` 실패 문구와 표시 위치, 저장 방식과 결과 불명 상태의 내부 판정은 남아 있다. 다음 질문은 파티 확장의 두 번째 업적 조건이다. 기존 후보는 상점의 구매 가능한 파티 칸 2개를 모두 구매하는 조건이다.
변경과 의미 검수: 제품 결정, 거래 계약, Q-03과 재개 안내를 확인했다. 실패 전용 재시도 흐름 제외와 내부 중복 방지 유지를 구분했다. 기존 정상 조작을 차단하는 규칙을 추가하지 않았다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs` 통과: 문서 47개, 파일 링크 754개, JSON 7개. `git diff --check` 통과. 설계 문서만 변경했다. 기능 구현과 빌드·런타임 검사는 진행하지 않았다.

### 설계 중간 저장과 두 번째 업적 보류

날짜: 2026-09-21. 사용자 요청: “2번째 파티확장칸은 아직 업적 고민중이야. 지금까지 했던거 정리해서 commit push”. 두 번째 파티 칸 업적의 조건은 미정으로 유지한다. 상점 칸 2개 구매 조건은 채택하지 않은 과거 후보로 남긴다.
목표와 범위: 이번 대화의 설계 문서, SC-01~SC-11, 사탕 참고 자료와 재개 안내를 중간 저장한다. 사용자가 커밋과 푸시를 명시적으로 요청했다. 앱 코드와 Figma는 변경하지 않는다.
SSOT: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`, `docs/terms.md`, `docs/progress.md`. 확인 기록은 기존 `review.md`와 `feedback.md`에 남긴다.
위험과 수용 조건: 설계 중간 저장을 전체 설계 완료나 구현 승인으로 표시하지 않는다. 철회한 새 ID 지급과 코스모움 두 개체안을 다시 적용하지 않는다. 미정 업적과 가격·경험치·확률을 임의 확정하지 않는다. 다음 세션이 두 번째 업적 조건을 즉시 다시 묻지 않도록 안내한다. 시나리오와 참고 자료도 커밋 범위에 포함한다.
실행 범위: S5 기능 구현은 미시작으로 유지한다. 남은 설계 항목과 기존 Figma 문제는 후속 작업으로 명시한다. 사용자 요청에 따라 이 중간 설계 상태를 공유한다.
문서 정리 결과: 두 번째 업적 보류를 현재 계약과 재개 안내에 반영했다. 일반 진화와 공유 개체 예외의 적용 범위를 정리했다. 과거 결정·철회 기록은 보존했다. 설계 중간 저장을 월별 이력에 추가했다. 사용자 결정·제안·미정과 문장별 조건·용어를 검수했다. 검사 결과는 [중간 저장 검수](review.md#설계-중간-저장-검수)를 따른다. 기능 구현은 미시작이다.

### 먹이 쿨타임의 진행과 정지

날짜: 2026-09-21. 사용자 답변: “진행”. 직전 제안의 파티 진행과 박스·잠금·절전·종료 정지를 채택했다. 기능 구현 미시작.
목표와 범위: 먹이 쿨타임을 기존 게임 시간 정책에 연결한다. 타이머 구현과 실제 대기시간은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 실행 상태별 시간, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`. 다음 행동은 `docs/progress.md`에 기록한다.
확정 범위: 앱 실행 중이고 PC를 사용할 수 있으면 파티 개체의 쿨타임이 진행한다. 숨김 상태에서도 진행한다. 박스 보관·PC 잠금·절전·앱 종료 중에는 남은 시간을 보존한다. 복귀 후 남은 시간부터 진행한다. 중단 기간은 소급 적용하지 않는다.
위험: 박스 이동이나 앱 재시작으로 쿨타임을 초기화하면 연속 먹이 사용 제한을 우회할 수 있다.
수용 조건: 남은 시간이 있는 개체를 박스에 넣었다 꺼내면 보존한 시간부터 이어간다. 앱을 종료했다 다시 실행해도 같은 원칙을 적용한다. 절전 해제 후 PC가 잠겨 있으면 정지를 유지한다.
미정: 실제 대기시간, 성공·실패 때의 타이머 처리, 저장 및 플랫폼별 감지 구현. 기본먹이 제공 방식도 후속 질문으로 남긴다.
변경과 의미 검수: 제품 결정, 먹이 계약, 실행 상태별 시간, SC-02, 용어사전, 진행 현황의 변경 문장을 검토했다. 파티 숨김의 진행과 박스 보관의 정지를 구분했다. 복귀 시 남은 시간을 유지하는 조건을 확인했다. 사용 성공·실패 처리와 실제 수치는 미정으로 유지했다.
문서 검사: `node scripts/check-docs.cjs`에서 문서 46개, 파일 링크 624개, JSON 5개를 통과했다. `git diff --check`도 통과했다. 설계 문서만 변경했다. 빌드와 런타임 검사는 미실행이다.

### 부화·진화·교체의 트랜잭션 적용

날짜: 2026-09-22. 사용자 답변: “동일 적용 (Recommended)” 선택. 부화·진화·교체에 구매·사용·보상 수령과 같은 트랜잭션 원칙을 적용하는 직전 질문을 승인했다. 선택지 설명에는 부화 실패 후 다시 열면 그 시점의 조건으로 결과를 새로 정하는 내용이 포함되어 있었다. 기능 구현 미시작.
목표와 범위: Q-03의 복합 동작 중 부화·진화·교체의 실패 처리 원칙을 확정한다. 거래 식별·저장 기술과 실패 문구는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 거래 실패 원칙, `docs/specs/s5-scenarios.md`의 SC-04·SC-06·SC-07과 Q-03, `docs/progress.md`.
확정 범위: 부화는 알 소비, 개체 생성, 파티 또는 박스 배치, 돌보미집 칸 비우기, 도감 획득·발견 기록을 하나의 거래로 처리한다.
진화는 종 변경과 도감 해금·획득 기록을 하나의 거래로 처리한다. 기존 개체 정보 유지 규칙은 그대로 적용한다.
교체는 파티 개체와 박스 개체의 위치 변경을 하나의 거래로 처리한다.
세 동작 모두 성공하면 전부 반영한다. 실패하면 관련 상태 전부를 이전 상태로 유지한다. 실패는 실패로 처리하고 끝낸다. 다시 시도 버튼과 자동 재시도는 제공하지 않는다. 완료된 동작을 재시도해도 개체와 기록을 중복 반영하지 않는다.
부화 저장에 실패하면 결과를 저장하지 않는다. 알은 부화 가능 상태로 돌보미집 칸에 남는다. 사용자가 그 알을 다시 열면 그 시점까지 충족한 조건으로 결과를 새로 정한다. 다시 열기는 실패 전용 재시도가 아니라 정상 열기 조작이다. 처음 열 때의 결과를 먼저 저장해 고정하는 선택지는 채택하지 않았다.
위험과 수용 조건: 부화에 실패하면 알이 사라지거나 개체만 생기지 않는다. 돌보미집 칸이 비지 않는다. 발견 기록만 남지 않는다. 부화 완료로 표시하지 않는다.
진화에 실패하면 종과 도감 기록이 진화 전 상태로 남는다. 진화 완료로 표시하지 않는다.
교체에 실패하면 두 개체가 원래 위치에 남는다. 한 개체가 두 곳에 있거나 어느 곳에도 없는 상태를 만들지 않는다.
완료된 부화를 재시도해도 개체를 더 만들지 않는다. 완료된 진화를 재시도해도 다음 단계로 한 번 더 진화하지 않는다. 완료된 교체를 재시도해도 위치를 원래대로 되돌리지 않는다.
처리 결과가 불명확하면 저장된 결과를 확인한 뒤 판단하는 기존 원칙을 세 동작에도 적용한다.
`[스펙 미확정]` 거래 식별·저장 방식, 결과 불명 상태의 내부 판정, 실패 문구와 표시 위치는 남아 있다. 진화·교체의 확정 전 취소, 공유 `sid` 종 교체, 첫 선택과 놀이공간 적용의 저장 실패는 이번 결정 범위 밖이다. 다음 질문은 업적 보상의 1회 수령과 미수령 상태 유지다.
변경과 의미 검수: 제품 결정, 거래 실패 원칙, 교체·부화·진화 계약, SC-03·SC-04·SC-06·SC-07, Q-03과 재개 안내를 확인했다. 확정한 실패 처리와 `[스펙 미확정]`으로 남긴 취소·연출·저장 구현을 구분했다. 이미 확정한 알 결과 결정 시점을 미정으로 읽게 하는 SC-03·SC-04의 문구를 정정했다. 두 사실을 묶은 문장을 나눴다. 조건과 용어 일치를 확인했다.
검증: `node scripts/check-docs.cjs`는 이 Windows 작업 폴더에서 실패했다. 실패 항목은 이번에 변경하지 않은 `docs/work/runtime-e2e/record.md`의 절 누락 4건이다. 원인은 `core.autocrlf=true`로 체크아웃된 CRLF 줄바꿈이다. 검사 스크립트는 `\n## 설계\n` 형식만 찾는다. 커밋된 LF 내용에서는 네 절을 모두 찾았다. 읽기 결과의 CRLF를 LF로 바꾸는 임시 preload로 다시 실행한 결과는 통과다. 문서 47개, 파일 링크 764개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 업적 보상의 1회 수령과 미수령 유지

날짜: 2026-09-22. 사용자 답변: “제안한대로 진행.” 직전 제안인 업적당 1회 수령과 미수령 보상의 기한 없는 유지를 채택했다. 반복형 업적은 필요할 때 별도 종류로 설계하는 제안도 함께 채택했다. 수령 기한과 반복형 업적의 즉시 포함은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: Q-07의 업적 달성·수령 상태 보존을 확정한다. 안내 상태의 저장과 두 번째 파티 칸 업적 조건은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 파티 칸 확장과 첫 구매·업적 보상 수령, `docs/specs/s5-scenarios.md`의 SC-05·SC-10과 Q-07, `docs/terms.md`, `docs/progress.md`.
확정 범위: 각 업적의 보상은 한 번만 받는다. 수령을 완료한 업적은 다시 수령할 수 없다.
업적을 달성하고 보상을 받지 않았으면 미수령 상태로 유지한다. 업적창을 닫아도 유지한다. 앱을 다시 실행해도 유지한다. 수령 기한은 두지 않는다.
사용자는 미수령 보상을 업적창에서 언제든 받을 수 있다. 반복해서 달성·수령하는 업적이 필요하면 별도 업적 종류로 나중에 설계한다.
위험과 수용 조건: 재실행 후에도 달성한 업적의 미수령 상태가 업적창에 남는다. 시간이 지나도 미수령 보상이 사라지지 않는다. 수령 완료한 업적에서 받기를 다시 선택할 수 없다. 1회 수령 규칙을 반복형 업적 전체의 금지로 확대하지 않는다. 배너를 다시 표시하지 않는 기존 규칙과 미수령 상태 유지를 구분한다.
`[스펙 미확정]` 수령 전 파티 구성이 달라졌을 때의 처리, 반복형 업적의 채택과 규칙, 저장 구현은 남아 있다. 다음 질문은 앱 종료로 끊긴 진행 중 안내의 처리다.
변경과 의미 검수: 제품 결정, 파티 칸 확장과 업적 보상 수령 계약, SC-05·SC-10, Q-07, 용어사전과 재개 안내를 확인했다. 1회 수령과 미수령 유지의 확정, 반복형 업적과 파티 구성 변경 처리의 미정을 구분했다. SC-10의 보존 목록에서 확정한 업적 상태만 분리했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 768개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 앱 종료로 끊긴 안내의 처음부터 재표시

날짜: 2026-09-22. 사용자 답변: “제안대로 진행.” 직전 제안인 끊긴 안내의 미완료 유지와 재실행 후 처음부터 재표시를 채택했다. 앱 종료를 스킵으로 처리하는 안과 끊긴 단계부터 이어가는 안은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: Q-07의 앱 종료로 끊긴 진행 중 안내의 처리를 확정한다. 안내 내용, 가이드북 구성과 저장 구현은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 행동에 따른 단계별 안내, `docs/specs/s5-scenarios.md`의 SC-10과 Q-07, `docs/work/s5-design-system-v2/feedback.md`의 SR-02, `docs/progress.md`.
확정 범위: 진행 중인 안내가 앱 종료로 끊기면 해당 안내를 스킵으로 처리하지 않는다. 미완료 상태로 남긴다. 비정상 종료와 PC 종료도 같은 원칙을 따른다.
앱을 다시 실행하면 끊긴 안내를 처음부터 다시 보여준다. 끊긴 단계를 저장해 그 단계부터 이어가지 않는다.
다시 보여주는 안내도 기존 순차 표시 규칙을 따른다. 안내 때문에 화면을 강제로 전환하지 않는다.
위험과 수용 조건: 사용자가 직접 닫은 안내는 기존 규칙대로 재실행 후에도 보여주지 않는다. 앱 종료와 사용자 닫기를 같은 상태로 저장하지 않는다. 파티 안내 2단계에서 앱이 종료되면 재실행 후 파티 안내 1단계부터 보여준다. 이 재표시를 튜토리얼 다시보기 기능으로 확대하지 않는다. 재표시로 시작 포인트나 업적 보상을 다시 지급하지 않는다.
`[스펙 미확정]` 재표시 시점에 해당 행동을 이미 완료한 경우의 안내 내용, 여러 안내가 대기 중일 때의 순서, 안내 상태의 저장 구현은 남아 있다. 다음 질문은 진행 중인 안내가 없을 때 여러 안내 조건이 동시에 생긴 경우의 순서다.
변경과 의미 검수: 제품 결정, 단계별 안내 계약, SC-10, Q-07, SR-02와 재개 안내를 확인했다. 사용자 닫기의 스킵과 앱 종료의 미완료를 구분했다. 처음부터 재표시와 튜토리얼 다시보기를 구분했다. 이미 확정한 항목을 s5.md의 미정 목록에서 제거했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 773개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 동시 안내 조건의 발생 순서와 흐름 우선순위

날짜: 2026-09-22. 사용자 답변: “제안대로 진행.” 직전 제안인 발생 순서 우선과 같은 순간의 흐름 순서 우선순위를 채택했다. 대기 안내의 앱 종료 처리 제안도 함께 채택했다. 고정 순서만 사용하는 안과 최근 조건 우선 안은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: 진행 중인 안내가 없을 때 여러 안내 조건이 생긴 경우의 표시 순서를 확정한다. 대기 안내의 표시 위치와 안내 내용은 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 행동에 따른 단계별 안내, `docs/specs/s5-scenarios.md`의 SC-01·SC-05·SC-10과 Q-07, `docs/work/s5-design-system-v2/feedback.md`의 SR-02, `docs/progress.md`.
확정 범위: 여러 안내 조건이 생기면 먼저 생긴 조건의 안내부터 보여준다.
같은 순간에 생긴 조건은 첫 돌봄 → 상점 → 부화 → 파티 → 업적 → 놀이공간 순서로 보여준다.
기다리는 안내는 앞 안내를 마치거나 스킵한 뒤 하나씩 보여준다. 기존 강제 전환 금지 규칙을 유지한다.
기다리는 안내가 표시되기 전에 앱이 종료되면 미완료 상태로 남긴다. 재실행 후 같은 순서 규칙으로 보여준다.
위험과 수용 조건: 첫 선택 직후 첫 돌봄과 상점 조건이 함께 생기면 첫 돌봄 안내를 먼저 보여준다. 새 개체 획득과 첫 업적 달성이 같은 순간이면 파티 안내를 먼저 보여준다. 상점 안내가 진행 중일 때 생긴 부화 조건은 기존 규칙대로 기다린다. 놀이공간 안내의 시작 계기는 아직 정하지 않았다. 순서 규칙을 놀이공간 안내의 시작 계기 확정으로 해석하지 않는다. 대기만 한 안내를 스킵으로 처리하지 않는다.
`[스펙 미확정]` 대기 안내의 표시 위치, 대기 중 해당 행동을 이미 완료한 경우의 안내 내용, 안내 상태의 저장 구현은 남아 있다. 다음 질문은 아직 표시하지 않은 대기 배너의 재실행 처리와 대기 중 처리된 대상의 배너 처리다.
변경과 의미 검수: 제품 결정, 단계별 안내 계약, SC-05·SC-10, Q-07, SR-02와 재개 안내를 확인했다. 발생 순서 우선과 같은 순간의 흐름 순서를 구분했다. 대기와 스킵을 구분했다. 순서 규칙과 놀이공간 안내의 시작 계기 미정을 구분했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 778개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 대기 배너의 무효화와 재실행 처리

날짜: 2026-09-22. 사용자 답변: “제안대로 진행.” 직전 제안인 대기 중 대상 변경 시 배너 폐기, 표시 전 종료 배너의 재실행 후 표시, 표시 중 종료 배너의 표시 완료 처리를 채택했다. 재실행 시 대기 배너 전체 폐기 안과 처리된 대상의 배너 유지 안은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: SR-10의 대기 배너 처리를 확정한다. 동시 발생 우선순위, 표시 시간·위치와 저장 구현은 제외한다.
SSOT: `docs/design.md`의 상태 알림 배너, `docs/specs/s5.md`의 알림 배너의 개별 표시, `docs/specs/s5-scenarios.md`의 SC-10, `docs/work/s5-design-system-v2/feedback.md`의 SR-10, `docs/progress.md`.
확정 범위: 배너가 기다리는 동안 대상이 더 이상 부화 가능·진화 가능·업적 미수령 상태가 아니면 그 배너를 표시하지 않는다. 대기열에서 제거한다.
아직 표시하지 않은 배너가 있는 상태에서 앱이 종료되면 재실행 후 이어서 표시한다. 대상이 여전히 해당 미처리 상태일 때만 표시한다.
배너를 표시하는 중에 앱이 종료되면 표시한 것으로 처리한다. 재실행 후 같은 상태를 다시 알리지 않는다. 해당 메뉴의 미처리 상태는 유지한다.
위험과 수용 조건: 부화 배너가 기다리는 동안 사용자가 그 알을 직접 열었으면 부화 배너를 표시하지 않는다. 진화 배너가 기다리는 동안 진화 조건이 더 이상 맞지 않으면 진화 배너를 표시하지 않는다. 업적 배너가 기다리는 동안 보상을 받았으면 업적 배너를 표시하지 않는다. 재실행 후 대기 배너를 이어서 표시해도 이미 표시한 배너는 반복하지 않는다. 배너 폐기는 대상의 게임 상태를 바꾸지 않는다.
`[스펙 미확정]` 동시 발생 시 우선순위, 표시 시간과 위치, 배너 상태의 저장 구현, 저장 실패 처리, 대기열의 소리 재생 시점은 남아 있다. 다음 질문은 배너 동시 발생 시 우선순위다.
변경과 의미 검수: 상태 알림 배너 결정, 알림 배너 계약, SC-10, SR-10과 재개 안내를 확인했다. 대기 배너 폐기와 게임 상태 변경을 구분했다. 표시 전 종료와 표시 중 종료를 구분했다. 이번 세션의 요약 문장 중 두 사실을 쉼표로 묶은 문장을 나눴다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 783개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 배너 동시 발생의 표시 순서

날짜: 2026-09-22. 사용자 답변: “그렇게 진행해.” 직전 제안인 발생 순서 우선, 같은 순간의 종류별 순서와 같은 종류의 목록 순서를 채택했다. 업적 우선 안과 구현 위임 안은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: SR-10의 배너 동시 발생 우선순위를 확정한다. 표시 시간·위치와 저장 구현은 제외한다.
SSOT: `docs/design.md`의 상태 알림 배너, `docs/specs/s5.md`의 알림 배너의 개별 표시, `docs/specs/s5-scenarios.md`의 SC-04, `docs/work/s5-design-system-v2/feedback.md`의 SR-10, `docs/progress.md`.
확정 범위: 여러 배너가 기다리면 먼저 생긴 배너부터 표시한다.
같은 순간에 생긴 배너의 종류가 다르면 부화 → 진화 → 업적 순서로 표시한다.
같은 순간에 생긴 배너의 종류가 같으면 화면의 목록 순서를 따른다. 부화 배너는 돌보미집 칸 순서를 따른다. 진화 배너는 파티 칸 순서를 먼저 따르고 그다음 박스 순서를 따른다. 업적 배너는 업적창 목록 순서를 따른다.
위험과 수용 조건: 사탕 사용으로 진화 가능과 업적 달성이 같은 순간에 생기면 진화 배너를 먼저 표시한다. 두 알이 같은 순간에 준비되면 돌보미집의 앞 칸 알부터 표시한다. 순서 규칙은 개별 표시와 순차 표시 규칙을 바꾸지 않는다. 대기 중 처리된 대상의 배너 폐기 규칙을 유지한다. 순서 규칙을 안내 표시 순서와 하나의 대기열로 합치는 결정으로 해석하지 않는다.
`[스펙 미확정]` 배너 표시 시간·위치·다중 화면 기준, 안내와 배너가 함께 생길 때의 표시 관계, 박스 목록의 정렬 기준, 배너 상태의 저장 구현은 남아 있다.
변경과 의미 검수: 상태 알림 배너 결정, 알림 배너 계약, 부화 계약, SC-04, SR-10과 재개 안내를 확인했다. 발생 순서, 같은 순간의 종류별 순서, 같은 종류의 목록 순서를 구분했다. 배너 순서와 안내 순서를 하나의 대기열로 합치지 않았다. 박스 정렬 기준은 미정으로 남겼다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 788개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 확정 전 취소와 남은 저장 실패의 처리

날짜: 2026-09-22. 사용자 답변: “그렇게 진행해.” 직전 제안인 진화·교체의 확정 전 취소, 공유 `sid` 종 교체, 첫 선택, 놀이공간 적용의 처리 4항목을 채택했다. 첫 선택 예외로 기본 후보를 자동 지급하는 안은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: Q-03에 남은 취소와 저장 실패 처리를 확정한다. 거래 식별·저장 기술과 실패 문구는 제외한다.
SSOT: `docs/design.md`, `docs/specs/s5.md`의 거래 실패 원칙, `docs/specs/s5-scenarios.md`의 SC-01·SC-06·SC-07·SC-08과 Q-03, `docs/progress.md`.
확정 범위: 진화나 교체의 결과 확인 단계에서 사용자가 취소하면 아무 상태도 바꾸지 않는다. 진화 조건과 교체 대상도 그대로 유지한다.
공유 `sid` 종 교체는 일반 교체와 같은 거래로 처리한다. 실패하면 현재 종과 파티 칸을 그대로 유지한다.
첫 선택을 확정하기 전에 창을 닫으면 개체를 만들지 않는다. 다음 실행 때 선택 화면을 다시 연다.
첫 선택 저장에 실패하면 선택 화면에 남는다. 사용자는 다시 고를 수 있다. 이 조작은 실패 전용 재시도가 아니라 정상 선택이다.
확정한 첫 개체와 시작 포인트는 재실행해도 중복 지급하지 않는다.
놀이공간 변경을 취소하거나 저장에 실패하면 적용 전 영역을 유지한다.
위험과 수용 조건: 진화 확인 화면에서 취소한 개체는 진화 가능 상태를 유지한다. 교체 확인에서 취소하면 두 개체의 위치와 표시 상태가 그대로다. 베베놈→아고용 교체 저장에 실패하면 파티 칸의 베베놈이 그대로 남는다. 첫 선택 전 앱을 종료해도 개체와 시작 포인트가 생기지 않는다. 첫 선택 저장 실패 후 다른 후보를 골라도 이전 실패 선택이 함께 지급되지 않는다. 놀이공간 저장 실패 후에도 포켓몬은 이전 영역에서 활동한다.
`[스펙 미확정]` 거래 식별·저장 방식, 결과 불명 상태의 내부 판정, 실패 문구와 표시 위치, 모니터 구성 변경 시 영역 복원은 남아 있다.
변경과 의미 검수: 제품 결정, 거래 실패 원칙, 교체·진화 계약, SC-01·SC-06·SC-07·SC-08, Q-03과 재개 안내를 확인했다. 확정 전 취소와 저장 실패를 구분했다. 첫 선택의 다시 고르기와 실패 전용 재시도를 구분했다. 교체 조작 순서와 영역 복원은 `[스펙 미확정]`으로 남겼다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 796개, JSON 7개다. `git diff --check` 통과. 설계 문서만 변경했다. 빌드와 런타임 검사는 실행하지 않았다.

### 문서 트리와 시나리오 트리 작성

날짜: 2026-09-22. 사용자 요청: “지금 작성중인 문서/시나리오 트리같은거 만들수있어? figma에 같이 적으면 좋을거같은데. 알아보기쉽게”. 기능 구현 미시작.
목표와 범위: 문서 구조와 SC-01~SC-11의 확정·미정 상태를 한눈에 보는 그림을 만든다. 저장소 문서에는 Mermaid로 둔다. Figma에는 같은 내용을 옮긴다. 새 게임 규칙은 추가하지 않는다.
SSOT: 그림의 원본은 `docs/README.md`의 문서 트리와 `docs/specs/s5-scenarios.md`의 시나리오 트리다. 그림과 본문이 다르면 본문을 따른다.
작업: `docs/README.md`에 폴더 구성과 결정 기록 순서를 보여주는 문서 트리를 추가했다. `docs/specs/s5-scenarios.md`에 흐름별 확정 규칙·열린 결정·보류 항목을 색으로 구분한 시나리오 트리를 추가했다.
위험과 수용 조건: 그림의 요약이 본문의 확정 범위를 넓히지 않는다. 가격·확률·성장 곡선을 확정으로 표시하지 않는다. 두 번째 업적 칸 조건은 보류로 표시한다. Figma 사본은 저장소 그림을 대체하지 않는다.
변경과 의미 검수: 시나리오 트리의 각 확정 노드를 해당 SC 본문과 대조했다. 열린 노드는 각 SC의 열린 결정에서 옮겼다. 보류 노드는 두 번째 업적 칸 조건 하나다. 문서 트리의 역할 설명은 문서 안내의 현재 기준 표와 대조했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 796개, JSON 7개다. `git diff --check` 통과. Mermaid 렌더링은 도구 설치 없이 확인할 수 없어 미실행이다.
Figma 반영: 사용자 지시 “여기가 이 프로젝트 작업하던 figma인데, 페이지 하나새로파서 작업해.”에 따라 pokebuddy 파일에 [06 · 문서·시나리오 트리](https://www.figma.com/design/MA3K41Y6omAi5mRu6YDFly/pokebuddy?node-id=236-121) 페이지 `236:121`을 새로 만들었다. 후속 정정으로 이 페이지의 이름과 프레임을 교체했다. [전체 설계 트리로 범위 정정](#전체-설계-트리로-범위-정정)을 따른다. 기존 페이지와 노드는 변경하지 않았다. 시나리오 트리 프레임은 `236:122`이다. 크기는 1166×1907이다. 문서 트리 프레임은 `237:121`이다. 크기는 751×1289다.
Figma 표현: 기존 Inter·Outfit 스타일은 한글 글리프가 없어 본문에 `Noto Sans KR`을 사용했다. 배경 `#F1F2EE`와 강조 `#0F766E`는 기존 기준을 따랐다. 상태는 컬러 테두리 대신 점과 옅은 배경으로 구분했다. 같은 행의 카드는 같은 높이로 맞췄다. 변수 바인딩은 하지 않은 문서용 그림이다.
Figma 검수: 각 단계의 `screenshot()`으로 11개 흐름, 3개 묶음, 보류 1개, 문서 트리의 `docs/` 하위 항목 18개와 결정 기록 순서 5단계를 확인했다. 잘린 문자와 겹침은 관측되지 않았다. 내용은 저장소 Mermaid와 같은 문구를 사용했다.

### 전체 설계 트리로 범위 정정

날짜: 2026-09-22. 사용자 정정: “아니 S5만이 아니잖아. 지금 프로젝트 전체 설계를 진행하는데 S5시나리오 트리라니 뭐하자는거지?” 직전 트리는 `s5-scenarios.md`의 파일 이름을 기준으로 범위를 S5로 좁혔다. 문서 트리도 `work/`에 S5 작업 폴더 하나만 넣었다. 이 범위 해석을 철회한다.
목표와 범위: 트리를 프로젝트 전체 설계와 전체 문서 구조로 다시 만든다. 새 게임 규칙은 추가하지 않는다.
SSOT: 전체 설계 트리의 원본은 `docs/progress.md`의 전체 설계 트리다. 영역별 상태는 같은 문서의 다음 행동 표와 `docs/design.md`를 따른다. 문서 트리의 원본은 `docs/README.md`다.
작업: `docs/specs/s5-scenarios.md`의 S5 한정 시나리오 트리를 제거했다. 대신 전체 설계 트리로 연결하는 문장을 넣었다. `docs/progress.md`에 전체 설계 트리를 추가했다. 루트는 제품 목적이다. 게임 규칙 6개 영역, 제품·시스템 4개 영역, 밸런스와 확장을 둔다. 영역마다 관련 SC 흐름과 확정·미정·보류·향후 항목을 표시했다. `docs/README.md`의 문서 트리를 `work/` 작업 폴더 10개와 `history/`·`contributing/`·`archive/`를 포함한 전체 구조로 바꿨다.
Figma 수정: 페이지 이름을 `06 · 전체 설계·문서 트리`로 바꿨다. 직전에 만든 S5 한정 프레임 `236:122`와 문서 트리 `237:121`을 삭제했다. 전체 설계 트리 `238:121`을 만들었다. 크기는 1196×2269다. 전체 문서 트리 `238:372`를 만들었다. 크기는 776×1699다. 기존 00~05 페이지는 변경하지 않았다.
위험과 수용 조건: 트리 요약이 기준 문서의 확정 범위를 넓히지 않는다. 확정 노드는 `docs/design.md`의 채택 결정과 이번 세션의 기록에서 옮겼다. 미정 노드는 다음 행동 표, 각 SC의 열린 결정, `docs/design.md`의 `[스펙 미확정]`에서 옮겼다. 두 번째 업적 칸 조건은 보류로 표시했다. 던전 탐험과 게임 시간은 향후 확장으로 표시했다. `s5` 파일·폴더 이름은 변경하지 않았다. 이름의 유래와 현재 범위를 문서 트리에 적었다.
변경과 의미 검수: 각 영역의 확정 항목을 제품 결정과 대조했다. 현재 구현 S4, 새 설계 기능 구현 미시작, S6 배포 대기, 플랫폼 확인 7건을 진행 현황과 대조했다. 저장소 Mermaid와 Figma 사본의 문구를 같게 유지했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 799개, JSON 7개다. `git diff --check` 통과. Figma는 각 단계의 `screenshot()`으로 3개 묶음, 12개 영역, 문서 트리의 `docs/` 하위 항목을 확인했다. 잘린 문자와 겹침은 관측되지 않았다. Mermaid 렌더링은 미실행이다.

### 업적 배너의 바로가기와 업적 안내 제거

날짜: 2026-09-22. 사용자 질문과 제안: “업적안내가 여러번 생기는 경우가 있나? 업적배너가 업적안내랑 같은역할을 하면 되는거아니야?” 사용자는 배너 구성을 `업적달성` / `[A] aaa달성` / `[수령하기]`로 예시했다. 확인 질문 뒤 사용자 결정: “수령하기가 아니라 바로가기로 바꾸고, 업적창으로 이동. 업적안내는 제거.” 기능 구현 미시작.
배경: 업적 안내는 튜토리얼 안내로 한 번만 시작한다. 업적 배너와 겹치는 경우는 첫 업적 달성 한 번뿐이었다. 배너에서 바로 수령하는 제안은 채택하지 않았다. 업적창 첫 진입 때만 안내를 유지하는 안도 채택하지 않았다.
목표와 범위: 업적 배너의 구성과 이동을 정한다. 업적 안내 튜토리얼을 제거한다. 배너의 크기·위치·표시 시간과 다른 배너의 버튼 구성은 제외한다.
SSOT: `docs/design.md`의 놀이공간과 직접 돌봄·도감·해금, `docs/specs/s5.md`의 알림 배너·첫 구매와 업적 보상 수령·단계별 안내, `docs/specs/s5-scenarios.md`의 SC-05, `docs/terms.md`, `docs/work/s5-design-system-v2/feedback.md`의 SR-02, `docs/progress.md`.
확정 범위: 업적 배너는 `업적 달성` 제목, 업적 표시와 이름, `바로가기` 버튼으로 구성한다. `바로가기`를 누르면 업적창의 해당 업적 수령 화면으로 이동한다. 보상 수령은 기존 규칙대로 업적창에서 사용자가 직접 한다.
업적 안내 튜토리얼을 제거한다. 업적창 첫 진입과 첫 업적 달성은 안내 시작 계기가 아니다. 업적 이용은 업적 배너와 업적창으로 익힌다. 사용법은 가이드북에서 확인한다.
동시 안내 순서에서 업적을 뺀다. 같은 순간의 순서는 첫 돌봄 → 상점 → 부화 → 파티 → 놀이공간이다.
철회: [파티와 업적 안내의 시작 조건](#파티와-업적-안내의-시작-조건)의 업적 안내 시작 계기를 현재 계약에서 철회한다. 파티 안내의 시작 조건은 유지한다.
위험과 수용 조건: 배너 표시와 `바로가기`만으로 보상을 지급하지 않는다. 배너를 누르지 않고 사라져도 업적은 업적창에 미수령으로 남는다. 업적창을 처음 열어도 튜토리얼 안내가 나오지 않는다. 1회 수령, 미수령 유지, 배너 대기·순서 규칙은 바꾸지 않는다. 이 결정을 부화·진화 배너의 버튼 구성으로 확대하지 않는다.
`[스펙 미확정]` 업적 표시 `[A]`의 형태, 배너 본문 클릭 동작, 부화·진화 배너에 같은 버튼 구성을 쓸지는 정하지 않았다. 다른 계기로 생긴 안내와 배너의 동시 표시 관계도 남아 있다.
변경과 의미 검수: 제품 결정, 알림 배너 계약, 첫 구매와 업적 보상 수령, 단계별 안내, SC-05, 용어사전, SR-02, 진행 현황과 전체 설계 트리를 확인했다. 배너의 이동과 업적창의 수령을 구분했다. 업적 안내 제거와 파티 안내 유지를 구분했다. 과거 결정 기록은 지우지 않고 철회를 연결했다. 조건과 용어 일치를 확인했다.
검증: 긴 heredoc의 따옴표 해석 오류로 첫 반영 명령은 실행되지 않았다. 변경 파일이 없음을 확인한 뒤 같은 내용을 스크립트 파일로 실행했다. CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 806개, JSON 7개다. `git diff --check` 통과. Figma 전체 설계 트리의 안내·업적·알림 행 `238:254`의 확정·미정 문구를 같은 내용으로 고쳤다. 스크린샷으로 잘림이 없음을 확인했다.

### 업적 알림의 우측 아래 표시와 업적창 dot

날짜: 2026-09-22. 사용자 정정: “안내랑 배너가 왜 계속 말을 겹치게하는거지?? 업적완료하면 완료했다 알림만 띄워. 거기에 업적창 바로가기있으면 바로가기하고 안하면은 업적창메뉴에 dot떠있는 구조 있잖아.” 후속 정정: “내가생각하는 업적관련은 우측아래에 띡 한번 뜨고 끝이야. 그게 알림이고 그게 베너고 두개가 같은거고.” 기능 구현 미시작.
배경: 튜토리얼 안내와 알림 배너는 서로 다른 기능이다. 업적 안내는 직전 결정으로 제거했다. 그런데도 직전 기록에 안내와 배너의 동시 표시 관계를 미정으로 남겼다. 이 미정 항목은 필요하지 않은 질문이었다.
확정 범위: 업적을 완료하면 업적 알림을 화면 우측 아래에 한 번 표시한다. 업적 알림과 업적 배너는 같은 것이다. 알림의 `바로가기`를 누르면 업적창으로 이동한다. 누르지 않으면 알림은 사라진다. 미수령 보상이 있으면 업적창 메뉴에 dot를 표시한다.
철회: [업적 배너의 바로가기와 업적 안내 제거](#업적-배너의-바로가기와-업적-안내-제거)와 [배너 동시 발생의 표시 순서](#배너-동시-발생의-표시-순서)에 남긴 안내와 배너의 동시 표시 관계 미정 항목을 철회한다. 안내와 배너 사이의 규칙은 설계 대상이 아니다.
위험과 수용 조건: 업적 알림을 같은 업적에 다시 띄우지 않는다. 알림을 놓쳐도 업적창 메뉴의 dot로 미수령을 확인할 수 있다. 우측 아래 위치와 dot 표시를 부화·진화 알림으로 확대하지 않는다.
`[스펙 미확정]` 부화·진화 알림의 위치, 다중 화면에서의 우측 아래 기준, 업적창 메뉴의 위치는 정하지 않았다.
변경과 의미 검수: 제품 결정, 알림 배너 계약, SC-05, 용어사전, 진행 현황과 전체 설계 트리에서 안내와 배너의 관계 표현을 검색했다. 현재 기준 문서에는 남지 않았다. 과거 결정 기록의 해당 미정 문장은 지우지 않고 이 절에서 철회했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 810개, JSON 7개다. `git diff --check` 통과. Figma 안내·업적·알림 행 `238:254`의 문구를 고치고 스크린샷으로 확인했다.

### 튜토리얼 용어 통일

날짜: 2026-09-22. 사용자 정정: “대체 왜 튜토리얼이 안내인거지?? 왜 너마음대로 용어를 그렇게 쓰는거야? 튜토리얼은 튜토리얼인데?”
배경: `docs/terms.md`의 정식 용어는 `튜토리얼`이다. 현재 기준 문서는 튜토리얼을 `파티 안내`, `부화 안내`, `단계별 안내`처럼 불렀다. 이번 세션도 그 표현을 이어 쓰고 늘렸다. 용어사전과 맞지 않는 사용이었다.
확정 범위: 튜토리얼 개념은 `튜토리얼`로만 부른다. `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`, `docs/progress.md`, `docs/work/s5-design-system-v2/feedback.md`의 튜토리얼 문맥에서 `안내`를 `튜토리얼`로 바꿨다. 튜토리얼 내용을 설명하는 동사 `안내한다`는 `알려준다`로 바꿨다. `docs/specs/s5.md`의 절 제목을 `행동에 따른 단계별 튜토리얼`로 바꾸고 그 앵커를 참조하는 링크를 고쳤다. 전체 설계 트리의 영역 이름을 `튜토리얼·업적·알림`으로 바꿨다.
유지 범위: 튜토리얼이 아닌 `안내`는 바꾸지 않았다. 예시는 돌보미집 만석 때의 이동 안내, 되돌릴 수 없음 안내, 상태별 안내 문구, 재개 안내, Figma `00 · Foundations`의 안내 페이지다. 과거 결정 기록의 제목과 문장은 이력으로 보존했다. 링크 대상의 과거 앵커도 바꾸지 않았다.
용어사전: `docs/terms.md`의 절 이름을 `튜토리얼과 가이드북`으로 바꿨다. 튜토리얼을 `안내`로 부르지 않는다는 사용 규칙을 추가했다.
Figma: 전체 설계 트리의 영역 이름과 행 `238:254`를 `튜토리얼·업적·알림`으로 바꿨다. 확정·미정 문구의 `안내`도 `튜토리얼`로 바꿨다. 페이지에 남은 `안내`는 문서 트리의 `README.md` 설명 하나이며 튜토리얼이 아니다.
사고와 복구: 용어 변환 스크립트의 마지막 단계가 이 파일을 쓰기 모드로 먼저 열어 기존 내용을 비웠다. 파일 링크 수가 810개에서 712개로 줄어 발견했다. 커밋 `206be6a`의 이 파일에 이번 세션에 추가한 12개 절을 원래 순서로 다시 붙여 복구했다. 절 원문은 이번 세션의 실행 명령과 스크립트에서 가져왔다. 커밋본 대비 변경은 앵커 한 줄 갱신과 추가 절뿐임을 `git diff`로 확인했다.
변경과 의미 검수: 튜토리얼 문맥의 `안내`와 일반 설명의 `안내`를 줄 단위로 구분했다. 남은 `안내` 14곳은 모두 튜토리얼이 아닌 이동·경고·문구·재개 설명이다. 조사를 받침에 맞게 고쳤다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 810개, JSON 7개다. `git diff --check` 통과.

### 관리 창의 탭과 헤더 구성

날짜: 2026-09-22. 제안: 탭은 파티 / 돌보미집 / 도감 / 상점 / 가방으로 둔다. 파티 탭의 파티 칸 아래에 박스 목록을 둔다. 업적창과 설정은 헤더 아이콘으로 둔다. 업적창 아이콘에는 미수령 보상이 있으면 dot를 표시한다. 사용자 답변: “박스목록 안에 돌보미집을 넣어.” 돌보미집의 위치만 수정했다. 나머지 제안에는 수정 지시가 없어 제안대로 반영했다. 박스·돌보미집·업적을 모두 탭으로 두는 안은 채택하지 않았다. 기능 구현 미시작.
목표와 범위: 관리 창의 탭과 헤더 구성을 정한다. 각 화면의 세부 배치와 Figma 수정은 제외한다.
SSOT: `docs/design.md`의 S5 설정창과 메뉴, `docs/specs/s5.md`의 화면 구조, `docs/specs/s5-scenarios.md`의 SC-04·SC-05와 화면·모듈 연결, `docs/terms.md`, `docs/progress.md`.
확정 범위: 관리 창의 탭은 파티 / 도감 / 상점 / 가방이다. 헤더에는 보유 포인트, 업적창 아이콘, 설정 아이콘을 둔다.
업적창은 헤더의 업적창 아이콘으로 연다. 미수령 보상이 있으면 업적창 아이콘에 dot를 표시한다. 직전 결정의 `업적창 메뉴`는 이 아이콘이다.
설정은 탭에서 빼고 헤더의 설정 아이콘으로 연다.
파티 탭에는 파티 칸 아래에 박스 목록을 둔다. 돌보미집은 박스 목록 안에 둔다.
위험과 수용 조건: 부화 배너를 누르면 파티 탭의 박스 목록 안 돌보미집으로 이동한다. 돌보미집이 가득 차서 알 구매를 막을 때도 같은 위치로 안내한다. 알은 구매 직후 돌보미집에 들어가므로 가방에 알 분류를 두지 않는다. 기존 Figma의 탭 5종은 이전 구성이며 이번 결정으로 수정 대상이 되었다. Figma는 이번 작업에서 수정하지 않았다.
`[스펙 미확정]` 박스 목록 안에서 돌보미집의 위치·접기·표시 형태, 박스 목록의 정렬 기준, 헤더 아이콘의 모양, 트레이가 여는 마지막 탭의 기준은 정하지 않았다.
변경과 의미 검수: 제품 결정, 화면 구조 계약, 돌보미집 계약, SC-04·SC-05, 화면·모듈 연결, 용어사전, 진행 현황과 전체 설계 트리를 확인했다. 사용자 수정 범위와 수정 지시 없이 반영한 제안 범위를 구분했다. 가방의 알 분류를 뺀 근거가 구매 직후 돌보미집 배치 결정임을 확인했다. 현재 기준 문서의 `업적창 메뉴`를 `업적창 아이콘`으로 맞췄다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 816개, JSON 7개다. `git diff --check` 통과. 이 파일은 반영 전 1995줄, 반영 후 2007줄로 기존 내용이 보존됐다. Figma 전체 설계 트리의 화면·모듈 행 `238:326` 문구를 고치고 스크린샷으로 확인했다.

### 로그인 시 시작의 기본값 켜짐

날짜: 2026-09-22. 제안: 설정에 PC 시작 시 자동 실행 옵션을 두고 기본값은 꺼짐으로 한다. 사용자 답변: “기본값은 켜짐”. 옵션 제공은 유지하고 기본값만 켜짐으로 정했다. 옵션 없이 항상 직접 실행하는 안은 채택하지 않았다. 기능 구현 미시작.
배경: 제품 목적에 컴퓨터를 켠 직후 앱을 켜고 싶다는 설명이 있다. 기존 S5 설정 설계의 일반 항목에 `로그인 시 시작`이 있다. 현재 앱 문구는 `settings.autostart`의 `자동 시작`이다. 새 명칭은 만들지 않았다.
SSOT: `docs/design.md`의 제품 목적, `docs/specs/s5.md`의 설정과 연결, `docs/progress.md`.
확정 범위: 설정 일반의 `로그인 시 시작` 항목은 기본값을 켜짐으로 둔다. 사용자는 설정에서 끌 수 있다.
위험과 수용 조건: 새로 설치한 앱은 OS 로그인 뒤 자동으로 실행된다. 사용자가 끄면 다음 로그인부터 자동으로 실행하지 않는다. 사용자의 “자동기능은 없고” 발언의 제외 범위는 이 결정으로 정의하지 않는다. 개발 실행에서는 기존 계약대로 `앱 설치 후 사용 가능`으로 표시한다.
`[스펙 미확정]` 기존 설치의 설정값을 새 기본값으로 바꿀지, OS별 등록 방식과 실패 처리는 이식·구현 설계에서 정한다.
변경과 의미 검수: 제품 목적, 설정과 연결 계약, 진행 현황과 전체 설계 트리를 확인했다. 기존 `로그인 시 시작` 항목을 사용하고 새 명칭을 만들지 않았다. 기본값 결정과 “자동기능은 없고” 발언의 범위를 구분했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 819개, JSON 7개다. `git diff --check` 통과. 이 파일은 2009줄에서 2018줄로 늘어 기존 내용이 보존됐다. Figma 제품·실행 행 `238:278` 문구를 고치고 스크린샷으로 확인했다.

### 자동기능 없음의 범위

날짜: 2026-09-22. 제안: “자동기능은 없고” 발언을 게임 진행을 대신 해 주는 기능이 없다는 뜻으로 정리한다. 사용자 답변: “맞아.” 기능 구현 미시작.
확정 범위: 자동기능 없음은 게임 진행을 대신 해 주는 기능이 없다는 뜻이다. 자동 먹이, 자동 부화, 자동 진화, 자동 보상 수령, 자동 교체를 제공하지 않는다.
로그인 시 시작 같은 앱 실행 편의 기능은 이 범위에 포함하지 않는다.
위험과 수용 조건: 기존에 사용자가 직접 하도록 정한 먹이·부화·진화·보상 수령·교체 규칙과 충돌하지 않는다. 새 규칙을 추가하지 않고 기존 결정의 공통 원칙으로 기록한다. 시간에 따른 친밀도·포인트 적립과 만복도 변화는 게임 상태의 시간 진행이며 자동기능이 아니다.
SSOT: `docs/design.md`의 제품 목적, `docs/progress.md`.
변경과 의미 검수: 제품 목적과 진행 현황, 전체 설계 트리를 확인했다. 자동기능 없음과 시간에 따른 상태 진행을 구분했다. 앱 실행 편의 기능을 범위에서 뺐다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 821개, JSON 7개다. `git diff --check` 통과. Figma 제품·실행 행 `238:278` 문구를 고치고 스크린샷으로 확인했다.
후속 수정: 튜토리얼 용어 변환에서 `docs/specs/s5.md`의 놀이공간 행 `화면 설명으로 안내`가 `화면 설명으로 튜토리얼`로 잘못 바뀐 것을 발견했다. `화면 설명으로 알려준다`로 고쳤다. 같은 유형의 잘못된 변환을 현재 기준 문서에서 검색했고 추가 발견은 없었다.

### 놀이공간 튜토리얼의 시작 계기

날짜: 2026-09-22. 제안: 놀이공간 튜토리얼은 첫 돌봄 튜토리얼이 끝나면 시작한다. 사용자 답변: “제안대로 진행.” 설정의 놀이공간 항목을 처음 열 때 시작하는 안은 채택하지 않았다. 기능 구현 미시작.
확정 범위: 첫 돌봄 튜토리얼을 마치거나 스킵하면 놀이공간 튜토리얼의 시작 조건이 생긴다. 스킵을 끝으로 보는 것은 기존 튜토리얼 진행 규칙과 개별 스킵 원칙을 따른다.
놀이공간 튜토리얼은 변경 방법을 화면 설명으로 알려준다. 실제 영역 변경을 요구하지 않는 기존 규칙을 유지한다.
위험과 수용 조건: 첫 선택 직후에는 첫 돌봄과 상점 조건이 먼저 생긴다. 발생 순서 규칙에 따라 첫 돌봄 → 상점 → 놀이공간 순서로 보인다. 첫 돌봄 튜토리얼을 스킵해도 놀이공간 튜토리얼은 따로 보인다. 놀이공간 튜토리얼을 닫으면 다시 보이지 않는다.
SSOT: `docs/design.md`의 놀이공간과 직접 돌봄, `docs/specs/s5.md`의 행동에 따른 단계별 튜토리얼, `docs/specs/s5-scenarios.md`의 SC-01, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 단계별 튜토리얼 표, SC-01과 진행 현황을 확인했다. 시작 계기와 동시 순서 규칙이 충돌하지 않음을 확인했다. 개별 스킵 원칙과 맞췄다. 조건과 용어 일치를 확인했다.

### 알 돌봄 행동의 채택

날짜: 2026-09-22. 제안: 쓰다듬기와 노래 들려주기를 알 돌봄의 기본 행동으로 채택한다. 부화시간 단축 도구는 지금 넣지 않는다. 사용자 답변: “제안대로 진행.” 세 후보를 모두 채택하는 안은 채택하지 않았다. 기능 구현 미시작.
배경: 세 후보는 사용자가 앞서 제시했다. 알 돌봄은 부화 준비 시간을 줄이고 결과 종을 정하는 행동 조건이 된다.
확정 범위: 알 돌봄 행동은 쓰다듬기와 노래 들려주기다. 두 행동은 선택한 알 하나에 적용한다.
상점의 부화시간 단축 도구는 채택하지 않는다. 기본 대기가 최대 5분이라 필요성이 낮다. 기본 대기 시간을 늘리면 다시 검토한다.
위험과 수용 조건: 알 돌봄과 포켓몬에게 하는 밥 주기·놀아주기를 구분한다. 알 A를 돌보면 알 A의 시간과 조건만 바뀐다. 상점에 부화시간 단축 도구를 추가하지 않는다.
`[스펙 미확정]` 행동별 단축량, 반복 인정 간격, 행동 조건표에서 두 행동을 조합하는 방식은 정하지 않았다.
SSOT: `docs/design.md`의 상점, `docs/specs/s5.md`의 활동에 따른 부화 대기시간 단축, `docs/specs/s5-scenarios.md`의 SC-04, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 부화 계약, SC-04, 진행 현황과 전체 설계 트리를 확인했다. 알 돌봄과 포켓몬 돌봄을 구분했다. 단축 도구 미채택과 재검토 조건을 구분했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 828개, JSON 7개다. `git diff --check` 통과. Figma 알·부화 행 `238:206` 문구를 고치고 스크린샷으로 확인했다.

### 진화의돌 채택

날짜: 2026-09-22. 제안: 원작에서 돌로 진화하는 종은 진화의돌을 진화 조건으로 둔다. 상점에서 사서 가방에 보관한다. 가방에서 사용하면 진화 확인 화면으로 이어진다. 확인하면 진화하고, 취소하면 돌을 쓰지 않는다. 사용자 답변: “채택”. 돌 진화 종에 레벨 등 다른 조건을 붙이는 안은 채택하지 않았다. 기능 구현 미시작.
배경: 진화의돌은 기존 S5 문서의 상품 제안이었다. 사용자 채택 근거가 없어 `[스펙 미확정]`으로 두었다. 이번 답변으로 기능 채택을 확인했다.
확정 범위: 원작에서 돌로 진화하는 종은 진화의돌이 진화 조건이다. 진화의돌은 상점에서 구매한다. 구매한 돌은 공통 가방에 보관한다.
가방에서 진화의돌과 대상 개체를 고르면 진화 확인 화면으로 이어진다. 확인하면 진화하고 돌 1개를 소비한다. 취소하면 돌과 개체를 그대로 둔다.
기존 직접 진화 원칙을 유지한다. 돌 사용과 진화는 하나의 거래로 처리한다. 실패하면 돌과 개체를 이전 상태로 유지한다.
위험과 수용 조건: 돌을 가졌다는 이유로 자동 진화하지 않는다. 대상이 아닌 개체에는 돌을 사용할 수 없다. 사례: 피카츄→라이츄, 이브이→부스터·샤미드·쥬피썬더.
`[스펙 미확정]` 돌 종류, 대상 종, 가격, 친밀도·시간 조건과의 관계, 여러 분기가 있는 종의 확인 화면은 종별 진화 조건표와 함께 정한다. 민트의 채택 여부는 별도로 확인한다.
SSOT: `docs/design.md`의 도감·해금과 상점, `docs/specs/s5.md`의 상세와 진화·상점과 가방, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 상세와 진화·상점과 가방 계약, SC-06, 용어사전, 진행 현황과 전체 설계 트리를 확인했다. 진화의돌 채택과 민트 미확인을 분리했다. 돌 사용과 직접 진화·거래 원칙을 맞췄다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 832개, JSON 7개다. `git diff --check` 통과. Figma 성장·진화 행 `238:222` 문구를 고치고 스크린샷으로 확인했다.

### 민트 유지

날짜: 2026-09-22. 제안: 민트를 유지한다. 상점에서 사서 가방에 보관한다. 가방에서 민트, 대상 개체, 바꿀 성격을 고르고 확인하면 성격이 바뀐다. 사용자 답변: “제안대로 진행”. 민트를 없애는 안은 채택하지 않았다. 기능 구현 미시작.
배경: 현재 S4 앱은 민트를 40포인트에 판매한다. 구매하면 성격이 바로 바뀐다. 새 설계에서는 채택 근거가 없어 `[스펙 미확정]`으로 두었다. 성격은 포켓몬의 움직임과 상태 변화에 영향을 준다.
확정 범위: 민트는 성격을 바꾸는 도구다. 상점에서 구매한다. 구매한 민트는 공통 가방에 보관한다.
가방에서 민트, 대상 개체, 바꿀 성격을 차례로 고른다. 확인하면 성격이 바뀌고 민트 1개를 소비한다.
현재와 같은 성격으로는 바꿀 수 없다. 이때 민트를 소비하지 않는다.
파티와 박스의 보유 개체 모두에게 사용할 수 있다. 숨긴 파티 개체도 대상이다. 사용으로 배치·표시 상태를 바꾸지 않는다.
민트 사용은 하나의 거래로 처리한다. 실패하면 민트와 개체의 성격을 이전 상태로 유지한다.
위험과 수용 조건: 현재 S4의 구매 즉시 적용과 새 설계의 가방 보관 후 사용을 구분한다. 성격을 바꿔도 개체 ID와 친밀도·레벨 등 다른 기록은 유지한다.
`[스펙 미확정]` 가격, 기존 저장의 민트 구매 이력 처리, 성격 선택 화면의 구성은 정하지 않았다.
SSOT: `docs/design.md`의 상점과 S5 설정창과 메뉴, `docs/specs/s5.md`의 상세와 진화·상점과 가방, `docs/specs/s5-scenarios.md`의 SC-06, `docs/terms.md`, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 상세와 진화·상점과 가방 계약, SC-06, 용어사전, 진행 현황과 전체 설계 트리를 확인했다. 현재 S4의 즉시 적용과 새 설계의 가방 사용을 구분했다. 사탕과 같은 대상 범위와 거래 원칙을 맞췄다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 837개, JSON 7개다. `git diff --check` 통과. Figma 성장·진화 행 `238:222` 문구를 고치고 스크린샷으로 확인했다.

### 오래 놀아주기 버프와 장난감 채택

날짜: 2026-09-22. 제안: 오래 놀아주기 버프와 장난감을 향후 확장으로 둔다. 사용자 결정: “둘 다 할거임.” 제안을 채택하지 않았다.
사용자 설명(오래 놀아주기): “오래 놀아주기는 한번 클릭이 아니라, 10초 내에 클릭이 또 있는걸 3번정도? 30초 정도 놀아주는건데, 10초내에 한번씩만 누르면 오래놀아주기 버프 활성화되는 그런느낌이야. 연속3번클릭말고.”
사용자 설명(장난감): “장난감은 넣고, 반응 애니메이션은 일반 반응과 동일하게하지만 파티클? 그런거만 더 나오게 추가하는거로.”
확정 범위(오래 놀아주기): 놀아주기를 누르면 놀이가 시작된다. 마지막 놀아주기 후 10초 안에 다시 놀아주기를 누르면 놀이가 이어진다. 놀이가 약 30초 이어지면 오래 놀아주기 버프를 활성화한다. 사용자 예시로는 10초 안에 이어 누르기를 3번 정도 한다.
짧은 시간에 빠르게 3번 누르는 것은 조건이 아니다. 빠르게 여러 번 눌러도 약 30초 유지 조건을 줄이지 않는다. 10초 안에 다시 누르지 않으면 놀이가 끊긴다.
확정 범위(장난감): 장난감을 채택한다. 장난감으로 놀아줄 때의 반응 애니메이션은 일반 놀아주기 반응과 같다. 파티클 같은 효과만 추가로 표시한다.
명칭: 사용자는 이 입력 방식의 이름을 기억하지 못했다. 게임에서는 보통 `콤보`라고 부른다. 제한 시간은 `콤보 유지 시간`이라고 부른다. 이 명칭은 채택 전 후보다. 문서는 사용자 용어 `오래 놀아주기`를 쓴다.
위험과 수용 조건: 한 번 클릭만으로 오래 놀아주기 버프가 켜지지 않는다. 1초 안에 3번 눌러도 켜지지 않는다. 10초보다 긴 간격이 생기면 놀이 유지 시간을 처음부터 다시 센다. 장난감이 새 반응 애니메이션 세트를 요구하지 않는다.
`[스펙 미확정]` 오래 놀아주기 버프의 효과·지속시간·대상 범위, 10초와 약 30초의 최종 수치, 버프 재활성화 조건을 정하지 않았다. 장난감의 입수처·가격·소모 여부·수치 효과, 장난감 놀이가 오래 놀아주기에 포함되는지도 정하지 않았다. `콤보` 명칭 채택은 사용자 확인이 필요하다.
SSOT: `docs/design.md`의 놀이공간과 직접 돌봄, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02와 Q-08, `docs/terms.md`, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 일상 사용 계약, SC-02, Q-08, 용어사전, 진행 현황과 전체 설계 트리를 확인했다. 사용자 예시 수치와 최종 수치 미정을 구분했다. 빠른 연속 클릭과 유지 조건을 구분했다. `콤보`를 채택 전 후보로 표시했다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 841개, JSON 7개다. `git diff --check` 통과. Figma 일상·돌봄 행 `238:169` 문구를 고치고 스크린샷으로 확인했다.

### 오래 놀아주기 버프 효과와 1회성 장난감

날짜: 2026-09-22. 제안: 오래 놀아주기 버프는 놀아준 한 마리의 친밀도 증가량을 일정 시간 높인다. 버프가 남아 있을 때 다시 달성하면 기본 지속시간으로 갱신한다. 장난감은 상점에서 한 번 사면 계속 쓰는 영구 도구로 한다. 장난감 놀이도 오래 놀아주기 유지 시간에 포함한다.
사용자 답변: “오래놀아주기 버프는 프리미엄먹이버프와 같은느낌이긴한데, 좀 더 낮은 효과로. 장난감은 1회성”. 영구 장난감 제안은 채택하지 않았다.
확정 범위(오래 놀아주기 버프): 프리미엄먹이 버프와 같은 방식이다. 놀아준 한 마리의 친밀도 증가량을 일정 시간 높인다. 효과는 프리미엄먹이 버프보다 낮다. 버프가 남아 있을 때 다시 달성하면 기본 지속시간으로 갱신한다. 남은 시간을 더하지 않는다. 갱신 방식은 수정 지시가 없어 제안대로 반영했다.
확정 범위(장난감): 장난감은 1회성이다. 장난감으로 한 번 놀아주면 1개를 소비한다.
위험과 수용 조건: 오래 놀아주기 버프는 다른 개체에 복제되지 않는다. 오래 놀아주기 버프의 배율이 프리미엄먹이 버프 배율 이상이 되지 않는다. 장난감이 없으면 장난감 놀이를 할 수 없다. 일반 놀아주기는 장난감 없이 계속 쓸 수 있다.
`[스펙 미확정]` 두 버프의 실제 배율과 지속시간, 프리미엄먹이 버프와 동시에 있을 때의 계산, 장난감의 입수처·가격, 장난감 놀이가 오래 놀아주기 유지 시간에 포함되는지는 정하지 않았다. 1회성 결정으로 포함 제안의 의미가 달라져 미정으로 남긴다.
SSOT: `docs/design.md`의 놀이공간과 직접 돌봄, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 일상 사용 계약, SC-02, 용어사전, 진행 현황과 전체 설계 트리를 확인했다. 사용자 수정 범위와 수정 지시 없이 반영한 갱신 방식을 구분했다. 1회성 결정으로 달라진 포함 여부를 미정으로 남겼다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 845개, JSON 7개다. `git diff --check` 통과. Figma 일상·돌봄 행 `238:169` 문구를 고치고 스크린샷으로 확인했다.

### 장난감의 드래그 사용과 같은 버프

날짜: 2026-09-22. 제안: 장난감은 상점에서 산다. 업적 보상으로도 줄 수 있게 연다. 장난감 놀이도 오래 놀아주기의 10초 이어가기에 포함한다.
사용자 답변: “장난감버프랑 오래놀아주기버프랑 같다고 보면돼. 장난감은 가방에서 드래그-포켓몬위에드랍 하는거로 놀아주기 같은 느낌이야.”
확정 범위: 장난감을 사용하면 오래 놀아주기 버프와 같은 버프를 준다. 30초 유지 조건 없이 장난감 사용으로 버프를 받는다. 같은 버프이므로 버프가 남아 있으면 기본 지속시간으로 갱신한다. 효과를 더하지 않는다.
장난감은 가방에 보관한다. 가방에서 장난감을 드래그해 포켓몬 위에 드롭하면 사용한다. 놀아주기 같은 반응을 보여준다. 반응은 일반 놀아주기와 같고 파티클 같은 효과를 더한다. 사용하면 장난감 1개를 소비한다.
해소: 장난감 놀이를 오래 놀아주기 유지 시간에 포함할지는 더 정하지 않는다. 장난감 사용 자체가 같은 버프를 준다.
위험과 수용 조건: 버프는 장난감을 드롭한 한 마리에게만 적용한다. 드롭을 취소하거나 포켓몬이 아닌 곳에 놓으면 장난감을 소비하지 않는다. 장난감과 오래 놀아주기로 버프를 두 개 겹쳐 받지 않는다.
`[스펙 미확정]` 장난감의 입수처와 가격은 답변에 포함되지 않아 미정이다. 드롭 대상이 놀이공간의 포켓몬인지 파티 칸의 개체인지, 관리 창과 놀이공간 사이 드래그의 구현, 숨긴 개체와 박스 개체에게 사용할 수 있는지는 정하지 않았다.
SSOT: `docs/design.md`의 놀이공간과 직접 돌봄, `docs/specs/s5.md`의 일상 사용 계약, `docs/specs/s5-scenarios.md`의 SC-02, `docs/terms.md`, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 일상 사용 계약, SC-02, 용어사전, 진행 현황과 전체 설계 트리를 확인했다. 같은 버프의 갱신 규칙을 기존 버프 규칙과 맞췄다. 답변에 없는 입수처는 미정으로 남겼다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 849개, JSON 7개다. `git diff --check` 통과. Figma 일상·돌봄 행 `238:169` 문구를 고치고 스크린샷으로 확인했다.

### 저해상도 와이어프레임 설계

날짜: 2026-09-22. 사용자 요청: “제안대로 하고 중간저장진행”. 직전 제안인 저해상도 와이어프레임 작성을 승인했다. 기능 구현 미시작.
목표와 범위: 확정한 관리 창 구성과 흐름을 흑백 박스로 그린다. 대상은 관리 창의 파티 탭(파티·박스 목록·돌보미집), 도감, 상점, 가방, 업적창, 설정과 놀이공간의 배너·장난감 드롭이다. 시각 디자인, 실제 문구, 수치는 제외한다. 기존 00~05 페이지는 변경하지 않는다.
방식: pokebuddy 파일에 `07 · 와이어프레임` 페이지를 만든다. 가장 많이 바뀐 파티 탭을 먼저 그리고 사용자 확인 뒤 나머지 화면을 이어서 그린다. 화면 폭은 기존 Screen 기준 720이다. 각 영역에 관련 흐름 ID를 적는다.
SSOT: 화면 구조는 `docs/specs/s5.md`의 화면 구조를 따른다. 와이어프레임은 배치 검토용이며 계약을 새로 정하지 않는다. 그림에서 새로 정한 배치는 `[스펙 미확정]` 제안으로 기록한다.
위험과 수용 조건: 확정하지 않은 배치를 확정 규칙으로 기록하지 않는다. 기존 Figma 탭 5종은 수정하지 않는다. 컬러 테두리로 강조하지 않는다.
작업(WF-01): pokebuddy 파일에 [07 · 와이어프레임](https://www.figma.com/design/MA3K41Y6omAi5mRu6YDFly/pokebuddy?node-id=252-121) 페이지 `252:121`을 만들었다. `WF-01 관리 창 · 파티 탭` 프레임 `252:122`은 720×945다. 헤더에 포인트·업적(dot)·설정 아이콘을 두었다. 탭은 파티 / 도감 / 상점 / 가방이다. 파티 칸 2열×3행에 표시·숨김·빈 칸·잠긴 칸을 넣었다. 그 아래 박스 목록 안에 돌보미집 알 6칸과 보관 개체 목록을 넣었다.
`[스펙 미확정]` 그림에서 새로 둔 배치는 제안이다. 대상은 칸 카드 안의 친밀도·만복도 표시, 돌보미집을 박스 목록 맨 위에 두는 위치, 알 칸의 `열기` 버튼, 보관 개체 4열 목록, 정렬 버튼 자리다. 780 높이를 넘는 부분은 본문 스크롤로 본다.
검수: `screenshot()`으로 잘린 문자와 겹침이 없음을 확인했다. 흑백 톤만 사용했다. 컬러 테두리 강조는 쓰지 않았다. 빈 칸은 회색 점선, 잠긴 칸은 회색 면으로 구분했다.
후속 수정(WF-01): 사용자 요청: “돌보미집 ui만 좀 더 개선해봐. 너무 길어보여.” 돌보미집 카드 `252:190`을 삭제했다. 한 줄 띠 `255:121`로 바꿨다. 왼쪽에 `돌보미집`과 `알 2 / 6`을 두었다. 오른쪽에 80×84 알 칸 6개를 두었다. 알 칸에는 알 그림과 남은 시간 또는 `열기` 버튼 한 줄만 둔다. 빈 칸은 글자 없는 점선 칸이다. 설명은 카드 밖 한 줄 주석으로 옮겼다. 카드 높이는 약 200에서 110으로 줄었다. 화면 높이는 945에서 874다. `[스펙 미확정]` 한 줄 띠 배치는 제안이다.
작업(WF-02~WF-08): 사용자 요청: “나머지 화면 이어서 진행해”. 같은 페이지에 7개 프레임을 추가했다. 모든 프레임은 y=0에 가로로 배치했다.

| 프레임 | 노드 | 크기 | 내용 |
|---|---|---|---|
| WF-02 업적창 | `256:121` | 720×627 | 헤더 업적 아이콘 활성. 받기·미달성·받음 상태와 보류한 두 번째 칸 업적 |
| WF-03 가방 | `257:121` | 720×873 | 분류 칩, 도구 12종 목록, 경험사탕 사용 패널(대상 → 수량 → 미리보기 → 사용) |
| WF-04 상점 | `258:121` | 720×977 | 알·포켓몬·도구·파티 칸 분류. 가격은 `000P` 자리 표시 |
| WF-05 도감 | `259:121` | 720×809 | 지방·검색·필터, 획득·해금·미해금 목록, 리전폼 별도 항목, 이로치 표시, 입수 방법 상세 |
| WF-06 놀이공간 | `260:121`, 주석 `260:165` | 1280×720 | 우측 아래 업적 배너, 배고픔 말풍선, 우클릭 메뉴, 가방에서 포켓몬 위로 장난감 드롭 |
| WF-07 설정 | `261:121` | 720×975 | 헤더 설정 아이콘 활성. 일반 항목(로그인 시 시작 켜짐, 알림 소리, 놀이공간)과 연결 탭 미리보기 |
| WF-08 첫 선택 | `261:230` | 720×462 | 후보 목록, 선택 표시, 진화 경로, `함께하기` |

선택 상태는 컬러 테두리 대신 왼쪽 세로 막대와 옅은 배경으로 표시했다. 같은 줄의 칩과 버튼은 테두리 유무에 맞춰 안쪽 여백을 보정했다.
`[스펙 미확정]` 그림에서 새로 둔 배치는 제안이다. 업적창·설정을 탭 본문 자리에 여는 형태, 업적 목록 정렬, 가방 분류 이름과 사용 패널 위치, 상점 분류와 구매 확인 단계, 도감 상세 위치와 필터 이름, 가이드북을 설정에 두는 진입점, 첫 선택 후보 목록, 모든 화면 문구가 해당한다.
검수: 각 프레임의 `screenshot()`과 페이지 전체 캡처로 잘린 문자와 겹침을 확인했다. WF-06의 주석이 우클릭 메뉴를 가려 장면 밖 아래로 옮겼다. WF-03의 경험사탕 XL 수량 `×0`을 `×1`로 고쳤다. WF-07의 연결 영역 제목을 `연결 탭 미리보기`로 바꿨다. 기존 00~06 페이지는 변경하지 않았다.
기록 반영: `docs/progress.md`의 화면·모듈 행, 재개 순서, 재개 방향과 전체 설계 트리를 갱신했다. Figma 전체 설계 트리의 화면·모듈 행 `238:326`도 같은 문구로 고쳤다. CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 854개, JSON 7개다. `git diff --check` 통과. 이 기록은 아직 커밋하지 않았다.
후속 수정(WF-01 파티 칸): 사용자 지적: “파티탭의 파티 ui가 기존에만든거랑 많이달라. 기존거에 만복도가 없긴한데”. 대상은 사용자가 준 노드 `252:153`이 속한 파티 칸 목록이다.
기준: 05 · Screens의 `Party / Approved UI` `217:1705`를 확인했다. 카드는 왼쪽 원형 초상, 오른쪽의 레벨·상태·이름·타입 배지·성격·친밀도 수치와 막대로 구성된다. 빈 칸과 잠긴 칸은 가운데 아이콘·제목·설명으로 구성된다.
작업: 기존 칸 카드 6개 `252:153`~`252:179`를 삭제했다. 승인 UI 구조를 흑백으로 옮긴 카드 6개 `263:121`~`263:189`를 만들었다. 친밀도 막대 아래에 만복도 수치와 막대만 추가했다. 카드는 330×196이며 같은 줄의 카드 높이를 맞췄다. 빈 칸은 승인 UI처럼 흰 카드로 바꿨다. 막대 채움은 수치 비율에 맞췄다. WF-01 높이는 1208이다.
용어: 승인 UI의 `빈 슬롯`, `잠긴 파티칸`, `도감에서 소환`은 용어사전과 현재 규칙에 맞지 않는다. 와이어프레임은 `빈 칸`, `잠긴 칸`, `박스에서 배치`를 사용했다. 05 · Screens는 수정하지 않았다. `[스펙 미확정]` 승인 UI 문구 정리와 만복도 막대의 시각 디자인은 Figma 후속 작업이다.
후속 수정(WF-01 파티 칸): 사용자 요청: “너무긴데? 친밀도랑 만복도를 같은 1줄로.” 포켓몬 칸의 친밀도와 만복도를 한 줄에 나란히 두었다. 각 항목은 이름, `85/100` 형식의 수치, 막대로 구성한다. 칸 카드 높이는 승인 UI와 같은 160으로 줄였다. WF-01 높이는 1100이다. 막대 채움은 수치 비율에 맞췄다. `[스펙 미확정]` 두 막대를 한 줄에 두는 배치는 제안이다.

### 박스 메뉴 분리

날짜: 2026-09-22. 사용자 지시: “박스도 메뉴로 구성하자.” 파티 탭의 파티 칸 아래에 박스 목록을 두던 [관리 창 구성](#관리-창의-탭과-헤더-구성)을 바꾼다. 기능 구현 미시작.
해석: 박스를 파티 탭 안의 목록이 아니라 관리 창의 별도 메뉴로 둔다. 메뉴는 기존 탭 줄의 한 항목으로 해석했다. 헤더 아이콘으로 두는 뜻이면 사용자 정정이 필요하다.
확정 범위: 관리 창의 탭은 파티 / 박스 / 도감 / 상점 / 가방이다. 박스는 파티 바로 다음에 둔다. 파티 탭에는 파티 칸만 둔다.
돌보미집은 [박스 목록 안에 둔 결정](#관리-창의-탭과-헤더-구성)을 유지해 박스 메뉴 안에 둔다. 업적창과 설정은 헤더 아이콘으로 유지한다.
위험과 수용 조건: 부화 배너를 누르면 박스 메뉴의 돌보미집으로 이동한다. 돌보미집이 가득 차서 알 구매를 막을 때도 같은 위치로 안내한다. 파티가 비었을 때의 복귀 경로는 트레이 → 관리 화면 → 박스에서 개체 배치가 된다. 파티 칸과 박스 개체의 교체는 다른 탭 사이의 조작이 된다.
`[스펙 미확정]` 파티 칸에서 교체를 시작할 때 박스 개체를 고르는 화면, 박스 메뉴 안 돌보미집의 위치, 박스 목록의 정렬 기준은 정하지 않았다.
SSOT: `docs/design.md`의 상점과 S5 설정창과 메뉴, `docs/specs/s5.md`의 화면 구조, `docs/specs/s5-scenarios.md`의 SC-04·SC-07과 화면·모듈 연결, `docs/terms.md`, `docs/progress.md`.
변경과 의미 검수: 제품 결정, 화면 구조 계약, SC-04, 화면·모듈 연결, 용어사전, 진행 현황과 전체 설계 트리에서 `박스 목록`과 탭 목록을 검색해 고쳤다. 돌보미집 위치 결정은 유지했다. 메뉴의 해석을 기록해 사용자 정정을 받을 수 있게 했다. 조건과 용어 일치를 확인했다.
Figma 반영: WF-01의 박스 영역 `252:184`를 새 프레임 `WF-01b 관리 창 · 박스 탭` `265:139`로 옮겼다. WF-01 아래 y=802에 두었고 크기는 720×539다. WF-01은 파티 칸만 남아 높이가 722가 됐다. WF-01·02·03·04·05·07의 탭 줄에 `박스` 탭을 파티 다음에 추가했다. 화면별 활성 탭을 확인했다. WF-05에서 복제로 생긴 박스 탭 활성 표시와 WF-07의 이름 없는 밑줄을 바로잡았다. Figma 전체 설계 트리의 화면·모듈 행 `238:326`도 고쳤다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 859개, JSON 7개다. `git diff --check` 통과. 현재 기준 문서에서 `파티 탭 안 박스 목록` 표현이 남지 않았음을 검색했다.
후속 수정(WF-03 사용 패널): 사용자 요청: 노드 `257:223` “이거 ui 개선”. 대상은 경험사탕 사용 패널이다. 단계 번호를 세로로 나열하던 패널 `257:223`을 삭제했다. 두 열 패널 `269:121`로 바꿨다. 머리에 아이템 아이콘·이름·보유 수량과 닫기를 두었다. 왼쪽에 `파티 | 박스` 전환과 초상·이름·레벨·숨김 표시가 있는 대상 목록을 두었다. 오른쪽에 대상의 현재 경험치 막대, 수량 조절과 `최대`, 레벨 변화·획득·소멸 미리보기, `취소`와 `2개 사용`을 두었다. 선택한 대상은 왼쪽 막대와 옅은 배경으로 표시했다. 패널 높이는 313에서 289다. WF-03 높이는 849다.
흐름 확인: 대상 → 수량 → 미리보기 → 사용 순서와 한 개체 대상, 수량 한도, 미리보기만으로 상태를 바꾸지 않는 계약은 그대로다. `[스펙 미확정]` 두 열 배치, `최대` 버튼, 사용 버튼에 수량을 적는 문구는 제안이다.

### 박스와 파티 사이 배치·교체 흐름

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-1. 박스가 별도 메뉴가 된 뒤 박스 개체를 파티로 보낼 진입점이 없었다. 사용자 지시: “p1하나씩 같이 처리하고”. 제안에 대한 사용자 답변: “제안대로 진행”. 기능 구현 미시작.
확정 범위: 박스 개체의 상세에서 파티에 빈 칸이 있으면 `파티에 배치`를 제공한다. 빈 칸이 없으면 `교체`를 제공한다. `교체`를 누르면 파티 칸 고르기로 이어진다.
파티 개체의 상세에서 `교체`를 누르면 박스 개체 고르기로 이어진다. 빈 파티 칸을 누르면 박스 개체 고르기로 이어진 뒤 그 칸에 배치한다.
박스 개체 고르기는 박스 목록을 검색·정렬과 함께 보여준다. 파티 칸 고르기는 파티 2열×3행을 작게 보여준다. 세 경로 모두 같은 개체 선택 창을 쓴다.
확인하면 배치나 교체를 한 번에 처리한다. 들어온 개체는 숨김 상태다. 취소하면 아무것도 바꾸지 않는다. 기존 교체 거래·취소 규칙을 따른다.
위험과 수용 조건: 박스 탭에서 고른 개체를 파티 탭으로 옮겨 가 칸을 다시 찾지 않아도 된다. 파티가 가득 찬 상태에서 `파티에 배치`를 보여주지 않는다. 공유 `sid` 종 교체는 기존 규칙대로 같은 `sid`가 있는 칸을 교체한다.
`[스펙 미확정]` 정렬 기준과 검색 대상 필드, 개체 선택 창의 창·모달 형태는 정하지 않았다.
SSOT: `docs/specs/s5.md`의 개체 획득과 파티 교체, `docs/specs/s5-scenarios.md`의 SC-07, `docs/progress.md`.
Figma 반영: WF-01b의 박스 설명 `252:189`를 상세의 `파티에 배치`·`교체` 경로로 고쳤다. `WF-09a 개체 선택 · 박스 개체 고르기` `270:121`와 `WF-09b 개체 선택 · 파티 칸 고르기` `270:164`를 x=6960, 7760에 추가했다. 두 프레임은 720×780이며 어두운 배경 위 600폭 대화상자다. 09a는 이름 검색, 정렬 자리, 박스 개체 목록, `취소`·`교체`를 둔다. 09b는 파티 2열×3행 미니 칸, 숨김 마커, 고를 수 없는 잠긴 칸, `취소`·`교체`를 둔다. 선택은 왼쪽 막대와 옅은 배경이다. `[스펙 미확정]` 대화상자 형태, 제목 문구, 정렬 기준은 제안이다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 863개, JSON 7개다. `git diff --check` 통과. 두 프레임은 `screenshot()`으로 잘림과 겹침이 없음을 확인했다.

### 숨김 표시 통일

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-2. WF-03 가방 사용 패널의 대상 목록은 숨김을 `숨김` 글자 태그로 표시했다. 사용자 질문: “숨김이 어디에있는거야?” 위치를 확인한 뒤 사용자 답변: “제안대로 진행해.” 마커와 글자를 함께 쓰는 안은 채택하지 않았다.
작업: 이브이 행 `269:141`의 글자 태그 `269:146`을 삭제했다. 작은 초상에 숨김 마커 `274:122`를 붙였다. 숨김 표시는 파티 칸, 사용 패널, 파티 칸 고르기에서 모두 초상 우측 위 몬스터볼 마커다. 기존 규칙을 적용한 화면 수정이며 새 규칙은 없다.

### 원작식 박스 구조와 검색

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-3. 박스 목록의 검색·정렬을 논의하던 중 사용자 질문: “박스는 기존 포켓몬처럼 박스 41개인가? 그거로 생각하긴했는데”. 원작처럼 여러 박스를 넘겨 보는 구조를 생각했다는 뜻으로 확인했다. 기존 [박스 수용 정책](#파티-칸과-박스-수용-정책)의 “제한없이”와 충돌하지 않도록 박스 개수를 물었다. 사용자 답변: “제안대로 진행”. 41개 고정안은 채택하지 않았다. 원작의 박스 수는 확인하지 않았다.
확정 범위: 박스는 원작처럼 여러 개의 박스로 나눈다. 한 박스는 30칸(6열×5행)이다. 사용자는 `◀ 박스 1 ▶`처럼 박스를 넘겨 본다. 각 박스는 이름과 `12 / 30` 같은 사용 칸 수를 보여준다.
박스가 모두 차면 새 박스를 자동으로 추가한다. 박스 보유 개체 수 제한 없음과 박스 수량 제한으로 획득을 거부하지 않는 기존 결정을 유지한다.
검색은 모든 박스를 대상으로 한다. 검색 결과를 누르면 그 개체가 있는 박스로 이동한다. 박스 목록이 나오는 박스 탭, 박스 개체 고르기, 가방 사용 패널의 박스 목록은 같은 검색 줄을 쓴다. 가방 사용 패널에서는 `박스`를 골랐을 때만 검색 줄이 나타난다.
위험과 수용 조건: 박스 수가 늘어도 원하는 개체를 검색으로 찾을 수 있다. 파티가 가득 찬 상태에서 새 개체를 얻으면 빈 칸이 있는 박스에 들어간다. 모든 박스가 차면 새 박스를 만든 뒤 넣는다.
`[스펙 미확정]` 새 개체가 들어갈 칸의 순서(앞 박스의 첫 빈 칸 제안), 박스 이름 변경, 박스 안 이동과 정렬 기준, 가방 사용 패널의 박스 표시 형태는 정하지 않았다.
SSOT: `docs/design.md`의 도감·해금, `docs/specs/s5.md`의 파티 표시와 박스 보관·파티 칸 확장과 박스 수용·화면 구조, `docs/specs/s5-scenarios.md`의 SC-07, `docs/terms.md`, `docs/progress.md`.
Figma 반영: WF-01b의 보관 개체 목록 `252:217`·`252:222`를 삭제했다. 박스 넘김·검색 줄 `275:121`과 6×5 박스 칸 `275:134`를 넣었다. 예시는 박스 1의 12 / 30이다. 넘김 버튼 높이를 검색칸과 같은 30으로 맞췄다. WF-09a의 개체 목록을 박스 넘김 `276:121`과 6×5 칸 `276:129`로 바꿨다. WF-09a의 검색칸 문구를 `이름 검색 (모든 박스)`로 맞췄다. WF-03 사용 패널의 설명을 박스 선택 시 검색 줄과 박스 넘김이 나타나는 내용으로 고쳤다. WF-01b 높이는 923이다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 869개, JSON 7개다. `git diff --check` 통과. 바뀐 프레임은 `screenshot()`으로 확인했다.

### 업적창과 설정의 모달

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-4. 업적창·설정 화면의 `←` 복귀 대상과 탭 줄 표시가 정해지지 않았다. 사용자 결정: “업적이랑 설정은 모달처럼.” 사용자가 WF-09a의 대화상자 `270:123`을 예로 들었다. 직전 제안인 탭 본문 자리에 여는 형태와 탭 줄 숨김 안은 채택하지 않았다. 기능 구현 미시작.
확정 범위: 헤더의 업적창 아이콘과 설정 아이콘은 보던 탭 위에 모달을 연다. 모달은 어두운 배경 위 대화상자다. 모달을 닫으면 보던 탭으로 돌아간다. 탭은 바뀌지 않는다. `←`는 두지 않고 닫기 `✕`를 둔다.
해소: 복귀 대상과 탭 줄 표시 문제는 모달 형태로 생기지 않는다.
위험과 수용 조건: 가방에서 업적창을 열고 닫으면 가방이 그대로 보인다. 모달이 열린 동안 뒤의 탭은 조작하지 않는다. 설정은 내용이 길어 모달 안에서 스크롤한다. 알림 배너의 `바로가기`는 업적창 모달을 연다.
`[스펙 미확정]` 관리 창이 닫혀 있을 때 `바로가기`로 열 탭, Esc와 바깥 클릭으로 닫기, 모달 크기는 정하지 않았다. 관리 창이 닫혀 있으면 마지막 탭을 열고 그 위에 모달을 띄우는 안을 제안한다. 트레이가 관리 화면의 마지막 탭을 여는 기존 계약과 맞춘 안이다.
SSOT: `docs/design.md`의 S5 설정창과 메뉴, `docs/specs/s5.md`의 화면 구조와 첫 구매·업적 보상 수령, `docs/specs/s5-scenarios.md`의 화면·모듈 연결, `docs/terms.md`, `docs/progress.md`.
Figma 반영: 기존 WF-02 `256:121`와 WF-07 `261:121`을 삭제했다. 같은 자리에 `WF-02 업적창 (모달)` `278:121`과 `WF-07 설정 (모달)` `278:243`을 만들었다. 두 장면은 720×780이다. 뒤에 파티 탭 복제본을 두고 어두운 배경 위에 600폭 대화상자를 올렸다. 헤더의 해당 아이콘은 활성 표시다. 업적 대화상자는 기존 업적 행 4개와 `✕`를 담는다. 설정 대화상자는 높이 680으로 고정하고 안쪽을 스크롤 영역으로 두었다. `← 설정` 제목은 없앴다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 873개, JSON 7개다. `git diff --check` 통과. 두 장면은 `screenshot()`으로 확인했다.

### 포켓몬 위 우클릭 메뉴

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-5. 개체 항목과 앱 전체 항목이 구분되지 않아 `종료`를 잘못 누르기 쉬웠다. 사용자 결정: “종료는 빼고 제안한대로 구분선 넣고 진행. 잘 구분해야하는게 놀이공간의 우클릭이 아니라, 포켓몬위에서 우클릭이야. 놀이공간우클릭으로 해버리면 화면 모든 우클릭 막을 수 있으니 조심해야해”. `pokebuddy 종료` 이름 변경 제안은 종료 제거로 대체했다.
확정 범위: 우클릭 메뉴는 포켓몬 위에서 우클릭할 때만 연다. 포켓몬이 없는 놀이공간 영역의 우클릭은 가로채지 않는다. 그 우클릭은 뒤의 다른 앱으로 그대로 넘긴다.
메뉴는 `이름·상태` / `밥 주기`·`놀아주기` / `설정`의 세 묶음이다. 묶음 사이에 구분선을 둔다. `종료`는 우클릭 메뉴에서 뺀다. 종료는 트레이 메뉴에서 한다.
위험과 수용 조건: 놀이공간을 화면 전체로 두어도 빈 곳 우클릭으로 다른 앱의 메뉴가 열린다. 포켓몬 그림의 투명한 부분은 포켓몬으로 보지 않는다. 기존 구현은 그림의 알파값으로 대상을 판정하고 대상이 없으면 클릭을 통과시킨다. 우클릭 메뉴에서 앱을 끌 수 없다.
`[스펙 미확정]` `docs/specs/s5.md`의 “우클릭 메뉴는 개체 상세를 연다”와 `이름·상태` 줄의 관계, 겹친 포켓몬 위 우클릭의 대상, 알파 판정의 임계값은 정하지 않았다.
SSOT: `docs/design.md`의 현재 표시 구현과 S5 설정창과 메뉴, `docs/specs/s5.md`의 창과 보안, `docs/progress.md`.
Figma 반영: WF-06의 우클릭 메뉴 `260:133`을 `이름·상태` / `밥 주기`·`놀아주기` / `설정` 세 묶음과 구분선 두 개로 다시 만들었다. `종료`를 뺐다. 메뉴를 피카츄 바로 위로 옮겼다. 빈 곳 우클릭을 뒤 앱으로 넘긴다는 주석 `280:133`과 우클릭 지점 표시 `280:136`을 추가했다. 장면 주석 `260:169`도 고쳤다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 47개, 파일 링크 877개, JSON 7개다. `git diff --check` 통과. 장면은 `screenshot()`으로 확인했다.

### 리자드 명칭 정정

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-6. 사용자 답변: “그렇게해”.
정정: 파이리의 진화형 공식 명칭은 `리자드`다. [개체 레벨과 종별 입수처](#개체-레벨과-종별-입수처)의 사용자 원문은 `리저드`로 적었다. 이후 문서가 이 표기를 따랐다. 사용자 원문 인용과 당시 결정 기록의 `리저드`는 이력으로 보존한다. 그 기록의 `리저드`는 `리자드`로 읽는다.
작업: `docs/design.md`, `docs/specs/s5.md`, `docs/specs/s5-scenarios.md`의 `리저드`를 `리자드`로 고쳤다. 고친 곳은 각각 2곳, 3곳, 2곳이다. Figma WF-05 도감과 WF-08 첫 선택의 표기도 고쳤다. 코드와 데이터에는 이 표기가 없었다.

### 빠진 상태 예시 추가

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P1-7. 정상 상태만 있고 빈 상태와 불가 상태가 없었다. 사용자 답변: “진행”.
작업: 각 화면 바로 아래에 상태 예시 프레임을 추가했다. `WF-08b 첫 선택 · 미선택` `282:121`은 선택 표시가 없고 `함께하기`가 비활성이다. `WF-04b 상점 · 구매 불가` `282:165`는 돌보미집 6 / 6인 랜덤알과 포인트가 부족한 경험사탕 L의 비활성 `구매`, `돌보미집 보기`를 보여준다. `WF-01c 박스 · 빈 박스` `282:193`은 0 / 30 박스와 `보관 개체 없음` 한 줄을 보여준다. `WF-05b 도감 · 검색 결과 없음` `282:326`은 검색어와 `검색 결과 없음` 한 줄을 보여준다.
표현: 비활성 버튼은 회색 면과 흐린 글자다. 불가 이유는 카드 안 한 줄로 쓴다. 빈 상태 문구는 한 줄만 둔다.
`[스펙 미확정]` 상태 문구, 미해금 종의 검색 포함 여부, `돌보미집 보기`의 위치는 제안이다. 기존 규칙인 만석 시 구매 차단과 차감 없음은 그대로다.
검증: 네 프레임은 `screenshot()`으로 확인했다.

### 검수 P2 가벼운 항목 처리

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P2. 사용자 지시: “p2는 p1다 한 다음에 한번 보자.” 가벼운 항목과 결정이 필요한 항목을 나눈 제안에 사용자 답변: “그렇게 하자”.
처리: 돌보미집의 남은 시간을 `3:12 남음`으로 적었다. 빈 알 칸의 점선을 빼고 빈 박스 칸과 같은 옅은 회색 면으로 바꿨다. 두 변경은 WF-01b와 WF-01c에 적용했다.
업적 모달의 `미달성`을 `받음`과 같은 회색 면 상태 표시로 바꿨다. 예시 순서를 미수령 → 미달성 → 수령 완료로 바꿨다.
모든 탭 줄의 선택 탭에 승인 UI처럼 옅은 배경을 더했다. 선택 밑줄은 사용자 수정 내비게이션 규칙대로 유지했다. 탭의 안쪽 여백을 모두 같게 맞췄다.
WF-06의 가방 패널 제목을 `관리 창 · 가방 탭`으로 바꿨다.
변경 없음: 잠긴 칸 순서는 이미 구매 2칸이 이어지고 업적 칸이 마지막이다. 출처별 칸을 고정 순서로 해석하지 않는 기존 규칙이 있어 바꾸지 않았다.
남은 P2: 결정이 필요한 연결 `미설치` 행, 놀이공간 선택과 `영역 지정`, 선택 표현 규칙, 상점 버튼 반복과 0P 동사, 우클릭 상태 표현을 하나씩 다룬다. 현재·예상 경험치 막대 구분과 해금·획득 초상 구분은 시각 디자인 단계로 넘긴다.

### 검수 P2 결정 항목

날짜: 2026-09-22. 근거: [와이어프레임 UI/UX 검수](review.md#2026-09-22-와이어프레임-uiux-검수)의 P2 중 결정이 필요한 항목이다. 하나씩 묻고 기록한다.
P2-4 연결 `미설치` 행: 제안은 `다시 확인` 버튼이다. 사용자 답변: “제안대로 진행”. `설치 방법` 버튼 안은 채택하지 않았다. 미설치 행에 `다시 확인`을 둔다. 설치 후 누르면 다시 감지한다. 에이전트 연결은 선택 기능이므로 설치를 권하는 링크는 두지 않는다. WF-07 설정 모달의 Gemini CLI 행에 `다시 확인` `286:121`을 추가했다. 세 행 모두 오른쪽에 버튼이 있다.
P2-5 놀이공간 설정: 제안은 두 칸 전환 버튼이다. 사용자 답변: “그렇게 진행”. 드롭다운에서 고르면 바로 그리기로 넘어가는 안은 채택하지 않았다. 설정의 놀이공간 항목을 `화면 전체 | 영역 지정` 전환 버튼 하나로 둔다. `영역 지정`을 고르면 `영역 그리기`가 나타난다. 그린 뒤에는 `다시 그리기`로 바뀐다. 그리기·미리보기·적용 절차는 SC-08의 기존 초안을 따른다. WF-07의 드롭다운 `261:197`과 버튼 `261:199`를 삭제하고 전환 버튼 `287:122`를 넣었다.
P2-8 선택 표현 규칙: 사용자 답변: “진행”. 요소 종류별 선택 표현을 정했다. 카드·목록 항목은 왼쪽 세로 막대와 옅은 배경이며 테두리를 쓰지 않는다. 메인 탭은 옅은 배경과 밑줄이다. 필터 칩은 진한 채움과 흰 글자다. 두 칸 전환은 회색 틀 안의 흰 칸이다. 헤더 아이콘은 모달이 열린 동안 진한 배경이다. 카드 규칙은 기존 사용자 선호를 따른다. 칩과 전환은 크기가 작아 채움 방식을 쓴다. `docs/design.md`의 S5 디자인 시스템 v2 표에 `선택 표현` 행을 추가했다. WF-07의 `일반 | 연결` 칩 `Segments`를 두 칸 전환 `288:121`로 바꿨다.
P2-9 상점 버튼 반복과 0P 동사: 제안은 카드 누르기로 여는 구매 창이다. 사용자 답변: “제안대로 진행”. 카드의 `구매` 버튼을 유지하고 0P만 바꾸는 안은 채택하지 않았다. 상품 카드에는 가격과 `›`만 둔다. 카드를 누르면 구매 창을 연다. 여러 개 사는 상품은 수량·합계·보유 포인트를 확인한다. 하나씩 사는 상품은 수량 줄이 없다. 0P 상품은 `받기`다. 구매할 수 없으면 구매 창에서 이유 한 줄과 비활성 `구매`를 보여준다. 돌보미집이 가득 차면 `돌보미집 보기`를 둔다. 이 결정으로 이전의 구매 확인 단계와 수량 선택 후 구매 미정 항목이 해소됐다.
Figma: WF-04의 카드 12개에서 `구매` 버튼을 빼고 `›`를 넣었다. 카드 버튼 방식의 예전 WF-04b `282:165`를 삭제했다. `WF-04b 상점 · 구매 창` `290:121`은 상점 탭 위 구매 창 장면이다. `WF-04c 상점 · 구매 창 상태` `290:305`는 돌보미집 가득, 포인트 부족, 0P `받기`의 세 구매 창이다. 같은 줄 두 창의 높이를 253으로 맞추고 버튼 줄을 아래에 붙였다.
P2-12 우클릭 상태 표현: 날짜 2026-09-23. 제안은 메뉴의 문구 유지와 구간 이름 대응이다. 사용자 답변: “제안한대로 진행”. 메뉴에 작은 막대를 넣는 안은 채택하지 않았다. 우클릭 메뉴의 이름·상태 줄은 만복도 구간 이름과 기분 구간 이름을 문구로 보여준다. 관리 창은 같은 정보를 수치 막대로 보여준다. 만복도 구간은 배고픔 말풍선의 구간과 같다. `배부름 / 보통 / 배고픔 / 매우 배고픔`은 예시다. 구간 수와 경계는 밸런스 설계에서 정한다. Figma 변경은 없다.
P2 정리: 결정 항목 P2-4·5·8·9·12를 처리했다. 가벼운 항목 P2-1·2·3·6·11·13은 앞 절에서 처리했다. P2-7 현재·예상 경험치 막대 구분과 P2-10 해금·획득 초상 구분은 시각 디자인 단계로 넘겼다.

### UI 컴포넌트 계약 추출 설계

날짜: 2026-09-23. 사용자 지시: “커밋하고 이제 다음 작업 진행해보자.” 진행 현황의 재개 순서 5번인 컴포넌트 계약 추출을 제안했다. 사용자 답변: “진행”. 기능 구현 미시작.
목표와 범위: WF-01~WF-09와 상태 예시에서 반복되는 UI를 컴포넌트 계약으로 정리한다. 각 계약에 역할, 상태·변형, 내용, 규칙, 사용 화면을 적는다. 기존 Figma 00~04 자산과 짝을 지어 재사용·수정·신규로 판정한다. Figma 수정과 모듈 책임 정리는 이번 범위에서 제외한다.
근거: Figma `01 · Atoms` 29개, `02 · Molecules` 12개, `03 · Organisms` 13개, `04 · Templates` 3개의 이름·변형·속성을 읽기 전용으로 확인했다. `Status Banner`는 창 안 상태 알림이며 바탕화면 업적 배너와 다르다.
SSOT: 새 기능 계약 `docs/specs/ui-components.md`. 규칙은 `docs/design.md`의 S5 디자인 시스템 v2와 `docs/specs/s5.md`를 따른다. 파일 이름은 프로젝트 전체 기준으로 지었다.
위험과 수용 조건: 와이어프레임의 배치 제안을 확정 계약으로 기록하지 않는다. 확정 규칙에서 온 동작과 그림에서 온 배치를 구분한다. 기존 자산을 근거 없이 삭제 대상으로 판정하지 않는다.
작업: `docs/specs/ui-components.md`를 만들었다. 공통 규칙 7개와 컴포넌트 계약 C-01~C-22를 적었다. 판정은 수정 6개, 신규 15개, `[스펙 미확정]` 1개다. 수정 대상은 앱 헤더, 메인 탭, 파티 칸 카드, 수치 막대, 대화상자, 설정 행이다. 우클릭 메뉴는 OS 기본 메뉴를 쓸지 정하지 않아 미정이다. 상세 화면 자산은 이번 와이어프레임에 쓰이지 않았지만 삭제 대상으로 판정하지 않았다.
연결: `docs/README.md`의 현재 기준 표와 문서 트리, `docs/progress.md`의 재개 순서·재개 방향·전체 설계 트리, `docs/design.md`의 S5 디자인 시스템 v2 절에 새 계약을 연결했다.
Figma 반영: `06 · 전체 설계·문서 트리`의 문서 트리 `specs/` 아래에 `ui-components.md` 행 `293:121`을 추가했다. 전체 설계 트리 화면·모듈 행의 미정 항목 `238:341`을 고쳤다.
변경과 의미 검수: 각 계약의 동작을 현재 설계·기능 계약·검수 반영 기록과 대조했다. 그림에서 온 크기·배치·문구는 `[스펙 미확정]`이나 와이어프레임 크기로 구분했다. 기존 자산의 판정 근거를 Figma 속성 확인 결과로 적었다. 상세 화면 자산은 유지로 두었다. 조건과 용어 일치를 확인했다.
검증: CRLF를 LF로 바꾸는 임시 preload로 `node scripts/check-docs.cjs`를 실행했다. 결과는 통과다. 문서 48개, 파일 링크 901개, JSON 7개다. `git diff --check` 통과. 빌드와 런타임 검사는 실행하지 않았다.

### 앱이 그리는 우클릭 메뉴

날짜: 2026-09-23. 질문: 포켓몬 위 우클릭 메뉴를 OS 기본 메뉴로 둘지 앱이 그리는 메뉴로 할지. 사용자 질문: “앱이 직접 그리는 메뉴가 좋지않아?” 장단점을 설명한 뒤 사용자 답변: “진행”. OS 기본 메뉴 안은 채택하지 않았다.
확정 범위: 우클릭 메뉴는 앱이 직접 그린다. 구분선·상태 문구·여백은 관리 창과 같은 디자인 규칙을 따른다. Windows와 macOS에서 같은 모양이다.
구현 조건: 방향키·Enter·Esc 조작, 바깥 클릭 닫기, 화면 가장자리에서 방향 뒤집기, 다중 화면과 배율 맞춤을 제공한다. 투명한 놀이공간 창은 메뉴 영역과 포켓몬 그림만 클릭을 받고 나머지는 통과시킨다. 기존 구현은 그림의 알파값으로 클릭 대상을 판정한다.
근거: `docs/guide.md`의 확인 필요 항목에 따르면 Windows에서 초점을 받지 않는 창의 기본 우클릭 메뉴가 바로 닫힐 수 있다. 앱이 그리는 메뉴는 이 문제를 피한다.
변경: `docs/specs/ui-components.md`의 C-21 판정을 신규로 바꾸고 형태와 구현 조건을 적었다. 판정은 수정 6개, 신규 16개가 됐다. `docs/design.md`와 `docs/specs/s5.md`의 우클릭 문장에 연결했다.

### UI 컴포넌트 수정 6개 Figma 반영

날짜: 2026-09-23. 사용자 지시: “커밋하고 1번으로 진행”. 1번은 계약대로 Figma `01`~`04`의 컴포넌트를 고치고, 만든 결과를 보며 검토하는 안이다. 수정 6개부터 시작했다. 기능 구현 미시작.
범위: C-01, C-02, C-03, C-04, C-05, C-13, C-18. C-02는 판정이 신규지만 C-01에 필요해서 함께 만들었다. 나머지 신규 15개와 템플릿 수정은 이번 범위에서 제외했다.
작업 방식: 기존 컴포넌트는 새로 만들지 않고 제자리에서 고쳤다. 바꾸기 전과 후에 인스턴스 수와 속성 값을 세었다. 새 면·선·간격·모서리는 기존 변수에 연결했다.
결정 1: `Stat Meter`의 채움 길이는 `Stat Meter Bar`의 `Fill` 변형으로 고른다. 단위는 10%다. 근거: 임시 인스턴스로 시험했다. Figma는 인스턴스 안 레이어의 크기 덮어쓰기를 받지 않았다. 임시 노드는 시험 직후 삭제했다.
결정 2: 대화상자의 실패 알림 `error-notice`는 본문 slot 밖에 둔다. 근거: slot 안으로 옮기자 `Show Error` 연결이 끊겼다. 구매·사용·받기·교체 대화상자는 모두 실패 알림이 필요하다.
결정 3: 꺼진 토글 막대용 변수 `bg/control-off`를 `PB / Color`에 추가했다. `neutral/500`의 별칭이다. 흰 배경 대비는 약 4.8:1이다. 기존 채움 변수에는 대비 3:1을 넘는 중간 회색이 없었다.
결정 4: 파티 칸의 숨김 상태는 별도 변형 대신 기존 `Show Visibility Marker` 속성으로 켠다. 대화상자의 닫기 버튼은 `Header Icon Button`을 재사용한다.
`[스펙 미확정]` 헤더 dot 색은 `Status Dot`의 `Danger`로 두었다. 토글·헤더 아이콘의 `Focus`는 `Button`과 같은 `border/focus` 2px다. 두 선택은 사용자 검토 전이다.
Figma `01 · Atoms`: `Icon / Box` `294:353`, `Icon / Achievement` `295:123`, `Icon / Close` `299:166`, `Stat Meter Bar` `298:155`, `Toggle` `299:3593`을 새로 만들었다.
Figma `02 · Molecules`: `Header Icon Button` `295:3083`을 새로 만들었다. `Friendship`을 `Stat Meter` `296:190`으로 바꿨다. `Party Slot` `151:158`의 문구와 `Setting Row` `134:1023`을 고쳤다.
Figma `03 · Organisms`: `Primary Navigation` `208:542`, `App Header` `154:1017`, `Party Slot Card` `194:317`을 고쳤다. `Confirm Dialog`를 `Dialog` `299:3575`로 바꿨다. `Dialog` 세트는 `Status Banner`와 겹치지 않도록 x=2340, y=700으로 옮겼다. 상태 예시 `295:3297`과 `298:3408`을 추가했다.
검증: 컴포넌트마다 구조를 읽고 스크린샷으로 확인했다. 인스턴스 수는 전후가 같았다. `Primary Navigation` 5, `Stat Meter` 16, `Party Slot Card`의 `State=Pokemon` 6, `Dialog` 4, `Setting Row` 9다. `Party Layout` `196:189`와 `Pokemon Detail Layout`의 `Dialog=Error` `226:858`에 변경이 전파됐다.
남은 문제: `Party Layout`의 제목 줄 `3마리 소환 중 · 슬롯 4 / 6 개방`은 옛 용어다. 템플릿 수정 단계에서 고친다. `Primary Navigation`의 `Active` 선택지 목록은 `Party|Pokédex|Shop|Bag|Box` 순서로 남았다. 변형의 배치 순서는 탭 순서와 같다.
문서 검수: 바뀐 문장을 쓰기 점검표로 확인했다. 한 문장 한 사실, 용어 일치, 확정과 제안의 구분을 확인했다.
SSOT: `docs/specs/ui-components.md`의 C-01~C-05, C-13, C-18.

### 와이어프레임 스크롤 화면과 최소 높이

날짜: 2026-09-23. 사용자 지시: “figma에서 스크롤일때를 고려해서 화면이 스크롤일때는 길게만들고 좀 했음 좋겠어.” 사용자가 WF-07 설정 모달 `278:243`과 대화상자 `278:358`을 예로 들었다. 사용자 지적: “이런거는 최소높이도 안맞춰놨고.” 예는 WF-05b의 `Empty Result` `282:447`이다. 기능 구현 미시작.
확정 규칙: 스크롤하는 화면과 모달은 내용 끝까지 펼쳐 그린다. 창 끝 y=780에 점선을 긋는다. 모달 안에서 스크롤하면 대화상자 끝에 점선을 긋는다. 점선의 설명 글자는 틀 오른쪽 여백에 둔다. 창보다 짧은 화면은 최소 높이 780으로 그린다.
점검 결과: 07 페이지의 틀 17개를 확인했다. 설정 대화상자는 내용 785 중 606만 보였다. WF-04c의 구매 창 1개는 아래 14px가 잘렸다. WF-05의 도감 종 카드 9개는 아래 여백 10px가 사라졌다. WF-01, WF-05b, WF-08, WF-08b는 780보다 짧았다.
Figma 변경: WF-01 `252:122`, WF-05b `282:326`, WF-08 `261:230`, WF-08b `282:121`에 최소 높이 780을 줬다. 커진 높이와 겹치지 않도록 WF-08b, WF-01b, WF-01c, WF-05b를 80 간격으로 내렸다.
Figma 변경: 설정 대화상자와 스크롤 영역을 내용 높이로 펼쳤다. 대화상자는 859, 틀은 959가 됐다. 뒤 배경에 최소 높이 780을 줬다. 스크롤 막대 사각형 `278:364`는 펼친 그림과 맞지 않아 삭제했다.
Figma 변경: 구매 창 `290:310`과 `290:331`을 같은 줄 내용 최댓값 268로 맞췄다. 두 창 안의 `Push`는 남는 공간을 채워서 창 높이를 내용에 맞출 수 없었다. 그래서 `Push`를 잠시 고정해 높이를 쟀다. 도감 종 카드 10개는 모두 127로 맞췄다.
Figma 변경: 780을 넘는 WF-01b, WF-01c, WF-03, WF-04, WF-05에 창 끝 점선을 넣었다. WF-07에는 대화상자 끝 y=720에 점선을 넣었다. 점선과 설명은 `Fold`와 `Fold Label` 이름으로 찾는다.
검증: 틀마다 자식 내용이 잘린 곳을 다시 찾았다. 잘린 곳은 남지 않았다. WF-07, WF-05b, WF-04를 스크린샷으로 확인했다. 설명 글자가 다른 틀과 겹치지 않음을 확인했다.
SSOT: `docs/design.md`의 S5 디자인 시스템 v2 표 `화면 크기` 행.

### 와이어프레임 배치 정리

날짜: 2026-09-23. 사용자 지시: “와이어프레임 순서좀 정리해놔바. 지금 조금 주구난방느낌”. 기능 구현 미시작.
원인: 틀을 만든 순서대로 오른쪽에 붙여서 탭 화면·모달·첫 실행·데스크톱이 섞여 있었다. 상태 화면도 원래 화면 아래에 있는 것과 없는 것이 섞여 있었다.
확정 배치: 07 페이지를 섹션 4개로 묶었다. `1 · 관리 창 탭` `307:121`은 앱 탭 순서다. 순서는 파티 WF-01, 박스 WF-01b·01c, 도감 WF-05·05b, 상점 WF-04·04b·04c, 가방 WF-03이다. 상태 화면은 원래 화면 아래에 둔다. `2 · 관리 창 모달` `307:122`은 업적창 WF-02, 설정 WF-07, 개체 선택 WF-09a·09b다. `3 · 첫 실행` `307:123`은 WF-08·08b다. `4 · 놀이공간 (데스크톱)` `307:124`는 WF-06과 주석이다.
유지: 틀 이름과 WF 번호는 문서에서 참조하므로 바꾸지 않았다. 틀 사이 간격은 80이다. 스크롤 경계 설명 글자는 틀과 함께 옮겼다. 레이어 패널도 화면 배치와 같은 순서로 맞췄다.
추가 수정: WF-02 뒤 배경 `278:122`에 최소 높이 780을 줬다. [최소 높이 규칙](#와이어프레임-스크롤-화면과-최소-높이)을 따른다.
검증: 섹션 안 틀의 겹침과 섹션 밖으로 나간 노드를 찾았다. 둘 다 없었다. 섹션 1·2를 스크린샷으로 확인했다.

### 시나리오 빈 화면 와이어프레임

날짜: 2026-09-23. 사용자 질문: “시나리오상 필요한 화면들은 다 설계가 된건가?” 시나리오 SC-01~SC-11과 07 와이어프레임을 대조했다. 본 흐름에 있는데 그림이 없는 화면 7개를 찾았다. 추천 순서를 제안했다. 사용자 지시: “추천 순서대로 진행.” 기능 구현 미시작.
대조 결과: 개체 상세, 진화 확인, 알 돌봄과 부화 결과, 부화·진화 배너, 가방의 다른 도구, 놀이공간 영역 그리기, 튜토리얼 5종이 비어 있었다. 가이드북, 트레이 메뉴, 우클릭 메뉴의 `밥 주기` 불가 상태, `나올 수 있는 종 보기`, 공유 `sid` 종 교체 표시는 가볍거나 후속 설계라서 이번 범위에서 뺐다. SC-10은 새 화면이 필요 없다. SC-11은 설정의 연결 탭으로 충분하다.
Figma `2 · 개체 상세·육성` `320:122`: WF-10 파티 개체 상세 `310:121`은 프로필, 돌봄, 표시, 성장·진화, 파티 관리를 둔다. 진화 가능이면 `진화`가 나타난다. WF-10b 박스 개체 상세 `311:121`은 돌봄·표시 없이 `파티에 배치`를 둔다. WF-11 진화 확인 `311:256`은 진화 전후와 유지 정보, 되돌릴 수 없음 알림, `취소`·`진화`를 둔다. WF-11b 진화 분기 선택 `311:419`은 후보 두 개 중 하나를 고른다. WF-03b 민트 `316:121`, WF-03c 진화의돌 `316:323`, WF-03d 프리미엄먹이 `316:502`는 가방 사용 패널의 도구별 장면이다.
Figma `3 · 돌보미집·부화` `320:125`: WF-12 알 돌봄 `313:121`은 고른 알 아래에 돌봄 패널을 연다. WF-12b 부화 결과 `313:279`는 태어난 포켓몬과 들어간 자리를 보여준다.
Figma `6 · 놀이공간 (데스크톱)`: WF-14 영역 그리기 `317:121`은 영역 밖을 어둡게 하고 도구 막대에 `다시 그리기`·`취소`·`적용`을 둔다. WF-13 알림 배너 `315:121`은 부화·진화·업적 배너와 공통 규칙이다.
Figma `7 · 튜토리얼` `320:127`: WF-15 상점 `318:121`, WF-15b 부화 `318:294`, WF-15c 파티 `318:446`, WF-15d 첫 돌봄 `319:121`, WF-15e 놀이공간 `319:189`.
용어: 옛 `소환 해제` 버튼 자리는 용어집의 `박스 보관`에 맞춰 `박스에 보관`으로 그렸다. 표시 버튼은 `숨기기`·`꺼내기`다.
`[스펙 미확정]` 그림에서 새로 둔 제안은 다음과 같다. 상세의 `박스에 보관` 위치. 박스 개체 상세에 돌봄 항목을 두지 않는 안. 부화 결과의 `파티에서 보기`. 부화·진화 배너에도 `바로가기` 버튼을 두는 안. 튜토리얼을 대상만 밝게 두고 말풍선 카드를 붙이는 코치마크 형태. 튜토리얼 대상이 다른 탭에 있을 때 화면을 바꾸지 않고 그 탭을 가리키는 안. 진화 확인의 되돌릴 수 없음 문구. 분기 후보 예시. 영역 그리기의 도구 막대 배치.
규칙 적용: 부화 배너와 부화 튜토리얼은 결과 포켓몬이나 정답 행동을 보여주지 않는다. 진화 배너는 결과 종을 보여주지 않는다. 결과는 진화할 때의 조건으로 정한다. 고를 수 없는 대상은 회색 면·흐린 글자와 이유 한 줄로 둔다. 선택은 왼쪽 막대와 옅은 배경이다. 튜토리얼의 강조 영역에는 둥근 테두리를 두르지 않는다. 주변을 어둡게 해서 대상을 드러낸다.
배치: 모달 장면은 창 720×780을 따로 두고 설명을 창 아래에 둔다. 배경 화면 위에 설명이 겹치지 않는다. 780을 넘는 화면은 창 끝 점선과 설명을 둔다. 07 페이지를 섹션 7개로 다시 묶었다.
검증: 새 틀마다 스크린샷을 확인했다. 잘림 탐지에서 잘린 곳이 없었다. 화면 틀은 모두 780 이상이다. 섹션 안 겹침과 섹션 밖 노드는 없었다. 확인 중 고친 것: 상세 프로필 카드 폭, 크기 단계 버튼 높이, 진화 분기 배경 개체, 가방 도구 장면의 수량과 최소 높이, 대상 행의 숨김 마커, 영역 그리기 미리보기 위치.
SSOT: `docs/specs/s5.md`의 상세와 진화·알림 배너·튜토리얼·가방, `docs/specs/s5-scenarios.md`의 SC-01~SC-08, `docs/terms.md`.

### 빈 화면 제안 검토

날짜: 2026-09-23. 근거: [시나리오 빈 화면 와이어프레임](#시나리오-빈-화면-와이어프레임)의 `[스펙 미확정]` 제안 5개. 사용자 지시: “검토할것들 하나씩 물어봐”. 하나씩 묻고 기록한다. 기능 구현 미시작.
1. 튜토리얼 형태: 제안은 코치마크다. 가운데 설명 창과 구석 작은 카드 안은 채택하지 않았다. 사용자 답변: “추천대로 진행”. 튜토리얼은 화면을 어둡게 하고 알려줄 대상만 밝게 남긴다. 대상 옆에 말풍선 카드를 붙인다. 카드는 단계 수, 제목, 설명, `다음`, `✕`를 담는다. `✕`는 닫기이며 스킵으로 처리한다. 대상에는 강조 테두리를 두르지 않는다. 화면별 말풍선 위치는 각 와이어프레임을 따른다.
2. 부화·진화 배너의 `바로가기`: 제안은 세 배너 모두 `바로가기` 버튼으로 이동하는 안이다. 부화·진화 배너만 본문 클릭으로 이동하는 기존 안은 채택하지 않았다. 사용자 답변: “a로진행”. 부화·진화 배너도 업적 배너처럼 제목, 대상 표시와 이름, `바로가기` 버튼으로 구성한다. 부화 배너의 `바로가기`는 돌보미집을 연다. 진화 배너의 `바로가기`는 해당 개체의 상세를 연다. 부화 배너는 결과 포켓몬을 보여주지 않는다. 진화 배너는 결과 종을 보여주지 않는다. 배너 본문 클릭 동작은 두지 않는다.
3. 박스 개체 상세의 돌봄: 제안은 돌봄·표시 항목을 두지 않는 안이다. 비활성으로 두는 안과 박스에서도 돌보는 안은 채택하지 않았다. 사용자 답변: “a로.” 박스 개체 상세에는 성장·진화, 도구 사용, `파티에 배치` 또는 `교체`만 둔다. 박스에서는 친밀도·만복도·적립이 멈추고 놀이공간에 나오지 않는다. 가방 도구는 박스 개체에게도 쓸 수 있다.
같은 답변의 추가 지시: “경험치는 레벨옆에.” 사용자가 WF-10b의 `경험치` 막대 `311:183`을 가리켰다. 개체 상세 프로필의 경험치는 레벨 옆에 짧은 막대와 `Lv.9까지 20%`처럼 다음 레벨까지의 비율로 붙인다. 아래 막대 줄에는 친밀도·만복도만 둔다. WF-10, WF-10b와 WF-11·11b의 배경 상세에 반영했다.
4. 부화 결과 창의 버튼: 제안은 `확인`만 두는 안이다. `파티에서 보기`로 이동하는 안과 결과 창에서 바로 `꺼내기`하는 안은 채택하지 않았다. 사용자 답변: “a로 진행”. 부화 결과 창은 태어난 개체와 들어간 자리를 보여주고 `확인`만 둔다. 닫으면 돌보미집에 그대로 머문다. 화면을 전환하지 않는다. 꺼내는 방법은 파티 튜토리얼이 알려준다.
5. `박스에 보관` 위치: 제안은 파티 개체 상세의 `파티 관리`에 `교체`와 나란히 두는 안이다. 파티 칸 카드의 `⋯` 메뉴 안은 채택하지 않았다. 사용자 답변: “a로 진행.” 버튼을 누르면 확인 창을 연다. 확인하면 파티 칸이 비고 개체는 박스에 들어간다. 옛 `소환 해제` 자리와 확인 창을 이어 쓴다. 이 결정으로 SC-07의 “박스로만 보내는 조작 위치” 미정 항목이 해소됐다.
정리: 제안 5개를 모두 확정했다. Figma의 해당 설명 글자에서 `[스펙 미확정]` 표시를 고쳤다. WF-12b의 `파티에서 보기`를 지웠다. `박스에 보관` 확인 창 WF-10c `328:121`을 섹션 2의 WF-10b 다음에 추가했다. 개체 상세 프로필의 경험치를 레벨 옆으로 옮겼다.

### UI 신규 컴포넌트 Figma 반영

날짜: 2026-09-23. 사용자 지시: “진행”. 남은 항목 중 추천 순서 1번인 신규 컴포넌트 제작을 진행했다. 기능 구현 미시작.
범위: C-06~C-12, C-14~C-17, C-19~C-22와 추가 계약 C-23~C-25. 템플릿과 05 Screens는 제외했다.
후보 판정: 빈 화면에서 반복한 코치마크 말풍선, 돌봄 타일, 정보 상자를 C-23~C-25로 더했다. 진화 전후 비교는 한 화면에서만 쓰여 넣지 않았다.
변수: `PB / Color`에 `bg/subtle`(빈 칸·비활성 면)과 `text/disabled`(비활성 글자)를 더했다. `PB / Primitive`에 `neutral/400` `#9CA3AF`를 더했다. 기존 `Button`은 투명도 45%로 비활성을 표현한다. 계약의 “회색 면과 흐린 글자”를 표현할 변수가 없었다. 변수는 91개가 됐다.
기초 자산: `Portrait` `330:135`를 Large 80, Medium 40, Small 28 세트로 바꿨다. `Visibility Marker` `330:143`에 Small 12를 더했다. 기존 인스턴스 2곳씩은 연결을 유지했다.
만든 컴포넌트: `01 · Atoms`에 `Filter Chip` `331:130`, `Segmented Control` `331:145`. `02 · Molecules`에 `Quantity Stepper` `332:187`, `Empty State` `332:199`, `Box Slot` `333:231`, `Target Row` `333:290`, `Egg Slot` `334:238`, `Daycare Strip` `334:239`, `Care Tile` `334:268`, `Info Box` `334:269`, `Bag Item Card` `335:257`, `Product Card` `335:258`, `Species Card` `336:299`, `Achievement Row` `336:341`, `Box Toolbar` `337:284`, `Menu Item` `337:319`. `03 · Organisms`에 `Notification Banner` `338:732`, `Speech Bubble` `338:733`, `Context Menu` `338:738`, `Coach Bubble` `338:764`.
구성 원칙: 글자는 기존 `Text / Label·Caption·Micro·Caption Block` 인스턴스를 쓴다. 바꿀 글자와 버튼은 노출한 인스턴스로 둔다. 선택은 왼쪽 막대(`action/primary`)와 옅은 배경(`bg/accent`)이다. 빈 칸과 비활성은 `bg/subtle`과 `text/disabled`다. 둥근 요소에 컬러 테두리를 쓰지 않는다.
높이 통일: 이 파일의 auto-layout은 테두리를 크기에 넣는다. 테두리가 있는 쪽의 여백을 축마다 1px 줄여 같은 줄의 높이를 맞췄다. 필터 칩 두 상태는 28, 검색 줄의 버튼·입력칸과 업적 행의 버튼·상태 표시는 32다.
검증: 컴포넌트마다 스크린샷을 확인했다. 새 컴포넌트의 채움·선에서 변수 미연결 색을 찾았다. 결과는 `Visibility Marker`의 몬스터볼 색 8곳뿐이다. 이 색은 원래 마커의 원시 색이며 2026-09-21 기록의 후속 수정 대상이다. 확인 중 고친 것: 필터 칩 높이, 정보 상자·돌봄 타일·배너·코치마크의 세로 크기 자동 설정.
남은 문제: 템플릿 수정, 05 Screens 갱신, V2-01~V2-12, 몬스터볼 원시 색. 헤더 dot 색과 포커스 표현은 사용자 확인 전이다.
SSOT: `docs/specs/ui-components.md`의 C-06~C-25.

### 템플릿 수정

날짜: 2026-09-23. 남은 항목 추천 순서 2번. 사용자 지시: “진행”. 기능 구현 미시작.
App Shell `195:319`: 헤더와 메인 탭이 인스턴스라서 C-01·C-03 수정이 이미 반영됐다. 바꾼 것이 없다.
Party Layout `196:189`: 제목 줄의 옛 문구 `3마리 소환 중 · 슬롯 4 / 6 개방`을 `2마리 표시 중 · 4 / 6칸 사용 가능`으로 바꿨다. 파티 칸 카드는 C-04 수정으로 친밀도·만복도가 이미 반영됐다.
Box Layout `340:3665`: 새로 만들었다. 헤더, `Active=Box` 탭, 제목 줄, `Daycare Strip`, `Box Toolbar`, `Box Slot` 30칸(6열×5행)이다. 높이는 내용에 맞추고 최소 780이다.
Pokemon Detail Layout `226:913`: 부품을 WF-10 구조로 고쳤다. `Pokemon Profile` `155:173`은 레벨 옆 경험치, 상태 줄과 버프 칩(`Show Buff`), 친밀도·만복도 막대 줄을 둔다. `Growth & Tools Section` `162:241`은 `성장·진화`의 진화 줄(`Show Evolve`)과 도구 줄이다. `Party Management Section` `341:4238`은 `Location=Party`(`교체`·`박스에 보관`)와 `Location=Box`(`파티에 배치`)의 세트다. 템플릿에 `Show Status Banner`(기본 끔), `Show Care`, `Show Display`를 더하고 파티 관리 인스턴스를 노출했다. 박스 개체는 두 항목을 끄고 `Location=Box`를 고른다. `Dialog=Confirm`·`Error`의 문구를 박스 보관 확인으로 바꿨다. 본문 높이를 내용에 맞췄다. 세 변형은 996 높이다.
시각 변경: `Progress / Track` `116:121`의 채움을 `bg/canvas`에서 `bg/subtle`로 바꿨다. 근거: 상세 프로필은 회색 배경 위에 있어서 막대 바탕이 배경과 같은 색으로 보이지 않았다. 모든 막대의 바탕이 조금 진해진다. `박스에 보관`은 되돌릴 수 있는 조작이라 보조 버튼으로 바꿨다. 두 변경은 사용자 확인 전이다.
검증: 인스턴스 연결은 전후가 같았다. `Party Management Section` 8곳, 템플릿의 05 Screens 사용처 5곳이 그대로다. 템플릿마다 스크린샷을 확인했다.
남은 문제: 확인 창의 배경막이 거의 불투명하다. 기존 문제 목록의 항목이며 05 Screens 갱신 때 처리한다.
SSOT: `docs/specs/ui-components.md`의 템플릿 줄.

### 05 Screens 갱신과 V2 문제 처리

날짜: 2026-09-23. 남은 항목 추천 순서 3번. 범위 질문에 사용자 답변: “a로진행”. 기존 6개 화면을 새 템플릿으로 갱신하고 박스 화면을 더한다. 와이어프레임 전체의 고해상도 조립은 뒤로 미룬다. 기능 구현 미시작.
화면: `Party / Approved UI`, `Detail / Base`, `Detail / Hidden`, `Detail / Care Success`, `Detail / Box Keep Confirm`, `Detail / Box Keep Save Error`, 새 `Box / Base` `353:2510`이다. 옛 이름 `Detail / Dismiss *`는 박스 보관 용어에 맞춰 바꿨다. 화면은 내용 끝까지 펼치고 창 끝 780에 점선과 설명을 넣었다.
V2 문제: 12개 중 11개를 처리했다. 상태와 근거는 [열린 디자인 항목](feedback.md#열린-디자인-항목)의 표를 따른다. `V2-04` 그림·데이터 연결만 열려 있다.
타입 배지 색: 18개 중 14개는 글자색만 바꿔 4.5:1을 넘겼다. 불꽃 `#e62829`→`#e52829`, 풀 `#3fa129`→`#358722`, 에스퍼 `#ef4179`→`#d53a6c`, 물 `#2980ef`→`#2675db`로 배경을 기준까지만 어둡게 했다. 원작 색과 달라지므로 사용자 확인이 필요하다.
배경막: 45%로 정했다. 인스턴스에서는 채움 투명도가 유지되지 않는다. 그래서 원시값 `neutral/900-a45` `356:121`에 알파를 넣고 `overlay/scrim`이 이 값을 참조한다. `Modal Scrim`의 안쪽 인스턴스는 사각형으로 바꿨다.
사고와 복구: `resetOverrides`로 배경막 인스턴스를 초기화하자 위치 덮어쓰기까지 지워졌다. `modal-overlay`가 가로 자동 배치라서 배경막과 대화상자가 한 줄로 밀렸다. 두 자식을 절대 위치로 되돌리고 배경막은 전체 덮기, 대화상자는 가운데로 다시 맞췄다.
돌봄 성공 화면: 상태 안내를 기본 끄기로 바꾸면서 안내가 사라졌다. 이 화면에서만 다시 켜고 문구를 `돌봄 완료 · 밥을 줬어요. 만복도가 올랐어요.`로 바꿨다.
검증: 화면마다 스크린샷을 확인했다. 연결은 13개이며 모두 버튼·카드·탭 노드에 붙었다. 처음에 화면 전체(layout)에 붙은 연결 2개는 제거하고 다시 걸었다.
남은 문제: `V2-04` 그림과 종 데이터. 와이어프레임 전체의 고해상도 조립. 타입 배지 4색과 막대 바탕색, 돌봄 버튼 위계는 사용자 확인 전이다.
SSOT: `docs/work/s5-design-system-v2/feedback.md`의 열린 디자인 항목, `docs/specs/ui-components.md`.

### 모듈 책임과 저장 구조 설계

날짜: 2026-09-23. 사용자 지시: “커밋 후 다음작업 진행해보자”. 진행 현황의 재개 순서에서 남은 구현 직전 단계인 모듈 책임을 제안했다. 결정 3개를 하나씩 물었다. 코드 구현 미시작.
결정 1 저장 스키마: 제안은 `SaveV3` 신설과 기존 `SaveV2`의 한 번 변환이다. V2에 필드만 더하는 안은 채택하지 않았다. 사용자 답변: “a로 진행”. 근거: `party: Pet[]`에는 빈 칸과 잠긴 칸 개념이 없어 파티 칸 규칙과 어긋난다. 변환 전에 원본을 백업한다.
결정 2 거래 처리 위치: 제안은 거래 실행기 모듈 하나다. 도메인 모듈이 각자 저장을 다루는 안은 채택하지 않았다. 사용자 답변: “a로 진행”. 원자성과 중복 반영 방지 규칙을 한 곳에 둔다. 도메인 모듈은 순수 함수로 두어 검사하기 쉽게 한다.
결정 3 폴더 구조: 제안은 도메인별 폴더 추가다. `src/state`에 모두 넣는 안은 채택하지 않았다. 사용자 답변: “a로 진행”. `src/party`, `src/box`, `src/egg`, `src/bag`, `src/achievement`, `src/tutorial`, `src/tx`를 새로 만든다. 기존 폴더는 그대로 둔다.
근거 확인: 현재 `src/`는 15개 폴더다. `src/state` 223줄, `src/save` 710줄, `src/dex` 464줄, `src/shop` 74줄이다. 저장은 `SaveV2`이며 `party: Pet[]`에 개체를 직접 넣는다. 명령은 mailbox 규약을 쓴다.
작업: `docs/specs/modules.md`를 만들었다. 경계 원칙 5개, 모듈 표 16개, 거래 실행기 처리 순서, `SaveV3` 영역 11개와 변환 절차, 명령 계약 17개를 적었다.
`[스펙 미확정]` 저장 필드 이름과 타입, 요청 ID 보관 기간과 동시 요청 처리, 알림 배너 모듈 분리, 기존 `party.summon`·`party.dismiss` 폐기 시점, VS Code 확장 명령 호환은 정하지 않았다.
SSOT: `docs/specs/modules.md`. 게임 규칙은 `docs/specs/s5.md`, 흐름은 `docs/specs/s5-scenarios.md`를 따른다.

### SaveV3 필드와 변환 규칙

날짜: 2026-09-23. 사용자 지시: “커밋하고 다음거 하나씩 처리해보자”. 모듈 계약의 남은 일 2번인 저장 필드 확정을 진행했다. 코드 구현 미시작.
근거 확인: 현재 `src/shared/types.ts`의 `SaveV2`는 `party: Pet[]`, `slots`, `inventory`, `unlocked`, `acc`, `log`를 둔다. 개체는 `hunger`(높을수록 배고픔), `mood`, `affinity`, `fedAt`, `playedAt`, `nick`, `look`, `shown`을 가진다.
결정 1 만복도: 저장은 `fullness`(높을수록 배부름)로 둔다. 변환은 `fullness = 100 − hunger`다. 근거: 용어사전과 화면이 만복도를 쓴다. 코드와 문서의 용어를 하나로 맞춘다.
결정 2 시간 값: 멈추는 값은 절대 시각 대신 남은 시간으로 저장한다. 대상은 밥 주기 쿨타임, 버프, 알 준비 시간이다. 근거: PC 잠금·절전·종료에서 시간을 정지하고 남은 시간을 보존하는 기존 규칙이다.
결정 3 옛 필드: `nick`과 `look`은 화면에서 쓰지 않지만 `legacy` 영역에 보존한다. 근거: 원본 백업 없이 지우지 않는다는 기존 계약이다.
작업: `docs/specs/modules.md`에 영역별 필드표 15개, V2 → V3 변환 규칙 9줄, 변환 검사 항목, 저장 시점을 적었다. 변환 검사는 개체 수, 개체 식별자, 친밀도 합계, 포인트, 해금 종 수를 전후로 비교한다. 칸에 없는 개체는 박스 1에 넣는다.
`[스펙 미확정]` 주기 간격, 부분 진행의 반올림 단위, 기존 먹이 재고와 이로치 권리의 대응표, 저장 실패가 이어질 때의 처리는 남겼다.
SSOT: `docs/specs/modules.md`의 저장 구조.

### 콘텐츠 데이터 결정

날짜: 2026-09-23. 사용자 지시: 저장 필드 다음 항목인 콘텐츠 데이터를 하나씩 정한다. 코드 구현과 데이터 파일 작성은 미시작.
근거 확인: `data/unlocks.json`에 `starter: true`인 종이 29개 있다. 역대 스타터 27종과 피카츄, 이브이다. 모두 진화 전 종이다. `data/species.defaults.json`은 1089종을 담는다.
결정 1 첫 선택 후보: 제안은 기존 29종을 그대로 쓰는 안이다. 10종·5종으로 줄이는 안은 채택하지 않았다. 사용자 답변: “a로 진행”. 첫 선택 화면은 29칸 그리드다.
결정 2 초기 알 후보: 제안은 첫 선택 후보 29종을 모두 알 입수 허용으로 두는 안이다. 피카츄·이브이만 허용하는 안과 일부 세대만 허용하는 안은 채택하지 않았다. 사용자 답변: “a로 진행”. 초기 알은 이 29종에서 무작위로 나온다.
결정 3 알 행동 조건: 제안은 행동 횟수 기준의 조건을 소수 종에만 붙이는 안이다. 모든 종에 조건을 붙이는 안과 조건 없이 무작위만 두는 안은 채택하지 않았다. 사용자 답변: “포켓몬에서 인기도 높거나 600족이라 불리는 등의 일반몬스터에게 a로할거야.” 조건은 인기 종과 600족 계열의 진화 전 종에 붙인다. 예는 미뇽, 애버라스, 아공이, 메탕, 딥상어동 계열이다. 이 종들은 일반 무작위 후보에 넣지 않는다. 행동 조건을 찾아야 부화한다. 단일 종은 알에서 제외하는 기존 규칙을 유지한다.
결정 4 진화의돌 종류: 제안은 공통 `진화의돌` 1종이었다. 사용자 답변: “a”. 원작대로 돌 종류를 나눈다. 불꽃·물·천둥·리프·얼음·달·태양 같은 돌을 각각 상품으로 둔다. 종별로 맞는 돌을 지정한다. 돌이 결과를 정하는 종(예: 이브이)은 고른 돌이 곧 결과다. 분기 선택 창은 돌 밖의 조건이 여럿 겹칠 때만 쓴다.
데이터: 진화 데이터 `data/evo.json`에는 돌 정보가 없다. 낮·밤 조건만 있다. 종별 돌 지정을 새로 채운다. `[스펙 미확정]` 지정 위치는 `data/evo.json`의 항목에 돌 식별자를 더하는 안을 제안한다. 돌 이름과 가격은 상점 데이터에 둔다.
결정 5 수집 난이도: 제안은 원작 지표로 1~5를 자동 계산하는 안이다. 종별 수동 지정과 균등 확률 안은 채택하지 않았다. 사용자 답변: “일단 a로 할건데, 나중에 개별로 수정가능하지?” 자동 계산을 기본으로 두고 종별로 덮어쓸 수 있게 한다. 덮어쓰기는 기존 `data/species.overrides.json`에 종별 값을 적는 방식을 제안한다. 이 파일은 지금도 기본값을 덮어쓰는 용도다.
계산 입력 제안: 종족값 합계, 진화 단계, 전설·환상 여부다. `data/species.defaults.json`에 이미 있는 값을 쓴다.
`[스펙 미확정]` 실제 조건 대상 종 목록, 행동 횟수 임계값, 난이도 구간 경계와 가중치 값, 돌 종류 목록과 종별 지정표, 돌 가격은 밸런스·콘텐츠 단계에서 정한다.
SSOT: `docs/specs/s5.md`의 개체 레벨과 입수처, `docs/specs/s5-scenarios.md`의 SC-01·SC-03·SC-04·SC-09, `data/unlocks.json`.

### 수용 확인 구분 (SR-05)

날짜: 2026-09-23. 근거: [시나리오 후속 항목](feedback.md#시나리오-설계-후속-항목)의 SR-05. 수용 확인 줄이 확정 규칙 검사인지 미정 값에 기댄 검증인지 구분되지 않았다. 사용자 결정이 필요한 항목이 아니다. 기능 구현 미시작.
확정 범위: `확인:`으로 끝나는 줄은 수용 조건이다. 기본은 확정한 규칙을 검사한다. 값이 아직 없어 지금 검사할 수 없는 줄에는 `[스펙 미확정]`을 앞에 붙인다.
표시한 줄: 기본먹이 회복 확인(회복량 미정), 복수 조건 확인(가중치 미정), 일반 무작위 확인(가중치 미정), 이로치 확인(확률·시점 미정), 폼 확인(리전폼 조건 미정)이다. 다섯 줄이다.
나머지 수용 확인 줄은 확정 규칙 검사다. 값이 정해지면 표시를 지운다.
SSOT: `docs/specs/s5-scenarios.md`의 읽는 기준.

### 시간 값의 저장 정밀도 (SR-09)

날짜: 2026-09-23. 근거: [시나리오 후속 항목](feedback.md#시나리오-설계-후속-항목)의 SR-09 잔여 항목이다. 기능 구현 미시작.
질문: 남은 시간과 부분 진행을 저장할 때의 단위다. 제안은 1초 단위 반올림이었다. 사용자 답변: “b로 해. 표기는 정수로.” 1초 단위 반올림과 10초 단위 안은 채택하지 않았다.
확정 범위: 저장은 ms 단위 그대로 둔다. 값은 정수 ms로 쓴다. 소수점을 남기지 않는다. 화면 표기는 정수 단위로 반올림한다. 예는 `3:12 남음`과 `쿨타임 0:40`이다.
적용 대상: 밥 주기 쿨타임, 버프 남은 시간, 알 준비 남은 시간, 포인트 부분 진행이다.
효과: 저장과 복원을 반복해도 값이 깎이지 않는다. 화면에서는 초 단위로 읽힌다.
SSOT: `docs/specs/modules.md`의 저장 구조.

### 동시 사건 처리 순서 (SR-09)

날짜: 2026-09-23. 근거: [시나리오 후속 항목](feedback.md#시나리오-설계-후속-항목)의 SR-09 잔여 항목이다. 기능 구현 미시작.
질문: 같은 순간에 여러 사건이 겹칠 때의 처리 순서다. 사용자 답변: “a로 진행”. 사건마다 즉시 처리하는 안은 채택하지 않았다.
확정 범위: 밀린 시간을 한 번에 처리한다. 순서는 네 단계다. 1) 멈춰 있던 시간을 적용한다. 2) 값을 바꾼다. 대상은 만복도, 친밀도, 포인트, 밥 주기 쿨타임, 버프 남은 시간, 알 준비 시간이다. 3) 상태를 판정한다. 대상은 부화 가능, 진화 가능, 업적 달성, 배고픔 구간 진입이다. 4) 알림 배너를 만든다.
배너 순서: 같은 순간에 생긴 배너는 기존 규칙대로 부화 → 진화 → 업적 순서로 표시한다. [배너 표시 순서](#배너-동시-발생의-표시-순서)를 따른다.
효과: 복귀 시점이 언제든 결과가 같다. 재실행해도 같은 순서로 처리한다.
SSOT: `docs/specs/modules.md`의 거래 실행기와 저장 시점, `docs/specs/s5.md`의 실행 상태별 시간.

### 가이드북 진입과 구성 (SR-02)

날짜: 2026-09-23. 근거: [시나리오 후속 항목](feedback.md#시나리오-설계-후속-항목)의 SR-02 잔여 항목이다. 튜토리얼을 닫거나 스킵하면 사용법은 가이드북에서 확인한다는 규칙만 있고 가이드북 자체가 비어 있었다. 기능 구현 미시작.
질문: 진입 위치와 구성이다. 제안은 설정 모달 안의 항목이다. 탭 추가 안과 헤더 아이콘 추가 안은 채택하지 않았다. 사용자 답변: “a로진행”.
확정 범위: 설정 모달의 `가이드북` 행에서 `열기 ›`로 연다. 모달을 겹치지 않는다. 설정 모달 안에서 가이드북 화면으로 바뀐다. `‹ 설정`으로 돌아간다. 닫으면 보던 탭으로 돌아간다.
구성: 주제별 짧은 글이다. 돌봄, 상점과 알, 파티와 박스, 진화, 업적의 다섯 주제다. 목록에서 주제를 고르면 본문을 본다.
정보 공개: 아직 발견하지 않은 알 행동 조건의 정답은 적지 않는다. 기존 정보 공개 원칙을 따른다. 가이드북 열람은 게임 조작이나 보상 지급을 실행하지 않는다.
Figma: `WF-07b 가이드북 (설정 안)` `375:121`을 섹션 `4 · 관리 창 모달`의 설정 다음 자리에 만들었다. 설정 모달의 가이드북 행 문구도 고쳤다.
`[스펙 미확정]` 주제별 실제 문구, 그림 사용 여부, 검색 제공 여부는 정하지 않았다.
SSOT: `docs/specs/s5.md`의 행동에 따른 단계별 튜토리얼, `docs/terms.md`.

### 공유 개체의 박스 표시 (SR-08)

날짜: 2026-09-23. 근거: [시나리오 후속 항목](feedback.md#시나리오-설계-후속-항목)의 SR-08 잔여 항목이다. 지정한 단일 포켓몬 진화 계열은 같은 `sid`의 한 개체이며 두 종을 박스 교체 대상으로 제공한다는 규칙만 있었다. 박스 표시 방법이 비어 있었다. 기능 구현 미시작.
질문: 박스에서 두 종을 어떻게 보여줄지다. 제안은 두 종을 각각 칸으로 보여주는 안이다. 칸 하나만 두고 상세에서 모습을 바꾸는 안은 채택하지 않았다. 사용자 답변: “a로 진행”.
확정 범위: 공유 계열의 두 종은 박스에서 각각 칸으로 보인다. 파티에 나가 있는 종의 칸은 `파티에 있음`으로 두고 고를 수 없다. 다른 종의 칸을 누르면 파티의 같은 개체 칸이 그 종으로 바뀐다. 빈 칸이 있어도 추가로 배치하지 않는다.
표시: 두 칸에 같은 개체임을 알리는 작은 공유 표시를 붙인다. 박스 사용 칸 수는 1마리로 센다. 친밀도·레벨·만복도는 한 개체의 값이라 두 칸에 같게 보인다.
예: 베베놈과 아고용, 솔가레오와 루나아라다. 코스모움에서 한 번 진화하면 두 종이 함께 제공된다.
Figma: `Box Slot` `333:231`에 `State=InParty` 두 변형과 `Show Shared` 속성을 더했다. WF-01b의 설명에 공유 개체 규칙 한 줄을 더했다.
`[스펙 미확정]` 공유 표시의 모양, 대상 묶음 목록, 박스 정렬에서 두 칸을 붙여 둘지는 정하지 않았다.
SSOT: `docs/specs/s5.md`의 상세와 진화, `docs/specs/s5-scenarios.md`의 SC-07, `docs/specs/ui-components.md`의 C-06.

### 대기 튜토리얼과 완료한 행동 (SR-02)

날짜: 2026-09-23. 근거: [시나리오 후속 항목](feedback.md#시나리오-설계-후속-항목)의 SR-02 잔여 항목이다. 기능 구현 미시작.
질문 1 대기 표시: 튜토리얼이 진행 중일 때 생긴 다음 조건을 사용자에게 알릴지다. 사용자 답변: “a”. 대기 표시는 두지 않는다. 앞 튜토리얼을 마치거나 닫으면 다음 튜토리얼이 바로 뜬다. 대기 표시를 두는 안은 채택하지 않았다. 업적 dot와 혼동할 수 있다.
질문 2 완료한 행동: 튜토리얼이 뜨기 전에 사용자가 이미 그 행동을 끝낸 경우다. 사용자 답변: “a로 진행”. 끝낸 단계는 건너뛰고 남은 단계부터 보여준다. 남은 단계가 없으면 그 튜토리얼을 완료로 기록하고 띄우지 않는다. 처음부터 전부 보여주는 안과 아예 띄우지 않는 안은 채택하지 않았다.
저장: 튜토리얼별 상태에 단계별 완료 여부를 함께 저장한다. 앱 종료로 끊긴 튜토리얼은 기존 규칙대로 처음부터 다시 보여주되, 이미 끝낸 행동의 단계는 같은 규칙으로 건너뛴다.
SSOT: `docs/specs/s5.md`의 행동에 따른 단계별 튜토리얼, `docs/specs/modules.md`의 저장 구조.

### 밸런스 기준점과 가격표

날짜: 2026-09-23. 사용자 지시: 시나리오 항목을 마친 뒤 밸런스 수치로 넘어간다. 코드 구현 미시작.
질문 1 기준점: 하루 사용 시간과 상품 하나의 관계다. 사용자 답변: “4시간정도하면 알하나 살 수 있을정도면 좋다. a정도면.” 이틀 1개와 반나절 1개 안은 채택하지 않았다.
계산: 개체 1마리당 2분에 1포인트를 기준으로 두면 4시간에 120포인트다. 친밀도 100이면 1분에 1포인트로 최대 2배가 된다. 파티 개체마다 각자 적립하므로 마릿수가 늘면 수입도 는다.
질문 2 가격표: 기준점에서 계산한 제안을 냈다. 사용자 답변: “우선은 제안대로 진행”. 랜덤알 120P, 시작 포인트 120P, 경험사탕 20·40·80·160·320P, 이상한사탕 200P, 프리미엄먹이 60P, 장난감 40P, 민트 100P, 진화의돌 150P, 모습이 바뀌는 약 1,200P, 돌아오는 약 0P, 파티 칸 300P와 600P다. 종 지정 구매는 기존 `data/unlocks.json` 가격을 유지한다.
작업: `docs/specs/balance.md`를 만들었다. 기준점, 가격표, 남은 수치 8종을 적었다.
`[스펙 미확정]` 경험치 곡선, 만복도 감소와 구간, 밥 주기 쿨타임, 버프 배율·지속시간, 친밀도 획득량, 확률, 알 단축량, 두 번째 파티 칸 업적은 남았다.
SSOT: `docs/specs/balance.md`.

### 경험치 곡선과 사탕 경험치량

날짜: 2026-09-23. 질문: 레벨별 필요 경험치와 사탕별 경험치량이다. 제안은 `레벨 × 20`의 완만한 자체 곡선이었다. 사용자 답변: “경험치양은 원작포켓몬 따라 데이터 세팅해”. 자체 곡선 세 안은 채택하지 않았다. 코드 구현 미시작.
확정 범위: 레벨 곡선은 원작의 성장 속도를 따른다. 종마다 성장 속도를 데이터에 넣는다. 원작의 여섯 가지는 빠름, 보통 빠름, 보통 느림, 느림, 불규칙, 변동이다. 100레벨까지 필요한 누적 경험치는 약 60만에서 164만이다.
경험사탕 값도 원작을 따른다. XS 100, S 800, M 3,000, L 10,000, XL 30,000 경험치다. 이상한사탕은 레벨 1 상승이라는 기존 결정을 유지한다.
환산 확인: 보통 빠름 종은 100레벨까지 약 100만 경험치다. XL 사탕 약 34개이며 가격은 10,880P다. 하루 4시간 기준 약 3주다. 작은 사탕은 단가가 비싸고 미세 조정용이다.
데이터: 현재 `data/species.defaults.json`에는 성장 속도가 없다. 종별 성장 속도를 새로 채운다. 출처는 종 데이터와 같은 경로를 쓴다.
`[스펙 미확정]` 성장 속도 데이터의 필드 이름과 채우는 방법, 레벨 구간별 진화 조건과의 관계는 정하지 않았다.
SSOT: `docs/specs/balance.md`.

### 만복도와 밥 주기 수치

날짜: 2026-09-23. 질문: 만복도 감소 속도, 구간, 기본먹이 회복량, 밥 주기 쿨타임이다. 제안은 8시간에 100 감소였다. 사용자 답변: “만복도는 2시간정도에 밥한번씩 주게. 할거라서 한 2시간에 60떨어지는느낌. 배고프다는 메시지는 배고픔상태일떄, 보통일땐 안뜨게. 대신 포켓몬 상세에서 밥은 줄수있는.” 8시간 안은 채택하지 않았다. 코드 구현 미시작.
확정 범위: 만복도는 시간당 30 줄어든다. 2시간에 60이다. 구간은 배부름 100~60, 보통 60~40, 배고픔 40~15, 매우 배고픔 15~0이다. 말풍선은 배고픔과 매우 배고픔에 들어갈 때만 한 번 표시한다. 보통 구간에서는 표시하지 않는다.
밥 주기: 만복도가 가득이 아니고 쿨타임이 아니면 언제나 줄 수 있다. 개체 상세와 우클릭 메뉴에서 준다. 기본먹이는 20을 회복하고 쿨타임은 10분이다. 프리미엄먹이는 100까지 채운다.
디버프: 배고픔 구간은 친밀도 증가량 30%를 줄인다. 매우 배고픔은 60%를 줄인다. 누적 친밀도는 깎지 않는다.
환산: 2시간 두면 100에서 40이 된다. 배고픔 구간에 들어가 말풍선이 한 번 뜬다. 기본먹이 세 번이면 30분에 다시 가득 찬다.
SSOT: `docs/specs/balance.md`.

### 버프와 친밀도 수치

날짜: 2026-09-23. 질문: 버프 배율과 지속시간, 친밀도 획득량이다. 사용자 답변: “그렇게 진행”. 코드 구현 미시작.
확정 범위: 프리미엄먹이 버프는 친밀도 증가량 2배이며 2시간이다. 오래 놀아주기 버프는 1.5배이며 30분이다. 장난감 버프는 오래 놀아주기와 같다. 같은 먹이를 다시 주면 남은 시간을 기본 지속시간으로 갱신한다. 시간을 더하지 않는다.
친밀도: 함께한 시간 10분에 1 오른다. 숨김 상태도 같다. 밥 주기는 2, 놀아주기는 3 오른다. 쿨타임마다 한 번이다. 하루 상한은 두지 않는다. 기존 S4의 하루 상한은 승계하지 않는다.
환산: 친밀도 100까지 약 16시간이다. 친밀도가 오르면 포인트 적립 간격이 2분에서 1분으로 짧아진다. 며칠 키우면 수입이 두 배가 된다.
SSOT: `docs/specs/balance.md`.

### 확률과 알 수치

날짜: 2026-09-23. 질문: 이로치 확률, 수집 난이도 가중치, 알 돌봄 단축량이다. 사용자 답변: “진행”. 코드 구현 미시작.
확정 범위: 이로치 확률은 1/1000이다. 알에서 태어날 때와 부화 결과 모두 같다. 사용자 예시 값을 그대로 쓴다.
수집 난이도 가중치: 1등급 100, 2등급 50, 3등급 20, 4등급 5, 5등급 1이다. 5등급은 1등급보다 100배 귀하다. 난이도는 종족값 합계, 진화 단계, 전설과 환상 여부로 자동 계산한다. 종별 예외는 덮어쓴다.
알: 준비 시간 기준은 5분이다. 돌봄 한 번에 30초를 줄인다. 돌봄 쿨타임은 1분이다. 쓰다듬기와 노래 들려주기의 단축량은 같다. 결과 조건만 다르다.
환산: 열심히 돌보면 약 3분까지 줄어든다. 방치해도 5분이면 열 수 있다.
SSOT: `docs/specs/balance.md`.

### 고해상도 조립 1묶음 · 관리 창 탭

날짜: 2026-09-23. 사용자 지시: 와이어프레임을 실제 컴포넌트로 조립한다. 1묶음은 관리 창 탭이다. 기능 구현 미시작.
만든 템플릿: `Dex Layout` `378:1524`, `Shop Layout` `379:1757`, `Bag Layout` `380:2064`. 각 템플릿은 헤더, 해당 탭, 제목 줄, 분류·검색 줄, 목록, 상세 또는 사용 패널로 구성한다. 모두 최소 높이 780이며 내용이 길면 펼친다.
만든 화면: `05 · Screens`에 `Dex / Base` `381:6028`, `Shop / Base` `381:6264`, `Bag / Base` `381:6555`, `Dex / Empty Result` `381:6813`, `Box / Empty` `381:7076`을 추가했다. 화면은 12개가 됐다.
새 컴포넌트: `Field` `377:303`을 만들었다. 검색·선택 입력칸이며 높이 32로 버튼과 맞춘다. `Show Caret`으로 선택칸의 ▾를 켠다.
막힌 점과 처리: 인스턴스 안에는 노드를 넣을 수 없다. 그래서 빈 상태를 화면에서 추가하지 못했다. `Dex Layout`과 `Box Layout`에 `Show Empty`와 목록 표시 속성을 넣고 화면에서 속성만 바꾸는 방식으로 바꿨다.
수정: 종 카드 5열이 들어가도록 본문 좌우 여백을 24로 맞췄다. 미해금 카드가 배경과 같은 색이라 `bg/subtle`로 바꿨다. 빈 박스 화면의 사용 칸 수를 `0 / 30`으로 고쳤다.
가격 반영: 상점 템플릿의 가격은 [밸런스 수치](../../specs/balance.md)를 따른다. 랜덤알 120P, 사탕 20~320P, 프리미엄먹이 60P, 진화의돌 150P, 이로치 약 1,200P, 파티 칸 300P다.
남은 묶음: 모달, 상세·부화, 바탕화면이다.
SSOT: `docs/specs/ui-components.md`의 템플릿 줄.

### 고해상도 조립 2묶음 · 모달

날짜: 2026-09-23. 1묶음에 이어 모달 화면을 조립했다. 기능 구현 미시작.
만든 화면: `Achievements / Base` `384:4819`, `Settings / Base` `385:5145`, `Settings / Guidebook` `385:5534`, `Picker / Box Pokemon` `387:5827`, `Picker / Party Slot` `387:6257`, `Shop / Buy` `387:6618`, `Shop / Buy Blocked` `387:6958`이다. `05 · Screens`는 화면 19개가 됐다.
구성: 배경은 해당 탭 템플릿 인스턴스다. 그 위에 `Modal Scrim`과 `Dialog`를 올린다. 대화상자 본문은 slot이라 화면마다 내용을 채운다. 업적창은 업적 행 4개, 설정은 설정 행 9개, 가이드북은 주제 5개, 박스 개체 고르기는 검색 줄과 박스 칸 18개, 파티 칸 고르기는 대상 행 6개(잠긴 칸은 비활성), 구매 창은 수량 조절과 합계, 구매 불가는 이유와 비활성 구매다.
배경 상태: 업적창을 열면 헤더의 업적 아이콘이 열림 상태이고 dot가 켜져 있다. 설정 화면도 설정 아이콘이 열림 상태다.
수정: `Field`가 빈 공간 채움 때문에 폭이 줄지 않아 설정의 선택칸이 잘렸다. 빈 공간을 없애고 양끝 정렬로 바꿔 내용 폭을 따르게 했다. 설정 대화상자는 규칙대로 내용 끝까지 펼쳐 840 높이가 됐다. 화면은 940이며 창 끝 점선을 넣었다.
정렬: 05 페이지를 흐름 순서로 다시 배치했다. 겹침은 없다. 780을 넘는 화면에는 점선과 설명을 붙였다.
남은 묶음: 상세·부화, 바탕화면이다.
SSOT: `docs/specs/ui-components.md`의 템플릿 줄.

### 고해상도 조립 3묶음 · 상세·부화

날짜: 2026-09-23. 2묶음에 이어 개체 상세와 알 흐름을 조립했다. 기능 구현 미시작.
만든 화면: `Detail / Box Pokemon` `389:7210`, `Detail / Evolution Confirm` `389:7454`, `Detail / Evolution Branch` `389:7717`, `Box / Egg Care` `389:9211`, `Box / Hatch Result` `389:9488`이다. `05 · Screens`는 화면 24개가 됐다.
박스 개체 상세: 상태 안내와 돌봄과 표시 전환을 끈다. 박스 개체는 돌봄 대상이 아니기 때문이다. 아래 버튼은 파티에 넣기 하나다. 내비게이션은 박스 탭이 활성이다. 예시는 꼬부기 Lv.8이며 다음 레벨까지 20%다.
진화 확인: 박스 개체 상세 위에 대화상자를 올린다. 진화 전과 후를 나란히 놓는다. 조건과 결과를 한 줄씩 적는다. 주 버튼은 진화이고 보조 버튼은 취소다.
진화 분기: 결과가 둘 이상인 종에서 후보를 고른다. 후보 카드를 나란히 놓고 하나를 고른 상태로 보여준다.
알 돌봄: `Box Layout` `340:3665`에 돌봄 패널과 `Show Egg Care` 속성을 넣었다. 패널은 알 제목, 준비 막대, 돌봄 타일 2개, 반응 안내로 구성한다. 타일은 쓰다듬기와 노래 들려주기이며 한 번에 30초를 줄인다. 수치는 [밸런스 수치](../../specs/balance.md)를 따른다.
부화 결과: 박스 배경 위에 대화상자를 올린다. 종, 타입, 레벨, 성격을 보여준다. 어느 자리에 들어갔는지 안내 상자로 알린다. 버튼은 확인 하나다.
정렬: 05 페이지를 다시 배치했다. 새 화면 5개를 박스 화면 뒤에 넣고 뒤쪽 화면 11개와 라벨 4개를 오른쪽으로 옮겼다. 겹침은 없다. 780을 넘는 `Box / Egg Care`에 점선과 설명을 붙였다.
막힌 점: 화면 프레임은 자동 배치가 아니라서 `layoutPositioning`을 `ABSOLUTE`로 지정할 수 없다. 접힘선은 좌표로 직접 놓았다.
남은 묶음: 바탕화면이다. 놀이공간, 영역 그리기, 알림 배너 3종, 튜토리얼 5종이 남는다.
SSOT: `docs/specs/ui-components.md`의 템플릿 줄.

### 고해상도 조립 4묶음 · 바탕화면과 튜토리얼

날짜: 2026-09-23. 3묶음에 이어 바탕화면과 튜토리얼을 조립했다. 기능 구현 미시작.
만든 화면: `Tutorial / Shop` `399:8590`, `Tutorial / Hatch` `399:8901`, `Tutorial / Party` `399:9159`, `Tutorial / First Care` `397:8552`, `Tutorial / Playground` `397:8596`, `Playground / Base` `394:8423`, `Playground / Context Menu` `395:8434`, `Playground / Banner Hatch` `395:8492`, `Playground / Banner Evolution` `395:8524`, `Playground / Banner Achievement` `395:8559`, `Playground / Region Draw` `396:8541`이다. `05 · Screens`는 화면 35개가 됐다.
화면 크기: 바탕화면 화면은 1280×720이다. 관리 창이 아니라 화면 전체이므로 창 끝 780 규칙을 쓰지 않는다. 관리 창 튜토리얼 3개는 창 규칙 그대로 내용 끝까지 펼치고 780에 점선을 넣었다.
바탕화면 바탕: 배경은 바탕화면 그림 모형이고 그 위에 사용자가 쓰는 앱 창 모형을 둔다. 둘 다 제품 UI가 아니라 상황 설명용이라서 변수에 연결하지 않는다. 숨김 마커의 몬스터볼 색과 같은 그림 예외다.
놀이공간: 포켓몬 3마리를 `Portrait`로 놓고 크기를 56·48·52로 맞췄다. 기본 화면은 배고픔 말풍선을 보여준다. 우클릭 메뉴 화면은 말풍선을 빼고 `Context Menu`를 포켓몬 위에 놓는다.
알림 배너: 부화·진화·업적 3개 화면을 만들었다. 배너는 `Notification Banner`의 `Type` 변형이며 오른쪽 아래에 24 여백으로 붙는다. 한 화면에 하나만 둔다.
영역 그리기: 영역은 880×420이다. 바깥 네 곳을 배경막으로 덮고 테두리와 모서리 손잡이 4개를 둔다. 크기 표시와 미리보기 안내는 어두운 알약 배경에 흰 글자로 둔다. 도구 막대는 안내 문구와 다시 그리기·취소·적용 버튼이다.
튜토리얼: 대상만 밝히고 나머지를 배경막으로 덮는 방식이다. 상점은 랜덤알 카드, 부화는 돌보미집 줄, 파티는 첫 개체 칸을 밝힌다. 파티 화면은 안내 문구에 맞춰 첫 칸의 숨김 마커를 켰다. 코치마크는 밝힌 곳 바로 아래에 둔다. 바탕화면 튜토리얼 2개는 포켓몬 한 마리만 두고 나머지 2마리를 뺐다.
막힌 점: 문구로 대상을 찾을 때 상위로 거슬러 올라가는 반복에 종료 조건이 없어 널 참조가 났다. 부모가 있을 때만 올라가도록 고쳤다.
남은 화면: 첫 선택 화면 2개(`WF-08`, `WF-08b`)는 묶음 계획에서 빠져 있었다. 다음 작업으로 남긴다.
SSOT: `docs/specs/ui-components.md`의 템플릿 줄.

### 첫 선택 화면 조립

날짜: 2026-09-23. 4묶음에서 빠진 첫 선택 화면을 조립했다. 기능 구현 미시작.
만든 화면: `First Run / Starter Selected` `402:9417`, `First Run / Starter Empty` `402:9579`다. 높이는 둘 다 857이며 창 끝 780에 점선을 넣었다. `05 · Screens`는 화면 37개가 됐다.
배치: 첫 선택은 첫 실행 화면이라 05 페이지의 맨 앞에 두었다. 기존 화면 35개와 라벨을 오른쪽으로 옮겼다.
새 컴포넌트: `Candidate Card` `401:315`을 만들고 계약 [C-26](../../specs/ui-components.md)으로 적었다. `Species Card`를 쓰지 않은 이유는 도감 번호와 획득 상태를 끄는 속성이 없어서다.
선택 표시: 왼쪽 blade와 tonal 배경으로 보여준다. 두 변형 모두 1px 중립 테두리를 두어 높이를 같게 했다.
구성: 제목과 한 줄 안내, 후보 29개를 5열로 놓는 격자, 선택 요약과 함께하기 버튼이다. 미선택 화면은 요약 자리에 `포켓몬을 고르세요`를 넣고 버튼을 비활성으로 둔다.
문구 정리: 와이어프레임의 후보 설명은 설계 주석이라 화면에 넣지 않았다. 대신 `함께 시작할 포켓몬을 한 마리 고르세요`를 넣었다.
막힌 점: 자동 배치 프레임에 `resize`를 부르면 세로 크기 방식이 고정으로 바뀐다. 내용이 잘려서 자식을 다 넣은 뒤에 다시 자동으로 바꿨다.
SSOT: `docs/specs/ui-components.md`의 C-26.

### 저장과 거래의 남은 값 확정

날짜: 2026-09-23. 사용자 지시: “커밋하고 다음진행”. 재개 순서 6번인 저장 필드 확정을 이어갔다. 코드 구현 미시작.
코드 확인 결과 2개: `party.summon`과 `party.dismiss`는 저장소 어디에도 없다. 현재 이름은 `party.show`와 `party.hide`다. VS Code 확장(`vscode-extension/extension.js`, 247줄)은 게임 명령을 보내지 않는다. 창 식별자, 호스트 PID, 터미널 수, 포커스 여부를 `windows/` 아래 JSON으로 쓸 뿐이다. 두 미확정 항목은 질문이 아니라 사실 정정이었다.
기존 가방 확인: `SaveV2`의 `inventory`는 자유로운 `Record<string, number>`다. 실제로 쓰는 키는 `shiny:<개체 식별자>` 하나뿐이다(`src/shop/core.ts`, `src/dex/progress.ts`). 먹이 재고는 없다. 기존 구현의 밥 주기는 무료다.
결정 이로치 약의 소모 단위: 제안은 개체별 권리를 저장해 두 약을 무료로 오가는 안이었다. 사용자 답변: “바꿀 때마다 다시 산다”. 모습이 바뀌는 약은 1회 소모품이다. 기존 `shiny:<개체 식별자>` 기록은 권리로 옮기지 않고 `legacy`에 보존한다. 이미 이로치인 개체는 `pets[].shiny`로 남으므로 색을 잃지 않는다.
구현 세부 확정 4개: 요청 ID와 결과는 저장의 `tx`에 두고 최근 200건 또는 24시간 중 큰 쪽을 남긴다. 거래는 한 번에 하나만 처리하고 들어온 순서대로 줄을 세운다. 주기 저장은 30초마다 한다. 저장이 3회 이어서 실패하면 관리 창 상태 안내에 남기고 조작은 막지 않는다.
결정 알림 배너 모듈: `src/notify`로 뺀다. 상태 판정은 도메인 모듈이 하고 `src/notify`는 줄 세우기와 표시만 맡는다. 모듈은 17개가 됐다.
SSOT: `docs/specs/modules.md`. 이로치 약 규칙은 `docs/specs/s5.md`와 `docs/specs/balance.md`를 함께 고쳤다.

### 진화 조건 대체표 결정

날짜: 2026-09-23. 사용자 지시: “커밋하고 다음진행”. 콘텐츠 데이터를 채우기 전에 진화 조건을 확정했다. 코드 구현 미시작.
조사: `data/evo.json`은 진화 사슬과 낮·밤만 담는다. 조건은 없다. 생성 도구 `src/tools/build-evo.ts`가 PokeAPI CSV에서 만든다. 원본에는 조건이 다 들어 있다. 도감 종 기준 분포는 레벨 347종, 도구 64종, 교환 26종, 친밀도 20종, 기술 16종, 장소 6종, 시간대 4종, 그 밖의 특수 12종이다.
결정 1 없는 조건의 대체: 제안은 조건 종류별로 나누는 안, 전부 친밀도로 하는 안, 전부 레벨로 하는 안 3개였다. 사용자 답변은 종류별 지정이다. “교환은 유대의끈 이라는 아이템으로. 장소는 대표장소에 맞는 돌 같은거 있잖아. 그거로. 예를들어 레어코일 자포코일은 천둥의돌로 한다던지. 기술은 추후 확장을 위해 기술이 생길 수 있긴한데, 지금은 빈CD로 진화되게. 특수는 친밀도. 저승갓숭같은경우는 기술+특수잖아. 그냥 기술있으면 되는느낌.”
결정 2 특수 도구 16종: 제안은 원작 도구 그대로 두는 안, 결로 묶어 6종으로 줄이는 안, 돌로 대체하는 안 3개였다. 사용자 답변: “1번으로 하는데, 진화용 아이템은 상점에 탭 따로 구분해놓고 찾기쉽게 해놔.” 원작 도구를 그대로 두고 상점에 진화 탭을 만든다.
정리한 우선순위: 도구 → 교환 → 기술 → 장소 → 그 밖의 특수 → 원작 값 그대로다. 기술과 특수가 겹치면 기술로 본다. 사용자 발언의 저승갓숭이 그 예다.
장소 6종의 돌: 자포코일·대코파스·투구뿌논은 천둥의돌, 리피아는 리프의돌, 글레이시아와 털겅숭이는 얼음의돌이다. 원작 9세대도 리피아와 글레이시아를 리프의돌과 얼음의돌로 진화시킨다.
새 도구 2종: `유대의끈`과 `빈 CD`다. 이름은 가칭이다. 빈 CD는 나중에 기술 체계를 넣을 자리다. 사용자가 개체마다 기술 1개를 넣을 계획을 말했다.
진화용 도구 수: 진화의돌 10종, 특수 도구 16종, 새 도구 2종으로 28종이다. 상점 분류에 진화 탭을 더한다.
`[스펙 미확정]` 도구별 가격과 분기 확인 화면은 종별 조건표를 만든 뒤 정한다. 기술 체계는 이번 범위가 아니다.
SSOT: `docs/specs/s5.md`의 진화 계약.

### 콘텐츠 데이터 생성

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음작업 진행”. 확정한 진화 조건 대체표를 데이터로 만들었다. 설계 문서가 아니라 코드와 데이터를 바꾼 첫 작업이다.
방침: 손으로 표를 쓰지 않는다. 기존 생성 도구가 PokeAPI CSV에서 만들고 있으므로 그 도구를 늘렸다. 원작 값을 사람이 옮겨 적으면 틀린다.
바꾼 파일: `src/shared/types.ts`(`GrowthRate`·`EvoNeed` 추가), `src/dex/species.ts`(기본 프로필), `src/dex/evo.ts`(`EvoStep.need`), `src/tools/build-species.ts`, `src/tools/build-evo.ts`, 새 `src/tools/build-evo-items.ts`, `package.json`의 `data:build`, `src/tools/selftest-dex.ts`다.
종 데이터: `growthRate`, `bst`(종족값 합계), `stage`(사슬 깊이), `rank`(수집 난이도)를 1089종에 더했다. 성장 속도는 보통 빠름 450, 느림 266, 보통 느림 263, 빠름 67, 불규칙 29, 변동 14다. 난이도는 1등급 499, 2등급 260, 3등급 183, 4등급 31, 5등급 116이다. 4등급은 600족 계열이고 5등급은 전설·환상이다.
진화 조건: 간선 484개에 `need`를 붙였다. 레벨 348, 도구 100, 친밀도 36이다. 시간대 16개는 그대로 뒀다.
우선순위 정정: 처음에는 도구를 가장 먼저 봤다. 그러자 야돈이 원작 레벨 37 대신 가라두꽃으로 잡혔다. 레벨과 친밀도를 먼저 보도록 고쳤다. 야돈은 레벨 37, 야도왕은 가라두구머리장식이 됐다. 이 정정을 `docs/specs/s5.md`의 표에 반영했다.
막힌 점: 데스판은 피해 조건에 장소가 붙어 있어 장소 규칙에 걸렸다. 장소는 레벨업으로 진화할 때만 보도록 좁혔다. 데스판은 그 밖의 특수로 가서 친밀도 100이 됐다.
도구 수: 계획은 28종이었으나 결과는 24종이다. 야돈이 레벨로 가며 가라두꽃이 빠졌다. 결과가 같은 대체 도구 3종(이빠진 주전자, 명품 찻잔, 물의 족자)은 종 단위 표라 하나씩만 남았다. 원작에서 이 셋은 폼을 가르지만 우리 도감은 종 단위다.
새 데이터: `data/evo-items.json`에 도구 24종의 한국어·영어 이름과 대상 종을 담았다. 이름은 PokeAPI의 한국어 이름을 그대로 쓴다. 우리 도구 2종의 이름만 직접 준다.
검증: `npm run build` 통과. `npx tsc --noEmit` 통과. `npm run selftest` 통과. 기본 프로필을 비교하던 `selftest-dex`의 기대값에 새 필드 4개를 더했다.
`[스펙 미확정]` 알 행동 조건의 종별 표와 도구별 가격은 아직 없다. 기술 체계도 없다.
SSOT: `docs/specs/s5.md`의 진화 계약, `docs/specs/balance.md`.

정정 2026-09-24: 데스판을 친밀도 100에서 어둠의돌로 바꿨다. 사용자 지적은 “데스판은 어둠의돌 이런느낌이 나을듯? 친밀도보단”이다. 원작 조건이 폐허에서 바위 아치를 지나는 것이라 장소가 뜻을 가진다. 장소를 레벨업일 때만 보던 제한을 없애고 데스판을 장소 표에 넣었다. 진화 방식이 배틀이나 피해라도 장소가 있으면 장소 규칙을 먼저 쓴다. 결과는 도구 101개, 친밀도 35개다. 어둠의돌 대상은 5종이 됐다. `npm run selftest` 다시 통과.

### 기능 계약의 미확정 문장 정리

날짜: 2026-09-24. 사용자 지시: “추천대로 진행”. 새 결정 없이 이미 확정된 것을 반영하는 정리 작업이다. 기능 구현은 이번 범위가 아니다.
배경: `docs/specs/s5.md`는 밸런스와 모듈 설계보다 먼저 쓰였다. 그래서 `docs/specs/balance.md`와 `docs/specs/modules.md`, 새 데이터가 이미 답한 것을 아직 미확정으로 적고 있었다. 뒤 작업이 잘못된 문서를 근거로 삼는 것을 막으려고 먼저 정리했다.
닫은 문장 10개: 만복도 수치, 먹이 쿨타임과 시작 시점, 버프 배율과 지속시간과 장난감 가격, 상점 분류, 종별 진화 조건표, 저장 정밀도와 같은 시점 처리, 알 난이도 가중치와 종별 배정, 알 돌봄 차감, 알 돌봄 행동별 단축량과 인정 간격, 종별 경험치 곡선이다.
일부만 닫은 것 6개: 버프 중첩 계산, 진화 외 추가 조건, 저장 실패 복원, 후보 정규화와 가중치 변경 시점, 두 행동의 조합 방식, 하루 상한 승계와 진화 연출 중 시간은 아직 답이 없어 남겼다. 표식은 62개에서 58개로 줄었다.
새로 정한 것 2개: 먹이 쿨타임은 사용이 성공한 순간부터 센다. 진화로 경험치 곡선이 바뀌면 누적 경험치를 그대로 두고 레벨만 새 곡선으로 다시 읽는다. 원작과 같은 처리이며 실제로 곡선이 바뀌는 간선은 사과벌레 계열 2개뿐임을 데이터로 확인했다.
상점 분류: `전체 / 알 / 포켓몬 / 도구 / 진화 / 파티 칸`으로 적었다. Figma `Shop Layout`에는 아직 반영하지 않았다.
확인: 기본먹이 20 회복과 시간당 30 감소, 쿨타임 10분이면 배고픔 구간에서 한 번에 순증 15다. 기본먹이만으로 최대 만복도까지 올릴 수 있다는 문장을 계산으로 확인했다.
SSOT: `docs/specs/s5.md`.

### 진화용 도구 가격과 노출

날짜: 2026-09-24. 사용자 지시: “추천대로 진행”. 남은 콘텐츠 데이터 중 가격을 정하고 상점 화면에 반영했다. 기능 구현 미시작.
결정 1 가격: 제안은 일반 150P와 전용 300P의 2단계, 전부 150P 공통, 대상 종 수에 반비례하는 세 가지였다. 사용자 답변: “전부 150P 공통”. 24종이 모두 같은 값이다. 값이 하나라 상점 표기와 관리가 간단하다.
결정 2 노출: 제안은 대상 종을 해금했을 때만 보이는 안과 항상 모두 보이는 안이었다. 사용자 답변: “항상 24종 모두”. 포켓몬 탭이 해금한 종만 보여주는 것과 다르다. 앞으로 무엇이 있는지 보여준다.
Figma 반영: `Shop Layout` `379:1757`에 분류 `진화`를 더하고 `section/진화` `404:2309`를 만들었다. 상품 24개를 대상 종 수 내림차순으로 놓았다. 설명 줄은 `대상 N종`이고 가격은 모두 150P다. 도구 절에 있던 상품 `진화의돌` 하나는 뺐다. 이제 도구 절은 11개다.
화면 높이: 상점 템플릿이 1106에서 2052로 늘었다. `Shop / Base` `381:6264`와 `Tutorial / Shop` `399:8590`의 프레임을 2052로 맞추고 튜토리얼의 아래쪽 배경막도 늘렸다. `Shop / Buy`와 `Shop / Buy Blocked`는 모달 화면이라 780을 유지한다.
막힌 점: 절 제목은 `Text / Heading`의 컴포넌트 속성이라 글자를 직접 쓰면 바뀌지 않는다. `Content` 속성으로 바꿨다. 상품 설명 줄의 이름도 `sub`가 아니라 `description`이었다.
하지 않은 것: 진화 분류를 고른 상태의 별도 화면은 만들지 않았다. 전체 보기에 진화 절이 이미 들어 있다.
SSOT: `docs/specs/s5.md`의 진화 계약과 화면 구성표, `docs/specs/balance.md`의 가격표.

### 알 행동 조건의 종별 표

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음작업 진행”. 남은 콘텐츠 데이터의 마지막이다. 기능 구현 미시작.
결정 1 조건 문법: 제안은 7가지로 나누는 안, 3가지로 두는 안, 순서까지 넣는 안이었다. 사용자 답변: 7가지다. 쓰다듬기와 노래 들려주기의 인정 횟수를 각각 세고 3~7회와 8회 이상으로 나눈다. 다른 행동이 0회면 `…만` 조건이다. 둘 다 0회는 방치 조건이다.
구간을 겹치지 않게 한 이유: 8회 조건이 3회 조건을 자동으로 만족하면 더 많이 돌본 보람이 없다. 그래서 3~7회를 닫힌 구간으로 두었다. 대신 쓰다듬기 10회와 노래 4회처럼 어디에도 맞지 않는 조합이 생긴다. 그런 알은 일반 추첨으로 간다. 부화를 막지 않는 기존 원칙과 같다.
결정 2 대상 종: 제안은 테마가 맞는 종, 귀한 종, 무작위 배정이었다. 사용자 답변: “2번으로 하는데, 도감 조건에 잘 적어놔야지.” 발견한 조건은 도감 상세에 문구 그대로 적는다.
정정: 첫 배정은 4·5등급에서 골라 29종이 모두 전설·환상이 됐다. 사용자 지적은 “지금 너가 대상으로 올린것들은 전설이나 환상이잖아. 이거는 알로 못구하는건데?”다. 확인하니 기능 계약 “단일 포켓몬은 알에서 획득할 수 없다. 알별 후보 범위와 행동 조건에 따른 부화 결과에서 모두 제외한다”를 정면으로 어겼다. 선택지에 “4·5등급”이라고만 적고 5등급이 곧 전설·환상이라는 사실을 밝히지 않은 것이 원인이다.
재배정: 3·4등급에서 전설·환상과 울트라비스트를 빼고 다시 골랐다. 조건 7개에 3~5종씩이다. 테마도 함께 봤다. 노래 조건에는 누리레느·님피아처럼 소리와 어울리는 종을, 쓰다듬기 조건에는 윈디·해피너스처럼 사람을 따르는 종을, 방치 조건에는 야느와르몽·산호르곤처럼 어둠과 고독을 두었다. 각각 3~7회에는 1세대 스타터 최종형 3마리를 묶었다. 4등급 네 종(망나뇽, 미끄래곤, 마기라스, 한카리아스)은 난도가 맞는 조건에 넣었다.
정정 2: 재배정 1차에 이브이 계열 세 종(에브이, 블래키, 님피아)을 서로 다른 조건에 넣었다. 사용자 지적은 “에브이 블래키 님피아가 왜 저기에 있지? 진화전이 이브이 1개잖아.”다. 진화 전이 하나로 모이는 계열을 갈라 넣으면 이브이를 얻어 진화시키는 정상 경로와 겹친다. 규칙을 더했다. 한 진화 계열에서는 한 종만 배정한다. 친밀도 진화라는 뜻이 맞는 블래키만 남기고 에브이와 님피아를 뺐다. 빈자리에 단독 종인 라프라스를 넣었다. 대상은 28종이 됐다.
대안 검토: 진화하지 않는 단독 종만 쓰는 안도 봤다. 3등급 이상 단독 종이 16종뿐이라 조건 7개를 채우지 못해 접었다.
정정 3: 리자몽·거북왕·이상해꽃을 넣은 것도 사용자가 지적했다. “스타팅은 그냥 구할 수 있게해야지.” 스타터는 첫 선택과 상점으로 평범하게 얻어야 한다. 규칙을 더했다. 첫 선택 후보 29종의 계열은 조건 대상에서 뺀다. 데이터로 걸러 보니 리자몽·거북왕·이상해꽃 말고도 누리레느(뿌리 누리공)와 블래키(뿌리 이브이)가 걸렸다. 다섯 종을 빼고 가디안, 메가자리, 렌트라, 플라이곤, 전룡을 넣었다. 대상은 28종을 유지한다.
정정 4: 사용자 지시로 가장 어려운 조건의 성격을 바꿨다. “각각 8회+ 에는 각 시리즈별의 600족들 ex.망나뇽 한카리아스 보만다 등 을 배치”. 세대별 종족값 600 계열 10종(망나뇽·마기라스·보만다·메타그로스·한카리아스·삼삼드래·미끄래곤·짜랑고우거·드래펄트·드닐레이브)을 모았다. 흩어져 있던 망나뇽과 미끄래곤을 옮기고 나머지 여덟 종을 더했다. 그 자리에 있던 갸라도스와 잠만보는 한 단계 쉬운 각각 3~7회로 옮겼다. 대상은 34종이 됐다. 조건마다 종 수를 맞추기보다 조건의 뜻을 맞춘다.
정정 5: 사용자가 “조금만 더 채워봐. 너무 부족한느낌?”이라고 했다. 조건마다 8종으로 늘렸다. 600족 조건만 10종을 유지한다. 대상은 58종이다. 새로 넣은 종도 테마를 따랐다. 쓰다듬기 3~7회에는 바랜드·이븐곰·마피티프처럼 사람을 따르는 종을, 쓰다듬기 8회 이상에는 강철톤·고래왕처럼 오래 함께해야 마음을 여는 큰 종을, 노래 조건에는 스트린더·화염레오처럼 소리를 내는 종을, 방치 조건에는 킬가르도·조로아크처럼 혼자 있는 종을 넣었다.
막힌 점과 결정 3: 첫 표를 검증하다 29종 중 27종이 `data/unlocks.json`에 해금 규칙이 없다는 것을 찾았다. 기존 규칙대로면 후보 범위 밖이라 조건을 충족해도 나오지 않는다. 기능 계약에도 범위 밖 예외가 미정으로 남아 있었다. 사용자 답변: 행동 조건이 범위를 넘어선다. 조건을 알아내는 것이 그 종의 또 다른 입수 경로다. 일반 무작위 부화는 지금처럼 후보 범위 안에서만 한다. 58종 중 56종은 해금 규칙이 있고 타타륜과 프테라만 없다. 두 종은 행동 조건이 유일한 입수 경로다.
새 데이터: `data/egg-conditions.json`을 만들었다. 조건 7개의 범위와 한국어 문구, 종 29개의 배정을 담는다. 생성 도구가 만들지 않는 손 데이터다.
검증: 배정한 58종이 모두 종 데이터에 있고 등급이 3 이상이며 전설·환상·울트라비스트가 아님을 확인했다. 진화 사슬의 뿌리를 거슬러 올라가 58종이 모두 다른 계열이며 첫 선택 후보 계열이 아님을 확인했다. 전설·환상 판정은 PokeAPI의 `is_legendary`·`is_mythical`로, 울트라비스트는 종 목록으로 걸렀다. 조건 7개가 0~12회 범위의 모든 조합에서 겹치지 않음을 확인했다. 오류 0건이다.
`[스펙 미확정]` 행동·열기 동시 요청 처리 순서, 결과 저장 구현, 발견 전 힌트 문구는 정하지 않았다.
SSOT: `docs/specs/s5.md`의 알 행동 조건, `data/egg-conditions.json`.

### 태고의돌 화석 전용 알

날짜: 2026-09-24. 사용자 구상: “화석포켓몬은 상점에 @@돌 이런식으로 전용알 느낌으로 하고싶은데 이름이 고민이네. 모든 화석포켓몬들 다 이거로 부화하게.” 기능 구현 미시작.
결정 1 이름: 제안은 태고의돌, 화석의돌, 잠든돌, 원시의돌이었다. 사용자 답변: 태고의돌이다. 원작에 없는 이름이라 우리 상품임이 분명하고 진화의돌과 헷갈리지 않는다.
결정 2 부화 범위: 제안은 진화 전과 단독 종만 넣는 안과 화석 25종 전부를 넣는 안이었다. 사용자 답변: 진화 전 10종과 단독 5종만이다. 진화형 10종은 키워서 얻는다. 원작과 같고 키우는 재미가 남는다.
화석 종 판정: 원작에서 화석으로 되살리는 25종을 목록으로 적었다. PokeAPI에 화석 표시가 없어 자동 판정이 되지 않는다.
규칙: 화석 25종은 랜덤알 후보와 행동 조건 대상에서 모두 뺀다. 화석은 태고의돌 하나의 경로로만 얻는다.
표 수정: 행동 조건 표에 있던 화석 두 종을 바꿨다. 노래 8회 이상의 아케오스를 대쓰여너로, 방치의 프테라를 버랜지나로 바꿨다. 대상은 58종을 유지한다.
새 데이터: `data/eggs.json`을 만들었다. 알 종류별로 한국어 이름, 가격, 후보 범위를 담는다. 랜덤알은 후보 범위가 `unlocked`이고 태고의돌은 종 15개 목록이다.
가격: 태고의돌 200P다. 랜덤알 120P보다 비싸다. 후보가 15종으로 좁고 화석 전용이라 값을 올렸다. 사용자 답변 “가격은 그렇게 하고”로 확정했다.
검증: 태고의돌 후보 15종이 모두 화석 목록에 있고 종 데이터에 있음을 확인했다. 행동 조건 58종에 화석이 남아 있지 않고, 모두 다른 계열이며 첫 선택 계열과 단일 포켓몬이 아님을 확인했다. 오류 0건이다.
이로치: 사용자 답변 “이로치 적용”이다. 태고의돌에도 1/1000을 랜덤알과 같게 적용한다.
Figma 반영: `Shop Layout` `379:1757`의 알 분류에 상품 `태고의돌` `405:2539`을 더했다. 랜덤알 옆에 나란히 놓여 절 높이는 그대로다.
`[스펙 미확정]` 태고의돌 그림은 정하지 않았다.
SSOT: `docs/specs/s5.md`의 알 계약, `data/eggs.json`.

### SaveV3 타입과 v2 이전 구현

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음작업 진행”. 추천 순서의 마지막인 구현을 시작했다. 확정한 [모듈 계약](../../specs/modules.md)을 코드로 옮긴 첫 조각이다.
범위: 저장 v3 의 모양, 빈 상태, 정규화, v2 이전과 검사다. 거래 실행기와 화면 연결은 다음이다. 기존 v2 경로는 그대로 두었다. 아직 아무도 v3 을 읽거나 쓰지 않는다.
만든 파일: `src/shared/save-v3.ts`(타입), `src/save/v3.ts`(빈 상태·정규화), `src/save/migrate-v3.ts`(이전·검사), `src/tools/selftest-save-v3.ts`(자체 검사)다. `src/save/rules.ts`에 `SAVE_V3_RULES`를 더하고 `package.json`의 `selftest`에 새 검사를 넣었다.
모양의 핵심: 개체를 파티 배열에 직접 두지 않는다. `pets`에 모으고 `party.slots`가 식별자로 가리킨다. 빈 칸과 잠긴 칸을 표현해야 파티 칸 규칙이 선다. 배고픔 대신 만복도를 쓴다. 멈추는 값은 시각이 아니라 남은 시간으로 저장한다.
기본값: 파티는 항상 여섯 칸이다. 시작은 두 칸이 열려 있고 상점으로 두 칸, 업적으로 두 칸을 연다. 박스는 30칸이고 하나로 시작한다. 밥 쿨타임 10분, 알 돌봄 인정 간격 1분, 주기 저장 30초, 요청 기록은 최근 200건 또는 24시간이다.
이전 규칙: `fullness = 100 − hunger`, `shown`을 칸의 `hidden`으로 뒤집기, `fedAt`을 남은 쿨타임으로, `inventory`를 `bag`으로, `acc`를 `points.progressMs`로 옮긴다. `shiny:<개체>`와 `nick`·`look`은 `legacy`에 보존한다. 열린 칸을 넘는 개체는 박스의 빈 칸으로 보낸다.
v2 에 없는 값: 레벨과 경험치다. 레벨 1, 경험치 0으로 시작한다. 모듈 계약의 보존 대상에 레벨이 있으나 v2 에 그 값이 없어 새로 시작하는 수밖에 없다.
검사 6개: 개체 수, 개체 식별자, 친밀도 합계, 포인트, 해금 종 수, 배치 누락이다. 하나라도 어긋나면 결과를 버리고 원본을 유지한다. 파일을 옮기는 일은 부르는 쪽이 한다.
검증: `npx tsc --noEmit` 통과. 새 자체 검사 8건 통과. `npm run selftest` 전체 통과.
`[스펙 미확정]` 거래 실행기, v3 파일 읽기·쓰기 경로, 화면 연결은 아직 없다.
SSOT: `docs/specs/modules.md`의 저장 구조.

### 거래 실행기 구현

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음작업 진행”. 저장 v3 에 이어 거래 실행기를 만들었다.
만든 파일: `src/tx/executor.ts`(실행기), `src/tx/handlers.ts`(명령 등록), `src/party/visibility.ts`(표시·숨김 도메인 규칙), `src/tools/selftest-tx.ts`(자체 검사)다. `package.json`의 `selftest`에 새 검사를 넣었다.
처리 순서: 요청 ID 확인 → 저장 읽기 → 도메인 계산 → 통과하면 한 번에 저장 → 실패하면 아무 상태도 바꾸지 않는다. 계약 그대로다.
동시 요청: 실행기를 전부 동기로 만들었다. 그래서 거래는 저절로 한 번에 하나이고 들어온 순서대로 처리된다. 따로 줄을 세우는 코드를 두지 않았다.
사본 격리: 처리기는 `structuredClone`으로 만든 사본만 고친다. 실패하면 사본을 버리므로 원본이 더럽혀지지 않는다. 자체 검사로 확인했다.
파일 분리: 실행기는 파일을 직접 다루지 않는다. 읽기·쓰기·시계를 받아서 쓴다. 그래서 자체 검사가 파일 없이 돈다.
중복 방지: 같은 요청 ID 를 다시 보내면 저장된 결과를 그대로 돌려준다. 쓰기는 한 번뿐이다.
저장 실패: 이어서 실패한 횟수를 센다. 3회를 넘으면 `shouldNotifySaveFail`이 참이 된다. 성공하면 0으로 돌아간다. 조작은 막지 않는다.
첫 명령 2개: `party.show`와 `party.hide`다. 칸의 `hidden` 하나만 바꾼다. 규칙은 `src/party/visibility.ts`의 순수 함수에 두었다. 이미 같은 상태면 실패로 본다.
검증: `npx tsc --noEmit` 통과. 새 자체 검사 7건 통과. `npm run selftest` 전체 통과.
`[스펙 미확정]` 나머지 명령 15개, v3 파일 읽기·쓰기 경로, mailbox 연결은 아직 없다.
SSOT: `docs/specs/modules.md`의 거래 실행기와 명령 계약.

### 저장 v3 파일 통로

날짜: 2026-09-24. 사용자 지시: “그래 그렇게하자”. 다음 작업 후보 중 1번인 파일 경로 연결을 골랐다. 저장 경로가 붙어야 나머지 구현이 실제로 도는지 확인할 수 있다.
만든 파일: `src/save/store-v3.ts`다. `src/tools/selftest-save-v3.ts`에 파일 통로 검사 5건을 더했다.
읽기 갈림: 파일의 `v` 를 보고 셋으로 갈린다. v3 이면 정규화해서 그대로 쓴다. v1 이나 v2 이면 원본을 백업하고 옮긴 뒤 검사한다. 그 밖은 파손으로 보고 `.bak` 으로 옮긴다.
백업 우선: 백업에 실패하면 옮기지 않는다. 사용자의 진행을 잃는 것보다 v3 을 늦게 쓰는 편이 낫다. 백업 이름은 `<파일>.v2.bak` 이고 이미 있으면 덮어쓰지 않는다. 첫 원본이 가장 값지다.
검사 실패 처리: 이전 검사 6개 중 하나라도 어긋나면 파일을 교체하지 않고 어긋난 검사 이름을 돌려준다. 원본은 그대로 남는다.
재사용: 쓰기는 `store.ts` 의 `writeAtomic` 을 그대로 쓴다. tmp 에 쓰고 rename 이라 반쪽 파일이 남지 않는다. v1 이전도 `store.normalize` 가 이미 맡고 있어 v1 파일도 한 번에 v3 까지 간다.
검증: `npx tsc --noEmit` 통과. 자체 검사 13건 통과(값 8건, 파일 5건). `npm run selftest` 전체 통과. 파일 검사는 임시 폴더에서만 돌고 끝나면 지운다.
`[스펙 미확정]` 앱이 어느 시점에 v3 을 읽을지, 실패했을 때 화면에 무엇을 보일지는 정하지 않았다. 기존 v2 경로는 아직 그대로 돈다.
SSOT: `docs/specs/modules.md`의 저장 구조.

### 파티 배치·교체·보관 명령

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음작업 진행”. 명령 계약의 파티·박스 세 개를 구현했다.
만든 파일: `src/box/slots.ts`(박스 칸 다루기), `src/party/placement.ts`(배치·교체·보관 규칙)다. `src/tx/handlers.ts`에 `party.place`·`party.swap`·`party.keep`을 등록하고 `src/tools/selftest-tx.ts`에 검사 6건을 더했다.
지킨 규칙 3개: 박스에서 파티로 들어온 개체는 숨김으로 시작한다. 교체로 들어와도 같다. 교체는 한 번에 맞바꾼다. 먼저 빈 칸을 만드는 조작을 요구하지 않는다. 개체의 값은 그대로 둔다. 칸의 이전 개체 값을 새 개체에 복사하지 않는다.
거절하는 경우: 없는 개체, 박스에 없는 개체, 없는 칸, 잠긴 칸, 이미 찬 칸, 빈 칸이 없음, 파티에 없음이다. 실패하면 박스도 파티도 그대로다. 거래 실행기의 사본 격리가 이를 보장한다.
박스 자동 추가: 앞 박스부터 첫 빈 칸을 찾는다. 자리가 없으면 박스를 새로 만들고 첫 칸에 넣는다. 검사로 확인했다.
검증: `npx tsc --noEmit` 통과. `selftest-tx` 13건 통과. `npm run selftest` 전체 통과.
`[스펙 미확정]` 나머지 명령 12개(알·가방·상점·업적·튜토리얼·돌봄)와 화면 연결은 아직 없다.
SSOT: `docs/specs/modules.md`의 명령 계약, `docs/specs/s5.md`의 개체 획득과 파티 교체.

### 시간 처리 구현

날짜: 2026-09-24. 사용자 지시: “순서대로 진행”. 다음 작업 1번인 시간 처리다.
결정 버프 중첩: 제안은 가장 큰 것 하나만, 곱하기, 더하기 셋이었다. 사용자 답변: 더한다. 기준 1.0 에 프리미엄먹이 +1.0, 오래 놀아주기 +0.5 를 더해 둘 다 있으면 2.5배다. 장난감은 오래 놀아주기와 같은 버프라서 따로 더하지 않는다. 그래서 `BuffKind` 에서 `toy` 를 뺐다.
만든 파일: `src/state/time-v3.ts`(시간 적용), `src/tools/selftest-time-v3.ts`(자체 검사 12건)다. `src/save/rules.ts` 에 `TIME_V3_RULES` 를 더하고 `package.json` 의 `selftest` 에 넣었다.
저장 필드 추가: 개체에 `affinityProgressMs` 와 `fullnessProgressMs` 를 더했다. 부분 진행을 ms 정수로 쌓아야 짧은 틱을 여러 번 돌려도 긴 틱 한 번과 결과가 같다.
적용 대상: 파티에 있는 개체만 만복도·친밀도·포인트·쿨타임·버프가 움직인다. 박스에 있는 개체는 멈춘다. 숨긴 개체는 파티에 있으므로 그대로 흐른다. 알은 파티와 무관하게 준비 시간과 돌봄 쿨타임이 흐른다.
검사가 찾은 문제: 짧은 틱과 긴 틱의 포인트가 달랐다. 포인트 적립 속도가 친밀도에 달려 있는데, 틱 안에서 오른 친밀도를 그 틱 전체에 소급 적용하고 있었다. 구간 시작 시점의 친밀도로 셈하도록 고쳤다. 그래도 포인트는 틱을 나누면 조금 더 정확해진다. 값의 성질이라 없앨 수 없어 검사를 1 이하 차이로 두고 이유를 적었다.
알림: 배고픔과 매우 배고픔 구간에 들어갈 때만 한 번 알린다. 보통 구간으로 내려가는 것은 알리지 않는다. 준비가 끝난 알도 한 번만 알린다.
아직 없는 것: 진화 가능과 업적 달성 판정이다. 배너 순서는 부화 → 진화 → 업적이므로 두 판정을 붙일 자리는 남겨 두었다.
검증: `npx tsc --noEmit` 통과. 새 자체 검사 12건 통과. `npm run selftest` 전체 통과.
SSOT: `docs/specs/modules.md`의 시간 처리 순서, `docs/specs/balance.md`.

### 알 명령 구현

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음거 진행”. 다음 작업 2번인 알 명령이다. 새로 만든 `data/egg-conditions.json` 과 종 데이터의 `rank` 를 처음 쓰는 코드다.
만든 파일: `src/egg/care.ts`(돌봄), `src/egg/conditions.ts`(조건 판정), `src/egg/hatch.ts`(결과 추첨), `src/egg/open.ts`(열기), `src/tools/selftest-egg.ts`(자체 검사 13건)다. `src/save/rules.ts` 에 `EGG_V3_RULES` 를 더하고 `src/tx/handlers.ts` 에 `egg.care` 와 `egg.open` 을 등록했다.
무작위 주입: 거래 실행기의 문맥에 `rand` 를 더했다. 없으면 `Math.random` 이다. 자체 검사가 결과를 정할 수 있어야 부화를 확인할 수 있다.
돌봄: 한 번에 30초를 줄이고 쿨타임 1분을 건다. 쿨타임 중에는 횟수도 늘지 않는다. 남은 시간은 0 에서 멈춘다. 준비가 끝난 뒤에도 조건은 계속 쌓인다.
결과 판정 순서: 조건에 맞으면 그 조건의 종에서, 아니면 알의 후보 범위에서 뽑는다. 어느 쪽이든 난이도 가중치를 쓴다. 이로치는 따로 같은 확률로 뽑는다.
조건은 범위를 넘어선다: 조건으로 나온 종은 해금 기록이 없을 수 있다. 그래서 획득 기록과 함께 해금 기록도 남긴다. 발견한 조건은 `dex.discovered` 에 적는다.
배치: 빈 파티 칸에 숨김으로 넣는다. 칸이 없으면 박스로 보낸다. 새 개체의 숨김 배치 규칙 그대로다.
검사로 확인한 것: 조건 일곱 개가 겹치지 않고 빈틈이 있다는 것, 쓰다듬기 10회와 노래 4회처럼 어디에도 맞지 않는 조합이 일반 추첨으로 간다는 것, 600족 조건이 열 종이라는 것이다.
검증: `npx tsc --noEmit` 통과. 새 자체 검사 13건 통과. `npm run selftest` 전체 통과.
`[스펙 미확정]` 알 구매(`shop.buy`)가 아직 없어 알은 검사에서 직접 넣는다. 힌트와 반응 표현도 없다.
SSOT: `docs/specs/s5.md`의 알과 알 행동 조건, `data/egg-conditions.json`.

### 상점 구매 명령

날짜: 2026-09-24. 사용자 지시: “진행”. 다음 작업 3번의 앞쪽인 `shop.buy` 다. 가방 사용은 다음으로 나눴다.
만든 파일: `data/items.json`(가방 도구 12종), `src/shop/catalog-v3.ts`(가격 찾기), `src/shop/buy.ts`(구매), `src/tools/selftest-shop-v3.ts`(자체 검사 10건)다. `src/save/rules.ts` 에 `SHOP_V3_RULES` 를 더하고 `src/tx/handlers.ts` 에 `shop.buy` 를 등록했다.
가격의 단일 원본: 알은 `data/eggs.json`, 진화용 도구는 공통 값 하나, 그 밖 도구는 `data/items.json`, 파티 칸은 규칙표, 종은 `data/unlocks.json` 이 가진다. 같은 값을 두 곳에 적지 않는다. 그래야 가격이 어긋나지 않는다.
알 구매: 돌보미집 여섯 칸이 차 있으면 거절한다. 사면 바로 들어가고 준비 시간 5분이 시작된다. 태고의돌은 화석 15종을 후보로 담고 랜덤알은 해금한 종을 담는다.
파티 칸: 상점으로 여는 칸은 둘이다. 첫 칸 300P, 둘째 칸 600P다. 셋째부터는 거절한다. 업적으로 여는 칸 둘은 그대로 남는다.
종 지정 구매: 해금한 종만 산다. 새 개체는 빈 파티 칸에 숨김으로, 없으면 박스로 간다. 지금 상점에서 파는 종은 잠만보 하나다.
사고와 복구: 새 파일을 만들면서 기존 `src/shop/catalog.ts` 를 덮어썼다. 타입 검사가 바로 잡아냈다. `git checkout` 으로 되돌리고 새 코드를 `catalog-v3.ts` 로 옮겼다. 기존 v2 경로는 그대로 돈다.
검사 보강: 종 지정 구매 검사가 피카츄를 찾다 건너뛰고 있었다. 실제로 상점 가격이 있는 종이 잠만보뿐임을 확인하고 검사를 잠만보로 바꿨다. 건너뛰는 검사는 검사가 아니다.
검증: `npx tsc --noEmit` 통과. 새 자체 검사 10건 통과. `npm run selftest` 전체 통과.
`[스펙 미확정]` 가방 사용(`bag.use`)은 아직 없다. 상품 목록 화면에 무엇을 보일지도 코드로는 정하지 않았다.
SSOT: `docs/specs/balance.md` 가격표, `docs/specs/s5.md` 상점.

### 가방 사용 명령과 레벨 곡선

날짜: 2026-09-24. 사용자 지시: “진행”. 다음 작업 3번의 뒤쪽인 `bag.use` 다.
만든 파일: `src/dex/growth.ts`(레벨 곡선), `src/bag/use.ts`(도구 사용), `src/tools/selftest-bag.ts`(자체 검사 14건)다. `src/save/rules.ts` 에 `BAG_V3_RULES` 를 더하고 `src/tx/handlers.ts` 에 `bag.use` 를 등록했다.
레벨 곡선: 원작 경험치 타입 6종의 공식을 그대로 옮겼다. 100레벨 누적값이 원작과 같은지 여섯 개 모두 확인했다. 빠름 800,000, 보통 빠름 1,000,000, 보통 느림 1,059,860, 느림 1,250,000, 불규칙 600,000, 변동 1,640,000이다. 1레벨부터 100레벨까지 값이 계속 오르는지도 확인했다.
효과 여덟 가지: 기본먹이와 프리미엄먹이는 만복도와 밥 쿨타임을 다룬다. 장난감은 오래 놀아주기 버프를 건다. 경험사탕은 경험치를 올리고 종의 곡선으로 레벨을 다시 읽는다. 이상한사탕은 레벨을 1 올리고 진행을 0으로 둔다. 민트는 성격을, 두 약은 이로치를 바꾼다.
기본먹이: 무료이며 무제한이라 가방에서 차감하지 않는다. `data/items.json` 의 `price` 가 null 인 도구가 그렇다.
버프 갱신: 같은 버프가 남아 있으면 남은 시간을 기본 지속시간으로 바꾼다. 더하지 않는다. 계약 그대로다.
이로치 되돌리기: 일반색으로 바꿔도 도감의 이로치 획득 기록은 지우지 않는다. 현재 색과 과거 획득을 구분하는 계약을 지켰다.
진화는 뺐다: 진화용 도구 24종은 여기서 다루지 않는다. 진화는 조건 판정과 결과 분기가 따로 있어 다음 작업으로 남긴다.
검증: `npx tsc --noEmit` 통과. 새 자체 검사 14건 통과. `npm run selftest` 전체 통과.
SSOT: `docs/specs/balance.md` 의 성장과 버프, `data/items.json`.

### 시나리오 통합 검사

날짜: 2026-09-24. 사용자 제안: “이제는 e2e로 작업해봐도 될거같은데 설계를 거의 다 한거같은데 어때?” 확인해 보니 지금까지 만든 v3 을 앱이 아직 쓰지 않는다. 그래서 진짜 e2e 는 앱 연결 뒤에 한다. 대신 화면 없이 끝에서 끝까지 이어 보는 검사를 먼저 만들었다.
만든 파일: `src/tools/selftest-flow.ts` 다. `package.json` 의 `selftest` 에 넣었다.
방식: 임시 폴더에 실제 저장 파일을 만들고, 거래 실행기에 명령을 순서대로 보낸다. 시계와 무작위는 손에 쥔다. 값은 저장 파일을 다시 읽어 확인한다. 조각 하나가 아니라 흐름이 이어 붙는지 본다.
따라간 흐름: 첫 선택 뒤 상태 → 랜덤알 구매 → 중복 요청 → 돌봄 3회 → 시간 경과와 준비 완료 → 부화와 조건 발견 → 꺼내기 → 2시간 경과 → 밥 주기와 쿨타임 → 경험사탕 → 박스 보관과 정지 → 재배치 → 파티 칸 구매 → 다시 열기다. 13단계다.
검사가 확인한 이음새: 구매한 알의 후보가 해금한 종이라는 것, 같은 요청 ID 로 알이 늘지 않는다는 것, 쓰다듬기 3회가 `pat-3` 조건으로 이어져 해금하지 않은 종이 나온다는 것, 박스에 있는 동안 만복도가 멈춘다는 것, 다시 열어도 완료한 요청이 남아 있다는 것이다.
검사를 세 번 고쳤다: 만복도와 포인트를 고정 숫자로 기대했는데, 앞 단계에서 이미 시간이 흘러 값이 달랐다. 세 번 모두 코드가 아니라 검사의 가정이 틀렸다. 증가분과 감소분으로 바꿨다. 흐름 검사는 앞 단계의 영향을 받으므로 절대값을 기대하면 안 된다.
검증: `npx tsc --noEmit` 통과. 새 검사 13단계 통과. `npm run selftest` 전체 통과.
남은 것: 앱 진입점이 v3 을 읽는 코드, mailbox 에서 거래 실행기로 가는 길, 화면이 v3 을 그리는 코드다. 이 셋이 붙어야 실제 e2e 가 성립한다.
SSOT: `docs/specs/s5-scenarios.md`.

### 커맨드 처리기와 거래 실행기의 다리

날짜: 2026-09-24. 사용자 지시: “커밋하고 다음거 진행”. 앱 연결의 첫 조각이다. 표면이 보낸 명령이 거래 실행기까지 가는 길을 놓았다.
만든 파일: `src/tx/bridge.ts` 다. `src/shared/types.ts` 의 `CommandName` 에 새 명령 여섯을 더하고 `src/tools/selftest-tx.ts` 에 검사 5건을 더했다.
하는 일: `Command` 를 `TxRequest` 로 바꾸고 결과를 `CommandResult` 로 되돌린다. 표면은 v3 을 알 필요가 없다. 우클릭·트레이·CLI·확장이 지금 쓰는 모양 그대로 보낸다.
인자 모양: 명령마다 `target` 이 가리키는 것이 다르다. 파티 명령은 개체, 알 명령은 알, 가방은 도구, 상점은 상품이다. 다리가 그 차이를 흡수한다.
요청 식별자: 보낸 쪽이 `args.reqId` 를 주면 그것을 쓴다. 없으면 보낸 곳·시각·명령·대상으로 만든다. 같은 순간에 같은 명령을 두 번 보내면 구분하지 못한다. 한 번만 반영해야 하는 조작은 보낸 쪽이 `reqId` 를 주는 것이 맞다. 주석에 적었다.
검사를 고쳤다: 처음에는 `dispatch` 가 비동기인데 기다리지 않고 `void` 로 흘려보냈다. 검사가 통과해도 확인한 것이 없는 상태였다. 비동기 함수로 묶어 기다리고 실패하면 종료 코드 1 로 끝내도록 고쳤다.
검증: `npx tsc --noEmit` 통과. `selftest-tx` 18건 통과. `npm run selftest` 전체 통과.
아직 하지 않은 것: 앱 진입점(`src/main/app.ts`)은 그대로 v2 를 쓴다. 다리를 실제로 거는 것은 화면이 v3 을 읽을 준비가 된 뒤다. 지금 걸면 v2 화면이 v3 저장을 못 읽는다.
SSOT: `docs/specs/modules.md`의 명령 계약.
