{
  "name": "pokebuddy",
  "version": "0.3.0",
  "private": true,
  "description": "터미널 위에 떠 있는 포켓몬 펫 오버레이 (macOS / Windows)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MilkLotion/terminal_pokemon.git"
  },
  "keywords": [
    "claude-code",
    "codex",
    "terminal",
    "pet",
    "desktop-pet",
    "electron"
  ],
  "main": "main.js",
  "bin": {
    "pokebuddy": "bin/pokebuddy",
    "pokebuddy-status": "bin/pokebuddy-status"
  },
  "files": [
    "main.js",
    "preload.js",
    "config.js",
    "art/",
    "buddy/",
    "cli/",
    "game/",
    "dist/",
    "data/",
    "lib/",
    "renderer/",
    "helpers/winbounds",
    "helpers/winbounds.ps1",
    "helpers/winbounds.swift",
    "bin/pokebuddy",
    "bin/pokebuddy-status",
    "scripts/postinstall.js",
    "scripts/build-helper.js",
    "vscode-extension/pokebuddy-active-terminal-*.vsix"
  ],
  "engines": {
    "node": ">=22.12"
  },
  "os": [
    "darwin",
    "win32"
  ],
  "scripts": {
    "start": "electron .",
    "build:helper": "node scripts/build-helper.js",
    "build:vsix": "node scripts/build-vsix.js",
    "prepack": "npm run build && node scripts/build-helper.js && node scripts/build-vsix.js",
    "postinstall": "node scripts/postinstall.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "selftest": "npm run build && node dist/tools/selftest-save.js && node dist/tools/selftest-dex.js && node dist/tools/selftest-agents.js && node scripts/selftest-game.js",
    "data:build": "npm run build && node dist/tools/build-names.js && node dist/tools/build-species.js && node dist/tools/build-evo.js && node dist/tools/build-unlocks.js"
  },
  "dependencies": {
    "electron": "44.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.5",
    "typescript": "^7.0.2"
  }
}
